import type { EmergencyStep } from "./contracts";
export function EmergencyPanel({ active, steps, onAcknowledge }: { active: boolean; steps: readonly EmergencyStep[]; onAcknowledge: () => void }): React.JSX.Element { return <section aria-labelledby="emergency-heading" className={active ? "emergency active" : "emergency"}><h2 id="emergency-heading">Emergency and recovery</h2><ol>{steps.map((step) => <li key={step.id}>{step.complete ? "✓" : "○"} {step.instruction}</li>)}</ol><button type="button" disabled={!active} onClick={onAcknowledge}>Acknowledge emergency</button></section>; }
