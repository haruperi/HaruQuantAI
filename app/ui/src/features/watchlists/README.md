# Watchlists

## Purpose

Owns account-watchlist presentation and interactions for `FEAT-UI-12`.
Backend watchlist policy remains in `app/services/api/workstation/watchlists/`.

## Public API

- `WatchlistWidget` through `index.ts`.

## Verification

- `WatchlistWidget.test.tsx` provides focused unit evidence.
