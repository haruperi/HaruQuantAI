/** Drift monitor route (FEAT-UI-28). */

"use client";

import type { ReactNode } from "react";

import { ResearchDrift } from "@/features/research";

export default function Page(): ReactNode {
  return <ResearchDrift />;
}
