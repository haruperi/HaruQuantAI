/** Run comparison route (FEAT-UI-28). */

"use client";

import type { ReactNode } from "react";

import { ResearchComparison } from "@/features/research";

export default function Page(): ReactNode {
  return <ResearchComparison />;
}
