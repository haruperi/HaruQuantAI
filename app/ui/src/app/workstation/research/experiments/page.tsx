/** Experiment ledger route (FEAT-UI-28). */

"use client";

import type { ReactNode } from "react";

import { ResearchExperimentList } from "@/features/research";

export default function Page(): ReactNode {
  return <ResearchExperimentList />;
}
