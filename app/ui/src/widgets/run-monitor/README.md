# FEAT-UI-RUN_MONITOR

Removable Jobs/Run Monitor presentation for `interfaces.operate-jobs@1`. It keeps infrastructure state, domain outcome, desired control, acknowledged control, worker presence, and progress semantics separate. Panel close only disposes observation state; it never cancels accepted work.
