# Deterministic Fake Tick Stream Provider

> **Provider ID:** `data.tick_stream.fake`
> **Capability:** `data.tick_stream.v1`
> **Lifecycle:** Scoped, `reversible_ephemeral`
> **Status:** Active

## Overview
Supplies a deterministic three-event tick sequence for `EURUSD` used in lifecycle, replacement, and removability validation without external I/O or network connections.
