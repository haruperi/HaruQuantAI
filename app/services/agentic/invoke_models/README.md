# FEAT-AGT-INVOKE_MODELS

Provides `agentic.model-inference@1`. Provider/model/profile identity is pinned before each structured call. Floating aliases are rejected. Fallbacks must be declared in advance and preserve task/privacy/retention scope. Current role eligibility and Agentic readiness are rechecked; host resources are admitted through Task 1.14. Missing or non-finite usage is never treated as zero. Provider/model substitution and over-budget usage fail closed, and private provider objects never cross the public result contract.
