# Strategy Diagnostics

This feature owns the stable Strategy error catalogue, the immutable diagnostic
contract, and bounded redacted diagnostic export. It never persists or routes
the Utils-owned audit envelope.

Its package-root exports and requirements are registered under `FEAT-STR-02` in
the domain README.
