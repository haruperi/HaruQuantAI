# Strategy Signal Library

This feature owns the approved immutable concrete signal evaluators. Each leaf
file contains one recovered signal rule set and delegates common execution
mechanics to the Signal Boundary feature.

Its package-root exports and requirements are registered under `FEAT-STR-10` in
the domain README. Adding catalogue content does not create a new Strategy
feature or usage program.
