# Strategy Signal Boundary

This feature owns the `SignalEvaluator` structural contract and the hash-bound,
point-in-time `evaluate_strategy_signals` boundary. It contains no concrete
strategy rules.

Its package-root exports and requirements are registered under `FEAT-STR-09` in
the domain README.
