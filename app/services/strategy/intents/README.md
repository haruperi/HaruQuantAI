# Strategy Intents

This feature owns the canonical non-executable `TradeIntent v1` proposal and its
deterministic builder. It never approves risk, creates orders or fills, or
mutates broker/account state.

Its package-root exports and requirements are registered under `FEAT-STR-04` in
the domain README.
