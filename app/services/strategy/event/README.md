# Event Strategy Evaluation

This feature owns deterministic invocation of the supported typed event hooks
and atomic validation of candidate Strategy-local state. It consumes immutable
external evidence and never mutates official execution state.

Its package-root exports and requirements are registered under `FEAT-STR-08` in
the domain README.
