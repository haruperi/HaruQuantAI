# Vectorized Strategy Evaluation

This feature owns atomic vectorized evaluator invocation over normalized Data
and Indicators contracts. It validates timing, identity, readiness, and resource
bounds before returning typed decisions, intents, diagnostics, and replay
metadata.

Its package-root exports and requirements are registered under `FEAT-STR-07` in
the domain README.
