# Strategy Replay

This pure feature owns the immutable replay-manifest contract and deterministic
manifest construction from validated identity, configuration, data, indicator,
simulation, timing, and seed evidence.

Its package-root exports and requirements are registered under `FEAT-STR-05` in
the domain README.
