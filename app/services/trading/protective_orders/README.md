# Protective-Order Lifecycle

This module owns `FEAT-TRD-10` and fail-closed stop/target coverage, OCO identity,
residual resizing, and reverse-exposure prevention. Broker submission remains
behind the injected Trading/Brokers authority boundary.
