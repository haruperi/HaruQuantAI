# Execution Realism Models

`FEAT-SIM-12` owns deterministic latency, queue, slippage, market-impact,
cancel/replace-race, and player-versus-venue projection models. All numerical
execution calculations use finite `Decimal` values and explicit input evidence.

Canonical stochastic realism accepts only exact-scope, unexpired Phase-19
calibration artifacts. Concern-isolated `sha256-counter-u256-v1` streams exclude
trace-only configuration from economic identity, publish golden vectors, and
serialize exact counters. Pathwise Level-2 queue position remains excluded;
scenario-owned seeded faults and authority-query crash recovery fail closed.

The published performance gates are 370,000 annual M1 draws within 5.0 seconds
and 64 MiB peak RSS growth, plus 100,000 draws across ten symbols within 2.0
seconds and the same memory bound.
