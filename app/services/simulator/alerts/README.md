# Alert Lifecycle

`FEAT-SIM-14` owns simulated-session alert identity, latching and lifecycle,
root-cause grouping, perception timing, and emergency-control availability. It
does not deliver external operational notifications.
