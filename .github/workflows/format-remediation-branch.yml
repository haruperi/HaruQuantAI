name: Format Remediation Branch

on:
  push:
    branches: [fix/final-composability-audit]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: format-remediation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  format:
    runs-on: windows-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/final-composability-audit
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          version: "latest"

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --frozen --dev

      - name: Apply Ruff formatting
        run: uv run --frozen ruff format .

      - name: Commit formatting changes
        shell: pwsh
        run: |
          if (-not (git status --porcelain)) {
            Write-Host "No formatting changes required."
            exit 0
          }
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "style: apply Ruff formatting to remediation changes"
          git push origin HEAD:fix/final-composability-audit
