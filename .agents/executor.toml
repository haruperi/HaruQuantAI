# executor agent — generated by .agents/configure.py (vendor: agy).
# agy's -p consumes the next token as its prompt and must remain last.
brand = "agy"
command = [
  "agy",
  "--dangerously-skip-permissions",
  "--model", "gemini-3.7-flash-high",
  "--effort", "high",
  "--print-timeout", "110m",
  "-p",
]
prompt_delivery = "file"
template = "docs/templates/prompt/executor.md"
