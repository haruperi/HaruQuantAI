# executor agent — generated by .agents/configure.py (vendor: agy).
brand = "agy"
session_adapter = "agy"
session_continuity = "required"
model = "gemini-3.7-flash-high"
effort = "high"
command = ["python", ".agents/session_runner.py", "--brand", "agy", "--role", "executor", "--model", "gemini-3.7-flash-high", "--effort", "high", "--print-timeout", "110m", "{prompt}"]
prompt_delivery = "file"
template = "docs/templates/prompt/executor.md"
