# planner agent — generated by .agents/configure.py (vendor: codex).
brand = "codex"
session_adapter = "codex"
session_continuity = "required"
model = "gpt-5.6-sol"
effort = "medium"
command = ["python", ".agents/session_runner.py", "--brand", "codex", "--role", "planner", "--model", "gpt-5.6-sol", "--effort", "medium", "{prompt}"]
prompt_delivery = "file"
template = "docs/templates/prompt/planner.md"
