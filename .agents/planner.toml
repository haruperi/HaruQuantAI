# Planner agent — persistent same-role Codex session per workflow run.
brand = "codex"
session_adapter = "codex"
session_continuity = "required"
model = "gpt-5.6-sol"
effort = "high"
command = ["python", ".agents/session_runner.py", "--brand", "codex", "--role", "planner", "--model", "gpt-5.6-sol", "--effort", "high", "{prompt}"]
prompt_delivery = "file"
template = "docs/templates/prompt/planner.md"
