# reviewer agent — generated by .agents/configure.py (vendor: codex).
brand = "codex"
session_adapter = "codex"
session_continuity = "required"
model = "gpt-5.6-sol"
effort = "low"
command = ["python", ".agents/session_runner.py", "--brand", "codex", "--role", "reviewer", "--model", "gpt-5.6-sol", "--effort", "low", "{prompt}"]
prompt_delivery = "file"
template = "docs/templates/prompt/reviewer.md"
