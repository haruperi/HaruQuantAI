# reviewer agent — generated by .agents/configure.py (vendor: cline).
# Cline -p is PLAN mode and must not be added for Reviewer act-mode verification.
brand = "cline"
command = ["cline", "-P", "zai-coding-plan", "-m", "glm-5.3", "--thinking", "xhigh"]
prompt_delivery = "file"
template = "docs/templates/prompt/reviewer.md"
