# ${title}

> **Feature ID:** `${feature_id}`
> **Status:** `IN_PROGRESS`

## Domain

`${domain}`

## Provides

- `${capability}`

## Purpose

This generated shell reserves the approved feature boundary. Business behavior,
failure policy, executable examples, and acceptance assertions remain incomplete.

## Persistent State

None is generated. If implementation discovers durable or shared state, stop and
return to planning; this stateless template is no longer applicable.

## Completion boundary

The shell is not usable or accepted until its public operations, feature-specific
tests, usage scenario, lifecycle behavior, and reviewed evidence are implemented.
