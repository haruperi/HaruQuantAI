"""Incomplete offline usage boundary for ${title}."""


def main() -> None:
    """Refuse to present generated structure as executable usage evidence.

    Raises:
        RuntimeError: Always, until real public operations are implemented.
    """
    raise RuntimeError("${feature_id} has no implemented usage scenario")


if __name__ == "__main__":
    main()
