# Phase 1 Implementation Plan - v1.1 - Dual walking skeleton: backtest and actual MT5 demo execution

> **Status:** Not started
> **Requirement count:** 675
> **Source of phase assignment:** `docs/dev/TRACEABILITY_MATRIX.md`
> **Release contract:** `docs/dev/AGILE_ROADMAP.md`, Phase 1
> **Completion evidence rule:** every checked item ends with implementation and test `path:line` evidence.

## 1. Purpose and authority

This document is the execution ledger for Phase 1. It translates the roadmap commitment and assigned traceability IDs into dependency-ordered work suitable for implementation by junior developers under review. It does not replace `AGENTS.md`, `docs/PROJECT.md`, `docs/ARCHITECTURE.md`, or a domain README. Those sources alone remain authoritative for behavior and boundaries. This plan is only a delivery ledger recording sequence, status, evidence, and phase acceptance; it creates no product requirement or implementation rule.

If this plan conflicts with an authoritative source, stop, record the item as `Pending`, and obtain an owner decision. Do not resolve ambiguity by inventing a trading rule, risk limit, provider behavior, result, or compatibility surface.

## 2. Phase outcome

- **Version:** `v1.1`.
- **Theme:** Dual walking skeleton: backtest and actual MT5 demo execution.
- **Entry condition:** The clean-room repository specifications, roadmap, and traceability ledger are accepted.
- **Definition of done:** every requirement in Section 8 is checked with evidence, all domain and cross-domain gates pass, the phase exit demonstration succeeds, and phase-close documentation reconciliation is complete.

### 2.1 Public-interface commitment

Define all registered v1 names, owners, schema IDs, capability-scoped ports, compatibility rules, and package-root operations. Narrow unsupported behavior fails deterministically; it never returns a synthetic success.

### 2.2 Exit criteria

- **Functional:** Run one MA backtest, then place, reconcile, and close one Risk-approved MT5 demo position and display factual results.
- **Integration:** The phase demo and every earlier demo pass only through public domain boundaries.
- **Coverage:** Changed code has targeted unit, integration, usage, and system evidence with at least 80% coverage.
- **Quality:** `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy .`, and affected tests pass.
- **Safety:** No invented data, fills, identifiers, or performance; no secret leakage; broker mutations are limited to the explicit MT5 demo proof.
- **Release:** Tag v1.1 only after documented automated checks and the phase exit demo pass.

### 2.3 Phase risks

Retires integration-late and broker-integration-late risk. Phase 1 is very large (675 IDs) because actual MT5 demo mutation pulls safety, persistence, and reconciliation forward.

## 3. Inherited implementation constraints (non-authoritative summary)

This section repeats constraints from the authoritative repository documents for developer convenience. It does not create or modify requirements. If any wording differs from an authoritative source, the source wins and this ledger must be corrected.

1. Deliver production-grade code for the narrow phase scope; no disposable implementation, temporary public API, silent fallback, or fake success path is allowed.
2. Keep the architecture acyclic and use only documented domain exports. Python remains the sole deterministic policy-enforcement authority.
3. Every broker-mutating proof uses an actual MT5 platform connected to an account whose environment is deterministically `demo`. Real-capital execution is prohibited.
4. A fake broker adapter may be used only where a later contract-test requirement explicitly mandates it; it cannot satisfy an actual MT5 integration or usage proof.
5. Risk approval, authorization evidence, route compatibility, kill switch state, idempotency, and audit persistence fail closed whenever required evidence is absent or stale.
6. Never record invented fills, performance, provider responses, or benchmark claims. Tests use declared fixtures; actual-provider proofs preserve factual returned evidence.
7. Use explicit types and Google-style docstrings for all signatures; use the project logger, never `print`; redact secrets before logs, errors, metrics, traces, audit payloads, or returned diagnostics.
8. Run targeted tests during implementation. Do not use the full test suite as the iterative inner loop.
9. During the phase, update only this plan's checkboxes and evidence. Reconcile affected domain READMEs and active system documentation during the phase-close stage.
10. A checked item without implementation and test `path:line` evidence is invalid and must be returned to unchecked state.

## 4. How to read the execution order

Section 8 is the sole implementation sequence for this phase. Execute it from the first numbered step to the last. Domain tables and ID manifests elsewhere in this document are coverage indexes only and must never be interpreted as delivery order.

Each step records its immediate ledger predecessor and its authoritative requirement dependencies. The default is strict top-to-bottom execution. Parallel work is allowed only when the lead reviewer records that the affected steps share no implementation, contract, migration, runtime, or verification predecessor; parallel completion must still be merged and evidenced in ledger order.

The ordering model is:

1. Phase-level system prerequisites.
2. Domain modules in architecture dependency order.
3. Within a module: component/package establishment before functional behavior.
4. Domain workflows after their contributing module behavior.
5. Capability acceptance after functional and workflow delivery.
6. Domain NFR verification after the behavior it verifies.
7. Cross-domain system workflows after all contributing domains.
8. System-wide quality verification and phase completion last.

## 5. Requirement distribution (coverage index only)

This table proves domain coverage. It is not an implementation sequence; Section 8 is the only execution order.

| Lane | Assigned IDs | Phase obligation |
|---|---:|---|
| System | 11 | Cross-domain architecture and workflow control. |
| Utils | 38 | Production shared contracts, structured logs, errors, IDs, UTC, canonical serialization, redaction, settings, and audit context. |
| Brokers | 125 | Production MT5 adapter only for the proof: connect/authenticate, real bars/account reads, check/place order, read authority state, and close position. No fake adapter. |
| Data | 38 | Normalize real MT5 bars/account/context evidence; minimum durable dataset/audit/execution-supporting persistence. |
| Indicators | 21 | Availability-aware SMA/EMA with production validation and deterministic manifests. |
| Strategy | 36 | One immutable MA-crossover strategy producing canonical TradeIntent lineage. |
| Risk | 56 | Configured sizing, proposal review, ActionPolicyVerdict, approval token, audit chain, and kill-switch block/clear needed by actual demo mutation. |
| Trading | 71 | One production path for sim and MT5 demo: session start, gate, idempotent place/close, factual receipt/record, persistence, basic reconciliation, safe shutdown. |
| Simulation | 30 | Simple official backtest loop with deterministic fills, results, and artifacts. |
| Analytics | 43 | Core reports from both SimulationResult and factual MT5 demo TradeRecord; never invent fills or performance. |
| Optimization | 46 | Bounded deterministic MA grid sweep through official Simulation. |
| Research | 58 | Minimal prepared-data/core-metric ResearchReport entry point. |
| Portfolio | 28 | Deterministic single-portfolio construction through stable contracts. |
| UI/API | 74 | Canonical FastAPI/Next.js app with authenticated controls for backtest and actual MT5 demo place/reconcile/close, plus kill switch and factual status. |

## 6. Developer execution protocol

For each requirement, the assigned developer must:

1. Open the cited source document and locate the requirement ID or cited component section.
2. Confirm dependency evidence and identify the owning package, public export, side effects, failure behavior, usage example, and tests.
3. Submit a scoped dry run naming files, tests, runtime actions, safety conditions, and rollback.
4. Implement the smallest complete production-grade behavior that satisfies the requirement without weakening the final seam.
5. Run source-named unit/usage tests plus targeted integration, security, determinism, and negative-path tests appropriate to the change.
6. Run Ruff and MyPy over the affected code; maintain at least 80% coverage for changed behavior.
7. Replace the requirement's pending evidence line with concrete code and test `path:line` references and the passing commands.
8. Obtain review, then change `[ ]` to `[X]`. Never check an item based only on intent, scaffolding, or an unverified provider mock.

### 6.1 Standard verification commands

```powershell
uv run pytest <targeted-test-file>
uv run ruff check <changed-code-and-test-paths>
uv run ruff format --check <changed-code-and-test-paths>
uv run mypy app
git diff --check
```

Use the exact source-named usage and integration commands in addition to these gates. Any command that mutates a broker requires the separately approved, deterministic MT5-demo execution procedure.

## 7. Cross-domain integration gates

- [ ] All consumed contracts are imported only from documented public exports. Evidence: Pending.
- [ ] Every cross-domain request propagates authentication, correlation, causation, and audit context as specified. Evidence: Pending.
- [ ] Missing, stale, malformed, unauthorized, unsupported, or uncertain evidence fails closed at the owning boundary. Evidence: Pending.
- [ ] Deterministic paths reproduce canonical outputs and hashes from identical inputs. Evidence: Pending.
- [ ] Persistence ownership, transactions, migrations, locks, and restart behavior match domain boundaries. Evidence: Pending.
- [ ] Logs, errors, traces, metrics, audit records, and returned diagnostics contain no secrets. Evidence: Pending.
- [ ] Actual-provider tests distinguish factual provider evidence from fixtures and simulated results. Evidence: Pending.
- [ ] The roadmap exit workflow succeeds end to end and its negative/recovery cases are evidenced. Evidence: Pending.

## 8. Sequential implementation ledger

This section is the mandatory implementation order. Start at Step 1 and proceed downward. A requirement appearing in a later domain, workflow, capability, or verification stage is not ready merely because it is independently understandable.

Every step is gated by the immediately preceding ledger step. The additional dependency evidence identifies authoritative prerequisites that must also be complete. Checkboxes and evidence are updated here during implementation; domain README reconciliation remains a phase-close activity.

### Stage 1 - System foundations

Resolve and evidence the phase-level system contracts and configuration prerequisites before domain implementation begins.

#### Step `P01-S0001` [X] `P-SYS-001` - Stable public ports, contract versioning, and acyclic domain boundaries

- **Execution domain:** `System`.
- **Execution position:** `1` of `675`.
- **Cannot start before:** Phase entry gate.
- **Authoritative dependency evidence:** None beyond the immediately preceding ledger step and phase entry gate.
- **Ordering basis:** Phase-level system foundation before domain code.
- **Delivery type:** Resolve specification before implementation.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** None.
- **Authoritative source:** `docs/PROJECT.md` - ``§7 System-Wide Requirements`` (`P-SYS-001`); matrix row `11`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-SYS-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: explicit import-safe package-root ports at `app/utils/__init__.py:1`, `app/services/brokers/__init__.py:1`, `app/services/data/__init__.py:1`, `app/services/indicators/__init__.py:1`, `app/services/strategy/__init__.py:1`, `app/services/risk/__init__.py:1`, `app/services/trading/__init__.py:1`, `app/services/simulator/__init__.py:1`, `app/services/analytics/__init__.py:1`, `app/services/optimization/__init__.py:1`, `app/services/research/__init__.py:1`, `app/services/portfolio/__init__.py:1`, and `app/services/api/__init__.py:1`. Tests: public-port/import checks at `tests/system/unit/test_public_ports.py:27`; internal-import and acyclic-graph audit at `tests/system/unit/test_public_ports.py:37`; paired contract-version/schema-identity audit at `tests/system/unit/test_public_ports.py:55`; registered-v1 compatibility audit at `tests/system/unit/test_public_ports.py:74`. Commands/results: `uv run pytest tests/system/unit/test_public_ports.py tests/system/unit/test_runtime.py tests/system/integration/test_runtime_initialization.py --cov=app.runtime --cov-fail-under=80` -> 41 passed, 100% coverage; `uv run ruff check app tests/system` -> passed; `uv run ruff format --check app tests/system` -> passed; `uv run mypy app` -> passed; `git diff --check` -> passed. Runtime/provider evidence: import and AST/document audits only; no environment read, filesystem mutation outside repository edits, network call, provider call, or broker action. Supporting code: `app/services/api/__init__.py:1`.

#### Step `P01-S0002` [X] `P-SYS-002` - Runtime profiles and execution-route compatibility

- **Execution domain:** `System`.
- **Execution position:** `2` of `675`.
- **Cannot start before:** Step `P01-S0001` (`P-SYS-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** Phase-level system foundation before domain code.
- **Delivery type:** Resolve specification before implementation.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `docs/PROJECT.md` - ``§7 System-Wide Requirements`` (`P-SYS-002`); matrix row `12`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-SYS-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: authoritative compatibility mapping at `app/runtime.py:5`; bounded typed initialization failure at `app/runtime.py:13`; fail-closed validator at `app/runtime.py:23`; application-boundary export at `app/__init__.py:3`. Tests: all valid, mismatched, unknown, case-variant, and whitespace-variant values at `tests/system/unit/test_runtime.py:16`, `tests/system/unit/test_runtime.py:30`, and `tests/system/unit/test_runtime.py:50`; application-boundary initialization behavior at `tests/system/integration/test_runtime_initialization.py:5`. Commands/results: `uv run pytest tests/system/unit/test_public_ports.py tests/system/unit/test_runtime.py tests/system/integration/test_runtime_initialization.py --cov=app.runtime --cov-fail-under=80` -> 41 passed, 100% coverage; `uv run ruff check app tests/system` -> passed; `uv run ruff format --check app tests/system` -> passed; `uv run mypy app` -> passed; `git diff --check` -> passed. Runtime/provider evidence: pure validation only; no environment read, runtime startup, external call, provider call, or broker mutation. Supporting code: `app/runtime.py:23`.

### Stage 2 - Utils

Implement `Utils` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0003` [X] `P-UTL-001` - contracts feature/component (provisional)

- **Execution domain:** `Utils`.
- **Execution position:** `3` of `675`.
- **Cannot start before:** Step `P01-S0002` (`P-SYS-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-UTL-001`); matrix row `62`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-UTL-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: public contracts package at `app/utils/contracts/__init__.py:1`. Tests: package-root boundary at `tests/utils/unit/test_boundaries.py:10`; Google-style semantic documentation audit for every Utils definition at `tests/utils/unit/test_docstrings.py:167`. Commands/results: final targeted Utils/system gate -> 77 passed with 87.39% `app.utils` coverage; Ruff, Google docstyle, formatting, and MyPy passed. Runtime evidence: import/contract/document checks only; no provider, network, or broker action.

#### Step `P01-S0004` [X] `FR-UTL-001` - Define immutable AuthContext v1 with only USER and SERVICE_ACCOUNT principal types and complete trace context.

- **Execution domain:** `Utils`.
- **Execution position:** `4` of `675`.
- **Cannot start before:** Step `P01-S0003` (`P-UTL-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.1 `contracts/` — Shared Context and Audit Contracts > Functional requirements`` (line 231); matrix row `31`.
- **Source responsibility:** Define immutable `AuthContext v1` with only `USER` and `SERVICE_ACCOUNT` principal types and complete trace context.
- **Source class / function / method:** `AuthContext`
- **Source side effects:** None
- **Source raises:** `ValidationError`: naive time, empty identity/trace field, or unsupported principal type
- **Source usage / test:** **Usage:** `tests/utils/usage/01_contracts.py::example_auth_context()`<br>**Unit:** `tests/utils/unit/test_auth.py::test_auth_context_rejects_naive_time()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: immutable `AuthContext` at `app/utils/contracts/auth.py:19`. Test: naive-time rejection at `tests/utils/unit/test_auth.py:27`; usage at `tests/utils/usage/01_contracts.py:12`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: pure Pydantic validation; no I/O.

#### Step `P01-S0005` [X] `FR-UTL-002` - Define immutable redacted AuditEvent v1 with bounded JSON-safe payload.

- **Execution domain:** `Utils`.
- **Execution position:** `5` of `675`.
- **Cannot start before:** Step `P01-S0004` (`FR-UTL-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.1 `contracts/` — Shared Context and Audit Contracts > Functional requirements`` (line 232); matrix row `32`.
- **Source responsibility:** Define immutable redacted `AuditEvent v1` with bounded JSON-safe payload.
- **Source class / function / method:** `AuditEvent`
- **Source side effects:** None
- **Source raises:** `ValidationError`: naive timestamp, empty identity/trace field, or unsafe payload
- **Source usage / test:** **Usage:** `tests/utils/usage/01_contracts.py::example_audit_event()`<br>**Unit:** `tests/utils/unit/test_audit.py::test_audit_event_requires_json_safe_payload()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: immutable, deeply frozen `AuditEvent` at `app/utils/contracts/audit.py:183`. Test: JSON-safe/redacted payload at `tests/utils/unit/test_audit.py:24`; usage at `tests/utils/usage/01_contracts.py:32`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: bounded in-memory validation only.

#### Step `P01-S0006` [X] `FR-UTL-003` - Reject naive timestamps, empty identity/trace fields, unsupported principal types, and malformed schema identity.

- **Execution domain:** `Utils`.
- **Execution position:** `6` of `675`.
- **Cannot start before:** Step `P01-S0005` (`FR-UTL-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.1 `contracts/` — Shared Context and Audit Contracts > Functional requirements`` (line 233); matrix row `33`.
- **Source responsibility:** Reject naive timestamps, empty identity/trace fields, unsupported principal types, and malformed schema identity.
- **Source class / function / method:** Strict contract-field validation used by `AuditEvent` and `AuthContext`
- **Source side effects:** None
- **Source raises:** `ValidationError`: naive time, empty field, unsupported principal type, or malformed schema identity
- **Source usage / test:** **Usage:** `tests/utils/usage/01_contracts.py::example_contract_validation()`<br>**Unit:** `tests/utils/unit/test_audit.py::test_contract_field_validation_rejects_malformed_schema()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: strict shared validators at `app/utils/contracts/audit.py:34` and `app/utils/contracts/auth.py:19`. Test: malformed-schema rejection at `tests/utils/unit/test_audit.py:31` and naive-time rejection at `tests/utils/unit/test_auth.py:27`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: fail-closed validation only.

#### Step `P01-S0007` [X] `P-UTL-002` - errors feature/component (provisional)

- **Execution domain:** `Utils`.
- **Execution position:** `7` of `675`.
- **Cannot start before:** Step `P01-S0006` (`FR-UTL-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.2` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-UTL-002`); matrix row `63`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-UTL-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: public errors package at `app/utils/errors/__init__.py:1`. Test: package-root boundary at `tests/utils/unit/test_boundaries.py:10`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: import-safe definitions only.

#### Step `P01-S0008` [X] `FR-UTL-004` - Provide focused shared base exceptions without domain-specific policy.

- **Execution domain:** `Utils`.
- **Execution position:** `8` of `675`.
- **Cannot start before:** Step `P01-S0007` (`P-UTL-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.2 `errors/` — Shared Errors, Metadata, and Routing > Functional requirements`` (line 256); matrix row `34`.
- **Source responsibility:** Provide focused shared base exceptions without domain-specific policy.
- **Source class / function / method:** `HaruQuantError`, `ConfigurationError`, `ValidationError`, `SecurityError`, `ExternalServiceError`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/02_errors.py::example_typed_error_codes()`<br>**Unit:** `tests/utils/unit/test_exceptions.py::test_shared_exception_hierarchy()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: focused hierarchy at `app/utils/errors/exceptions.py:12`. Test: hierarchy at `tests/utils/unit/test_exceptions.py:11`; usage at `tests/utils/usage/02_errors.py:9`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: exception construction only.

#### Step `P01-S0009` [X] `FR-UTL-005` - Preserve deterministic code and sanitized detail while never returning a raw provider exception across a boundary.

- **Execution domain:** `Utils`.
- **Execution position:** `9` of `675`.
- **Cannot start before:** Step `P01-S0008` (`FR-UTL-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.2 `errors/` — Shared Errors, Metadata, and Routing > Functional requirements`` (line 257); matrix row `35`.
- **Source responsibility:** Preserve deterministic code and sanitized detail while never returning a raw provider exception across a boundary.
- **Source class / function / method:** `map_exception`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/02_errors.py::example_exception_payload_mapping()`<br>**Unit:** `tests/utils/unit/test_mapping.py::test_map_exception_never_leaks_raw_provider_error()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: safe exception mapping at `app/utils/errors/mapping.py:6`. Test: raw-provider leak prevention at `tests/utils/unit/test_mapping.py:4`; usage at `tests/utils/usage/02_errors.py:21`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: synthetic exception mapping only; no provider call.

#### Step `P01-S0010` [X] `FR-UTL-006` - Require domains to define their own codes and boundary mapping above the shared base hierarchy.

- **Execution domain:** `Utils`.
- **Execution position:** `10` of `675`.
- **Cannot start before:** Step `P01-S0009` (`FR-UTL-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.2 `errors/` — Shared Errors, Metadata, and Routing > Functional requirements`` (line 258); matrix row `36`.
- **Source responsibility:** Require domains to define their own codes and boundary mapping above the shared base hierarchy.
- **Source class / function / method:** Shared exception extension contract
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/02_errors.py::example_exception_extension()`<br>**Unit:** `tests/utils/unit/test_exceptions.py::test_domains_extend_shared_base()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: typed extension seam at `app/utils/errors/exceptions.py:12`. Test: domain extension behavior at `tests/utils/unit/test_exceptions.py:21`; usage at `tests/utils/usage/02_errors.py:35`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: local subclass contract only.

#### Step `P01-S0011` [X] `FR-UTL-034` - Normalize an error code and look up immutable safe metadata without a mutable registry.

- **Execution domain:** `Utils`.
- **Execution position:** `11` of `675`.
- **Cannot start before:** Step `P01-S0010` (`FR-UTL-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.2 `errors/` — Shared Errors, Metadata, and Routing > Functional requirements`` (line 259); matrix row `57`.
- **Source responsibility:** Normalize an error code and look up immutable safe metadata without a mutable registry.
- **Source class / function / method:** `ErrorMetadata`, `normalize_error_code`, `get_error_metadata`
- **Source side effects:** None
- **Source raises:** `ValidationError`: empty or malformed error code
- **Source usage / test:** **Usage:** `tests/utils/usage/02_errors.py::example_error_metadata()`<br>**Unit:** `tests/utils/unit/test_error_metadata.py::test_normalize_and_lookup_error_metadata()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-034`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: `ErrorMetadata` and normalization/lookup at `app/utils/errors/metadata.py:13`. Test: deterministic normalization and lookup at `tests/utils/unit/test_error_metadata.py:12`; usage at `tests/utils/usage/02_errors.py:44`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: immutable in-memory metadata only.

#### Step `P01-S0012` [X] `FR-UTL-035` - Map an exception and synchronously deliver its safe payload to an explicitly injected sink.

- **Execution domain:** `Utils`.
- **Execution position:** `12` of `675`.
- **Cannot start before:** Step `P01-S0011` (`FR-UTL-034`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.2 `errors/` — Shared Errors, Metadata, and Routing > Functional requirements`` (line 260); matrix row `58`.
- **Source responsibility:** Map an exception and synchronously deliver its safe payload to an explicitly injected sink.
- **Source class / function / method:** `ErrorSink`, `route_error_event`
- **Source side effects:** Caller-provided sink invocation
- **Source raises:** Sink exception is propagated
- **Source usage / test:** **Usage:** `tests/utils/usage/02_errors.py::example_route_error_event()`<br>**Unit:** `tests/utils/unit/test_error_routing.py::test_route_error_event_invokes_injected_sink()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-035`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: injected synchronous error routing at `app/utils/errors/routing.py:10`. Test: sink delivery at `tests/utils/unit/test_error_routing.py:7`; usage at `tests/utils/usage/02_errors.py:53`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: injected in-memory sink only.

#### Step `P01-S0013` [X] `P-UTL-003` - identity feature/component (provisional)

- **Execution domain:** `Utils`.
- **Execution position:** `13` of `675`.
- **Cannot start before:** Step `P01-S0012` (`FR-UTL-035`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.3` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-UTL-003`); matrix row `64`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-UTL-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: public identity package at `app/utils/identity/__init__.py:1`. Test: identifier API coverage at `tests/utils/unit/test_identifiers.py:5`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: pure identifier operations only.

#### Step `P01-S0014` [X] `FR-UTL-007` - Generate prefixed UUID4 identifiers without embedded secrets.

- **Execution domain:** `Utils`.
- **Execution position:** `14` of `675`.
- **Cannot start before:** Step `P01-S0013` (`P-UTL-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.3 `identity/` — Trace Identifiers > Functional requirements`` (line 279); matrix row `37`.
- **Source responsibility:** Generate prefixed UUID4 identifiers without embedded secrets.
- **Source class / function / method:** `generate_id`
- **Source side effects:** Entropy read
- **Source raises:** `ValidationError`: unsupported prefix
- **Source usage / test:** **Usage:** `tests/utils/usage/03_identity.py::example_generate_id()`<br>**Unit:** `tests/utils/unit/test_identifiers.py::test_generate_id_is_prefixed_and_secret_free()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: prefixed UUID4 generation at `app/utils/identity/identifiers.py:28`. Test: generation syntax at `tests/utils/unit/test_identifiers.py:5`; usage at `tests/utils/usage/03_identity.py:7`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: local UUID generation only.

#### Step `P01-S0015` [X] `FR-UTL-008` - Validate supported prefixes and canonical identifier syntax.

- **Execution domain:** `Utils`.
- **Execution position:** `15` of `675`.
- **Cannot start before:** Step `P01-S0014` (`FR-UTL-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.3 `identity/` — Trace Identifiers > Functional requirements`` (line 280); matrix row `38`.
- **Source responsibility:** Validate supported prefixes and canonical identifier syntax.
- **Source class / function / method:** `validate_id`
- **Source side effects:** None
- **Source raises:** `ValidationError`: unsupported prefix or malformed identifier
- **Source usage / test:** **Usage:** `tests/utils/usage/03_identity.py::example_validate_id()`<br>**Unit:** `tests/utils/unit/test_identifiers.py::test_validate_id_rejects_malformed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: strict prefix/syntax validation at `app/utils/identity/identifiers.py:44`. Test: supported and rejected identifiers at `tests/utils/unit/test_identifiers.py:11`; usage at `tests/utils/usage/03_identity.py:13`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: fail-closed string validation only.

#### Step `P01-S0016` [X] `FR-UTL-009` - Derive deterministic SHA-256 identifiers from canonical, caller-supplied identity material.

- **Execution domain:** `Utils`.
- **Execution position:** `16` of `675`.
- **Cannot start before:** Step `P01-S0015` (`FR-UTL-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.3 `identity/` — Trace Identifiers > Functional requirements`` (line 281); matrix row `39`.
- **Source responsibility:** Derive deterministic SHA-256 identifiers from canonical, caller-supplied identity material.
- **Source class / function / method:** `derive_stable_id`
- **Source side effects:** None
- **Source raises:** `ValidationError`: empty or non-canonical identity material
- **Source usage / test:** **Usage:** `tests/utils/usage/03_identity.py::example_derive_stable_id()`<br>**Unit:** `tests/utils/unit/test_identifiers.py::test_derive_stable_id_is_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: stable SHA-256 derivation at `app/utils/identity/identifiers.py:77`. Test: repeatable derivation at `tests/utils/unit/test_identifiers.py:20`; usage at `tests/utils/usage/03_identity.py:19`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: caller-supplied in-memory material only.

#### Step `P01-S0017` [X] `P-UTL-004` - time feature/component (provisional)

- **Execution domain:** `Utils`.
- **Execution position:** `17` of `675`.
- **Cannot start before:** Step `P01-S0016` (`FR-UTL-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.4` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-UTL-004`); matrix row `65`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-UTL-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: public time package at `app/utils/time/__init__.py:1`. Test: clock/timestamp API coverage at `tests/utils/unit/test_clocks.py:15` and `tests/utils/unit/test_timestamps.py:14`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: local UTC clock/validation only.

#### Step `P01-S0018` [X] `FR-UTL-010` - Return aware UTC time from an injectable clock.

- **Execution domain:** `Utils`.
- **Execution position:** `18` of `675`.
- **Cannot start before:** Step `P01-S0017` (`P-UTL-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.4 `time/` — UTC Clocks and Timestamps > Functional requirements`` (line 301); matrix row `40`.
- **Source responsibility:** Return aware UTC time from an injectable clock.
- **Source class / function / method:** `Clock`, `SystemClock`, `utc_now`
- **Source side effects:** Clock read
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/04_time.py::example_utc_now()`<br>**Unit:** `tests/utils/unit/test_clocks.py::test_system_clock_returns_aware_utc()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: injectable clock and UTC helper at `app/utils/time/clocks.py:34`. Test: aware system clock at `tests/utils/unit/test_clocks.py:15`; usage at `tests/utils/usage/04_time.py:9`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: local system clock only.

#### Step `P01-S0019` [X] `FR-UTL-011` - Parse and format UTC timestamps using canonical Z output.

- **Execution domain:** `Utils`.
- **Execution position:** `19` of `675`.
- **Cannot start before:** Step `P01-S0018` (`FR-UTL-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.4 `time/` — UTC Clocks and Timestamps > Functional requirements`` (line 302); matrix row `41`.
- **Source responsibility:** Parse and format UTC timestamps using canonical `Z` output.
- **Source class / function / method:** `parse_utc_timestamp`, `format_utc_timestamp`
- **Source side effects:** None
- **Source raises:** `ValidationError`: naive, non-UTC, or malformed timestamp
- **Source usage / test:** **Usage:** `tests/utils/usage/04_time.py::example_parse_format_timestamp()`<br>**Unit:** `tests/utils/unit/test_timestamps.py::test_format_uses_canonical_z_suffix()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: UTC parse/format at `app/utils/time/timestamps.py:26`. Test: canonical `Z` formatting at `tests/utils/unit/test_timestamps.py:14`; usage at `tests/utils/usage/04_time.py:17`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: deterministic timestamp conversion only.

#### Step `P01-S0020` [X] `FR-UTL-012` - Calculate non-negative age and explicit freshness against an injected instant.

- **Execution domain:** `Utils`.
- **Execution position:** `20` of `675`.
- **Cannot start before:** Step `P01-S0019` (`FR-UTL-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.4 `time/` — UTC Clocks and Timestamps > Functional requirements`` (line 303); matrix row `42`.
- **Source responsibility:** Calculate non-negative age and explicit freshness against an injected instant.
- **Source class / function / method:** `age_seconds`, `is_fresh`
- **Source side effects:** None
- **Source raises:** `ValidationError`: naive or invalid reference instant
- **Source usage / test:** **Usage:** `tests/utils/usage/04_time.py::example_age_and_freshness()`<br>**Unit:** `tests/utils/unit/test_timestamps.py::test_age_seconds_is_non_negative()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: non-negative age/freshness at `app/utils/time/timestamps.py:70`. Test: injected-instant age at `tests/utils/unit/test_timestamps.py:21`; usage at `tests/utils/usage/04_time.py:25`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: pure datetime arithmetic only.

#### Step `P01-S0021` [X] `P-UTL-005` - serialization feature/component (provisional)

- **Execution domain:** `Utils`.
- **Execution position:** `21` of `675`.
- **Cannot start before:** Step `P01-S0020` (`FR-UTL-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.5` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-UTL-005`); matrix row `66`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-UTL-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: public serialization package at `app/utils/serialization/__init__.py:1`. Test: JSON-safe/canonical behavior at `tests/utils/unit/test_canonical.py:20`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: in-memory conversion only.

#### Step `P01-S0022` [X] `FR-UTL-013` - Convert supported datetimes, decimals, enums, dataclasses, mappings, and sequences to deterministic JSON-safe values.

- **Execution domain:** `Utils`.
- **Execution position:** `22` of `675`.
- **Cannot start before:** Step `P01-S0021` (`P-UTL-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.5 `serialization/` — Canonical Serialization > Functional requirements`` (line 322); matrix row `43`.
- **Source responsibility:** Convert supported datetimes, decimals, enums, dataclasses, mappings, and sequences to deterministic JSON-safe values.
- **Source class / function / method:** `to_json_safe`
- **Source side effects:** None
- **Source raises:** `ValidationError`: unsupported value type
- **Source usage / test:** **Usage:** `tests/utils/usage/05_serialization.py::example_to_json_safe()`<br>**Unit:** `tests/utils/unit/test_canonical.py::test_to_json_safe_converts_supported_types()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: supported-type conversion at `app/utils/serialization/canonical.py:155`. Test: supported conversion matrix at `tests/utils/unit/test_canonical.py:20`; usage at `tests/utils/usage/05_serialization.py:15`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: deterministic in-memory conversion only.

#### Step `P01-S0023` [X] `FR-UTL-014` - Produce stable UTF-8 JSON with sorted keys and no hidden redaction.

- **Execution domain:** `Utils`.
- **Execution position:** `23` of `675`.
- **Cannot start before:** Step `P01-S0022` (`FR-UTL-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.5 `serialization/` — Canonical Serialization > Functional requirements`` (line 323); matrix row `44`.
- **Source responsibility:** Produce stable UTF-8 JSON with sorted keys and no hidden redaction.
- **Source class / function / method:** `canonical_json`
- **Source side effects:** None
- **Source raises:** `ValidationError`: non-serializable value
- **Source usage / test:** **Usage:** `tests/utils/usage/05_serialization.py::example_canonical_json()`<br>**Unit:** `tests/utils/unit/test_canonical.py::test_canonical_json_sorts_keys()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: stable sorted canonical JSON at `app/utils/serialization/canonical.py:172`. Test: key-order stability at `tests/utils/unit/test_canonical.py:35`; usage at `tests/utils/usage/05_serialization.py:28`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: no hidden redaction or I/O.

#### Step `P01-S0024` [X] `FR-UTL-015` - Reject unsupported, cyclic, non-finite, or unsafe values deterministically.

- **Execution domain:** `Utils`.
- **Execution position:** `24` of `675`.
- **Cannot start before:** Step `P01-S0023` (`FR-UTL-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.5 `serialization/` — Canonical Serialization > Functional requirements`` (line 324); matrix row `45`.
- **Source responsibility:** Reject unsupported, cyclic, non-finite, or unsafe values deterministically.
- **Source class / function / method:** Serialization validation used by `to_json_safe` and `canonical_json`
- **Source side effects:** None
- **Source raises:** `ValidationError`: unsupported, cyclic, or non-finite value
- **Source usage / test:** **Usage:** `tests/utils/usage/05_serialization.py::example_reject_unsafe_value()`<br>**Unit:** `tests/utils/unit/test_canonical.py::test_serialization_rejects_cyclic_value()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: bounded fail-closed traversal at `app/utils/serialization/canonical.py:42`. Test: cyclic rejection at `tests/utils/unit/test_canonical.py:39` plus unsupported/non-finite cases in the same file. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: deterministic validation only.

#### Step `P01-S0025` [X] `FR-UTL-021` - Reject policies that allow protected credential fields.

- **Execution domain:** `Utils`.
- **Execution position:** `25` of `675`.
- **Cannot start before:** Step `P01-S0024` (`FR-UTL-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.6 `security/` — Shared Payload Redaction > Functional requirements`` (line 349); matrix row `46`.
- **Source responsibility:** Reject policies that allow protected credential fields.
- **Source class / function / method:** `RedactionPolicy` validation
- **Source side effects:** None
- **Source raises:** `SecurityError`: policy allows a protected credential field
- **Source usage / test:** **Usage:** `tests/utils/usage/06_security.py::example_policy_validation()`<br>**Unit:** `tests/utils/unit/test_redaction.py::test_policy_rejects_protected_credential_field()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: immutable policy validation at `app/utils/security/redaction.py:47`. Test: protected-field policy rejection at `tests/utils/unit/test_redaction.py:5`; usage at `tests/utils/usage/06_redaction.py:7`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: policy validation only; public Phase 2 redaction APIs remain unexposed.

#### Step `P01-S0026` [X] `FR-UTL-025` - Reserved/unused numbering gap — no implementation required

- **Execution domain:** `Utils`.
- **Execution position:** `26` of `675`.
- **Cannot start before:** Step `P01-S0025` (`FR-UTL-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Reserved ID — no implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Removed`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Appendix R — Reserved / Unused Requirement IDs`` (`FR-UTL-025`); matrix row `48`.
- **Source scope:** Cross-domain
- **Source workflow:** Shared settings bootstrap
- **Source input boundary:** Explicit mapping and environment
- **Source final outcome:** Immutable validated `RuntimeSettings`
- **Source requirement sequence:** `FR-UTL-022` through `FR-UTL-024`
- **Specification control:** Reserved requirement ID: this number is an unused numbering gap that defines no behavior. `WF-UTL-002` is fully realized by `FR-UTL-022`–`FR-UTL-024`. Owner-resolved 2026-07-16 (see `docs/CHANGELOG.md` → Decisions).
- **Implementation instructions:**
  1. No implementation, tests, or evidence are required; this ID defines no behavior.
  2. Mark this step complete and proceed to the next step.
- **Acceptance evidence required:** None — reserved ID.
- **Evidence:** Owner-resolved reserved no-op recorded at `app/utils/README.md:669` and `docs/CHANGELOG.md:18`; acceptance explicitly requires no implementation, test, command, or runtime artifact. Proceeded in ledger order only after `P01-S0025` completed.

#### Step `P01-S0027` [X] `FR-UTL-022` - Define the immutable central settings base and generic runtime/logging settings, including the approved human-readable default logging profile.

- **Execution domain:** `Utils`.
- **Execution position:** `27` of `675`.
- **Cannot start before:** Step `P01-S0026` (`FR-UTL-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.7 `settings/` — Runtime Settings > Functional requirements`` (line 370); matrix row `47`.
- **Source responsibility:** Define the immutable central settings base and generic runtime/logging settings, including the approved human-readable default logging profile.
- **Source class / function / method:** `AppSettings`, `RuntimeSettings`, `LoggingSettings`
- **Source side effects:** `.env`/environment read only when a settings instance is created
- **Source raises:** `ConfigurationError`: invalid generic setting value
- **Source usage / test:** **Usage:** `tests/utils/usage/07_settings.py::example_construct_configuration()`<br>**Unit:** `tests/utils/unit/test_models.py::test_default_logging_profile()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-022`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: frozen extra-forbidding settings models at `app/utils/settings/models.py:25`. Test: approved default logging profile at `tests/utils/unit/test_models.py:8`; usage at `tests/utils/usage/07_settings.py:7`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: explicit construction used; no implicit import-time environment read.

#### Step `P01-S0028` [X] `P-UTL-008` - logging feature/component (provisional)

- **Execution domain:** `Utils`.
- **Execution position:** `28` of `675`.
- **Cannot start before:** Step `P01-S0027` (`FR-UTL-022`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-UTL-008`); matrix row `67`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-UTL-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: import-safe public logging package at `app/utils/logging/__init__.py:1`. Test: no-handler import contract at `tests/utils/unit/test_logger.py:180`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: imports install no handlers, read no environment, and write no files.

#### Step `P01-S0029` [X] `FR-UTL-026` - Return stable child loggers without configuring handlers.

- **Execution domain:** `Utils`.
- **Execution position:** `29` of `675`.
- **Cannot start before:** Step `P01-S0028` (`P-UTL-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 392); matrix row `49`.
- **Source responsibility:** Return stable child loggers without configuring handlers.
- **Source class / function / method:** `get_logger`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::example_logger_access()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_get_logger_configures_no_handlers()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: stable child lookup at `app/utils/logging/logger.py:334`. Test: logger stability at `tests/utils/unit/test_logger.py:42`; usage at `tests/utils/usage/08_logging.py:21`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: lookup does not install handlers.

#### Step `P01-S0030` [X] `FR-UTL-027` - Atomically install deduplicated console and optional bounded rotating-file handlers from the approved default before the first runtime bound-log emission; ex...

- **Execution domain:** `Utils`.
- **Execution position:** `30` of `675`.
- **Cannot start before:** Step `P01-S0029` (`FR-UTL-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 393); matrix row `50`.
- **Source responsibility:** Atomically install deduplicated console and optional bounded rotating-file handlers from the approved default before the first runtime bound-log emission; explicit `configure_logging` replaces the active profile only for a specialized override.
- **Source class / function / method:** `BoundLogger`, `configure_logging`
- **Source side effects:** Logging configuration; directory creation; optional file write on first runtime emission or explicit override
- **Source raises:** `ConfigurationError`: invalid logging settings or sink
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::example_standard_levels()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_first_bound_log_activates_default_profile()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-027`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: locked atomic configuration at `app/utils/logging/logger.py:688`. Test: first-use and idempotency behavior at `tests/utils/unit/test_logger.py:46` and `tests/utils/unit/test_logger.py:170`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: bounded sinks exercised only under pytest temporary directories and shut down.

#### Step `P01-S0031` [X] `FR-UTL-028` - Redact messages and structured context before formatting.

- **Execution domain:** `Utils`.
- **Execution position:** `31` of `675`.
- **Cannot start before:** Step `P01-S0030` (`FR-UTL-027`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 394); matrix row `51`.
- **Source responsibility:** Redact messages and structured context before formatting.
- **Source class / function / method:** `RedactingFilter`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::example_logger_redaction()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_redacting_filter_runs_before_formatting()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: pre-format `RedactingFilter` at `app/utils/logging/logger.py:82` using private redaction mechanics at `app/utils/security/redaction.py:93`. Test: message/context secret removal at `tests/utils/unit/test_logger.py:89`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: synthetic secrets only; no real credentials.

#### Step `P01-S0032` [X] `FR-UTL-029` - Emit JSON or source-aware human-readable records with UTC time, padded level, module, function, line, message, and trace IDs. Human records use YYYY-MM-DD HH...

- **Execution domain:** `Utils`.
- **Execution position:** `32` of `675`.
- **Cannot start before:** Step `P01-S0031` (`FR-UTL-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 395); matrix row `52`.
- **Source responsibility:** Emit JSON or source-aware human-readable records with UTC time, padded level, module, function, line, message, and trace IDs. Human records use `YYYY-MM-DD HH:MM:SS.mmm | LEVEL    | module:function:line - message`; default-console ANSI color is restricted to the level and message content.
- **Source class / function / method:** `StructuredFormatter`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::example_standard_levels()`, `example_bound_context()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_human_formatter_uses_source_aware_layout()`, `test_human_formatter_colors_only_level_and_message()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-029`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: JSON/human `StructuredFormatter` at `app/utils/logging/logger.py:129`. Test: source-aware human layout at `tests/utils/unit/test_logger.py:103` and JSON integration at `tests/utils/integration/test_structured_logging.py:7`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: captured/temp log records only.

#### Step `P01-S0033` [X] `FR-UTL-030` - Surface sink failure through a bounded secret-safe fallback.

- **Execution domain:** `Utils`.
- **Execution position:** `33` of `675`.
- **Cannot start before:** Step `P01-S0032` (`FR-UTL-029`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 396); matrix row `53`.
- **Source responsibility:** Surface sink failure through a bounded secret-safe fallback.
- **Source class / function / method:** Logging failure handling in `configure_logging`
- **Source side effects:** Fixed `logging_configuration_failed` fallback emission to stderr
- **Source raises:** `ConfigurationError`: configured sink cannot be created
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::example_sink_failure()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_sink_failure_uses_safe_fallback()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: fixed safe stderr fallback plus `ConfigurationError` at `app/utils/logging/logger.py:688`. Test: sink-failure contract at `tests/utils/unit/test_logger.py:161`; usage at `tests/utils/usage/08_logging.py:43`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: deliberately invalid pytest temp sink; fallback contains no exception detail or secret.

#### Step `P01-S0034` [X] `FR-UTL-031` - Prevent duplicate handler or queue-listener installation across concurrent first use and repeated explicit configuration calls.

- **Execution domain:** `Utils`.
- **Execution position:** `34` of `675`.
- **Cannot start before:** Step `P01-S0033` (`FR-UTL-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 397); matrix row `54`.
- **Source responsibility:** Prevent duplicate handler or queue-listener installation across concurrent first use and repeated explicit configuration calls.
- **Source class / function / method:** Lazy activation and configuration idempotency
- **Source side effects:** Logging configuration
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::main()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_first_bound_log_is_thread_safe()`, `test_configure_logging_is_idempotent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: process lock and configuration lifecycle at `app/utils/logging/logger.py:45` and `app/utils/logging/logger.py:688`. Test: concurrent first use/idempotency at `tests/utils/unit/test_logger.py:46` and `tests/utils/unit/test_logger.py:170`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: test-owned threads joined and logging shut down.

#### Step `P01-S0035` [X] `FR-UTL-032` - Keep import free of handler registration, environment reads, and filesystem writes.

- **Execution domain:** `Utils`.
- **Execution position:** `35` of `675`.
- **Cannot start before:** Step `P01-S0034` (`FR-UTL-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 398); matrix row `55`.
- **Source responsibility:** Keep import free of handler registration, environment reads, and filesystem writes.
- **Source class / function / method:** Module import contract
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::example_import_safety()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_import_registers_no_handlers()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-032`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: inert module globals at `app/utils/logging/logger.py:45`. Test: import side-effect checks at `tests/utils/unit/test_logger.py:180` and `tests/utils/unit/test_logger.py:194`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: subprocess import created no handlers/directories.

#### Step `P01-S0036` [X] `FR-UTL-033` - Respect the shared LOG_LEVEL setting without redefining domain observability policy.

- **Execution domain:** `Utils`.
- **Execution position:** `36` of `675`.
- **Cannot start before:** Step `P01-S0035` (`FR-UTL-032`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 399); matrix row `56`.
- **Source responsibility:** Respect the shared `LOG_LEVEL` setting without redefining domain observability policy.
- **Source class / function / method:** Logging level application in `configure_logging`
- **Source side effects:** Logging configuration
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::main()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_configure_logging_applies_log_level()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-033`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: settings-driven level application at `app/utils/logging/logger.py:688`. Test: `LOG_LEVEL` behavior at `tests/utils/unit/test_logger.py:220`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: explicit in-memory settings only; no domain policy added.

#### Step `P01-S0037` [X] `FR-UTL-039` - Expose an import-safe global bound logger with standard levels, exception traceback capture, immutable context binding, and automatic approved-default activa...

- **Execution domain:** `Utils`.
- **Execution position:** `37` of `675`.
- **Cannot start before:** Step `P01-S0036` (`FR-UTL-033`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 400); matrix row `59`.
- **Source responsibility:** Expose an import-safe global bound logger with standard levels, exception traceback capture, immutable context binding, and automatic approved-default activation on the first runtime emission. Import-time log attempts remain inert.
- **Source class / function / method:** `BoundLogger`, `logger`
- **Source side effects:** First runtime call may configure logging and create bounded sinks; every runtime call emits a log record
- **Source raises:** `ConfigurationError`: default sink cannot be configured
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::example_standard_levels()`, `example_exception_logging()`, `example_bound_context()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_first_bound_log_activates_default_profile()`, `test_import_time_bound_log_does_not_activate_defaults()`, `test_bound_logger_preserves_context()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-039`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: immutable-context `BoundLogger` and global logger at `app/utils/logging/logger.py:348`. Test: lazy activation/import inertness/context at `tests/utils/unit/test_logger.py:46`, `tests/utils/unit/test_logger.py:194`, and `tests/utils/unit/test_logger.py:225`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: default activation exercised under isolated tests and shut down.

#### Step `P01-S0038` [X] `FR-UTL-040` - Route access-context records to access.log, exact DEBUG records to debug.log, and ERROR-or-higher records to errors.log.

- **Execution domain:** `Utils`.
- **Execution position:** `38` of `675`.
- **Cannot start before:** Step `P01-S0037` (`FR-UTL-039`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 401); matrix row `60`.
- **Source responsibility:** Route access-context records to `access.log`, exact DEBUG records to `debug.log`, and ERROR-or-higher records to `errors.log`.
- **Source class / function / method:** `configure_logging` specialized handlers
- **Source side effects:** Explicit bounded file writes
- **Source raises:** `ConfigurationError`: unavailable directory or file sink
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::example_specialized_routing()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_specialized_log_routing()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-040`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: specialized file-handler routing at `app/utils/logging/logger.py:599`. Test: access/debug/error routing at `tests/utils/unit/test_logger.py:247`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: writes confined to pytest temporary directories; no external sink.

#### Step `P01-S0039` [X] `FR-UTL-041` - Provide the approved lazy default profile: human-readable DEBUG stdout with ANSI color limited to level and message content, data/logs, 10 MB ZIP rotation, t...

- **Execution domain:** `Utils`.
- **Execution position:** `39` of `675`.
- **Cannot start before:** Step `P01-S0038` (`FR-UTL-040`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 4. Module and Requirement Specifications > 4.8 `logging/` — Structured Logging > Functional requirements`` (line 402); matrix row `61`.
- **Source responsibility:** Provide the approved lazy default profile: human-readable DEBUG stdout with ANSI color limited to level and message content, `data/logs`, 10 MB ZIP rotation, ten-day retention, ten backups, queued delivery, automatic process-exit cleanup, optional non-destructive synchronization, and deterministic explicit override/stop.
- **Source class / function / method:** `LoggingSettings`, `BoundLogger`, `configure_logging`, `flush_logging`, `shutdown_logging`
- **Source side effects:** First runtime bound-log emission or explicit override creates the directory, queue thread, and bounded files
- **Source raises:** `ConfigurationError`: invalid logging settings or sink
- **Source usage / test:** **Usage:** `tests/utils/usage/08_logging.py::main()`<br>**Unit:** `tests/utils/unit/test_logger.py::test_first_bound_log_activates_default_profile()`, `test_explicit_configuration_is_not_replaced_by_lazy_default()`, `test_human_formatter_colors_only_level_and_message()`, `test_flush_logging_synchronizes_delivery_without_shutdown()`, `test_zip_rollover_and_shutdown()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-UTL-041`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: approved defaults at `app/utils/settings/models.py:63`, flush/shutdown at `app/utils/logging/logger.py:665`, and configuration at `app/utils/logging/logger.py:688`. Test: flush at `tests/utils/unit/test_logger.py:288` and ZIP rollover/shutdown at `tests/utils/unit/test_logger.py:301`. Commands/results: targeted gate -> 76 passed, 87.39% coverage; Ruff/MyPy passed. Runtime evidence: bounded temp files/queue lifecycle only.

#### Step `P01-S0040` [X] `WF-UTL-001` - Structured Logging and Redaction

- **Execution domain:** `Utils`.
- **Execution position:** `40` of `675`.
- **Cannot start before:** Step `P01-S0039` (`FR-UTL-041`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Utils` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/utils/README.md` - ``Utils > 3. Workflows`` (line 168); matrix row `68`.
- **Source scope:** Cross-domain
- **Source workflow:** Structured logging and redaction
- **Source input boundary:** Domain log record and explicit context
- **Source final outcome:** Redacted structured record reaches the configured sink
- **Source requirement sequence:** `FR-UTL-026` through `FR-UTL-033`, `FR-UTL-039` through `FR-UTL-041`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-UTL-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Implementation: domain record -> `BoundLogger` -> `RedactingFilter` -> structured sink at `app/utils/logging/logger.py:348`, `app/utils/logging/logger.py:82`, and `app/utils/logging/logger.py:688`. Tests: end-to-end redacted structured sink at `tests/utils/integration/test_structured_logging.py:7`; semantic documentation audit at `tests/utils/unit/test_docstrings.py:167`. Commands/results: final targeted gate -> 77 passed, 87.39% coverage; Ruff, Google docstyle, formatting, and MyPy passed. Runtime evidence: isolated local temp sink only; no network, provider, broker, or live side effect.

### Stage 3 - Brokers

Implement `Brokers` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0041` [ ] `P-BRK-001` - contracts feature/component (provisional)

- **Execution domain:** `Brokers`.
- **Execution position:** `41` of `675`.
- **Cannot start before:** Step `P01-S0040` (`WF-UTL-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-BRK-001`); matrix row `204`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-BRK-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0042` [ ] `P-BRK-004` - mt5 feature/component (provisional)

- **Execution domain:** `Brokers`.
- **Execution position:** `42` of `675`.
- **Cannot start before:** Step `P01-S0041` (`P-BRK-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.3` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-BRK-004`); matrix row `206`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-BRK-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0043` [ ] `P-BRK-003` - registry feature/component (provisional)

- **Execution domain:** `Brokers`.
- **Execution position:** `43` of `675`.
- **Cannot start before:** Step `P01-S0042` (`P-BRK-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.8` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-BRK-003`); matrix row `205`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-BRK-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0044` [ ] `FR-BRK-110` - contracts/unsupported.py

- **Execution domain:** `Brokers`.
- **Execution position:** `44` of `675`.
- **Cannot start before:** Step `P01-S0043` (`P-BRK-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > 4.10 Private Helper and Export Requirements`` (line 1321); matrix row `195`.
- **Source assigned file:** `contracts/unsupported.py`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-110`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0045` [ ] `FR-BRK-113` - contracts/__init__.py

- **Execution domain:** `Brokers`.
- **Execution position:** `45` of `675`.
- **Cannot start before:** Step `P01-S0044` (`FR-BRK-110`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > 4.10 Private Helper and Export Requirements`` (line 1322); matrix row `198`.
- **Source assigned file:** `contracts/__init__.py`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-113`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0046` [ ] `FR-BRK-116` - mt5/transport.py

- **Execution domain:** `Brokers`.
- **Execution position:** `46` of `675`.
- **Cannot start before:** Step `P01-S0045` (`FR-BRK-113`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > 4.10 Private Helper and Export Requirements`` (line 1323); matrix row `200`.
- **Source assigned file:** `mt5/transport.py`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-116`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0047` [ ] `FR-BRK-117` - mt5/mapping.py

- **Execution domain:** `Brokers`.
- **Execution position:** `47` of `675`.
- **Cannot start before:** Step `P01-S0046` (`FR-BRK-116`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > 4.10 Private Helper and Export Requirements`` (line 1324); matrix row `201`.
- **Source assigned file:** `mt5/mapping.py`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-117`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0048` [ ] `FR-BRK-118` - mt5/__init__.py

- **Execution domain:** `Brokers`.
- **Execution position:** `48` of `675`.
- **Cannot start before:** Step `P01-S0047` (`FR-BRK-117`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > 4.10 Private Helper and Export Requirements`` (line 1325); matrix row `202`.
- **Source assigned file:** `mt5/__init__.py`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-118`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0049` [ ] `FR-BRK-133` - registry/__init__.py

- **Execution domain:** `Brokers`.
- **Execution position:** `49` of `675`.
- **Cannot start before:** Step `P01-S0048` (`FR-BRK-118`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > 4.10 Private Helper and Export Requirements`` (line 1340); matrix row `203`.
- **Source assigned file:** `registry/__init__.py`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-133`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0050` [ ] `FR-BRK-001` - identify MT5, cTrader, Binance Spot, Binance USD-M Futures, Binance Coin-M Futures, Dukascopy, and Yahoo without aliases or implicit fallback.

- **Execution domain:** `Brokers`.
- **Execution position:** `50` of `675`.
- **Cannot start before:** Step `P01-S0049` (`FR-BRK-133`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `enums.py` — Stable Canonical Values`` (line 663); matrix row `90`.
- **Source responsibility:** The system shall identify MT5, cTrader, Binance Spot, Binance USD-M Futures, Binance Coin-M Futures, Dukascopy, and Yahoo without aliases or implicit fallback.
- **Source class / function / method:** `class BrokerId(str, Enum)`
- **Source side effects:** None
- **Source raises:** `ValueError`: identifier is not registered.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_enums_broker_id()`<br>**Unit:** `tests/brokers/unit/test_enums.py::test_broker_id_has_exact_profiles()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0051` [ ] `FR-BRK-002` - require an explicit LIVE, DEMO, TESTNET, or SANDBOX environment and shall define no implicit live default.

- **Execution domain:** `Brokers`.
- **Execution position:** `51` of `675`.
- **Cannot start before:** Step `P01-S0050` (`FR-BRK-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `enums.py` — Stable Canonical Values`` (line 664); matrix row `91`.
- **Source responsibility:** The system shall require an explicit `LIVE`, `DEMO`, `TESTNET`, or `SANDBOX` environment and shall define no implicit live default.
- **Source class / function / method:** `class BrokerEnvironment(str, Enum)`
- **Source side effects:** None
- **Source raises:** `ValueError`: environment is unknown.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_enums_environment()`<br>**Unit:** `tests/brokers/unit/test_enums.py::test_environment_has_no_live_default()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0052` [ ] `FR-BRK-003` - expose the minimal validated lifecycle states DISCONNECTED, CONNECTING, READY, DEGRADED, CLOSING, and FAILED.

- **Execution domain:** `Brokers`.
- **Execution position:** `52` of `675`.
- **Cannot start before:** Step `P01-S0051` (`FR-BRK-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `enums.py` — Stable Canonical Values`` (line 665); matrix row `92`.
- **Source responsibility:** The system shall expose the minimal validated lifecycle states `DISCONNECTED`, `CONNECTING`, `READY`, `DEGRADED`, `CLOSING`, and `FAILED`.
- **Source class / function / method:** `class BrokerConnectionState(str, Enum)`
- **Source side effects:** None
- **Source raises:** `ValueError`: state is unknown.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_enums_connection_state()`<br>**Unit:** `tests/brokers/unit/test_enums.py::test_connection_states_match_reconciliation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0053` [ ] `FR-BRK-004` - expose the stable accepted BROKER_* error taxonomy and shall add codes only with an accepted operation.

- **Execution domain:** `Brokers`.
- **Execution position:** `53` of `675`.
- **Cannot start before:** Step `P01-S0052` (`FR-BRK-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `enums.py` — Stable Canonical Values`` (line 666); matrix row `93`.
- **Source responsibility:** The system shall expose the stable accepted `BROKER_*` error taxonomy and shall add codes only with an accepted operation.
- **Source class / function / method:** `class BrokerErrorCode(str, Enum)`
- **Source side effects:** None
- **Source raises:** `ValueError`: code is not registered.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_enums_error_code()`<br>**Unit:** `tests/brokers/unit/test_enums.py::test_error_codes_cover_accepted_failures()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0054` [ ] `FR-BRK-005` - provide one identifier for every accepted canonical adapter operation so capability reports cannot omit unsupported entries.

- **Execution domain:** `Brokers`.
- **Execution position:** `54` of `675`.
- **Cannot start before:** Step `P01-S0053` (`FR-BRK-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `enums.py` — Stable Canonical Values`` (line 667); matrix row `94`.
- **Source responsibility:** The system shall provide one identifier for every accepted canonical adapter operation so capability reports cannot omit unsupported entries.
- **Source class / function / method:** `class BrokerCapabilityId(str, Enum)`
- **Source side effects:** None
- **Source raises:** `ValueError`: capability is unknown.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_enums_capability_id()`<br>**Unit:** `tests/brokers/unit/test_enums.py::test_capabilities_match_protocol_methods()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0055` [ ] `FR-BRK-006` - carry immutable provider/profile, environment, composition-root-derived provider enablement, account reference, resolved in-memory credentia...

- **Execution domain:** `Brokers`.
- **Execution position:** `55` of `675`.
- **Cannot start before:** Step `P01-S0054` (`FR-BRK-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 693); matrix row `95`.
- **Source responsibility:** The system shall carry immutable provider/profile, environment, composition-root-derived provider enablement, account reference, resolved in-memory credential mapping, timeout, reconnect, circuit, buffer, and auto-connect configuration without accepting secret references or persisting secrets.
- **Source class / function / method:** `class BrokerConnectionConfig`
- **Source side effects:** None
- **Source raises:** `ValueError`: required identity/environment is absent or a numeric limit is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_connection_config()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_connection_config_is_immutable_and_explicit()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0056` [ ] `FR-BRK-007` - represent code, message, retryability, redacted provider evidence, capability, and diagnostic details for an operational failure.

- **Execution domain:** `Brokers`.
- **Execution position:** `56` of `675`.
- **Cannot start before:** Step `P01-S0055` (`FR-BRK-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 694); matrix row `96`.
- **Source responsibility:** The system shall represent code, message, retryability, redacted provider evidence, capability, and diagnostic details for an operational failure.
- **Source class / function / method:** `class BrokerError`
- **Source side effects:** None
- **Source raises:** `ValueError`: code/message is absent or details contain forbidden secrets.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_error()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_error_is_redacted_and_structured()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0057` [ ] `FR-BRK-008` - Every public operation shall return one versioned status/broker/operation/request/time/data/error/provider-metadata/latency envelope. Success has no error an...

- **Execution domain:** `Brokers`.
- **Execution position:** `57` of `675`.
- **Cannot start before:** Step `P01-S0056` (`FR-BRK-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 695); matrix row `97`.
- **Source responsibility:** Every public operation shall return one versioned status/broker/operation/request/time/data/error/provider-metadata/latency envelope. Success has no error and may carry `data=None` for `BrokerResult[None]`; error has a non-null error and null data.
- **Source class / function / method:** `class BrokerResult[T]`
- **Source side effects:** None
- **Source raises:** `ValueError`: the status/error/data invariants are inconsistent.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_result()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_result_supports_successful_none_and_exclusive_error()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0058` [ ] `FR-BRK-009` - List and history operations shall return bounded records with provider cursor and explicit truncation metadata.

- **Execution domain:** `Brokers`.
- **Execution position:** `58` of `675`.
- **Cannot start before:** Step `P01-S0057` (`FR-BRK-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 696); matrix row `98`.
- **Source responsibility:** List and history operations shall return bounded records with provider cursor and explicit truncation metadata.
- **Source class / function / method:** `class BrokerPage[T]`
- **Source side effects:** None
- **Source raises:** `ValueError`: page limit/count is negative or truncation metadata conflicts.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_page()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_page_exposes_cursor_and_truncation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0059` [ ] `FR-BRK-010` - Each capability shall report implementation, availability, access, requirement, verification, evidence references, release approval, reason, and execution mo...

- **Execution domain:** `Brokers`.
- **Execution position:** `59` of `675`.
- **Cannot start before:** Step `P01-S0058` (`FR-BRK-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 697); matrix row `99`.
- **Source responsibility:** Each capability shall report implementation, availability, access, requirement, verification, evidence references, release approval, reason, and execution model from one declaration source; a write capability is `AVAILABLE` only after the shared contract suite, provider sandbox/testnet execution, rejection and unknown-outcome tests, authenticated permission verification, and explicit Owner approval all pass.
- **Source class / function / method:** `class BrokerCapability`
- **Source side effects:** None
- **Source raises:** `ValueError`: capability dimensions are incomplete, evidence/approval is missing for an available write, or fields are inconsistent.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_capability()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_capability_requires_write_release_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0060` [ ] `FR-BRK-011` - return every catalogue entry for one provider/profile/account, including unsupported and untested operations, and shall keep every unapprove...

- **Execution domain:** `Brokers`.
- **Execution position:** `60` of `675`.
- **Cannot start before:** Step `P01-S0059` (`FR-BRK-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 698); matrix row `100`.
- **Source responsibility:** The system shall return every catalogue entry for one provider/profile/account, including unsupported and untested operations, and shall keep every unapproved write capability unavailable.
- **Source class / function / method:** `class BrokerFeatureFlags`
- **Source side effects:** None
- **Source raises:** `ValueError`: catalogue entries are missing/duplicated or an unapproved write is available.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_feature_flags()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_feature_flags_fail_closed_for_unapproved_writes()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0061` [ ] `FR-BRK-012` - distinguish transport, authentication, account authorization, trading permission, subscription readiness, environment, and lifecycle state.

- **Execution domain:** `Brokers`.
- **Execution position:** `61` of `675`.
- **Cannot start before:** Step `P01-S0060` (`FR-BRK-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 699); matrix row `101`.
- **Source responsibility:** The system shall distinguish transport, authentication, account authorization, trading permission, subscription readiness, environment, and lifecycle state.
- **Source class / function / method:** `class BrokerConnectionStatus`
- **Source side effects:** None
- **Source raises:** `ValueError`: status dimensions conflict with lifecycle state.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_connection_status()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_connection_status_is_not_boolean_only()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0062` [ ] `FR-BRK-013` - Every lifecycle transition shall expose previous/new state, reason, UTC time, session generation, optional reconnect attempt, and resync requirement.

- **Execution domain:** `Brokers`.
- **Execution position:** `62` of `675`.
- **Cannot start before:** Step `P01-S0061` (`FR-BRK-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 700); matrix row `102`.
- **Source responsibility:** Every lifecycle transition shall expose previous/new state, reason, UTC time, session generation, optional reconnect attempt, and resync requirement.
- **Source class / function / method:** `class BrokerConnectionEvent`
- **Source side effects:** None
- **Source raises:** `ValueError`: transition or UTC/session evidence is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_connection_event()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_connection_event_records_transition()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0063` [ ] `FR-BRK-014` - expose provider, API/terminal version, endpoint metadata, immutable profile, and environment without secrets.

- **Execution domain:** `Brokers`.
- **Execution position:** `63` of `675`.
- **Cannot start before:** Step `P01-S0062` (`FR-BRK-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 701); matrix row `103`.
- **Source responsibility:** The system shall expose provider, API/terminal version, endpoint metadata, immutable profile, and environment without secrets.
- **Source class / function / method:** `class BrokerPlatformInfo`
- **Source side effects:** None
- **Source raises:** `ValueError`: provider/environment identity is absent.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_platform_info()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_platform_info_is_redacted()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0064` [ ] `FR-BRK-015` - expose only permissions reported for the authenticated provider session.

- **Execution domain:** `Brokers`.
- **Execution position:** `64` of `675`.
- **Cannot start before:** Step `P01-S0063` (`FR-BRK-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 702); matrix row `104`.
- **Source responsibility:** The system shall expose only permissions reported for the authenticated provider session.
- **Source class / function / method:** `class BrokerPermissions`
- **Source side effects:** None
- **Source raises:** `ValueError`: an unknown permission is represented as granted.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_permissions()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_permissions_preserve_unknown()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0065` [ ] `FR-BRK-016` - preserve provider account identity, currency, balances, equity, margin, status, and provider/retrieval timestamps without certifying freshness.

- **Execution domain:** `Brokers`.
- **Execution position:** `65` of `675`.
- **Cannot start before:** Step `P01-S0064` (`FR-BRK-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 703); matrix row `105`.
- **Source responsibility:** The system shall preserve provider account identity, currency, balances, equity, margin, status, and provider/retrieval timestamps without certifying freshness.
- **Source class / function / method:** `class BrokerAccountInfo`
- **Source side effects:** None
- **Source raises:** `ValueError`: mandatory provider identity/time or decimal is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_account_info()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_account_info_preserves_provider_truth()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0066` [ ] `FR-BRK-017` - represent provider-reported asset/currency balance values with exact decimals and explicit units.

- **Execution domain:** `Brokers`.
- **Execution position:** `66` of `675`.
- **Cannot start before:** Step `P01-S0065` (`FR-BRK-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 704); matrix row `106`.
- **Source responsibility:** The system shall represent provider-reported asset/currency balance values with exact decimals and explicit units.
- **Source class / function / method:** `class BrokerBalance`
- **Source side effects:** None
- **Source raises:** `ValueError`: asset/unit is absent or mandatory value is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_balance()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_balance_uses_decimal_and_unit()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0067` [ ] `FR-BRK-018` - represent provider asset/currency metadata without canonical identity policy or currency conversion.

- **Execution domain:** `Brokers`.
- **Execution position:** `67` of `675`.
- **Cannot start before:** Step `P01-S0066` (`FR-BRK-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 705); matrix row `107`.
- **Source responsibility:** The system shall represent provider asset/currency metadata without canonical identity policy or currency conversion.
- **Source class / function / method:** `class BrokerAssetInfo`
- **Source side effects:** None
- **Source raises:** `ValueError`: provider asset identifier is absent.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_asset_info()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_asset_info_is_structural_only()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0068` [ ] `FR-BRK-019` - preserve the exact provider-native symbol identifier, specifications, sessions, units, and trading flags without canonical identity, friendl...

- **Execution domain:** `Brokers`.
- **Execution position:** `68` of `675`.
- **Cannot start before:** Step `P01-S0067` (`FR-BRK-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 706); matrix row `108`.
- **Source responsibility:** The system shall preserve the exact provider-native symbol identifier, specifications, sessions, units, and trading flags without canonical identity, friendly names, or aliases; Data performs identity-to-provider-symbol conversion before the call.
- **Source class / function / method:** `class BrokerSymbolInfo`
- **Source side effects:** None
- **Source raises:** `ValueError`: provider symbol or required unit metadata is absent.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_symbol_info()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_symbol_info_contains_only_provider_native_identity()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0069` [ ] `FR-BRK-020` - represent provider-reported open, closed, halted, or unknown market state.

- **Execution domain:** `Brokers`.
- **Execution position:** `69` of `675`.
- **Cannot start before:** Step `P01-S0068` (`FR-BRK-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 707); matrix row `109`.
- **Source responsibility:** The system shall represent provider-reported open, closed, halted, or unknown market state.
- **Source class / function / method:** `class BrokerMarketStatus`
- **Source side effects:** None
- **Source raises:** `ValueError`: status lacks provider symbol/time evidence.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_market_status()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_market_status_allows_unknown()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0070` [ ] `FR-BRK-021` - represent provider-supplied trading windows as timezone-aware UTC intervals with native metadata retained.

- **Execution domain:** `Brokers`.
- **Execution position:** `70` of `675`.
- **Cannot start before:** Step `P01-S0069` (`FR-BRK-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 708); matrix row `110`.
- **Source responsibility:** The system shall represent provider-supplied trading windows as timezone-aware UTC intervals with native metadata retained.
- **Source class / function / method:** `class BrokerTradingSession`
- **Source side effects:** None
- **Source raises:** `ValueError`: interval is unordered or timezone-naive.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_trading_session()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_trading_session_is_utc()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0071` [ ] `FR-BRK-022` - expose only genuine bid/ask/last values with exact decimals, nullable missing fields, explicit units, and provider/retrieval times.

- **Execution domain:** `Brokers`.
- **Execution position:** `71` of `675`.
- **Cannot start before:** Step `P01-S0070` (`FR-BRK-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 709); matrix row `111`.
- **Source responsibility:** The system shall expose only genuine bid/ask/last values with exact decimals, nullable missing fields, explicit units, and provider/retrieval times.
- **Source class / function / method:** `class BrokerQuote`
- **Source side effects:** None
- **Source raises:** `ValueError`: no genuine price exists or mandatory price/time is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_quote()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_quote_never_fabricates_price()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-022`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0072` [ ] `FR-BRK-023` - preserve provider sequence, event/receipt time, nullable bid/ask/last and quantities, and tick type without invented sequence evidence.

- **Execution domain:** `Brokers`.
- **Execution position:** `72` of `675`.
- **Cannot start before:** Step `P01-S0071` (`FR-BRK-022`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 710); matrix row `112`.
- **Source responsibility:** The system shall preserve provider sequence, event/receipt time, nullable bid/ask/last and quantities, and tick type without invented sequence evidence.
- **Source class / function / method:** `class BrokerTick`
- **Source side effects:** None
- **Source raises:** `ValueError`: mandatory event/receipt evidence is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_tick()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_tick_preserves_optional_values()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0073` [ ] `FR-BRK-024` - preserve UTC open/close time, closed state, trade/tick volume distinctions, and native/requested timeframe while storing conversion evidence...

- **Execution domain:** `Brokers`.
- **Execution position:** `73` of `675`.
- **Cannot start before:** Step `P01-S0072` (`FR-BRK-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 711); matrix row `113`.
- **Source responsibility:** The system shall preserve UTC open/close time, closed state, trade/tick volume distinctions, and native/requested timeframe while storing conversion evidence once in page metadata.
- **Source class / function / method:** `class BrokerBar`
- **Source side effects:** None
- **Source raises:** `ValueError`: OHLC/time ordering or mandatory decimals are invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_bar()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_bar_has_explicit_time_and_volume_semantics()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0074` [ ] `FR-BRK-025` - represent order-book snapshot/delta state, levels, provider sequence/checksum, depth truncation, and resnapshot requirement without invented...

- **Execution domain:** `Brokers`.
- **Execution position:** `74` of `675`.
- **Cannot start before:** Step `P01-S0073` (`FR-BRK-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 712); matrix row `114`.
- **Source responsibility:** The system shall represent order-book snapshot/delta state, levels, provider sequence/checksum, depth truncation, and resnapshot requirement without invented sequence IDs.
- **Source class / function / method:** `class BrokerOrderBook`
- **Source side effects:** None
- **Source raises:** `ValueError`: levels or supplied sequence range is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_order_book()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_order_book_exposes_resnapshot_state()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0075` [ ] `FR-BRK-026` - represent immutable metadata for one adapter-scoped bounded subscription, including capability, exact provider-native symbols, creation time...

- **Execution domain:** `Brokers`.
- **Execution position:** `75` of `675`.
- **Cannot start before:** Step `P01-S0074` (`FR-BRK-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 713); matrix row `115`.
- **Source responsibility:** The system shall represent immutable metadata for one adapter-scoped bounded subscription, including capability, exact provider-native symbols, creation time, delivery sequence, active state, and resync state.
- **Source class / function / method:** `class BrokerSubscriptionInfo`
- **Source side effects:** None
- **Source raises:** `ValueError`: subscription ID or bounded-buffer evidence is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_subscription_info()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_subscription_info_is_adapter_scoped()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0076` [ ] `FR-BRK-027` - preserve provider position ID, symbol, side, exact quantities/prices/P&L fields, partial state, and timestamps.

- **Execution domain:** `Brokers`.
- **Execution position:** `76` of `675`.
- **Cannot start before:** Step `P01-S0075` (`FR-BRK-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 714); matrix row `116`.
- **Source responsibility:** The system shall preserve provider position ID, symbol, side, exact quantities/prices/P&L fields, partial state, and timestamps.
- **Source class / function / method:** `class BrokerPosition`
- **Source side effects:** None
- **Source raises:** `ValueError`: mandatory provider identity/quantity/time is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_position()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_position_preserves_provider_profit()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-027`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0077` [ ] `FR-BRK-028` - express structural order filters only, without selection policy or unbounded history.

- **Execution domain:** `Brokers`.
- **Execution position:** `77` of `675`.
- **Cannot start before:** Step `P01-S0076` (`FR-BRK-027`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 715); matrix row `117`.
- **Source responsibility:** The system shall express structural order filters only, without selection policy or unbounded history.
- **Source class / function / method:** `class BrokerOrderFilter`
- **Source side effects:** None
- **Source raises:** `ValueError`: date interval is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_order_filter()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_order_filter_is_structural()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0078` [ ] `FR-BRK-029` - express structural position filters only.

- **Execution domain:** `Brokers`.
- **Execution position:** `78` of `675`.
- **Cannot start before:** Step `P01-S0077` (`FR-BRK-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 716); matrix row `118`.
- **Source responsibility:** The system shall express structural position filters only.
- **Source class / function / method:** `class BrokerPositionFilter`
- **Source side effects:** None
- **Source raises:** `ValueError`: supplied filter value is structurally invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_position_filter()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_position_filter_is_structural()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-029`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0079` [ ] `FR-BRK-030` - preserve provider order IDs, caller IDs, product-applicable fields, exact quantity/unit, partial state, prices, and timestamps without fabri...

- **Execution domain:** `Brokers`.
- **Execution position:** `79` of `675`.
- **Cannot start before:** Step `P01-S0078` (`FR-BRK-029`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 717); matrix row `119`.
- **Source responsibility:** The system shall preserve provider order IDs, caller IDs, product-applicable fields, exact quantity/unit, partial state, prices, and timestamps without fabricating acceptance.
- **Source class / function / method:** `class BrokerOrder`
- **Source side effects:** None
- **Source raises:** `ValueError`: mandatory identity/state/quantity evidence is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_order()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_order_preserves_partial_state_and_ids()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0080` [ ] `FR-BRK-031` - preserve provider deal/fill ID, order reference, exact quantity/price/fee, partial state, and timestamps.

- **Execution domain:** `Brokers`.
- **Execution position:** `80` of `675`.
- **Cannot start before:** Step `P01-S0079` (`FR-BRK-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 718); matrix row `120`.
- **Source responsibility:** The system shall preserve provider deal/fill ID, order reference, exact quantity/price/fee, partial state, and timestamps.
- **Source class / function / method:** `class BrokerDeal`
- **Source side effects:** None
- **Source raises:** `ValueError`: mandatory provider deal evidence is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_deal()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_deal_never_invents_fill()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0081` [ ] `FR-BRK-032` - represent provider-reported deposits, withdrawals, fees, swaps, and account transactions with exact values and units.

- **Execution domain:** `Brokers`.
- **Execution position:** `81` of `675`.
- **Cannot start before:** Step `P01-S0080` (`FR-BRK-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 719); matrix row `121`.
- **Source responsibility:** The system shall represent provider-reported deposits, withdrawals, fees, swaps, and account transactions with exact values and units.
- **Source class / function / method:** `class BrokerAccountTransaction`
- **Source side effects:** None
- **Source raises:** `ValueError`: transaction identity/value/time is invalid.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_account_transaction()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_account_transaction_preserves_type()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-032`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0082` [ ] `FR-BRK-033` - require one complete V1 order request with the exact side, order type, positive finite quantity/unit, applicable finite prices, approved tim...

- **Execution domain:** `Brokers`.
- **Execution position:** `82` of `675`.
- **Cannot start before:** Step `P01-S0081` (`FR-BRK-032`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 720); matrix row `122`.
- **Source responsibility:** The system shall require one complete V1 order request with the exact side, order type, positive finite quantity/unit, applicable finite prices, approved time-in-force/UTC expiration, non-negative deviation points/magic, account/environment binding, and caller identifiers listed in the field manifest; it shall infer nothing and shall expose no untyped or Futures-only product-field mapping.
- **Source class / function / method:** `class BrokerOrderRequest`
- **Source side effects:** None
- **Source raises:** `ValueError`: a required field is absent, a value is invalid, or a field conflicts with the V1 manifest.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_order_request()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_order_request_does_not_infer_fields()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-033`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0083` [ ] `FR-BRK-034` - identify exactly one provider order and only caller-supplied modifications.

- **Execution domain:** `Brokers`.
- **Execution position:** `83` of `675`.
- **Cannot start before:** Step `P01-S0082` (`FR-BRK-033`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 721); matrix row `123`.
- **Source source context:** | Missing | `FR-BRK-034` | The system shall identify exactly one provider order and only caller-supplied modifications. | `class BrokerOrderModificationRequest` | None | `ValueError`: target ID is absent or no modification is supplied. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_order_modification()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_order_modification_has_one_target()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-034`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0084` [ ] `FR-BRK-035` - distinguish provider validation/preview from final order acceptance.

- **Execution domain:** `Brokers`.
- **Execution position:** `84` of `675`.
- **Cannot start before:** Step `P01-S0083` (`FR-BRK-034`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 722); matrix row `124`.
- **Source source context:** | Missing | `FR-BRK-035` | The system shall distinguish provider validation/preview from final order acceptance. | `class BrokerOrderCheck` | None | `ValueError`: provider check evidence is inconsistent. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_order_check()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_order_check_is_not_acceptance()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-035`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0085` [ ] `FR-BRK-036` - represent explicit provider acknowledgement, rejection, unknown outcome, partial fill, and provider identifiers without synthetic success.

- **Execution domain:** `Brokers`.
- **Execution position:** `85` of `675`.
- **Cannot start before:** Step `P01-S0084` (`FR-BRK-035`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 723); matrix row `125`.
- **Source source context:** | Missing | `FR-BRK-036` | The system shall represent explicit provider acknowledgement, rejection, unknown outcome, partial fill, and provider identifiers without synthetic success. | `class BrokerOrderResult` | None | `ValueError`: success lacks acknowledgement or identifiers are fabricated/inconsistent. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_order_result()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_order_result_requires_acknowledgement()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-036`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0086` [ ] `FR-BRK-037` - identify one position and only provider-supported caller-supplied stop/take-profit modifications.

- **Execution domain:** `Brokers`.
- **Execution position:** `86` of `675`.
- **Cannot start before:** Step `P01-S0085` (`FR-BRK-036`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 724); matrix row `126`.
- **Source source context:** | Missing | `FR-BRK-037` | The system shall identify one position and only provider-supported caller-supplied stop/take-profit modifications. | `class BrokerPositionModificationRequest` | None | `ValueError`: target ID is absent or fields are structurally invalid. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_position_modification()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_position_modification_has_one_target()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-037`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0087` [ ] `FR-BRK-038` - identify one position and exact caller-supplied close/reduce quantity and unit.

- **Execution domain:** `Brokers`.
- **Execution position:** `87` of `675`.
- **Cannot start before:** Step `P01-S0086` (`FR-BRK-037`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 725); matrix row `127`.
- **Source source context:** | Missing | `FR-BRK-038` | The system shall identify one position and exact caller-supplied close/reduce quantity and unit. | `class BrokerPositionCloseRequest` | None | `ValueError`: target or positive quantity/unit is absent. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_position_close()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_position_close_has_one_target()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-038`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0088` [ ] `FR-BRK-039` - carry only fields required for a provider-native margin request.

- **Execution domain:** `Brokers`.
- **Execution position:** `88` of `675`.
- **Cannot start before:** Step `P01-S0087` (`FR-BRK-038`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 726); matrix row `128`.
- **Source source context:** | Missing | `FR-BRK-039` | The system shall carry only fields required for a provider-native margin request. | `class BrokerMarginRequest` | None | `ValueError`: provider-required structural input is absent. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_margin_request()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_margin_request_is_provider_native()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-039`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0089` [ ] `FR-BRK-040` - carry only fields required for a provider-native profit request, including explicit open/close prices and units.

- **Execution domain:** `Brokers`.
- **Execution position:** `89` of `675`.
- **Cannot start before:** Step `P01-S0088` (`FR-BRK-039`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 727); matrix row `129`.
- **Source source context:** | Missing | `FR-BRK-040` | The system shall carry only fields required for a provider-native profit request, including explicit open/close prices and units. | `class BrokerProfitRequest` | None | `ValueError`: provider-required structural input is absent. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_profit_request()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_profit_request_has_explicit_prices()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-040`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0090` [ ] `FR-BRK-041` - represent a provider-native fee/commission estimate with exact value, currency/unit, and provider evidence.

- **Execution domain:** `Brokers`.
- **Execution position:** `90` of `675`.
- **Cannot start before:** Step `P01-S0089` (`FR-BRK-040`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 728); matrix row `130`.
- **Source source context:** | Missing | `FR-BRK-041` | The system shall represent a provider-native fee/commission estimate with exact value, currency/unit, and provider evidence. | `class BrokerFeeEstimate` | None | `ValueError`: amount or unit evidence is invalid. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_fee_estimate()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_fee_estimate_is_not_local_formula()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-041`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0091` [ ] `FR-BRK-042` - expose provider time, local send/receive UTC times, estimated offset, and round-trip latency without silently correcting business timestamps.

- **Execution domain:** `Brokers`.
- **Execution position:** `91` of `675`.
- **Cannot start before:** Step `P01-S0090` (`FR-BRK-041`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `models.py` — Canonical DTOs and Results`` (line 729); matrix row `131`.
- **Source source context:** | Missing | `FR-BRK-042` | The system shall expose provider time, local send/receive UTC times, estimated offset, and round-trip latency without silently correcting business timestamps. | `class BrokerServerTime` | None | `ValueError`: timestamps are timezone-naive/unordered or latency is negative. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_models_server_time()`<br>**Unit:** `tests/brokers/unit/test_models.py::test_server_time_exposes_clock_evidence()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-042`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0092` [ ] `FR-BRK-043` - define the genuine market-data and subscription read surface independently of execution capabilities.

- **Execution domain:** `Brokers`.
- **Execution position:** `92` of `675`.
- **Cannot start before:** Step `P01-S0091` (`FR-BRK-042`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 744); matrix row `132`.
- **Source responsibility:** The system shall define the genuine market-data and subscription read surface independently of execution capabilities.
- **Source class / function / method:** `class MarketDataProvider(Protocol)`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_market_data_provider()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_market_data_protocol_is_runtime_checkable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-043`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0093` [ ] `FR-BRK-044` - define account/platform/state reads independently of mutation capabilities.

- **Execution domain:** `Brokers`.
- **Execution position:** `93` of `675`.
- **Cannot start before:** Step `P01-S0092` (`FR-BRK-043`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 745); matrix row `133`.
- **Source responsibility:** The system shall define account/platform/state reads independently of mutation capabilities.
- **Source class / function / method:** `class AccountProvider(Protocol)`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_account_provider()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_account_protocol_is_runtime_checkable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-044`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0094` [ ] `FR-BRK-045` - define only single-target provider mutation primitives.

- **Execution domain:** `Brokers`.
- **Execution position:** `94` of `675`.
- **Cannot start before:** Step `P01-S0093` (`FR-BRK-044`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 746); matrix row `134`.
- **Source responsibility:** The system shall define only single-target provider mutation primitives.
- **Source class / function / method:** `class TradeExecutionProvider(Protocol)`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_trade_execution_provider()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_execution_protocol_excludes_bulk_methods()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-045`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0095` [ ] `FR-BRK-046` - define provider-native calculation requests without local fallback formulas.

- **Execution domain:** `Brokers`.
- **Execution position:** `95` of `675`.
- **Cannot start before:** Step `P01-S0094` (`FR-BRK-045`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 747); matrix row `135`.
- **Source responsibility:** The system shall define provider-native calculation requests without local fallback formulas.
- **Source class / function / method:** `class CalculationProvider(Protocol)`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_calculation_provider()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_calculation_protocol_is_provider_native()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-046`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0096` [ ] `FR-BRK-047` - compose lifecycle and focused capabilities into one async adapter, expose read-only contract_version="v1" and schema_id="brokers.adapter.v1"...

- **Execution domain:** `Brokers`.
- **Execution position:** `96` of `675`.
- **Cannot start before:** Step `P01-S0095` (`FR-BRK-046`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 748); matrix row `136`.
- **Source responsibility:** The system shall compose lifecycle and focused capabilities into one async adapter, expose read-only `contract_version="v1"` and `schema_id="brokers.adapter.v1"` properties, support deterministic `async with` cleanup, and expose no sync/strict façade initially.
- **Source class / function / method:** `class BrokerAdapter(MarketDataProvider, AccountProvider, TradeExecutionProvider, CalculationProvider, Protocol)`; `BrokerAdapter.contract_version -> Literal["v1"]`; `BrokerAdapter.schema_id -> Literal["brokers.adapter.v1"]`; `BrokerAdapter.__aenter__() -> BrokerAdapter`; `BrokerAdapter.__aexit__(exc_type: type[BaseException] | None, exc: BaseException | None, traceback: TracebackType | None) -> None`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels connect/disconnect; consumer exception still propagates after cleanup.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_async_context()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_adapter_context_always_disconnects()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-047`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0097` [ ] `FR-BRK-112` - expose each provider-event subscription as a typed bounded FIFO asynchronous stream with immutable metadata and explicit unsubscribe; termin...

- **Execution domain:** `Brokers`.
- **Execution position:** `97` of `675`.
- **Cannot start before:** Step `P01-S0096` (`FR-BRK-047`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 749); matrix row `197`.
- **Source responsibility:** The system shall expose each provider-event subscription as a typed bounded FIFO asynchronous stream with immutable metadata and explicit unsubscribe; terminal provider failure is yielded once as a canonical error event and then iteration ends.
- **Source class / function / method:** `class BrokerSubscription[TEvent](Protocol)`; `info: BrokerSubscriptionInfo`; `events() -> AsyncIterator[TEvent | BrokerError]`; `async unsubscribe() -> BrokerResult[None]`
- **Source side effects:** Consumes adapter-local queue; unsubscribe mutates local/provider subscription state
- **Source raises:** `asyncio.CancelledError`: consumer cancels iteration/unsubscribe.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_subscription_events()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_subscription_is_bounded_fifo_and_explicitly_closed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-112`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0098` [ ] `FR-BRK-048` - explicitly establish and verify the configured transport, authentication, account, and environment before returning success.

- **Execution domain:** `Brokers`.
- **Execution position:** `98` of `675`.
- **Cannot start before:** Step `P01-S0097` (`FR-BRK-112`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 755); matrix row `137`.
- **Source responsibility:** The system shall explicitly establish and verify the configured transport, authentication, account, and environment before returning success.
- **Source class / function / method:** `async BrokerAdapter.connect() -> BrokerResult[None]`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels connection.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_connect()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_connect_requires_verified_provider_state()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-048`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0099` [ ] `FR-BRK-049` - idempotently close every session, task, terminal handle, reactor, and subscription owned by the adapter.

- **Execution domain:** `Brokers`.
- **Execution position:** `99` of `675`.
- **Cannot start before:** Step `P01-S0098` (`FR-BRK-048`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 756); matrix row `138`.
- **Source responsibility:** The system shall idempotently close every session, task, terminal handle, reactor, and subscription owned by the adapter.
- **Source class / function / method:** `async BrokerAdapter.disconnect() -> BrokerResult[None]`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels cleanup; adapter remains fail-closed.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_disconnect()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_disconnect_is_idempotent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-049`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0100` [ ] `FR-BRK-050` - recover only the same transport/session up to the configured bound and shall never replay interrupted reads or mutations.

- **Execution domain:** `Brokers`.
- **Execution position:** `100` of `675`.
- **Cannot start before:** Step `P01-S0099` (`FR-BRK-049`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 757); matrix row `139`.
- **Source responsibility:** The system shall recover only the same transport/session up to the configured bound and shall never replay interrupted reads or mutations.
- **Source class / function / method:** `async BrokerAdapter.reconnect() -> BrokerResult[None]`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels recovery.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_reconnect()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_reconnect_never_replays_operation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-050`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0101` [ ] `FR-BRK-051` - return verified current connectivity rather than a caller-local Boolean flag.

- **Execution domain:** `Brokers`.
- **Execution position:** `101` of `675`.
- **Cannot start before:** Step `P01-S0100` (`FR-BRK-050`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 758); matrix row `140`.
- **Source responsibility:** The system shall return verified current connectivity rather than a caller-local Boolean flag.
- **Source class / function / method:** `async BrokerAdapter.is_connected() -> BrokerResult[bool]`
- **Source side effects:** Read-only; external API call where required
- **Source raises:** `asyncio.CancelledError`: caller cancels verification.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_is_connected()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_is_connected_is_provider_verified()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-051`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0102` [ ] `FR-BRK-052` - return detailed lifecycle, authentication, account, permission, subscription, environment, and maintenance state.

- **Execution domain:** `Brokers`.
- **Execution position:** `102` of `675`.
- **Cannot start before:** Step `P01-S0101` (`FR-BRK-051`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 759); matrix row `141`.
- **Source responsibility:** The system shall return detailed lifecycle, authentication, account, permission, subscription, environment, and maintenance state.
- **Source class / function / method:** `async BrokerAdapter.get_connection_status() -> BrokerResult[BrokerConnectionStatus]`
- **Source side effects:** Read-only
- **Source raises:** `asyncio.CancelledError`: caller cancels provider status read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_connection_status()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_connection_status_is_detailed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-052`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0103` [ ] `FR-BRK-053` - perform only a provider-supported liveness probe and return unsupported otherwise.

- **Execution domain:** `Brokers`.
- **Execution position:** `103` of `675`.
- **Cannot start before:** Step `P01-S0102` (`FR-BRK-052`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 760); matrix row `142`.
- **Source responsibility:** The system shall perform only a provider-supported liveness probe and return unsupported otherwise.
- **Source class / function / method:** `async BrokerAdapter.ping() -> BrokerResult[None]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels probe.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_ping()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_ping_has_no_synthetic_success()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-053`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0104` [ ] `FR-BRK-054` - use only provider-supported token/session refresh and shall fail the session closed when refresh fails.

- **Execution domain:** `Brokers`.
- **Execution position:** `104` of `675`.
- **Cannot start before:** Step `P01-S0103` (`FR-BRK-053`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 761); matrix row `143`.
- **Source responsibility:** The system shall use only provider-supported token/session refresh and shall fail the session closed when refresh fails.
- **Source class / function / method:** `async BrokerAdapter.refresh_session() -> BrokerResult[None]`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels refresh.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_refresh_session()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_refresh_failure_invalidates_session()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-054`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0105` [ ] `FR-BRK-055` - return provider time and local clock/latency evidence when available, otherwise unsupported.

- **Execution domain:** `Brokers`.
- **Execution position:** `105` of `675`.
- **Cannot start before:** Step `P01-S0104` (`FR-BRK-054`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 762); matrix row `144`.
- **Source responsibility:** The system shall return provider time and local clock/latency evidence when available, otherwise unsupported.
- **Source class / function / method:** `async BrokerAdapter.get_server_time() -> BrokerResult[BrokerServerTime]`
- **Source side effects:** Read-only; external API call
- **Source raises:** `asyncio.CancelledError`: caller cancels time request.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_server_time()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_server_time_exposes_offset_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-055`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0106` [ ] `FR-BRK-056` - expose the adapter instance's latest redacted diagnostic error as non-authoritative state.

- **Execution domain:** `Brokers`.
- **Execution position:** `106` of `675`.
- **Cannot start before:** Step `P01-S0105` (`FR-BRK-055`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 763); matrix row `145`.
- **Source responsibility:** The system shall expose the adapter instance's latest redacted diagnostic error as non-authoritative state.
- **Source class / function / method:** `async BrokerAdapter.get_last_error() -> BrokerResult[BrokerError | None]`
- **Source side effects:** Read-only
- **Source raises:** `asyncio.CancelledError`: caller cancels provider diagnostic read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_last_error()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_last_error_is_redacted_and_non_authoritative()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-056`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0107` [ ] `FR-BRK-057` - yield one canonical event per validated lifecycle transition through a bounded async iterator.

- **Execution domain:** `Brokers`.
- **Execution position:** `107` of `675`.
- **Cannot start before:** Step `P01-S0106` (`FR-BRK-056`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 764); matrix row `146`.
- **Source responsibility:** The system shall yield one canonical event per validated lifecycle transition through a bounded async iterator.
- **Source class / function / method:** `BrokerAdapter.connection_events() -> AsyncIterator[BrokerConnectionEvent]`
- **Source side effects:** Read-only; consumes in-memory event stream
- **Source raises:** `asyncio.CancelledError`: consumer cancels iteration.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_connection_events()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_connection_events_cover_every_transition()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-057`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0108` [ ] `FR-BRK-058` - return a bounded page of exact provider-native symbols only. query, when supported, is transmitted or matched only against provider-native s...

- **Execution domain:** `Brokers`.
- **Execution position:** `108` of `675`.
- **Cannot start before:** Step `P01-S0107` (`FR-BRK-057`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 765); matrix row `147`.
- **Source responsibility:** The system shall return a bounded page of exact provider-native symbols only. `query`, when supported, is transmitted or matched only against provider-native symbols; Brokers performs no alias or canonical-identity resolution.
- **Source class / function / method:** `async MarketDataProvider.get_symbols(query: str | None = None, cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerSymbolInfo]]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_get_symbols()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_get_symbols_is_bounded_and_provider_native()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-058`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0109` [ ] `FR-BRK-059` - return direct provider specifications and trading flags for one symbol without canonical identity policy.

- **Execution domain:** `Brokers`.
- **Execution position:** `109` of `675`.
- **Cannot start before:** Step `P01-S0108` (`FR-BRK-058`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 766); matrix row `148`.
- **Source responsibility:** The system shall return direct provider specifications and trading flags for one symbol without canonical identity policy.
- **Source class / function / method:** `async MarketDataProvider.get_symbol_info(symbol: str) -> BrokerResult[BrokerSymbolInfo]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_get_symbol_info()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_symbol_info_has_no_guessed_fields()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-059`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0110` [ ] `FR-BRK-060` - perform only a provider watch-list selection and return unsupported when unavailable.

- **Execution domain:** `Brokers`.
- **Execution position:** `110` of `675`.
- **Cannot start before:** Step `P01-S0109` (`FR-BRK-059`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 767); matrix row `149`.
- **Source responsibility:** The system shall perform only a provider watch-list selection and return unsupported when unavailable.
- **Source class / function / method:** `async MarketDataProvider.select_symbol(symbol: str, enabled: bool = True) -> BrokerResult[None]`
- **Source side effects:** External API call; provider session mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels selection.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_select_symbol()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_select_symbol_is_transport_only()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-060`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0111` [ ] `FR-BRK-061` - return provider-reported market state without deriving calendars.

- **Execution domain:** `Brokers`.
- **Execution position:** `111` of `675`.
- **Cannot start before:** Step `P01-S0110` (`FR-BRK-060`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 768); matrix row `150`.
- **Source responsibility:** The system shall return provider-reported market state without deriving calendars.
- **Source class / function / method:** `async MarketDataProvider.get_market_status(symbol: str) -> BrokerResult[BrokerMarketStatus]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_market_status()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_market_status_is_provider_reported()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-061`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0112` [ ] `FR-BRK-062` - return provider-supplied trading windows within optional bounds without generating sessions.

- **Execution domain:** `Brokers`.
- **Execution position:** `112` of `675`.
- **Cannot start before:** Step `P01-S0111` (`FR-BRK-061`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 769); matrix row `151`.
- **Source responsibility:** The system shall return provider-supplied trading windows within optional bounds without generating sessions.
- **Source class / function / method:** `async MarketDataProvider.get_trading_sessions(symbol: str, start: datetime | None = None, end: datetime | None = None) -> BrokerResult[tuple[BrokerTradingSession, ...]]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_trading_sessions()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_sessions_are_not_generated()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-062`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0113` [ ] `FR-BRK-063` - return the latest genuine provider quote and shall return unsupported or invalid-response instead of fallback prices.

- **Execution domain:** `Brokers`.
- **Execution position:** `113` of `675`.
- **Cannot start before:** Step `P01-S0112` (`FR-BRK-062`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 770); matrix row `152`.
- **Source responsibility:** The system shall return the latest genuine provider quote and shall return unsupported or invalid-response instead of fallback prices.
- **Source class / function / method:** `async MarketDataProvider.get_quote(symbol: str) -> BrokerResult[BrokerQuote]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_quote()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_quote_never_uses_fallback_price()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-063`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0114` [ ] `FR-BRK-064` - return bounded genuine provider ticks with explicit sequence/provenance or unsupported when genuine ticks do not exist.

- **Execution domain:** `Brokers`.
- **Execution position:** `114` of `675`.
- **Cannot start before:** Step `P01-S0113` (`FR-BRK-063`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 771); matrix row `153`.
- **Source responsibility:** The system shall return bounded genuine provider ticks with explicit sequence/provenance or unsupported when genuine ticks do not exist.
- **Source class / function / method:** `async MarketDataProvider.get_ticks(symbol: str, start: datetime | None = None, end: datetime | None = None, cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerTick]]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_ticks()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_ticks_are_genuine_and_bounded()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-064`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0115` [ ] `FR-BRK-065` - return bounded provider bars using structural timeframe translation only, with no resampling or hidden default timeframe.

- **Execution domain:** `Brokers`.
- **Execution position:** `115` of `675`.
- **Cannot start before:** Step `P01-S0114` (`FR-BRK-064`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 772); matrix row `154`.
- **Source responsibility:** The system shall return bounded provider bars using structural timeframe translation only, with no resampling or hidden default timeframe.
- **Source class / function / method:** `async MarketDataProvider.get_historical_bars(symbol: str, timeframe: str, start: datetime | None = None, end: datetime | None = None, cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerBar]]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_historical_bars()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_bars_do_not_silently_change_timeframe()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-065`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0116` [ ] `FR-BRK-066` - return provider order-book truth with explicit depth/sequence/resnapshot evidence or deterministic unsupported.

- **Execution domain:** `Brokers`.
- **Execution position:** `116` of `675`.
- **Cannot start before:** Step `P01-S0115` (`FR-BRK-065`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 773); matrix row `155`.
- **Source responsibility:** The system shall return provider order-book truth with explicit depth/sequence/resnapshot evidence or deterministic unsupported.
- **Source class / function / method:** `async MarketDataProvider.get_order_book(symbol: str, depth: int | None = None) -> BrokerResult[BrokerOrderBook]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_order_book()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_order_book_has_sequence_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-066`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0117` [ ] `FR-BRK-067` - return a provider-reported spread only and shall never insert fixed or zero placeholder spread.

- **Execution domain:** `Brokers`.
- **Execution position:** `117` of `675`.
- **Cannot start before:** Step `P01-S0116` (`FR-BRK-066`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 774); matrix row `156`.
- **Source responsibility:** The system shall return a provider-reported spread only and shall never insert fixed or zero placeholder spread.
- **Source class / function / method:** `async MarketDataProvider.get_spread(symbol: str) -> BrokerResult[Decimal]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_spread()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_spread_is_provider_reported()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-067`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0118` [ ] `FR-BRK-068` - create one adapter-scoped bounded genuine quote stream and return its typed subscription handle.

- **Execution domain:** `Brokers`.
- **Execution position:** `118` of `675`.
- **Cannot start before:** Step `P01-S0117` (`FR-BRK-067`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 775); matrix row `157`.
- **Source responsibility:** The system shall create one adapter-scoped bounded genuine quote stream and return its typed subscription handle.
- **Source class / function / method:** `async MarketDataProvider.subscribe_quotes(symbols: tuple[str, ...]) -> BrokerResult[BrokerSubscription[BrokerQuote]]`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels subscription.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_subscribe_quotes()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_quote_subscription_is_bounded()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-068`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0119` [ ] `FR-BRK-069` - create a provider bar stream only where genuine provider events are supported.

- **Execution domain:** `Brokers`.
- **Execution position:** `119` of `675`.
- **Cannot start before:** Step `P01-S0118` (`FR-BRK-068`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 776); matrix row `158`.
- **Source responsibility:** The system shall create a provider bar stream only where genuine provider events are supported.
- **Source class / function / method:** `async MarketDataProvider.subscribe_bars(symbols: tuple[str, ...], timeframe: str) -> BrokerResult[BrokerSubscription[BrokerBar]]`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels subscription.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_subscribe_bars()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_bar_subscription_is_capability_gated()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-069`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0120` [ ] `FR-BRK-070` - create a provider order-book stream only where sequence-safe events are supported.

- **Execution domain:** `Brokers`.
- **Execution position:** `120` of `675`.
- **Cannot start before:** Step `P01-S0119` (`FR-BRK-069`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 777); matrix row `159`.
- **Source responsibility:** The system shall create a provider order-book stream only where sequence-safe events are supported.
- **Source class / function / method:** `async MarketDataProvider.subscribe_order_book(symbols: tuple[str, ...], depth: int | None = None) -> BrokerResult[BrokerSubscription[BrokerOrderBook]]`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels subscription.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_subscribe_order_book()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_order_book_subscription_requires_sequence_safety()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-070`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0121` [ ] `FR-BRK-071` - terminate exactly one owned subscription and report an unknown ID without affecting any other stream.

- **Execution domain:** `Brokers`.
- **Execution position:** `121` of `675`.
- **Cannot start before:** Step `P01-S0120` (`FR-BRK-070`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 778); matrix row `160`.
- **Source responsibility:** The system shall terminate exactly one owned subscription and report an unknown ID without affecting any other stream.
- **Source class / function / method:** `async MarketDataProvider.unsubscribe(subscription_id: str) -> BrokerResult[None]`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels unsubscribe.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_unsubscribe()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_unknown_unsubscribe_is_isolated()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-071`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0122` [ ] `FR-BRK-072` - list immutable metadata only for subscriptions owned by the current adapter instance.

- **Execution domain:** `Brokers`.
- **Execution position:** `122` of `675`.
- **Cannot start before:** Step `P01-S0121` (`FR-BRK-071`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 779); matrix row `161`.
- **Source responsibility:** The system shall list immutable metadata only for subscriptions owned by the current adapter instance.
- **Source class / function / method:** `async MarketDataProvider.list_subscriptions() -> BrokerResult[tuple[BrokerSubscriptionInfo, ...]]`
- **Source side effects:** Read-only
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_list_subscriptions()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_subscriptions_do_not_leak_between_adapters()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-072`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0123` [ ] `FR-BRK-073` - return the complete refreshed capability report for the connected profile/account, with untested or unapproved mutations unavailable regardl...

- **Execution domain:** `Brokers`.
- **Execution position:** `123` of `675`.
- **Cannot start before:** Step `P01-S0122` (`FR-BRK-072`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 780); matrix row `162`.
- **Source responsibility:** The system shall return the complete refreshed capability report for the connected profile/account, with untested or unapproved mutations unavailable regardless of SDK method presence.
- **Source class / function / method:** `async AccountProvider.get_feature_flags() -> BrokerResult[BrokerFeatureFlags]`
- **Source side effects:** Read-only; provider discovery call where needed
- **Source raises:** `asyncio.CancelledError`: caller cancels discovery.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_feature_flags()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_feature_flags_include_unsupported_and_unapproved_entries()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-073`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0124` [ ] `FR-BRK-074` - answer one capability from the complete report without probing a missing SDK attribute.

- **Execution domain:** `Brokers`.
- **Execution position:** `124` of `675`.
- **Cannot start before:** Step `P01-S0123` (`FR-BRK-073`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 781); matrix row `163`.
- **Source responsibility:** The system shall answer one capability from the complete report without probing a missing SDK attribute.
- **Source class / function / method:** `async AccountProvider.supports(capability: BrokerCapabilityId) -> BrokerResult[bool]`
- **Source side effects:** Read-only
- **Source raises:** `asyncio.CancelledError`: caller cancels discovery.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_supports()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_supports_uses_declaration_not_attribute_catch()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-074`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0125` [ ] `FR-BRK-075` - return direct provider platform/version/endpoint/environment metadata without secrets.

- **Execution domain:** `Brokers`.
- **Execution position:** `125` of `675`.
- **Cannot start before:** Step `P01-S0124` (`FR-BRK-074`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 782); matrix row `164`.
- **Source responsibility:** The system shall return direct provider platform/version/endpoint/environment metadata without secrets.
- **Source class / function / method:** `async AccountProvider.get_platform_info() -> BrokerResult[BrokerPlatformInfo]`
- **Source side effects:** External API call
- **Source raises:** `asyncio.CancelledError`: caller cancels read.
- **Source usage / test:** **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_platform_info()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_platform_info_is_redacted()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-075`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0126` [ ] `FR-BRK-076` - return provider-reported current permissions and shall not infer trade access from SDK method presence.

- **Execution domain:** `Brokers`.
- **Execution position:** `126` of `675`.
- **Cannot start before:** Step `P01-S0125` (`FR-BRK-075`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 783); matrix row `165`.
- **Source source context:** | Missing | `FR-BRK-076` | The system shall return provider-reported current permissions and shall not infer trade access from SDK method presence. | `async AccountProvider.get_permissions() -> BrokerResult[BrokerPermissions]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_permissions()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_permissions_are_authenticated_and_tested()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-076`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0127` [ ] `FR-BRK-077` - return a bounded page of provider-visible accounts where supported.

- **Execution domain:** `Brokers`.
- **Execution position:** `127` of `675`.
- **Cannot start before:** Step `P01-S0126` (`FR-BRK-076`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 784); matrix row `166`.
- **Source source context:** | Missing | `FR-BRK-077` | The system shall return a bounded page of provider-visible accounts where supported. | `async AccountProvider.list_accounts(cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerAccountInfo]]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_list_accounts()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_list_accounts_is_bounded()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-077`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0128` [ ] `FR-BRK-078` - The initial system shall reject in-place account switching as unsupported; callers create a new immutable adapter instance.

- **Execution domain:** `Brokers`.
- **Execution position:** `128` of `675`.
- **Cannot start before:** Step `P01-S0127` (`FR-BRK-077`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 785); matrix row `167`.
- **Source source context:** | Missing | `FR-BRK-078` | The initial system shall reject in-place account switching as unsupported; callers create a new immutable adapter instance. | `async AccountProvider.select_account(account_id: str) -> BrokerResult[None]` | None | `asyncio.CancelledError`: caller cancellation; operational result is always unsupported initially. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_select_account_unsupported()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_select_account_is_initially_unsupported()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-078`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0129` [ ] `FR-BRK-079` - return direct provider account identity and financial state without persisting or certifying freshness.

- **Execution domain:** `Brokers`.
- **Execution position:** `129` of `675`.
- **Cannot start before:** Step `P01-S0128` (`FR-BRK-078`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 786); matrix row `168`.
- **Source source context:** | Missing | `FR-BRK-079` | The system shall return direct provider account identity and financial state without persisting or certifying freshness. | `async AccountProvider.get_account_info() -> BrokerResult[BrokerAccountInfo]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_account_info()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_account_info_has_provider_and_retrieval_time()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-079`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0130` [ ] `FR-BRK-080` - return exact provider-reported balances without currency conversion.

- **Execution domain:** `Brokers`.
- **Execution position:** `130` of `675`.
- **Cannot start before:** Step `P01-S0129` (`FR-BRK-079`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 787); matrix row `169`.
- **Source source context:** | Missing | `FR-BRK-080` | The system shall return exact provider-reported balances without currency conversion. | `async AccountProvider.get_balances() -> BrokerResult[tuple[BrokerBalance, ...]]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_balances()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_balances_have_explicit_units()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-080`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0131` [ ] `FR-BRK-081` - return provider-known account/assets without constructing a canonical asset universe.

- **Execution domain:** `Brokers`.
- **Execution position:** `131` of `675`.
- **Cannot start before:** Step `P01-S0130` (`FR-BRK-080`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 788); matrix row `170`.
- **Source source context:** | Missing | `FR-BRK-081` | The system shall return provider-known account/assets without constructing a canonical asset universe. | `async AccountProvider.list_assets(cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerAssetInfo]]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_list_assets()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_assets_are_provider_native()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-081`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0132` [ ] `FR-BRK-082` - return direct provider metadata for one asset or an exact not-found result.

- **Execution domain:** `Brokers`.
- **Execution position:** `132` of `675`.
- **Cannot start before:** Step `P01-S0131` (`FR-BRK-081`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 789); matrix row `171`.
- **Source source context:** | Missing | `FR-BRK-082` | The system shall return direct provider metadata for one asset or an exact not-found result. | `async AccountProvider.get_asset_info(asset: str) -> BrokerResult[BrokerAssetInfo]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_asset_info()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_asset_not_found_is_explicit()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-082`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0133` [ ] `FR-BRK-083` - return a bounded canonical page of current positions matching structural filters.

- **Execution domain:** `Brokers`.
- **Execution position:** `133` of `675`.
- **Cannot start before:** Step `P01-S0132` (`FR-BRK-082`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 790); matrix row `172`.
- **Source source context:** | Missing | `FR-BRK-083` | The system shall return a bounded canonical page of current positions matching structural filters. | `async AccountProvider.get_positions(filter: BrokerPositionFilter | None = None, cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerPosition]]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_positions()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_positions_preserve_ids_and_partial_state()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-083`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0134` [ ] `FR-BRK-084` - return one provider position or BROKER_POSITION_NOT_FOUND.

- **Execution domain:** `Brokers`.
- **Execution position:** `134` of `675`.
- **Cannot start before:** Step `P01-S0133` (`FR-BRK-083`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 791); matrix row `173`.
- **Source source context:** | Missing | `FR-BRK-084` | The system shall return one provider position or `BROKER_POSITION_NOT_FOUND`. | `async AccountProvider.get_position(position_id: str) -> BrokerResult[BrokerPosition]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_position()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_position_not_found_is_distinct()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-084`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0135` [ ] `FR-BRK-085` - return a bounded page of provider orders matching structural filters.

- **Execution domain:** `Brokers`.
- **Execution position:** `135` of `675`.
- **Cannot start before:** Step `P01-S0134` (`FR-BRK-084`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 792); matrix row `174`.
- **Source source context:** | Missing | `FR-BRK-085` | The system shall return a bounded page of provider orders matching structural filters. | `async AccountProvider.get_orders(filter: BrokerOrderFilter | None = None, cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerOrder]]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_orders()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_orders_preserve_provider_states()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-085`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0136` [ ] `FR-BRK-086` - return one provider order or BROKER_ORDER_NOT_FOUND.

- **Execution domain:** `Brokers`.
- **Execution position:** `136` of `675`.
- **Cannot start before:** Step `P01-S0135` (`FR-BRK-085`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 793); matrix row `175`.
- **Source source context:** | Missing | `FR-BRK-086` | The system shall return one provider order or `BROKER_ORDER_NOT_FOUND`. | `async AccountProvider.get_order(order_id: str) -> BrokerResult[BrokerOrder]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_order()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_order_not_found_is_distinct()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-086`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0137` [ ] `FR-BRK-087` - return bounded provider order history with explicit page limits/cursors.

- **Execution domain:** `Brokers`.
- **Execution position:** `137` of `675`.
- **Cannot start before:** Step `P01-S0136` (`FR-BRK-086`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 794); matrix row `176`.
- **Source source context:** | Missing | `FR-BRK-087` | The system shall return bounded provider order history with explicit page limits/cursors. | `async AccountProvider.list_order_history(start: datetime | None = None, end: datetime | None = None, symbol: str | None = None, cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerOrder]]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_order_history()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_order_history_is_bounded()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-087`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0138` [ ] `FR-BRK-088` - return bounded provider deal/fill history preserving exact provider IDs and partial state.

- **Execution domain:** `Brokers`.
- **Execution position:** `138` of `675`.
- **Cannot start before:** Step `P01-S0137` (`FR-BRK-087`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 795); matrix row `177`.
- **Source source context:** | Missing | `FR-BRK-088` | The system shall return bounded provider deal/fill history preserving exact provider IDs and partial state. | `async AccountProvider.list_deal_history(start: datetime | None = None, end: datetime | None = None, symbol: str | None = None, cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerDeal]]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_deal_history()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_deal_history_is_bounded()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-088`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0139` [ ] `FR-BRK-089` - return one provider deal/fill or BROKER_DEAL_NOT_FOUND.

- **Execution domain:** `Brokers`.
- **Execution position:** `139` of `675`.
- **Cannot start before:** Step `P01-S0138` (`FR-BRK-088`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 796); matrix row `178`.
- **Source source context:** | Missing | `FR-BRK-089` | The system shall return one provider deal/fill or `BROKER_DEAL_NOT_FOUND`. | `async AccountProvider.get_deal(deal_id: str) -> BrokerResult[BrokerDeal]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_deal()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_deal_not_found_is_distinct()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-089`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0140` [ ] `FR-BRK-090` - return bounded direct provider account transactions where supported and deterministic unsupported otherwise.

- **Execution domain:** `Brokers`.
- **Execution position:** `140` of `675`.
- **Cannot start before:** Step `P01-S0139` (`FR-BRK-089`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 797); matrix row `179`.
- **Source source context:** | Missing | `FR-BRK-090` | The system shall return bounded direct provider account transactions where supported and deterministic unsupported otherwise. | `async AccountProvider.list_account_transactions(start: datetime | None = None, end: datetime | None = None, cursor: str | None = None, limit: int | None = None) -> BrokerResult[BrokerPage[BrokerAccountTransaction]]` | External API call | `asyncio.CancelledError`: caller cancels read. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_account_transactions()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_transactions_are_bounded_or_unsupported()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-090`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0141` [ ] `FR-BRK-091` - request provider validation/preview for one order and shall not present the result as acceptance.

- **Execution domain:** `Brokers`.
- **Execution position:** `141` of `675`.
- **Cannot start before:** Step `P01-S0140` (`FR-BRK-090`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 798); matrix row `180`.
- **Source source context:** | Missing | `FR-BRK-091` | The system shall request provider validation/preview for one order and shall not present the result as acceptance. | `async TradeExecutionProvider.check_order(request: BrokerOrderRequest) -> BrokerResult[BrokerOrderCheck]` | External API call | `asyncio.CancelledError`: caller cancels before known outcome. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_check_order()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_order_check_is_not_acceptance()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-091`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0142` [ ] `FR-BRK-092` - submit exactly one complete caller-defined order and report success only on explicit provider acknowledgement.

- **Execution domain:** `Brokers`.
- **Execution position:** `142` of `675`.
- **Cannot start before:** Step `P01-S0141` (`FR-BRK-091`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 799); matrix row `181`.
- **Source source context:** | Missing | `FR-BRK-092` | The system shall submit exactly one complete caller-defined order and report success only on explicit provider acknowledgement. | `async TradeExecutionProvider.place_order(request: BrokerOrderRequest) -> BrokerResult[BrokerOrderResult]` | Broker mutation | `asyncio.CancelledError`: caller cancels; uncertain transmission still records unknown outcome. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_place_order()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_place_order_requires_acknowledgement()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-092`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0143` [ ] `FR-BRK-093` - modify exactly one order using only supplied fields.

- **Execution domain:** `Brokers`.
- **Execution position:** `143` of `675`.
- **Cannot start before:** Step `P01-S0142` (`FR-BRK-092`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 800); matrix row `182`.
- **Source source context:** | Missing | `FR-BRK-093` | The system shall modify exactly one order using only supplied fields. | `async TradeExecutionProvider.modify_order(request: BrokerOrderModificationRequest) -> BrokerResult[BrokerOrderResult]` | Broker mutation | `asyncio.CancelledError`: caller cancels; possible transmission is unknown outcome. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_modify_order()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_modify_order_has_one_target()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-093`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0144` [ ] `FR-BRK-094` - cancel exactly one provider order and transmit the caller request ID where supported.

- **Execution domain:** `Brokers`.
- **Execution position:** `144` of `675`.
- **Cannot start before:** Step `P01-S0143` (`FR-BRK-093`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 801); matrix row `183`.
- **Source source context:** | Missing | `FR-BRK-094` | The system shall cancel exactly one provider order and transmit the caller request ID where supported. | `async TradeExecutionProvider.cancel_order(order_id: str, client_request_id: str | None = None) -> BrokerResult[BrokerOrderResult]` | Broker mutation | `asyncio.CancelledError`: caller cancels; possible transmission is unknown outcome. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_cancel_order()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_cancel_order_has_one_target()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-094`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0145` [ ] `FR-BRK-095` - modify provider-supported fields on exactly one position.

- **Execution domain:** `Brokers`.
- **Execution position:** `145` of `675`.
- **Cannot start before:** Step `P01-S0144` (`FR-BRK-094`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 802); matrix row `184`.
- **Source source context:** | Missing | `FR-BRK-095` | The system shall modify provider-supported fields on exactly one position. | `async TradeExecutionProvider.modify_position(request: BrokerPositionModificationRequest) -> BrokerResult[BrokerPosition]` | Broker mutation | `asyncio.CancelledError`: caller cancels; possible transmission is unknown outcome. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_modify_position()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_modify_position_has_one_target()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-095`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0146` [ ] `FR-BRK-096` - close or reduce exactly one position and preserve partial-close acknowledgement.

- **Execution domain:** `Brokers`.
- **Execution position:** `146` of `675`.
- **Cannot start before:** Step `P01-S0145` (`FR-BRK-095`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 803); matrix row `185`.
- **Source source context:** | Missing | `FR-BRK-096` | The system shall close or reduce exactly one position and preserve partial-close acknowledgement. | `async TradeExecutionProvider.close_position(request: BrokerPositionCloseRequest) -> BrokerResult[BrokerOrderResult]` | Broker mutation | `asyncio.CancelledError`: caller cancels; possible transmission is unknown outcome. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_close_position()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_close_position_preserves_partial_result()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-096`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0147` [ ] `FR-BRK-097` - request one provider-atomic replacement only where verified; it shall not emulate cancel-then-place.

- **Execution domain:** `Brokers`.
- **Execution position:** `147` of `675`.
- **Cannot start before:** Step `P01-S0146` (`FR-BRK-096`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 804); matrix row `186`.
- **Source source context:** | Missing | `FR-BRK-097` | The system shall request one provider-atomic replacement only where verified; it shall not emulate cancel-then-place. | `async TradeExecutionProvider.replace_order(request: BrokerOrderModificationRequest) -> BrokerResult[BrokerOrderResult]` | Broker mutation | `asyncio.CancelledError`: caller cancels; possible transmission is unknown outcome. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_replace_order()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_replace_order_is_never_emulated()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-097`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0148` [ ] `FR-BRK-098` - return a provider-native margin estimate or unsupported, never a local risk formula.

- **Execution domain:** `Brokers`.
- **Execution position:** `148` of `675`.
- **Cannot start before:** Step `P01-S0147` (`FR-BRK-097`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 805); matrix row `187`.
- **Source source context:** | Missing | `FR-BRK-098` | The system shall return a provider-native margin estimate or unsupported, never a local risk formula. | `async CalculationProvider.calculate_margin(request: BrokerMarginRequest) -> BrokerResult[Decimal]` | External API call | `asyncio.CancelledError`: caller cancels calculation. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_calculate_margin()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_margin_is_provider_native()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-098`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0149` [ ] `FR-BRK-099` - return a provider-native profit estimate or unsupported, never a locally approximated value.

- **Execution domain:** `Brokers`.
- **Execution position:** `149` of `675`.
- **Cannot start before:** Step `P01-S0148` (`FR-BRK-098`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 806); matrix row `188`.
- **Source source context:** | Missing | `FR-BRK-099` | The system shall return a provider-native profit estimate or unsupported, never a locally approximated value. | `async CalculationProvider.calculate_profit(request: BrokerProfitRequest) -> BrokerResult[Decimal]` | External API call | `asyncio.CancelledError`: caller cancels calculation. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_calculate_profit()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_profit_is_provider_native()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-099`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0150` [ ] `FR-BRK-100` - return a provider-native commission/fee estimate or deterministic unsupported.

- **Execution domain:** `Brokers`.
- **Execution position:** `150` of `675`.
- **Cannot start before:** Step `P01-S0149` (`FR-BRK-099`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Canonical error conditions > `protocols.py` — Capability Protocols and Operations`` (line 807); matrix row `189`.
- **Source source context:** | Missing | `FR-BRK-100` | The system shall return a provider-native commission/fee estimate or deterministic unsupported. | `async CalculationProvider.get_commission_estimate(request: BrokerOrderRequest) -> BrokerResult[BrokerFeeEstimate]` | External API call | `asyncio.CancelledError`: caller cancels calculation. | **Usage:** `tests/brokers/usage/test_usage_contracts.py::test_usage_protocols_commission_estimate()`<br>**Unit:** `tests/brokers/unit/test_protocols.py::test_commission_is_provider_native_or_unsupported()` |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-100`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0151` [ ] `FR-BRK-111` - Each adapter shall own one transport circuit with CLOSED, OPEN, and HALF_OPEN states. BROKER_CONNECTION_FAILED, BROKER_CONNECTION_LOST, BROKER_TIMEOUT, BROKE...

- **Execution domain:** `Brokers`.
- **Execution position:** `151` of `675`.
- **Cannot start before:** Step `P01-S0150` (`FR-BRK-100`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Requirements`` (line 858); matrix row `196`.
- **Source responsibility:** Each adapter shall own one transport circuit with `CLOSED`, `OPEN`, and `HALF_OPEN` states. `BROKER_CONNECTION_FAILED`, `BROKER_CONNECTION_LOST`, `BROKER_TIMEOUT`, `BROKER_PROVIDER_ERROR`, and `BROKER_UNKNOWN_OUTCOME` count as qualifying failures; configuration, authentication, authorization, rate-limit, request, provider-rejection, not-found, unsupported, and cancellation outcomes do not. The configured consecutive-failure threshold opens the circuit; expiry of the recovery timeout admits at most `circuit_half_open_max_calls`; any qualifying half-open failure reopens it, while that many consecutive successful probes close and reset it. An open circuit returns `BROKER_CIRCUIT_OPEN` without an SDK call. It never retries or replays an operation.
- **Source class / function / method:** private `_TransportCircuitBreaker`
- **Source side effects:** Adapter-local monotonic-time state mutation
- **Source raises:** `ValueError`: configured bounds are not positive; cancellation propagates.
- **Source usage / test:** **Usage:** `tests/brokers/usage/02_runtime.py` (standalone script, run via `python`)<br>**Unit:** `tests/brokers/unit/test_circuit_breaker.py::test_circuit_state_machine_and_failure_classification()` (plus half-open reopen/bounded-admission/non-qualifying/reset cases)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-111`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0152` [ ] `FR-BRK-114` - The runtime subscription shall implement FR-BRK-112 with one bounded queue per handle, FIFO delivery, explicit terminal errors, deterministic unsubscribe, an...

- **Execution domain:** `Brokers`.
- **Execution position:** `152` of `675`.
- **Cannot start before:** Step `P01-S0151` (`FR-BRK-111`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Requirements`` (line 859); matrix row `199`.
- **Source responsibility:** The runtime subscription shall implement FR-BRK-112 with one bounded queue per handle, FIFO delivery, explicit terminal errors, deterministic unsubscribe, and no implicit resubscription or silent drop.
- **Source class / function / method:** private `_BrokerSubscription[TEvent]`
- **Source side effects:** Adapter-local queue/task mutation; optional provider unsubscribe call
- **Source raises:** `asyncio.CancelledError`: caller cancellation.
- **Source usage / test:** **Usage:** `tests/brokers/usage/02_runtime.py` (standalone script, run via `python`)<br>**Unit:** `tests/brokers/unit/test_subscription.py::test_subscription_overflow_is_terminal_and_requires_resync()` (plus idempotent-unsubscribe-callback and terminal-`fail()` cases)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-114`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0153` [ ] `FR-BRK-101` - require an exact provider/profile ID and matching immutable config, reject provider_enabled=False before provider import, lazily import only...

- **Execution domain:** `Brokers`.
- **Execution position:** `153` of `675`.
- **Cannot start before:** Step `P01-S0152` (`FR-BRK-114`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `factory.py` and `catalogue.py` — Public Registry API`` (line 897); matrix row `190`.
- **Source responsibility:** The system shall require an exact provider/profile ID and matching immutable config, reject `provider_enabled=False` before provider import, lazily import only that provider, and return a new disconnected adapter or canonical error without fallback. `connect`/`is_connected` are the only capabilities the static catalogue ever marks `AVAILABLE`; every other capability remains gated by credential-verified release evidence.
- **Source class / function / method:** `create_broker_adapter(broker_id: BrokerId, config: BrokerConnectionConfig) -> BrokerResult[BrokerAdapter]`
- **Source side effects:** Local state mutation; lazy import
- **Source raises:** None; `BROKER_UNKNOWN`, `BROKER_CONFIGURATION_INVALID`, or `BROKER_DEPENDENCY_MISSING` returned.
- **Source usage / test:** **Usage:** `tests/brokers/usage/08_registry.py` (standalone script, run via `python`)<br>**Unit:** `tests/brokers/unit/test_factory.py::test_create_adapter_never_falls_back()`, `test_registry_created_adapter_can_connect_and_report_state()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-101`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0154` [ ] `FR-BRK-102` - list every canonical registered provider/profile, including profiles whose optional SDK is absent, without importing provider SDKs.

- **Execution domain:** `Brokers`.
- **Execution position:** `154` of `675`.
- **Cannot start before:** Step `P01-S0153` (`FR-BRK-101`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `factory.py` and `catalogue.py` — Public Registry API`` (line 898); matrix row `191`.
- **Source responsibility:** The system shall list every canonical registered provider/profile, including profiles whose optional SDK is absent, without importing provider SDKs.
- **Source class / function / method:** `get_registered_brokers() -> tuple[BrokerId, ...]`
- **Source side effects:** Read-only
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/brokers/usage/08_registry.py` (standalone script, run via `python`)<br>**Unit:** `tests/brokers/unit/test_factory.py::test_listing_does_not_import_optional_sdks()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-102`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0155` [ ] `FR-BRK-103` - generate one complete capability catalogue covering every protocol operation and registered profile from the static declaration table in reg...

- **Execution domain:** `Brokers`.
- **Execution position:** `155` of `675`.
- **Cannot start before:** Step `P01-S0154` (`FR-BRK-102`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `factory.py` and `catalogue.py` — Public Registry API`` (line 899); matrix row `192`.
- **Source responsibility:** The system shall generate one complete capability catalogue covering every protocol operation and registered profile from the static declaration table in `registry/catalogue.py`. Provider modules consume the resulting declaration and shall not duplicate capability lists.
- **Source class / function / method:** `get_broker_capability_catalogue() -> Mapping[BrokerId, tuple[BrokerCapability, ...]]`
- **Source side effects:** Read-only
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/brokers/usage/08_registry.py` (standalone script, run via `python`)<br>**Unit:** `tests/brokers/unit/test_catalogue.py::test_catalogue_is_the_single_complete_declaration_source()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-103`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0156` [ ] `FR-BRK-104` - expose all accepted MT5 lifecycle, genuine market-data, account/order/position/deal reads, supported provider calculations, and single mutat...

- **Execution domain:** `Brokers`.
- **Execution position:** `156` of `675`.
- **Cannot start before:** Step `P01-S0155` (`FR-BRK-103`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `adapter.py` — Canonical MT5 Adapter`` (line 1028); matrix row `193`.
- **Source responsibility:** The system shall expose all accepted MT5 lifecycle, genuine market-data, account/order/position/deal reads, supported provider calculations, and single mutations only through `BrokerAdapter` while preserving provider truth and isolating terminal calls; native SDK delegation and uncatalogued methods shall be unavailable.
- **Source class / function / method:** `class MT5BrokerAdapter(BrokerAdapter)`
- **Source side effects:** External API call; broker mutation for mutation methods; local session mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels; operational failures are canonical results.
- **Source usage / test:** **Usage:** `tests/brokers/usage/03_mt5.py` (standalone script, run via `python`)<br>**Unit:** `tests/brokers/unit/test_mt5_adapter.py` (connect verification, account mismatch, symbol/quote/tick/position mapping, platform info)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-104`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0157` [ ] `FR-BRK-105` - expose verified cTrader lifecycle, genuine reads/streams, account and execution state, supported provider calculations, and single mutations...

- **Execution domain:** `Brokers`.
- **Execution position:** `157` of `675`.
- **Cannot start before:** Step `P01-S0156` (`FR-BRK-104`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `adapter.py` — Canonical cTrader Adapter`` (line 1085); matrix row `194`.
- **Source responsibility:** The system shall expose verified cTrader lifecycle, genuine reads/streams, account and execution state, supported provider calculations, and single mutations through `BrokerAdapter`; each response shall match a reliable native request ID, or the adapter shall serialize operations with the same response type by session generation, while never fabricating prices, profit, IDs, or success. `network.py` provides the default Spotware transport and application/account/trader authentication handshake; an injected `sender` remains supported for deterministic tests.
- **Source class / function / method:** `class CTraderBrokerAdapter(BrokerAdapter)`
- **Source side effects:** External API call; broker mutation for mutation methods; local session/subscription mutation
- **Source raises:** `asyncio.CancelledError`: caller cancels; operational failures are canonical results.
- **Source usage / test:** **Usage:** `tests/brokers/usage/04_ctrader.py` (standalone script, run via `python`)<br>**Unit:** `tests/brokers/unit/test_ctrader_adapter.py` (connect/disconnect, credential validation, platform info)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-BRK-105`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0158` [ ] `WF-BRK-001` - Resolve Explicit Adapter

- **Execution domain:** `Brokers`.
- **Execution position:** `158` of `675`.
- **Cannot start before:** Step `P01-S0157` (`FR-BRK-105`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 3. Workflows > Workflow scope values`` (line 306); matrix row `207`.
- **Source scope:** Internal
- **Source workflow:** Resolve explicit adapter
- **Source requirement sequence:** `FR-BRK-101 → FR-BRK-102`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-BRK-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0159` [ ] `WF-BRK-002` - Connect and Authenticate Provider Session

- **Execution domain:** `Brokers`.
- **Execution position:** `159` of `675`.
- **Cannot start before:** Step `P01-S0158` (`WF-BRK-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 3. Workflows > Workflow scope values`` (line 307); matrix row `208`.
- **Source scope:** Internal
- **Source workflow:** Connect and authenticate
- **Source requirement sequence:** `FR-BRK-006 → FR-BRK-111 → FR-BRK-048 → FR-BRK-052 → FR-BRK-073`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-BRK-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0160` [ ] `WF-BRK-003` - Acquire Provider Market Data

- **Execution domain:** `Brokers`.
- **Execution position:** `160` of `675`.
- **Cannot start before:** Step `P01-S0159` (`WF-BRK-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 3. Workflows > Workflow scope values`` (line 308); matrix row `209`.
- **Source scope:** Cross-domain
- **Source workflow:** Acquire provider market data
- **Source requirement sequence:** `FR-BRK-058 → FR-BRK-067`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-BRK-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0161` [ ] `WF-BRK-008` - Handle Unsupported Operation

- **Execution domain:** `Brokers`.
- **Execution position:** `161` of `675`.
- **Cannot start before:** Step `P01-S0160` (`WF-BRK-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 3. Workflows > Workflow scope values`` (line 313); matrix row `210`.
- **Source scope:** Internal
- **Source workflow:** Handle unsupported operation
- **Source requirement sequence:** `FR-BRK-010 → FR-BRK-074`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-BRK-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0162` [ ] `WF-BRK-009` - Inject Canonical Broker into Execution

- **Execution domain:** `Brokers`.
- **Execution position:** `162` of `675`.
- **Cannot start before:** Step `P01-S0161` (`WF-BRK-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 3. Workflows > Workflow scope values`` (line 314); matrix row `211`.
- **Source scope:** Cross-domain
- **Source workflow:** Inject canonical broker into execution
- **Source requirement sequence:** `FR-BRK-101 → FR-BRK-046`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-BRK-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0163` [ ] `CAP-BRK-001` - Explicit registry/public API

- **Execution domain:** `Brokers`.
- **Execution position:** `163` of `675`.
- **Cannot start before:** Step `P01-S0162` (`WF-BRK-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Approved capability traceability`` (line 516); matrix row `87`.
- **Source final destination:** `registry/`; FR-BRK-101–103
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-BRK-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0164` [ ] `CAP-BRK-003` - Canonical results/errors/DTOs

- **Execution domain:** `Brokers`.
- **Execution position:** `164` of `675`.
- **Cannot start before:** Step `P01-S0163` (`CAP-BRK-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Approved capability traceability`` (line 518); matrix row `88`.
- **Source final destination:** `contracts/enums.py` and `models.py`; FR-BRK-001–042
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-BRK-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0165` [ ] `CAP-BRK-012` - MT5 adapter

- **Execution domain:** `Brokers`.
- **Execution position:** `165` of `675`.
- **Cannot start before:** Step `P01-S0164` (`CAP-BRK-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Brokers` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/brokers/README.md` - ``Brokers > 4. Module and Requirement Specifications > Approved capability traceability`` (line 527); matrix row `89`.
- **Source final destination:** `mt5/`; FR-BRK-104
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-BRK-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 4 - Data

Implement `Data` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0166` [ ] `P-DATA-001` - contracts feature/component (provisional)

- **Execution domain:** `Data`.
- **Execution position:** `166` of `675`.
- **Cannot start before:** Step `P01-S0165` (`CAP-BRK-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-DATA-001`); matrix row `306`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-DATA-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0167` [ ] `P-DATA-004` - access feature/component (provisional)

- **Execution domain:** `Data`.
- **Execution position:** `167` of `675`.
- **Cannot start before:** Step `P01-S0166` (`P-DATA-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` module `4.4` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-DATA-004`); matrix row `307`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-DATA-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0168` [ ] `P-DATA-008` - public_api feature/component (provisional)

- **Execution domain:** `Data`.
- **Execution position:** `168` of `675`.
- **Cannot start before:** Step `P01-S0167` (`P-DATA-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` module `4.8` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-DATA-008`); matrix row `308`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-DATA-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0169` [ ] `FR-DATA-001` - Validate UTC OHLCV with finite exact numerics, low open/close high, non-negative volume, provenance, and available_at.

- **Execution domain:** `Data`.
- **Execution position:** `169` of `675`.
- **Cannot start before:** Step `P01-S0168` (`P-DATA-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `records.py` — Canonical Records`` (line 642); matrix row `276`.
- **Source responsibility:** Validate UTC OHLCV with finite exact numerics, `low ≤ open/close ≤ high`, non-negative volume, provenance, and `available_at`.
- **Source class / function / method:** `OHLCVRecord`
- **Source side effects:** None
- **Source raises:** `DataError[VALIDATION_FAILED]`: field, UTC, order, or OHLC invariant fails
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_001_ohlcv_record()`<br>**Unit:** `tests/data/unit/test_records.py::test_ohlcv_record_rejects_invalid_range()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0170` [ ] `FR-DATA-002` - Validate UTC ticks with finite bid/ask/last, ask bid when both exist, volume metadata, provenance, and available_at.

- **Execution domain:** `Data`.
- **Execution position:** `170` of `675`.
- **Cannot start before:** Step `P01-S0169` (`FR-DATA-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `records.py` — Canonical Records`` (line 643); matrix row `277`.
- **Source responsibility:** Validate UTC ticks with finite bid/ask/last, `ask ≥ bid` when both exist, volume metadata, provenance, and `available_at`.
- **Source class / function / method:** `TickRecord`
- **Source side effects:** None
- **Source raises:** `DataError[VALIDATION_FAILED]`: invalid timestamp, numeric field, or bid/ask relation
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_002_tick_record()`<br>**Unit:** `tests/data/unit/test_records.py::test_tick_record_rejects_crossed_quote()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0171` [ ] `FR-DATA-003` - Validate spread records with declared unit/scale, non-negative exact spread, UTC timestamp, provenance, and available_at.

- **Execution domain:** `Data`.
- **Execution position:** `171` of `675`.
- **Cannot start before:** Step `P01-S0170` (`FR-DATA-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `records.py` — Canonical Records`` (line 644); matrix row `278`.
- **Source responsibility:** Validate spread records with declared unit/scale, non-negative exact spread, UTC timestamp, provenance, and `available_at`.
- **Source class / function / method:** `SpreadRecord`
- **Source side effects:** None
- **Source raises:** `DataError[VALIDATION_FAILED]`: missing unit/scale or invalid spread
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_003_spread_record()`<br>**Unit:** `tests/data/unit/test_records.py::test_spread_record_requires_unit()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0172` [ ] `FR-DATA-004` - Represent bounded quality evidence with status, score, issues, warnings, counts, truncation, schema version, UTC generation time, and governed blocking behav...

- **Execution domain:** `Data`.
- **Execution position:** `172` of `675`.
- **Cannot start before:** Step `P01-S0171` (`FR-DATA-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `market.py` — Requests, Datasets, Quality, and Availability`` (line 657); matrix row `279`.
- **Source responsibility:** Represent bounded quality evidence with status, score, issues, warnings, counts, truncation, schema version, UTC generation time, and governed blocking behavior.
- **Source class / function / method:** `DataQualityReport`
- **Source side effects:** None
- **Source raises:** `DataError[VALIDATION_FAILED]`: malformed or unbounded diagnostics
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_004_quality_report()`<br>**Unit:** `tests/data/unit/test_market_contracts.py::test_quality_report_bounds_samples()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0173` [ ] `FR-DATA-005` - Expose immutable normalized records with availability, quality, provenance, license, cache, workflow, schema, normalization, and precision metadata.

- **Execution domain:** `Data`.
- **Execution position:** `173` of `675`.
- **Cannot start before:** Step `P01-S0172` (`FR-DATA-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `market.py` — Requests, Datasets, Quality, and Availability`` (line 658); matrix row `280`.
- **Source responsibility:** Expose immutable normalized records with availability, quality, provenance, license, cache, workflow, schema, normalization, and precision metadata.
- **Source class / function / method:** `MarketDataset`
- **Source side effects:** None
- **Source raises:** `DataError[DATA_QUALITY_FAILED]`: dataset violates blocking contract
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_005_market_dataset()`<br>**Unit:** `tests/data/unit/test_market_contracts.py::test_market_dataset_never_contains_provider_objects()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0174` [ ] `FR-DATA-006` - Validate one typed internal request containing source, symbol, kind, optional timeframe/range/limit, cache/quality policies, UTC/IANA inputs, workflow, preci...

- **Execution domain:** `Data`.
- **Execution position:** `174` of `675`.
- **Cannot start before:** Step `P01-S0173` (`FR-DATA-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `market.py` — Requests, Datasets, Quality, and Availability`` (line 659); matrix row `281`.
- **Source responsibility:** Validate one typed internal request containing source, symbol, kind, optional timeframe/range/limit, cache/quality policies, UTC/IANA inputs, workflow, precision, explicit fallbacks, and request ID.
- **Source class / function / method:** `MarketDataRequest`
- **Source side effects:** None
- **Source raises:** `DataError[INVALID_INPUT]`: invalid enum/range/limit/timezone/fallback
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_006_market_request()`<br>**Unit:** `tests/data/unit/test_market_contracts.py::test_market_data_request_rejects_implicit_fallback()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0175` [ ] `FR-DATA-007` - Represent indexed ranges, gaps, overlap/completeness evidence, record count, source revision/readiness, and provenance without materializing the full dataset.

- **Execution domain:** `Data`.
- **Execution position:** `175` of `675`.
- **Cannot start before:** Step `P01-S0174` (`FR-DATA-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `market.py` — Requests, Datasets, Quality, and Availability`` (line 660); matrix row `282`.
- **Source responsibility:** Represent indexed ranges, gaps, overlap/completeness evidence, record count, source revision/readiness, and provenance without materializing the full dataset.
- **Source class / function / method:** `DataAvailability`
- **Source side effects:** None
- **Source raises:** `DataError[VALIDATION_FAILED]`: inconsistent range or count evidence
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_007_availability()`<br>**Unit:** `tests/data/unit/test_market_contracts.py::test_availability_requires_measured_gaps()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0176` [ ] `FR-DATA-008` - Expose immutable normalized account, balance, margin, position, order, connectivity, and staleness evidence with exact decimals and UTC snapshot time.

- **Execution domain:** `Data`.
- **Execution position:** `176` of `675`.
- **Cannot start before:** Step `P01-S0175` (`FR-DATA-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `broker.py` — Broker Boundary Contracts`` (line 666); matrix row `283`.
- **Source responsibility:** Expose immutable normalized account, balance, margin, position, order, connectivity, and staleness evidence with exact decimals and UTC snapshot time.
- **Source class / function / method:** `AccountStateSnapshot`
- **Source side effects:** None
- **Source raises:** `DataError[STALE_EVIDENCE]`: snapshot expired; `DataError[VALIDATION_FAILED]`: evidence incomplete
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_008_account_snapshot()`<br>**Unit:** `tests/data/unit/test_broker_contracts.py::test_account_snapshot_rejects_stale_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0177` [ ] `FR-DATA-075` - Validate a bounded request for session, calendar, spread, liquidity, volatility, correlation, and crisis evidence for one declared scope.

- **Execution domain:** `Data`.
- **Execution position:** `177` of `675`.
- **Cannot start before:** Step `P01-S0176` (`FR-DATA-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `market_context.py` — Market-Context Evidence`` (line 673); matrix row `302`.
- **Source responsibility:** Validate a bounded request for session, calendar, spread, liquidity, volatility, correlation, and crisis evidence for one declared scope.
- **Source class / function / method:** `MarketContextRequest`
- **Source side effects:** None
- **Source raises:** `DataError[INVALID_INPUT]`: invalid scope, timezone, or evidence request
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_075_market_context_request()`<br>**Unit:** `tests/data/unit/test_market_context.py::test_request_rejects_unknown_scope()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-075`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0178` [ ] `FR-DATA-076` - Produce immutable MarketContextEvidence v1 with separate contract version/schema ID, UTC freshness, provenance, and explicit missingness; never produce a Ris...

- **Execution domain:** `Data`.
- **Execution position:** `178` of `675`.
- **Cannot start before:** Step `P01-S0177` (`FR-DATA-075`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `market_context.py` — Market-Context Evidence`` (line 674); matrix row `303`.
- **Source responsibility:** Produce immutable `MarketContextEvidence v1` with separate contract version/schema ID, UTC freshness, provenance, and explicit missingness; never produce a Risk verdict.
- **Source class / function / method:** `get_market_context_evidence(request: MarketContextRequest, provider: MarketContextProvider) -> MarketContextEvidence`
- **Source side effects:** Read-only provider/source calls
- **Source raises:** `DataError[STALE_EVIDENCE|SOURCE_UNAVAILABLE|VALIDATION_FAILED]`: mandatory evidence unavailable, stale, or malformed
- **Source usage / test:** **Usage:** `tests/data/usage/04_market_account_read.py::example_fr_data_076_market_context_evidence()`<br>**Unit:** `tests/data/unit/test_market_context.py::test_missing_evidence_is_explicit()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-076`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0179` [ ] `FR-DATA-078` - Validate source/target currencies, UTC as_of, explicit maximum age, and explicit allowed-path policy; reject same-leg cycles and unbounded discovery.

- **Execution domain:** `Data`.
- **Execution position:** `179` of `675`.
- **Cannot start before:** Step `P01-S0178` (`FR-DATA-076`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `fx.py` — FX Conversion Evidence`` (line 680); matrix row `304`.
- **Source responsibility:** Validate source/target currencies, UTC `as_of`, explicit maximum age, and explicit allowed-path policy; reject same-leg cycles and unbounded discovery.
- **Source class / function / method:** `FXConversionRequest`
- **Source side effects:** None
- **Source raises:** `DataError[INVALID_INPUT, LIMIT_EXCEEDED]`
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_078_fx_request()`<br>**Unit:** `tests/data/unit/test_fx_contracts.py::test_request_requires_explicit_policy_and_freshness()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-078`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0180` [ ] `FR-DATA-079` - Deterministically select an allowed acyclic direct/synthesized path and publish exact rates, UTC freshness, policy version, and source provenance as FXConver...

- **Execution domain:** `Data`.
- **Execution position:** `180` of `675`.
- **Cannot start before:** Step `P01-S0179` (`FR-DATA-078`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `fx.py` — FX Conversion Evidence`` (line 681); matrix row `305`.
- **Source responsibility:** Deterministically select an allowed acyclic direct/synthesized path and publish exact rates, UTC freshness, policy version, and source provenance as `FXConversionEvidence v1`; never fabricate a rate.
- **Source class / function / method:** `get_fx_conversion_evidence(request: FXConversionRequest, provider: FXRateProvider) -> FXConversionEvidence`
- **Source side effects:** Read-only provider/source calls
- **Source raises:** `DataError[DATA_NOT_FOUND, STALE_EVIDENCE, SOURCE_UNAVAILABLE, VALIDATION_FAILED]`
- **Source usage / test:** **Usage:** `tests/data/usage/04_market_account_read.py::example_fr_data_079_fx_evidence()`<br>**Unit:** `tests/data/unit/test_fx_contracts.py::test_path_is_acyclic_exact_and_provenanced()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-079`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0181` [ ] `FR-DATA-010` - Declare source readiness, capabilities, credential/network/write requirements, schema/timezone/version metadata, promotion criteria, and sign-off evidence.

- **Execution domain:** `Data`.
- **Execution position:** `181` of `675`.
- **Cannot start before:** Step `P01-S0180` (`FR-DATA-079`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `sources.py` — Source Contracts`` (line 687); matrix row `284`.
- **Source responsibility:** Declare source readiness, capabilities, credential/network/write requirements, schema/timezone/version metadata, promotion criteria, and sign-off evidence.
- **Source class / function / method:** `SourceDescriptor`
- **Source side effects:** None
- **Source raises:** `DataError[VALIDATION_FAILED]`: declaration incomplete or contradictory
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_010_source_descriptor()`<br>**Unit:** `tests/data/unit/test_source_contracts.py::test_source_descriptor_requires_capabilities()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0182` [ ] `FR-DATA-011` - Declare permitted workflow contexts, export/retention/attribution restrictions, enforcement behavior, and license status for each source.

- **Execution domain:** `Data`.
- **Execution position:** `182` of `675`.
- **Cannot start before:** Step `P01-S0181` (`FR-DATA-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `sources.py` — Source Contracts`` (line 688); matrix row `285`.
- **Source responsibility:** Declare permitted workflow contexts, export/retention/attribution restrictions, enforcement behavior, and license status for each source.
- **Source class / function / method:** `SourceLicensePolicy`
- **Source side effects:** None
- **Source raises:** `DataError[LICENSE_RESTRICTION]`: metadata missing or use forbidden
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_011_license_policy()`<br>**Unit:** `tests/data/unit/test_source_contracts.py::test_license_policy_fails_closed_for_validation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0183` [ ] `FR-DATA-012` - Expose one redacted domain exception carrying a manifest code, safe details, retryability, severity, request ID, and operator action without raw exceptions.

- **Execution domain:** `Data`.
- **Execution position:** `183` of `675`.
- **Cannot start before:** Step `P01-S0182` (`FR-DATA-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `errors.py` — Deterministic Errors`` (line 694); matrix row `286`.
- **Source responsibility:** Expose one redacted domain exception carrying a manifest code, safe details, retryability, severity, request ID, and operator action without raw exceptions.
- **Source class / function / method:** `DataError`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_012_data_error()`<br>**Unit:** `tests/data/unit/test_errors.py::test_data_error_redacts_sensitive_details()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0184` [ ] `FR-DATA-013` - Expose one immutable manifest for active deterministic codes and reserve UNKNOWN_ERROR for failures not otherwise mapped.

- **Execution domain:** `Data`.
- **Execution position:** `184` of `675`.
- **Cannot start before:** Step `P01-S0183` (`FR-DATA-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `errors.py` — Deterministic Errors`` (line 695); matrix row `287`.
- **Source responsibility:** Expose one immutable manifest for active deterministic codes and reserve `UNKNOWN_ERROR` for failures not otherwise mapped.
- **Source class / function / method:** `DATA_ERROR_MANIFEST: Mapping[str, ErrorDefinition]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/data/usage/01_contracts.py::example_fr_data_013_error_manifest()`<br>**Unit:** `tests/data/unit/test_errors.py::test_error_manifest_is_complete_and_unique()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0185` [ ] `FR-DATA-016` - Grant at most one writer lease per resolved path, reject conflicts deterministically, and release it on exit or verified stale recovery.

- **Execution domain:** `Data`.
- **Execution position:** `185` of `675`.
- **Cannot start before:** Step `P01-S0184` (`FR-DATA-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `database.py`, `locking.py`, and `migrations.py``` (line 799); matrix row `288`.
- **Source responsibility:** Grant at most one writer lease per resolved path, reject conflicts deterministically, and release it on exit or verified stale recovery.
- **Source class / function / method:** `acquire_write_lock(path: Path, request_id: str) -> WriteLock`
- **Source side effects:** Local state mutation; persistence write
- **Source raises:** `DataError[CONCURRENT_WRITE_LOCKED]`
- **Source usage / test:** **Usage:** `tests/data/usage/02_storage.py::example_fr_data_016_write_lock()`<br>**Unit:** `tests/data/unit/test_locking.py::test_write_lock_is_path_scoped_and_exclusive()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0186` [ ] `FR-DATA-023` - Require bounded, deterministically ordered symbol discovery with cursor pagination and declared discovery capability.

- **Execution domain:** `Data`.
- **Execution position:** `186` of `675`.
- **Cannot start before:** Step `P01-S0185` (`FR-DATA-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public source API`` (line 864); matrix row `289`.
- **Source responsibility:** Require bounded, deterministically ordered symbol discovery with cursor pagination and declared discovery capability.
- **Source class / function / method:** `MarketDataSource.list_symbols(request: SymbolListRequest) -> SymbolPage`
- **Source side effects:** External API call or Read-only
- **Source raises:** `DataError[UNSUPPORTED_OPERATION|LIMIT_EXCEEDED]`
- **Source usage / test:** **Usage:** `tests/data/usage/03_sources.py::example_fr_data_023_symbol_discovery()`<br>**Unit:** `tests/data/unit/test_source_protocol.py::test_list_symbols_is_bounded_and_ordered()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0187` [ ] `FR-DATA-026` - Validate requested and explicit fallback sources in order against capability, readiness, license, context, timeout/rate, and breaker state and record every a...

- **Execution domain:** `Data`.
- **Execution position:** `187` of `675`.
- **Cannot start before:** Step `P01-S0186` (`FR-DATA-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public source API`` (line 867); matrix row `290`.
- **Source responsibility:** Validate requested and explicit fallback sources in order against capability, readiness, license, context, timeout/rate, and breaker state and record every attempt.
- **Source class / function / method:** `evaluate_source_policy(request: MarketDataRequest) -> SourcePlan`
- **Source side effects:** Read-only
- **Source raises:** `DataError[LICENSE_RESTRICTION|SOURCE_UNAVAILABLE|CIRCUIT_BREAKER_OPEN]`
- **Source usage / test:** **Usage:** `tests/data/usage/03_sources.py::example_fr_data_026_source_policy()`<br>**Unit:** `tests/data/unit/test_source_policy.py::test_policy_never_invents_fallback()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0188` [ ] `FR-DATA-028` - Return a fresh normalized AccountStateSnapshot v1 from read-only Brokers BrokerAdapter account reads without exposing credentials/provider objects.

- **Execution domain:** `Data`.
- **Execution position:** `188` of `675`.
- **Cannot start before:** Step `P01-S0187` (`FR-DATA-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public source API`` (line 869); matrix row `291`.
- **Source responsibility:** Return a fresh normalized `AccountStateSnapshot v1` from read-only Brokers `BrokerAdapter` account reads without exposing credentials/provider objects.
- **Source class / function / method:** `get_account_state_snapshot(request: AccountSnapshotRequest, adapter: BrokerAdapter) -> AccountStateSnapshot`
- **Source side effects:** External API call (read-only, via Brokers)
- **Source raises:** `DataError[SOURCE_UNAVAILABLE|STALE_EVIDENCE|VALIDATION_FAILED]`
- **Source usage / test:** **Usage:** `tests/data/usage/03_sources.py::example_fr_data_028_read_only_account_snapshot()`<br>**Unit:** `tests/data/unit/test_broker_source.py::test_account_snapshot_fails_closed_when_incomplete()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0189` [ ] `FR-DATA-030` - Execute bounded bars/ticks/spreads retrieval through explicit source policy, versioned cache, normalization, quality, and precision, returning MarketDataset.

- **Execution domain:** `Data`.
- **Execution position:** `189` of `675`.
- **Cannot start before:** Step `P01-S0188` (`FR-DATA-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public access API`` (line 925); matrix row `292`.
- **Source responsibility:** Execute bounded bars/ticks/spreads retrieval through explicit source policy, versioned cache, normalization, quality, and precision, returning `MarketDataset`.
- **Source class / function / method:** `fetch_market_dataset(request: MarketDataRequest) -> MarketDataset`
- **Source side effects:** Read-only; optional External API call and cache write
- **Source raises:** `DataError`: mapped retrieval/quality/policy code
- **Source usage / test:** **Usage:** `tests/data/usage/04_market_account_read.py::example_fr_data_030_historical_market_read()`<br>**Unit:** `tests/data/unit/test_historical_access.py::test_fetch_market_dataset_reports_actual_source()` <br>**Evidence:** [historical.py:L51](file:///c:/Users/rharu/AppDev/HaruquantAI/app/services/data/access/historical.py#L51)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0190` [ ] `FR-DATA-031` - Return a bounded deterministic symbol page with cursor, source readiness, and provenance.

- **Execution domain:** `Data`.
- **Execution position:** `190` of `675`.
- **Cannot start before:** Step `P01-S0189` (`FR-DATA-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public access API`` (line 926); matrix row `293`.
- **Source responsibility:** Return a bounded deterministic symbol page with cursor, source readiness, and provenance.
- **Source class / function / method:** `discover_symbols(request: SymbolListRequest) -> SymbolPage`
- **Source side effects:** Read-only or External API call
- **Source raises:** `DataError[LIMIT_EXCEEDED|UNSUPPORTED_OPERATION|SOURCE_UNAVAILABLE]`
- **Source usage / test:** **Usage:** `tests/data/usage/04_market_account_read.py::example_fr_data_031_symbol_discovery()`<br>**Unit:** `tests/data/unit/test_reference_access.py::test_discover_symbols_cursor_is_stable()` <br>**Evidence:** [reference.py:L28](file:///c:/Users/rharu/AppDev/HaruquantAI/app/services/data/access/reference.py#L28)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0191` [ ] `FR-DATA-032` - Return normalized asset-aware metadata and explicitly mark unknown optional fields without provider-derived optimistic defaults.

- **Execution domain:** `Data`.
- **Execution position:** `191` of `675`.
- **Cannot start before:** Step `P01-S0190` (`FR-DATA-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public access API`` (line 927); matrix row `294`.
- **Source responsibility:** Return normalized asset-aware metadata and explicitly mark unknown optional fields without provider-derived optimistic defaults.
- **Source class / function / method:** `fetch_symbol_metadata(request: SymbolMetadataRequest) -> SymbolMetadata`
- **Source side effects:** Read-only or External API call
- **Source raises:** `DataError[DATA_NOT_FOUND|MISSING_ASSET_METADATA]`
- **Source usage / test:** **Usage:** `tests/data/usage/04_market_account_read.py::example_fr_data_032_symbol_metadata()`<br>**Unit:** `tests/data/unit/test_reference_access.py::test_fetch_metadata_preserves_unknown_fields()` <br>**Evidence:** [reference.py:L67](file:///c:/Users/rharu/AppDev/HaruquantAI/app/services/data/access/reference.py#L67)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-032`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0192` [ ] `FR-DATA-033` - Compute ranges, gaps, overlaps, completeness, count, revision, and readiness from manifests/indexes and bounded probing, never hard-code certainty.

- **Execution domain:** `Data`.
- **Execution position:** `192` of `675`.
- **Cannot start before:** Step `P01-S0191` (`FR-DATA-032`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public access API`` (line 928); matrix row `295`.
- **Source responsibility:** Compute ranges, gaps, overlaps, completeness, count, revision, and readiness from manifests/indexes and bounded probing, never hard-code certainty.
- **Source class / function / method:** `inspect_availability(request: AvailabilityRequest) -> DataAvailability`
- **Source side effects:** Read-only; optional External API call
- **Source raises:** `DataError[LIMIT_EXCEEDED|SOURCE_UNAVAILABLE|DATABASE_ERROR]`
- **Source usage / test:** **Usage:** `tests/data/usage/04_market_account_read.py::example_fr_data_033_availability()`<br>**Unit:** `tests/data/unit/test_reference_access.py::test_availability_never_hardcodes_ready()` <br>**Evidence:** [reference.py:L198](file:///c:/Users/rharu/AppDev/HaruquantAI/app/services/data/access/reference.py#L198)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-033`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0193` [ ] `FR-DATA-034` - Return current configured hours and normalized UTC sessions, advance cross-midnight windows correctly, and reject historical reconstruction.

- **Execution domain:** `Data`.
- **Execution position:** `193` of `675`.
- **Cannot start before:** Step `P01-S0192` (`FR-DATA-033`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public access API`` (line 929); matrix row `296`.
- **Source responsibility:** Return current configured hours and normalized UTC sessions, advance cross-midnight windows correctly, and reject historical reconstruction.
- **Source class / function / method:** `get_current_schedule(request: ScheduleRequest, calendar: MarketCalendar) -> MarketSchedule`
- **Source side effects:** Read-only provider call
- **Source raises:** `DataError[UNSUPPORTED_OPERATION|VALIDATION_FAILED]`
- **Source usage / test:** **Usage:** `tests/data/usage/04_market_account_read.py::example_fr_data_034_current_schedule()`<br>**Unit:** `tests/data/unit/test_sessions.py::test_current_schedule_advances_midnight_end()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-034`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0194` [ ] `FR-DATA-035` - Return bounded source-native or derived volume as records, buckets, or summary with explicit volume kind/unit and provenance.

- **Execution domain:** `Data`.
- **Execution position:** `194` of `675`.
- **Cannot start before:** Step `P01-S0193` (`FR-DATA-034`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public access API`` (line 930); matrix row `297`.
- **Source responsibility:** Return bounded source-native or derived volume as records, buckets, or summary with explicit volume kind/unit and provenance.
- **Source class / function / method:** `fetch_historical_volume(request: VolumeRequest) -> VolumeResult`
- **Source side effects:** Read-only; optional External API call/cache write
- **Source raises:** `DataError[INVALID_INPUT|LIMIT_EXCEEDED|DATA_QUALITY_FAILED]`
- **Source usage / test:** **Usage:** `tests/data/usage/04_market_account_read.py::example_fr_data_035_historical_volume()`<br>**Unit:** `tests/data/unit/test_sessions.py::test_volume_modes_have_stable_contracts()` <br>**Evidence:** [sessions.py:L191](file:///c:/Users/rharu/AppDev/HaruquantAI/app/services/data/access/sessions.py#L191)
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-035`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0195` [ ] `FR-DATA-036` - Resample ordered canonical OHLCV only to a supported higher timeframe using deterministic OHLCV/spread aggregation and updated available_at.

- **Execution domain:** `Data`.
- **Execution position:** `195` of `675`.
- **Cannot start before:** Step `P01-S0194` (`FR-DATA-035`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public processing API`` (line 998); matrix row `298`.
- **Source responsibility:** Resample ordered canonical OHLCV only to a supported higher timeframe using deterministic OHLCV/spread aggregation and updated `available_at`.
- **Source class / function / method:** `resample_dataset(dataset: MarketDataset, target_timeframe: str) -> MarketDataset`
- **Source side effects:** None
- **Source raises:** `DataError[UNSUPPORTED_TIMEFRAME|VALIDATION_FAILED|DATA_QUALITY_FAILED]`
- **Source usage / test:** **Usage:** `tests/data/usage/05_processing.py::example_fr_data_036_resample_ohlcv()`<br>**Unit:** `tests/data/unit/test_transforms.py::test_resample_dataset_is_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-036`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0196` [ ] `FR-DATA-039` - Generate bounded canonical bars or ticks with GBM, exact parameters, and deterministic output when a seed is supplied; generation is not a source adapter.

- **Execution domain:** `Data`.
- **Execution position:** `196` of `675`.
- **Cannot start before:** Step `P01-S0195` (`FR-DATA-036`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public processing API`` (line 1001); matrix row `299`.
- **Source responsibility:** Generate bounded canonical bars or ticks with GBM, exact parameters, and deterministic output when a seed is supplied; generation is not a source adapter.
- **Source class / function / method:** `generate_synthetic_dataset(request: SyntheticRequest) -> MarketDataset`
- **Source side effects:** None
- **Source raises:** `DataError[INVALID_INPUT|LIMIT_EXCEEDED|PRECISION_MISMATCH]`
- **Source usage / test:** **Usage:** `tests/data/usage/05_processing.py::example_fr_data_039_synthetic_bars()`<br>**Unit:** `tests/data/unit/test_synthetic.py::test_synthetic_dataset_replays_from_seed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-039`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0197` [ ] `FR-DATA-041` - Derive the stable SHA-256 idempotency key from source, symbol, kind, timeframe, start/end, schema version, and normalization version.

- **Execution domain:** `Data`.
- **Execution position:** `197` of `675`.
- **Cannot start before:** Step `P01-S0196` (`FR-DATA-039`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public job runtime API`` (line 1052); matrix row `300`.
- **Source responsibility:** Derive the stable SHA-256 idempotency key from source, symbol, kind, timeframe, start/end, schema version, and normalization version.
- **Source class / function / method:** `derive_backfill_key(request: BackfillChunkRequest) -> str`
- **Source side effects:** None
- **Source raises:** `DataError[INVALID_INPUT]`
- **Source usage / test:** **Usage:** `tests/data/usage/06_update_jobs.py::example_fr_data_041_backfill_key()`<br>**Unit:** `tests/data/unit/test_backfill.py::test_backfill_key_is_canonical()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-041`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0198` [ ] `FR-DATA-046` - Start one internal feed only for a declared live-capable staging/production source, persist initial state, and expose no public subscription handle.

- **Execution domain:** `Data`.
- **Execution position:** `198` of `675`.
- **Cannot start before:** Step `P01-S0197` (`FR-DATA-041`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Public feed runtime API`` (line 1104); matrix row `301`.
- **Source responsibility:** Start one internal feed only for a declared live-capable staging/production source, persist initial state, and expose no public subscription handle.
- **Source class / function / method:** `start_internal_feed(config: FeedConfig) -> FeedStatus`
- **Source side effects:** Local state mutation; persistence write; External API call
- **Source raises:** `DataError[SOURCE_UNAVAILABLE|POLICY_BLOCKED|VALIDATION_FAILED]`
- **Source usage / test:** **Usage:** `tests/data/usage/07_realtime_feeds.py::example_fr_data_046_start_internal_feed()`<br>**Unit:** `tests/data/unit/test_feed_runtime.py::test_start_feed_requires_declared_capability()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-DATA-046`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0199` [ ] `WF-DATA-001` - Historical Bars, Ticks, and Spreads

- **Execution domain:** `Data`.
- **Execution position:** `199` of `675`.
- **Cannot start before:** Step `P01-S0198` (`FR-DATA-046`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 3. Workflows > Workflow scope values`` (line 361); matrix row `309`.
- **Source scope:** Cross-domain
- **Source workflow:** Historical bars/ticks/spreads retrieval
- **Source requirement sequence:** `FR-DATA-006 → 026 → 030`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-DATA-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0200` [ ] `WF-DATA-013` - Account Snapshot Service

- **Execution domain:** `Data`.
- **Execution position:** `200` of `675`.
- **Cannot start before:** Step `P01-S0199` (`WF-DATA-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 3. Workflows > Workflow scope values`` (line 373); matrix row `310`.
- **Source scope:** Cross-domain
- **Source workflow:** Account snapshot service
- **Source requirement sequence:** `FR-DATA-028 → 008`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-DATA-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0201` [ ] `WF-DATA-014` - Risk Market-Context Evidence

- **Execution domain:** `Data`.
- **Execution position:** `201` of `675`.
- **Cannot start before:** Step `P01-S0200` (`WF-DATA-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 3. Workflows > Workflow scope values`` (line 374); matrix row `311`.
- **Source scope:** Cross-domain
- **Source workflow:** Risk market-context evidence
- **Source requirement sequence:** `FR-DATA-075 → 076`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-DATA-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0202` [ ] `CAP-DATA-003` - Source protocol/registry/readiness/adapters

- **Execution domain:** `Data`.
- **Execution position:** `202` of `675`.
- **Cannot start before:** Step `P01-S0201` (`WF-DATA-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 2. Final Package Structure > Reconciliation capability coverage`` (line 313); matrix row `274`.
- **Source capability:** `CAP-DATA-003` Source protocol/registry/readiness/adapters
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-DATA-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0203` [ ] `CAP-DATA-004` - Canonical records/UTC/versioning

- **Execution domain:** `Data`.
- **Execution position:** `203` of `675`.
- **Cannot start before:** Step `P01-S0202` (`CAP-DATA-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Data` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/data/README.md` - ``Data > 2. Final Package Structure > Reconciliation capability coverage`` (line 314); matrix row `275`.
- **Source capability:** `CAP-DATA-004` Canonical records/UTC/versioning
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-DATA-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 5 - Indicators

Implement `Indicators` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0204` [ ] `P-INDI-001` - core feature/component (provisional)

- **Execution domain:** `Indicators`.
- **Execution position:** `204` of `675`.
- **Cannot start before:** Step `P01-S0203` (`CAP-DATA-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-INDI-001`); matrix row `405`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-INDI-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0205` [ ] `P-INDI-002` - trend feature/component (provisional)

- **Execution domain:** `Indicators`.
- **Execution position:** `205` of `675`.
- **Cannot start before:** Step `P01-S0204` (`P-INDI-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` module `4.2` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-INDI-002`); matrix row `406`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-INDI-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0206` [ ] `FR-INDI-001` - expose exactly the approved Core MVP codes: IND_INVALID_CONFIG, IND_INVALID_PARAMETER, IND_UNSUPPORTED_INDICATOR, IND_UNSUPPORTED_TIMEFRAME,...

- **Execution domain:** `Indicators`.
- **Execution position:** `206` of `675`.
- **Cannot start before:** Step `P01-S0205` (`P-INDI-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `errors.py` — Deterministic Error Contract`` (line 400); matrix row `388`.
- **Source responsibility:** The system shall expose exactly the approved Core MVP codes: `IND_INVALID_CONFIG`, `IND_INVALID_PARAMETER`, `IND_UNSUPPORTED_INDICATOR`, `IND_UNSUPPORTED_TIMEFRAME`, `IND_UNSUPPORTED_DTYPE`, `IND_INVALID_INPUT_SCHEMA`, `IND_MISSING_REQUIRED_COLUMN`, `IND_INVALID_OUTPUT_COLUMN`, `IND_OUTPUT_COLUMN_CONFLICT`, `IND_INVALID_OUTPUT_MODE`, `IND_INPUT_MUTATION_DETECTED`, `IND_DUPLICATE_TIMESTAMP`, `IND_NON_MONOTONIC_TIME`, `IND_AMBIGUOUS_TIMESTAMP`, `IND_INVALID_TIMEZONE`, `IND_INVALID_OHLC`, `IND_INSUFFICIENT_DATA`, `IND_LOOKAHEAD_RISK`, `IND_FORMULA_VERSION_MISMATCH`, `IND_RESOURCE_LIMIT_EXCEEDED`, `IND_TIMEOUT`, `IND_CANCELLED`, `IND_PARTIAL_RESULT`, and `IND_INTERNAL_ERROR`.
- **Source class / function / method:** `IndicatorErrorCode: StrEnum`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_errors_error_codes()`<br>**Unit:** `tests/indicators/unit/test_errors.py::test_error_code_catalog_contains_only_core_codes()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0207` [ ] `FR-INDI-002` - represent a deterministic, redacted failure with code, safe message, and structured details without exposing raw exceptions or sensitive inp...

- **Execution domain:** `Indicators`.
- **Execution position:** `207` of `675`.
- **Cannot start before:** Step `P01-S0206` (`FR-INDI-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `errors.py` — Deterministic Error Contract`` (line 401); matrix row `389`.
- **Source responsibility:** The system shall represent a deterministic, redacted failure with code, safe message, and structured details without exposing raw exceptions or sensitive input data.
- **Source class / function / method:** `IndicatorError(code: IndicatorErrorCode, message: str, details: Mapping[str, object] | None = None)`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_errors_indicator_error()`<br>**Unit:** `tests/indicators/unit/test_errors.py::test_indicator_error_serializes_redacted_details()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0208` [ ] `FR-INDI-003` - represent indicator ID, canonical parameters, source, output/precision/availability/quality policy, error mode, and basic limits in one immu...

- **Execution domain:** `Indicators`.
- **Execution position:** `208` of `675`.
- **Cannot start before:** Step `P01-S0207` (`FR-INDI-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `contracts.py` — Immutable Calculation Contracts`` (line 417); matrix row `390`.
- **Source responsibility:** The system shall represent indicator ID, canonical parameters, source, output/precision/availability/quality policy, error mode, and basic limits in one immutable batch config, excluding cache, calendar, backend, actor, tracing, SLO, and entitlement context.
- **Source class / function / method:** `IndicatorConfig`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_contracts_indicator_config()`<br>**Unit:** `tests/indicators/unit/test_contracts.py::test_indicator_config_is_immutable_and_core_only()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0209` [ ] `FR-INDI-004` - describe each official indicator's ID, name, versions, tier, required columns, parameter/output schemas, warmup policy, supported batch capa...

- **Execution domain:** `Indicators`.
- **Execution position:** `209` of `675`.
- **Cannot start before:** Step `P01-S0208` (`FR-INDI-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `contracts.py` — Immutable Calculation Contracts`` (line 418); matrix row `391`.
- **Source responsibility:** The system shall describe each official indicator's ID, name, versions, tier, required columns, parameter/output schemas, warmup policy, supported batch capabilities, import path, stability, and workflow eligibility.
- **Source class / function / method:** `IndicatorSpec`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_contracts_indicator_spec()`<br>**Unit:** `tests/indicators/unit/test_contracts.py::test_indicator_spec_contains_required_public_metadata()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0210` [ ] `FR-INDI-005` - expose the exact normalized history requirement for an indicator/config without fetching data, including minimum observations, source timefr...

- **Execution domain:** `Indicators`.
- **Execution position:** `210` of `675`.
- **Cannot start before:** Step `P01-S0209` (`FR-INDI-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `contracts.py` — Immutable Calculation Contracts`` (line 419); matrix row `392`.
- **Source responsibility:** The system shall expose the exact normalized history requirement for an indicator/config without fetching data, including minimum observations, source timeframe, required columns, and availability basis.
- **Source class / function / method:** `WarmupRequirement`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_contracts_warmup_requirement()`<br>**Unit:** `tests/indicators/unit/test_contracts.py::test_warmup_requirement_is_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0211` [ ] `FR-INDI-006` - expose a minimal structural batch protocol whose approved calculation accepts normalized pandas data plus IndicatorConfig and returns Indica...

- **Execution domain:** `Indicators`.
- **Execution position:** `211` of `675`.
- **Cannot start before:** Step `P01-S0210` (`FR-INDI-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `contracts.py` — Immutable Calculation Contracts`` (line 420); matrix row `393`.
- **Source responsibility:** The system shall expose a minimal structural batch protocol whose approved calculation accepts normalized pandas data plus `IndicatorConfig` and returns `IndicatorResult`; no inheritance, context, or state lifecycle is required.
- **Source class / function / method:** `IndicatorProtocol.calculate(data: pd.DataFrame, config: IndicatorConfig) -> IndicatorResult`
- **Source side effects:** None
- **Source raises:** `IndicatorError`: deterministic request/calculation failure under the approved error mode
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_contracts_indicator_protocol()`<br>**Unit:** `tests/indicators/unit/test_contracts.py::test_official_calculator_satisfies_indicator_protocol()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0212` [ ] `FR-INDI-007` - expose a standalone serializable deterministic manifest containing manifest/indicator/formula/output-schema versions, canonical parameter ha...

- **Execution domain:** `Indicators`.
- **Execution position:** `212` of `675`.
- **Cannot start before:** Step `P01-S0211` (`FR-INDI-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Manifest and Result Behavior`` (line 430); matrix row `394`.
- **Source responsibility:** The system shall expose a standalone serializable deterministic manifest containing manifest/indicator/formula/output-schema versions, canonical parameter hash, input/output checksums, output contract and shape, precision, availability policy, Data-provided provenance, and quality summary; volatile runtime/host data is excluded from identity.
- **Source class / function / method:** `IndicatorManifest`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_results_manifest()`<br>**Unit:** `tests/indicators/unit/test_results.py::test_manifest_is_stable_for_equivalent_inputs()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0213` [ ] `FR-INDI-008` - return timestamp/symbol-aligned values, canonical output columns, availability, quality, errors, and manifest as IndicatorSeries v1, preserv...

- **Execution domain:** `Indicators`.
- **Execution position:** `213` of `675`.
- **Cannot start before:** Step `P01-S0212` (`FR-INDI-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Manifest and Result Behavior`` (line 431); matrix row `395`.
- **Source responsibility:** The system shall return timestamp/symbol-aligned values, canonical output columns, availability, quality, errors, and manifest as `IndicatorSeries v1`, preserving warmup and unavailable rows and exposing no incremental state or metrics.
- **Source class / function / method:** `IndicatorResult`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_results_indicator_result()`<br>**Unit:** `tests/indicators/unit/test_results.py::test_indicator_result_matches_v1_contract()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0214` [ ] `FR-INDI-009` - expose a copy-safe projection containing generated indicator, availability, and quality columns without original OHLCV columns.

- **Execution domain:** `Indicators`.
- **Execution position:** `214` of `675`.
- **Cannot start before:** Step `P01-S0213` (`FR-INDI-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Manifest and Result Behavior`` (line 432); matrix row `396`.
- **Source responsibility:** The system shall expose a copy-safe projection containing generated indicator, availability, and quality columns without original OHLCV columns.
- **Source class / function / method:** `IndicatorResult.values_only: pd.DataFrame`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_results_values_only()`<br>**Unit:** `tests/indicators/unit/test_results.py::test_values_only_excludes_source_columns()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0215` [ ] `FR-INDI-010` - append generated columns to a copy of aligned source data while preserving original columns, row count/order, timestamp/symbol layout, warmu...

- **Execution domain:** `Indicators`.
- **Execution position:** `215` of `675`.
- **Cannot start before:** Step `P01-S0214` (`FR-INDI-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Manifest and Result Behavior`` (line 433); matrix row `397`.
- **Source responsibility:** The system shall append generated columns to a copy of aligned source data while preserving original columns, row count/order, timestamp/symbol layout, warmup rows, and input identity; collisions fail.
- **Source class / function / method:** `IndicatorResult.join_to(data: pd.DataFrame, mode: Literal["copy"] = "copy") -> pd.DataFrame`
- **Source side effects:** None
- **Source raises:** `IndicatorError`: invalid mode, misalignment, output collision, or detected mutation
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_results_join_to()`<br>**Unit:** `tests/indicators/unit/test_results.py::test_join_to_preserves_input_and_alignment()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0216` [ ] `FR-INDI-011` - resolve one official ID (ema, sma, adx, atr, adr, rolling_volatility, rsi, williams_r) to its immutable spec and reject all unknown IDs befo...

- **Execution domain:** `Indicators`.
- **Execution position:** `216` of `675`.
- **Cannot start before:** Step `P01-S0215` (`FR-INDI-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `registry.py` — Immutable Official Discovery`` (line 443); matrix row `398`.
- **Source responsibility:** The system shall resolve one official ID (`ema`, `sma`, `adx`, `atr`, `adr`, `rolling_volatility`, `rsi`, `williams_r`) to its immutable spec and reject all unknown IDs before calculation.
- **Source class / function / method:** `get_indicator(indicator_id: str) -> IndicatorSpec`
- **Source side effects:** None
- **Source raises:** `IndicatorError`: `IND_UNSUPPORTED_INDICATOR`
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_registry_get_indicator()`<br>**Unit:** `tests/indicators/unit/test_registry.py::test_get_indicator_rejects_unknown_id()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0217` [ ] `FR-INDI-012` - list official specs in stable indicator-ID order with no mutable registry handle.

- **Execution domain:** `Indicators`.
- **Execution position:** `217` of `675`.
- **Cannot start before:** Step `P01-S0216` (`FR-INDI-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `registry.py` — Immutable Official Discovery`` (line 444); matrix row `399`.
- **Source responsibility:** The system shall list official specs in stable indicator-ID order with no mutable registry handle.
- **Source class / function / method:** `list_indicators() -> tuple[IndicatorSpec, ...]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_registry_list_indicators()`<br>**Unit:** `tests/indicators/unit/test_registry.py::test_list_indicators_is_stable_and_immutable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0218` [ ] `FR-INDI-013` - expose a JSON/YAML-compatible matrix containing ID, versions, tier, batch/vectorized/multi-symbol/multi-timeframe support, unsupported optio...

- **Execution domain:** `Indicators`.
- **Execution position:** `218` of `675`.
- **Cannot start before:** Step `P01-S0217` (`FR-INDI-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `registry.py` — Immutable Official Discovery`` (line 445); matrix row `400`.
- **Source responsibility:** The system shall expose a JSON/YAML-compatible matrix containing ID, versions, tier, batch/vectorized/multi-symbol/multi-timeframe support, unsupported optional modes, dependencies, deterministic unsupported codes, and official-workflow eligibility.
- **Source class / function / method:** `get_capability_matrix() -> tuple[Mapping[str, object], ...]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_registry_capability_matrix()`<br>**Unit:** `tests/indicators/unit/test_registry.py::test_capability_matrix_matches_registry()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0219` [ ] `FR-INDI-014` - resolve the spec and atomically validate config, parameters, resource limits, lowercase/unique required columns, supported dtype/timeframe, ...

- **Execution domain:** `Indicators`.
- **Execution position:** `219` of `675`.
- **Cannot start before:** Step `P01-S0218` (`FR-INDI-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `validation.py` — Fail-Fast Request Validation`` (line 455); matrix row `401`.
- **Source responsibility:** The system shall resolve the spec and atomically validate config, parameters, resource limits, lowercase/unique required columns, supported dtype/timeframe, explicit UTC symbol/timestamp keys, monotonicity/duplicates, finite OHLC consistency, data sufficiency, output names/collisions, and formula version before calculation; upstream source-quality rules remain Data-owned.
- **Source class / function / method:** `validate_indicator(indicator_id: str, data: pd.DataFrame, config: IndicatorConfig) -> IndicatorSpec`
- **Source side effects:** None
- **Source raises:** `IndicatorError`: first deterministic Core validation failure
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_core.py::test_usage_validation_validate_indicator()`<br>**Unit:** `tests/indicators/unit/test_validation.py::test_validate_indicator_fails_before_formula_execution()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0220` [ ] `FR-INDI-015` - calculate EMA per symbol from a validated source using the approved seed/smoothing contract, return ema_{period} (or source-qualified name),...

- **Execution domain:** `Indicators`.
- **Execution position:** `220` of `675`.
- **Cannot start before:** Step `P01-S0219` (`FR-INDI-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `moving_averages.py` — EMA and SMA`` (line 516); matrix row `402`.
- **Source responsibility:** The system shall calculate EMA per symbol from a validated source using the approved seed/smoothing contract, return `ema_{period}` (or source-qualified name), preserve warmup rows, and expose causal availability and a deterministic manifest without mutating input.
- **Source class / function / method:** `ema(data: pd.DataFrame, *, period: int, source: str = "close", config: IndicatorConfig | None = None) -> IndicatorResult`
- **Source side effects:** None
- **Source raises:** `IndicatorError`: validation, formula-version, limit, timeout, or atomic calculation failure
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_trend.py::test_usage_moving_averages_ema()`<br>**Unit:** `tests/indicators/unit/test_moving_averages.py::test_ema_matches_approved_golden_fixture()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0221` [ ] `FR-INDI-016` - calculate SMA per symbol over the approved inclusive window, return deterministic source-qualified output, preserve warmup rows, and expose ...

- **Execution domain:** `Indicators`.
- **Execution position:** `221` of `675`.
- **Cannot start before:** Step `P01-S0220` (`FR-INDI-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `moving_averages.py` — EMA and SMA`` (line 517); matrix row `403`.
- **Source responsibility:** The system shall calculate SMA per symbol over the approved inclusive window, return deterministic source-qualified output, preserve warmup rows, and expose causal availability and a deterministic manifest without mutating input.
- **Source class / function / method:** `sma(data: pd.DataFrame, *, period: int, source: str = "close", config: IndicatorConfig | None = None) -> IndicatorResult`
- **Source side effects:** None
- **Source raises:** `IndicatorError`: validation, formula-version, limit, timeout, or atomic calculation failure
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_trend.py::test_usage_moving_averages_sma()`<br>**Unit:** `tests/indicators/unit/test_moving_averages.py::test_sma_matches_approved_golden_fixture()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0222` [ ] `FR-INDI-017` - calculate approved ADX, +DI, and -DI values per symbol from validated OHLC, return the three canonical columns with warmup/availability meta...

- **Execution domain:** `Indicators`.
- **Execution position:** `222` of `675`.
- **Cannot start before:** Step `P01-S0221` (`FR-INDI-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `directional.py` — ADX`` (line 525); matrix row `404`.
- **Source responsibility:** The system shall calculate approved ADX, +DI, and -DI values per symbol from validated OHLC, return the three canonical columns with warmup/availability metadata, and handle zero range deterministically.
- **Source class / function / method:** `adx(data: pd.DataFrame, *, period: int, config: IndicatorConfig | None = None) -> IndicatorResult`
- **Source side effects:** None
- **Source raises:** `IndicatorError`: validation, formula-version, limit, timeout, or atomic calculation failure
- **Source usage / test:** **Usage:** `tests/indicators/usage/test_usage_trend.py::test_usage_directional_adx()`<br>**Unit:** `tests/indicators/unit/test_directional.py::test_adx_matches_approved_golden_fixture()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-INDI-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0223` [ ] `WF-INDI-001` - Core Batch Indicator Calculation

- **Execution domain:** `Indicators`.
- **Execution position:** `223` of `675`.
- **Cannot start before:** Step `P01-S0222` (`FR-INDI-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 3. Workflows > Workflow register`` (line 215); matrix row `407`.
- **Source scope:** Internal
- **Source workflow:** Core batch indicator calculation
- **Source requirement sequence:** `FR-INDI-014 → FR-INDI-015..022 → FR-INDI-007..010`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-INDI-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0224` [ ] `WF-INDI-002` - Decision-Time Consumption

- **Execution domain:** `Indicators`.
- **Execution position:** `224` of `675`.
- **Cannot start before:** Step `P01-S0223` (`WF-INDI-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Indicators` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/indicators/README.md` - ``Indicators > 3. Workflows > Workflow register`` (line 216); matrix row `408`.
- **Source scope:** Cross-domain
- **Source workflow:** Decision-time consumption
- **Source requirement sequence:** `FR-INDI-014 → FR-INDI-015..022 → FR-INDI-008`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-INDI-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 6 - Strategy

Implement `Strategy` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0225` [ ] `P-STR-001` - contracts feature/component (provisional)

- **Execution domain:** `Strategy`.
- **Execution position:** `225` of `675`.
- **Cannot start before:** Step `P01-S0224` (`WF-INDI-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-STR-001`); matrix row `463`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-STR-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0226` [ ] `P-STR-004` - intents feature/component (provisional)

- **Execution domain:** `Strategy`.
- **Execution position:** `226` of `675`.
- **Cannot start before:** Step `P01-S0225` (`P-STR-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` module `4.4` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-STR-004`); matrix row `464`
- **Source type:** Determinism
- **Source responsibility:** Identical strategy version, config, data, indicators, context, seed, and interface version shall produce identical decisions and intents.
- **Source verification:** Golden and property replay tests
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-STR-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0227` [ ] `P-STR-006` - vectorized feature/component (provisional)

- **Execution domain:** `Strategy`.
- **Execution position:** `227` of `675`.
- **Cannot start before:** Step `P01-S0226` (`P-STR-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` module `4.6` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-STR-006`); matrix row `465`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-STR-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0228` [ ] `FR-STR-001` - enumerate only approved Strategy runtime profiles and reject unsupported values.

- **Execution domain:** `Strategy`.
- **Execution position:** `228` of `675`.
- **Cannot start before:** Step `P01-S0227` (`P-STR-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 458); matrix row `433`.
- **Source responsibility:** The system shall enumerate only approved Strategy runtime profiles and reject unsupported values.
- **Source class / function / method:** `StrategyEnvironment`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_strategy_environment()`**Unit:** `tests/strategy/unit/test_models.py::test_strategy_environment_rejects_shadow_initially()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0229` [ ] `FR-STR-002` - identify the decision timing policy used for every evaluation.

- **Execution domain:** `Strategy`.
- **Execution position:** `229` of `675`.
- **Cannot start before:** Step `P01-S0228` (`FR-STR-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 459); matrix row `434`.
- **Source responsibility:** The system shall identify the decision timing policy used for every evaluation.
- **Source class / function / method:** `StrategyTimingPolicy`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_strategy_timing_policy()`**Unit:** `tests/strategy/unit/test_models.py::test_timing_policy_serializes_stably()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0230` [ ] `FR-STR-003` - represent immutable registry lifecycle eligibility without granting governance approval itself.

- **Execution domain:** `Strategy`.
- **Execution position:** `230` of `675`.
- **Cannot start before:** Step `P01-S0229` (`FR-STR-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 460); matrix row `435`.
- **Source responsibility:** The system shall represent immutable registry lifecycle eligibility without granting governance approval itself.
- **Source class / function / method:** `StrategyLifecycleStatus`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_lifecycle_status()`**Unit:** `tests/strategy/unit/test_models.py::test_revoked_status_is_never_executable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0231` [ ] `FR-STR-004` - accept a non-empty strategy id, exact version or version constraint, environment, and trace identifiers for resolution.

- **Execution domain:** `Strategy`.
- **Execution position:** `231` of `675`.
- **Cannot start before:** Step `P01-S0230` (`FR-STR-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 461); matrix row `436`.
- **Source responsibility:** The system shall accept a non-empty strategy id, exact version or version constraint, environment, and trace identifiers for resolution.
- **Source class / function / method:** `StrategyRef`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_strategy_ref()`**Unit:** `tests/strategy/unit/test_models.py::test_strategy_ref_requires_exactly_one_version_selector()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0232` [ ] `FR-STR-005` - expose the single immutable registry entry selected for execution, including hashes and compatibility metadata.

- **Execution domain:** `Strategy`.
- **Execution position:** `232` of `675`.
- **Cannot start before:** Step `P01-S0231` (`FR-STR-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 462); matrix row `437`.
- **Source responsibility:** The system shall expose the single immutable registry entry selected for execution, including hashes and compatibility metadata.
- **Source class / function / method:** `ValidatedStrategyRef`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_validated_ref()`**Unit:** `tests/strategy/unit/test_models.py::test_validated_ref_is_immutable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0233` [ ] `FR-STR-006` - represent declarative JSON-compatible strategy configuration without executable values.

- **Execution domain:** `Strategy`.
- **Execution position:** `233` of `675`.
- **Cannot start before:** Step `P01-S0232` (`FR-STR-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 463); matrix row `438`.
- **Source responsibility:** The system shall represent declarative JSON-compatible strategy configuration without executable values.
- **Source class / function / method:** `StrategyConfig`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_strategy_config()`**Unit:** `tests/strategy/unit/test_models.py::test_strategy_config_rejects_executable_values()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0234` [ ] `FR-STR-007` - expose normalized defaults, schema version, and canonical configuration hash after validation.

- **Execution domain:** `Strategy`.
- **Execution position:** `234` of `675`.
- **Cannot start before:** Step `P01-S0233` (`FR-STR-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 464); matrix row `439`.
- **Source responsibility:** The system shall expose normalized defaults, schema version, and canonical configuration hash after validation.
- **Source class / function / method:** `ValidatedStrategyConfig`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_validated_config()`**Unit:** `tests/strategy/unit/test_models.py::test_validated_config_hash_is_canonical()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0235` [ ] `FR-STR-008` - define one applicability-aware manifest for identity, data, indicators, timing, environments, resources, local risk assumptions, execution p...

- **Execution domain:** `Strategy`.
- **Execution position:** `235` of `675`.
- **Cannot start before:** Step `P01-S0234` (`FR-STR-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 465); matrix row `440`.
- **Source responsibility:** The system shall define one applicability-aware manifest for identity, data, indicators, timing, environments, resources, local risk assumptions, execution preferences, and provenance.
- **Source class / function / method:** `StrategyManifest`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_strategy_manifest()`**Unit:** `tests/strategy/unit/test_models.py::test_manifest_requires_only_applicable_declarations()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0236` [ ] `FR-STR-009` - define the completeStrategyRegistrationRequest v1 receiver-owned command described in Section 1.

- **Execution domain:** `Strategy`.
- **Execution position:** `236` of `675`.
- **Cannot start before:** Step `P01-S0235` (`FR-STR-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 466); matrix row `441`.
- **Source responsibility:** The system shall define the complete`StrategyRegistrationRequest v1` receiver-owned command described in Section 1.
- **Source class / function / method:** `StrategyRegistrationRequest`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_registration_request()`**Unit:** `tests/strategy/unit/test_models.py::test_registration_request_rejects_user_file_path()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0237` [ ] `FR-STR-010` - define the completeStrategyParameterUpdateRequest v1 receiver-owned command described in Section 1.

- **Execution domain:** `Strategy`.
- **Execution position:** `237` of `675`.
- **Cannot start before:** Step `P01-S0236` (`FR-STR-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 467); matrix row `442`.
- **Source responsibility:** The system shall define the complete`StrategyParameterUpdateRequest v1` receiver-owned command described in Section 1.
- **Source class / function / method:** `StrategyParameterUpdateRequest`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_parameter_update_request()`**Unit:** `tests/strategy/unit/test_models.py::test_parameter_update_requires_exact_version()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0238` [ ] `FR-STR-011` - fix environment, decision timestamp, timing policy, seed, trace identifiers, dependency status, and immutable snapshot references for one ev...

- **Execution domain:** `Strategy`.
- **Execution position:** `238` of `675`.
- **Cannot start before:** Step `P01-S0237` (`FR-STR-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 468); matrix row `443`.
- **Source responsibility:** The system shall fix environment, decision timestamp, timing policy, seed, trace identifiers, dependency status, and immutable snapshot references for one evaluation.
- **Source class / function / method:** `StrategyExecutionContext`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_execution_context()`**Unit:** `tests/strategy/unit/test_models.py::test_execution_context_rejects_naive_time()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0239` [ ] `FR-STR-012` - represent one typed event without granting mutable access to official external state.

- **Execution domain:** `Strategy`.
- **Execution position:** `239` of `675`.
- **Cannot start before:** Step `P01-S0238` (`FR-STR-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 469); matrix row `444`.
- **Source responsibility:** The system shall represent one typed event without granting mutable access to official external state.
- **Source class / function / method:** `StrategyEvent`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_strategy_event()`**Unit:** `tests/strategy/unit/test_models.py::test_strategy_event_payload_is_immutable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0240` [ ] `FR-STR-013` - represent a neutral decision or proposed actions, rationale references, diagnostics facts, and candidate local-state update.

- **Execution domain:** `Strategy`.
- **Execution position:** `240` of `675`.
- **Cannot start before:** Step `P01-S0239` (`FR-STR-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 470); matrix row `445`.
- **Source responsibility:** The system shall represent a neutral decision or proposed actions, rationale references, diagnostics facts, and candidate local-state update.
- **Source class / function / method:** `StrategyDecision`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_strategy_decision()`**Unit:** `tests/strategy/unit/test_models.py::test_neutral_decision_contains_no_intent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0241` [ ] `FR-STR-014` - return ordered intents, diagnostics, replay metadata, and an optional validated local-state update as one atomic result.

- **Execution domain:** `Strategy`.
- **Execution position:** `241` of `675`.
- **Cannot start before:** Step `P01-S0240` (`FR-STR-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Public Contract Models`` (line 471); matrix row `446`.
- **Source responsibility:** The system shall return ordered intents, diagnostics, replay metadata, and an optional validated local-state update as one atomic result.
- **Source class / function / method:** `StrategyExecutionResult`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_models_execution_result()`**Unit:** `tests/strategy/unit/test_models.py::test_execution_result_rejects_partial_failed_batch()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0242` [ ] `FR-STR-015` - represent a stable Strategy error code, safe message, redacted details, and trace identifiers without exposing raw exceptions.

- **Execution domain:** `Strategy`.
- **Execution position:** `242` of `675`.
- **Cannot start before:** Step `P01-S0241` (`FR-STR-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `outcomes.py` — Structured Operation Outcomes`` (line 479); matrix row `447`.
- **Source responsibility:** The system shall represent a stable Strategy error code, safe message, redacted details, and trace identifiers without exposing raw exceptions.
- **Source class / function / method:** `StrategyError`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_outcomes_strategy_error()`**Unit:** `tests/strategy/unit/test_outcomes.py::test_strategy_error_rejects_unredacted_details()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0243` [ ] `FR-STR-016` - return exactly one typed success value or one structured error for every public operation.

- **Execution domain:** `Strategy`.
- **Execution position:** `243` of `675`.
- **Cannot start before:** Step `P01-S0242` (`FR-STR-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `outcomes.py` — Structured Operation Outcomes`` (line 480); matrix row `448`.
- **Source responsibility:** The system shall return exactly one typed success value or one structured error for every public operation.
- **Source class / function / method:** `StrategyOutcome[T]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_contracts.py::test_usage_outcomes_strategy_outcome()`**Unit:** `tests/strategy/unit/test_outcomes.py::test_outcome_exclusive_data_or_error()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0244` [ ] `FR-STR-017` - return a versioned immutable mutation result for every registration or parameter-version command, including exact record references/hashes, ...

- **Execution domain:** `Strategy`.
- **Execution position:** `244` of `675`.
- **Cannot start before:** Step `P01-S0243` (`FR-STR-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 525); matrix row `449`.
- **Source responsibility:** The system shall represent status, strategy/config/data identity, trace IDs, relevant timestamps, accepted error code, bounded safe details, dependency health, metrics, and redaction status in a versioned immutable schema.
- **Source class / function / method:** `StrategyDiagnostics`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_diagnostics.py::test_usage_models_strategy_diagnostics()`**Unit:** `tests/strategy/unit/test_diagnostics_models.py::test_diagnostics_require_trace_and_redaction_status()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0245` [ ] `FR-STR-018` - expose only the accepted deterministic codes listed below, includingSTRATEGY_ARBITRARY_CODE_REJECTED instead of the cross-domain SIM_* name.

- **Execution domain:** `Strategy`.
- **Execution position:** `245` of `675`.
- **Cannot start before:** Step `P01-S0244` (`FR-STR-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 526); matrix row `450`.
- **Source responsibility:** The system shall expose only the accepted deterministic codes listed below, including`STRATEGY_ARBITRARY_CODE_REJECTED` instead of the cross-domain `SIM_*` name.
- **Source class / function / method:** `StrategyErrorCode`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_diagnostics.py::test_usage_errors_strategy_error_code()`**Unit:** `tests/strategy/unit/test_errors.py::test_error_catalogue_excludes_deferred_codes()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0246` [ ] `FR-STR-019` - export schema-valid diagnostics after recursive redaction and payload-size enforcement.

- **Execution domain:** `Strategy`.
- **Execution position:** `246` of `675`.
- **Cannot start before:** Step `P01-S0245` (`FR-STR-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 527); matrix row `451`.
- **Source responsibility:** The system shall export schema-valid diagnostics after recursive redaction and payload-size enforcement.
- **Source class / function / method:** `export_strategy_diagnostics(context: StrategyExecutionContext, facts: Mapping[str, JsonValue]) -> StrategyOutcome[StrategyDiagnostics]`
- **Source side effects:** None
- **Source raises:** None; returns`STRATEGY_RESOURCE_LIMIT_EXCEEDED` or `STRATEGY_INTERNAL_ERROR`
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_diagnostics.py::test_usage_export_strategy_diagnostics()`**Unit:** `tests/strategy/unit/test_export.py::test_export_diagnostics_redacts_and_bounds()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0247` [ ] `FR-STR-020` - register one unique immutable strategy version only after command, schema, module, hash, provenance, and lifecycle-reference validation.

- **Execution domain:** `Strategy`.
- **Execution position:** `247` of `675`.
- **Cannot start before:** Step `P01-S0246` (`FR-STR-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 595); matrix row `452`.
- **Source responsibility:** The system shall register one unique immutable strategy version only after command, schema, module, hash, provenance, and lifecycle-reference validation.
- **Source class / function / method:** `register_strategy_version(request: StrategyRegistrationRequest, auth: AuthContext) -> StrategyOutcome[ValidatedStrategyRef]`
- **Source side effects:** Persistence write; event publication
- **Source raises:** None; returns deterministic registry/validation error
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_registry.py::test_usage_catalog_register_strategy_version()`**Unit:** `tests/strategy/unit/test_catalog.py::test_registration_is_immutable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0248` [ ] `FR-STR-021` - validate and record a parameter update as a new canonical configuration hash without mutating an approved prior record.

- **Execution domain:** `Strategy`.
- **Execution position:** `248` of `675`.
- **Cannot start before:** Step `P01-S0247` (`FR-STR-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 596); matrix row `453`.
- **Source responsibility:** The system shall validate and record a parameter update as a new canonical configuration hash without mutating an approved prior record.
- **Source class / function / method:** `update_strategy_parameters(request: StrategyParameterUpdateRequest, auth: AuthContext) -> StrategyOutcome[ValidatedStrategyConfig]`
- **Source side effects:** Persistence write; event publication
- **Source raises:** None; returns deterministic config/concurrency error
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_registry.py::test_usage_catalog_update_strategy_parameters()`**Unit:** `tests/strategy/unit/test_catalog.py::test_parameter_update_preserves_prior_hash()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0249` [ ] `FR-STR-023` - resolve exactly one approved immutable version and fail before execution for invalid identity, lifecycle, environment, module, or hashes.

- **Execution domain:** `Strategy`.
- **Execution position:** `249` of `675`.
- **Cannot start before:** Step `P01-S0248` (`FR-STR-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 598); matrix row `454`.
- **Source responsibility:** The system shall resolve exactly one approved immutable version and fail before execution for invalid identity, lifecycle, environment, module, or hashes.
- **Source class / function / method:** `validate_strategy_ref(ref: StrategyRef) -> StrategyOutcome[ValidatedStrategyRef]`
- **Source side effects:** Read-only
- **Source raises:** None; returns registry/lifecycle/hash error
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_registry.py::test_usage_validation_validate_strategy_ref()`**Unit:** `tests/strategy/unit/test_validation.py::test_version_constraint_resolves_exactly_one()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0250` [ ] `FR-STR-024` - validate declarative configuration, explicit defaults, unknown fields, types, enums, bounds, and resource limits, producing a canonical hash...

- **Execution domain:** `Strategy`.
- **Execution position:** `250` of `675`.
- **Cannot start before:** Step `P01-S0249` (`FR-STR-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 599); matrix row `455`.
- **Source responsibility:** The system shall validate declarative configuration, explicit defaults, unknown fields, types, enums, bounds, and resource limits, producing a canonical hash before execution.
- **Source class / function / method:** `validate_strategy_config(ref: ValidatedStrategyRef, config: StrategyConfig) -> StrategyOutcome[ValidatedStrategyConfig]`
- **Source side effects:** None
- **Source raises:** None; returns`STRATEGY_INVALID_CONFIG`, `STRATEGY_SCHEMA_VALIDATION_FAILED`, or resource error
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_registry.py::test_usage_validation_validate_strategy_config()`**Unit:** `tests/strategy/unit/test_validation.py::test_config_rejects_executable_injection()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0251` [ ] `FR-STR-025` - define and validate every field of the canonicalTradeIntent v1 contract described in Section 1.

- **Execution domain:** `Strategy`.
- **Execution position:** `251` of `675`.
- **Cannot start before:** Step `P01-S0250` (`FR-STR-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 625); matrix row `456`.
- **Source responsibility:** The system shall define and validate every field of the canonical`TradeIntent v1` contract described in Section 1.
- **Source class / function / method:** `TradeIntent`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_intents.py::test_usage_intent_trade_intent()`**Unit:** `tests/strategy/unit/test_intent.py::test_trade_intent_rejects_invalid_partial_fill_contract()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0252` [ ] `FR-STR-026` - build a schema-valid intent with deterministic IDs, monotonic sequence, canonical idempotency key, and preserved parent/lineage references.

- **Execution domain:** `Strategy`.
- **Execution position:** `252` of `675`.
- **Cannot start before:** Step `P01-S0251` (`FR-STR-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 626); matrix row `457`.
- **Source responsibility:** The system shall build a schema-valid intent with deterministic IDs, monotonic sequence, canonical idempotency key, and preserved parent/lineage references.
- **Source class / function / method:** `build_trade_intent(decision: StrategyDecision, context: StrategyExecutionContext, sequence: int) -> StrategyOutcome[TradeIntent]`
- **Source side effects:** None
- **Source raises:** None; returns config/schema/duplicate error
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_intents.py::test_usage_builder_build_trade_intent()`**Unit:** `tests/strategy/unit/test_builder.py::test_intent_identity_is_stable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0253` [ ] `FR-STR-029` - create a deterministic replay manifest from exact validated identities and input hashes.

- **Execution domain:** `Strategy`.
- **Execution position:** `253` of `675`.
- **Cannot start before:** Step `P01-S0252` (`FR-STR-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 665); matrix row `458`.
- **Source source context:** | Missing | `FR-STR-029` | The system shall create a deterministic replay manifest from exact validated identities and input hashes.                                        | `create_strategy_replay_manifest(ref: ValidatedStrategyRef, config: ValidatedStrategyConfig, context: StrategyExecutionContext, data_checksum: str, indicator_manifest_hash: str, simulation_config_hash: str | None) -> StrategyOutcome[StrategyReplayManifest]` | None                                                | None; returns artifact/dependency/internal error                                                                                                                                                                                       |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-029`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0254` [ ] `FR-STR-030` - serialize and checksum candidate local decision state only after redaction and size validation.

- **Execution domain:** `Strategy`.
- **Execution position:** `254` of `675`.
- **Cannot start before:** Step `P01-S0253` (`FR-STR-029`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 666); matrix row `459`.
- **Source responsibility:** The system shall serialize and checksum candidate local decision state only after redaction and size validation.
- **Source class / function / method:** `create_strategy_checkpoint(ref: ValidatedStrategyRef, config: ValidatedStrategyConfig, state: Mapping[str, JsonValue], authorization_ref: str) -> StrategyOutcome[StrategyCheckpoint]`
- **Source side effects:** None
- **Source raises:** None; returns checkpoint/resource/redaction error
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_replay.py::test_usage_checkpoints_create_strategy_checkpoint()`**Unit:** `tests/strategy/unit/test_checkpoints.py::test_checkpoint_is_bounded_and_redacted()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0255` [ ] `FR-STR-031` - reject corrupt, incompatible, mismatched, unauthorized, or oversized checkpoints before evaluation.

- **Execution domain:** `Strategy`.
- **Execution position:** `255` of `675`.
- **Cannot start before:** Step `P01-S0254` (`FR-STR-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 667); matrix row `460`.
- **Source responsibility:** The system shall reject corrupt, incompatible, mismatched, unauthorized, or oversized checkpoints before evaluation.
- **Source class / function / method:** `validate_strategy_checkpoint(checkpoint: StrategyCheckpoint, ref: ValidatedStrategyRef, config: ValidatedStrategyConfig) -> StrategyOutcome[Mapping[str, JsonValue]]`
- **Source side effects:** None
- **Source raises:** None; returns checkpoint incompatible/invalid error
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_replay.py::test_usage_checkpoints_validate_strategy_checkpoint()`**Unit:** `tests/strategy/unit/test_checkpoints.py::test_checkpoint_hash_mismatch_fails_before_restore()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0256` [ ] `FR-STR-032` - validate normalized data, indicator readiness, previous-close timing, fixed decision clock, environment, config, deterministic ordering, and...

- **Execution domain:** `Strategy`.
- **Execution position:** `256` of `675`.
- **Cannot start before:** Step `P01-S0255` (`FR-STR-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 696); matrix row `461`.
- **Source responsibility:** The system shall validate normalized data, indicator readiness, previous-close timing, fixed decision clock, environment, config, deterministic ordering, and resource bounds before returning one atomic intent batch and replay metadata.
- **Source class / function / method:** `run_vectorized_strategy_signals(ref: ValidatedStrategyRef, config: ValidatedStrategyConfig, market: MarketDataset, indicators: tuple[IndicatorSeries, ...], context: StrategyExecutionContext) -> StrategyOutcome[StrategyExecutionResult]`
- **Source side effects:** None
- **Source raises:** None; returns data/indicator/lookahead/stale/timeout/resource/internal error
- **Source usage / test:** **Usage:** `tests/strategy/usage/test_usage_vectorized.py::test_usage_runner_run_vectorized_strategy_signals()`**Unit:** `tests/strategy/unit/test_vectorized_runner.py::test_any_lookahead_discards_entire_batch()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-032`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0257` [ ] `FR-STR-033` - invoke one declared typed event hook in deterministic order using immutable external snapshots, and shall atomically return intents, diagnos...

- **Execution domain:** `Strategy`.
- **Execution position:** `257` of `675`.
- **Cannot start before:** Step `P01-S0256` (`FR-STR-032`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 733); matrix row `462`.
- **Source source context:** | Missing | `FR-STR-033` | The system shall invoke one declared typed event hook in deterministic order using immutable external snapshots, and shall atomically return intents, diagnostics, replay metadata, and validated local-state update without mutating official state. | `run_event_strategy_hook(ref: ValidatedStrategyRef, config: ValidatedStrategyConfig, event: StrategyEvent, context: StrategyExecutionContext, local_state: Mapping[str, JsonValue] | None = None) -> StrategyOutcome[StrategyExecutionResult]` | Local state mutation only after complete result validation | None; returns unsupported/data/indicator/duplicate/timeout/checkpoint/internal error |
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-STR-033`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0258` [ ] `WF-STR-001` - Validate Strategy Reference and Configuration

- **Execution domain:** `Strategy`.
- **Execution position:** `258` of `675`.
- **Cannot start before:** Step `P01-S0257` (`FR-STR-033`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 3. Workflows > Status values`` (line 296); matrix row `466`.
- **Source scope:** Internal
- **Source workflow:** Validate reference and configuration
- **Source requirement sequence:** `FR-STR-023 → FR-STR-024`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-STR-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0259` [ ] `WF-STR-002` - Generate Vectorized Strategy Decisions

- **Execution domain:** `Strategy`.
- **Execution position:** `259` of `675`.
- **Cannot start before:** Step `P01-S0258` (`WF-STR-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 3. Workflows > Status values`` (line 297); matrix row `467`.
- **Source scope:** Cross-domain
- **Source workflow:** Generate vectorized decisions
- **Source requirement sequence:** `FR-STR-023 → FR-STR-024 → FR-STR-032`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-STR-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0260` [ ] `WF-STR-004` - Build and Hand Off TradeIntent

- **Execution domain:** `Strategy`.
- **Execution position:** `260` of `675`.
- **Cannot start before:** Step `P01-S0259` (`WF-STR-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Strategy` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/strategy/README.md` - ``Strategy > 3. Workflows > Status values`` (line 299); matrix row `468`.
- **Source scope:** Cross-domain
- **Source workflow:** Build and hand off TradeIntent
- **Source requirement sequence:** `FR-STR-026`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-STR-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 7 - Risk

Implement `Risk` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0261` [ ] `P-RISK-001` - contracts feature/component (provisional)

- **Execution domain:** `Risk`.
- **Execution position:** `261` of `675`.
- **Cannot start before:** Step `P01-S0260` (`WF-STR-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RISK-001`); matrix row `538`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RISK-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0262` [ ] `FR-RISK-001` - Define approve, warn, needs_approval, needs_more_evidence, reject, block, and error exactly.

- **Execution domain:** `Risk`.
- **Execution position:** `262` of `675`.
- **Cannot start before:** Step `P01-S0261` (`P-RISK-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 488); matrix row `494`.
- **Source responsibility:** Define `approve`, `warn`, `needs_approval`, `needs_more_evidence`, `reject`, `block`, and `error` exactly.
- **Source class / function / method:** `DecisionState`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/risk/usage/test_usage_contracts.py::test_usage_enums_decision_state()`<br>**Unit:** `tests/risk/unit/test_enums.py::test_decision_state_values_are_stable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0263` [ ] `FR-RISK-002` - Define pass, warn, needs_more_evidence, fail, and blocked exactly.

- **Execution domain:** `Risk`.
- **Execution position:** `263` of `675`.
- **Cannot start before:** Step `P01-S0262` (`FR-RISK-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 489); matrix row `495`.
- **Source responsibility:** Define `pass`, `warn`, `needs_more_evidence`, `fail`, and `blocked` exactly.
- **Source class / function / method:** `LimitStatus`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_enums_limit_status()`<br>**Unit:** `test_enums.py::test_limit_status_values_are_stable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0264` [ ] `FR-RISK-003` - Define every accepted deterministic Risk error code; historical VaR/CVaR is the sole supported VaR method.

- **Execution domain:** `Risk`.
- **Execution position:** `264` of `675`.
- **Cannot start before:** Step `P01-S0263` (`FR-RISK-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 490); matrix row `496`.
- **Source responsibility:** Define every accepted deterministic Risk error code; historical VaR/CVaR is the sole supported VaR method.
- **Source class / function / method:** `RiskErrorCode`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_errors_codes()`<br>**Unit:** `test_errors.py::test_error_code_catalog()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0265` [ ] `FR-RISK-004` - Carry immutable account/position/pending-order/symbol/market evidence with UTC as_of, provenance, missingness, and schema version.

- **Execution domain:** `Risk`.
- **Execution position:** `265` of `675`.
- **Cannot start before:** Step `P01-S0264` (`FR-RISK-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 491); matrix row `497`.
- **Source responsibility:** Carry immutable account/position/pending-order/symbol/market evidence with UTC `as_of`, provenance, missingness, and schema version.
- **Source class / function / method:** `PortfolioState`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid version, naive time, non-finite Decimal, or malformed evidence
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_evidence_portfolio_state()`<br>**Unit:** `test_evidence.py::test_portfolio_state_preserves_missingness()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0266` [ ] `FR-RISK-005` - Carry reproducible base-currency metrics, limit results, assumptions, coverage, regime, request/workflow IDs, evidence refs, and config hash.

- **Execution domain:** `Risk`.
- **Execution position:** `266` of `675`.
- **Cannot start before:** Step `P01-S0265` (`FR-RISK-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 492); matrix row `498`.
- **Source responsibility:** Carry reproducible base-currency metrics, limit results, assumptions, coverage, regime, request/workflow IDs, evidence refs, and config hash.
- **Source class / function / method:** `PortfolioRiskSnapshot`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid or non-finite result
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_evidence_portfolio_snapshot()`<br>**Unit:** `test_evidence.py::test_snapshot_serializes_decimal_exactly()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0267` [ ] `FR-RISK-058` - Validate the consumed Data-owned MarketContextEvidence v1 version, UTC freshness, provenance, bounded values, and explicit missingness without redefining or ...

- **Execution domain:** `Risk`.
- **Execution position:** `267` of `675`.
- **Cannot start before:** Step `P01-S0266` (`FR-RISK-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 493); matrix row `535`.
- **Source responsibility:** Validate the consumed Data-owned `MarketContextEvidence v1` version, UTC freshness, provenance, bounded values, and explicit missingness without redefining or fetching it.
- **Source class / function / method:** `validate_market_context_evidence(evidence: MarketContextEvidence, *, now: datetime) -> None`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(MISSING_EVIDENCE, STALE_EVIDENCE, VALIDATION_FAILED)`: incompatible, stale, or malformed evidence
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_evidence_market_context()`<br>**Unit:** `test_evidence.py::test_market_context_uses_data_owned_contract()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-058`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0268` [ ] `FR-RISK-059` - Return ActionPolicyVerdict v1 bound to action, scope, policy version, approval attestation, decision, reservation, expiry, reasons, and trace IDs.

- **Execution domain:** `Risk`.
- **Execution position:** `268` of `675`.
- **Cannot start before:** Step `P01-S0267` (`FR-RISK-058`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 494); matrix row `536`.
- **Source responsibility:** Return `ActionPolicyVerdict v1` bound to action, scope, policy version, approval attestation, decision, reservation, expiry, reasons, and trace IDs.
- **Source class / function / method:** `ActionPolicyVerdict`
- **Source side effects:** None
- **Source raises:** `ValidationError`: inconsistent, unbound, or non-UTC verdict
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_action_policy()`<br>**Unit:** `test_results.py::test_action_policy_verdict_requires_reservation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-059`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0269` [ ] `FR-RISK-060` - Carry one ordered limit result with status, observed/threshold values, reason code, evidence refs, and precedence without granting approval.

- **Execution domain:** `Risk`.
- **Execution position:** `269` of `675`.
- **Cannot start before:** Step `P01-S0268` (`FR-RISK-059`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 495); matrix row `537`.
- **Source responsibility:** Carry one ordered limit result with status, observed/threshold values, reason code, evidence refs, and precedence without granting approval.
- **Source class / function / method:** `RiskLimitResult`
- **Source side effects:** None
- **Source raises:** `ValidationError`: inconsistent status/reason or non-finite value
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_limit()`<br>**Unit:** `test_results.py::test_limit_result_invariants()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-060`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0270` [ ] `FR-RISK-006` - Represent one non-executable risk-increasing proposal with intent reference, scope, direction, requested size, price/stop evidence, validity, and provenance.

- **Execution domain:** `Risk`.
- **Execution position:** `270` of `675`.
- **Cannot start before:** Step `P01-S0269` (`FR-RISK-060`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 496); matrix row `499`.
- **Source responsibility:** Represent one non-executable risk-increasing proposal with intent reference, scope, direction, requested size, price/stop evidence, validity, and provenance.
- **Source class / function / method:** `ProposedTrade`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid size/scope or required stop evidence absent
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_requests_proposed_trade()`<br>**Unit:** `test_requests.py::test_proposed_trade_requires_fixed_risk_stop()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0271` [ ] `FR-RISK-007` - Represent one of six sizing methods and its complete evidence/config references.

- **Execution domain:** `Risk`.
- **Execution position:** `271` of `675`.
- **Cannot start before:** Step `P01-S0270` (`FR-RISK-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 497); matrix row `500`.
- **Source responsibility:** Represent one of six sizing methods and its complete evidence/config references.
- **Source class / function / method:** `PositionSizingRequest`
- **Source side effects:** None
- **Source raises:** `ValidationError`: unknown method or incomplete method evidence
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_requests_sizing()`<br>**Unit:** `test_requests.py::test_sizing_request_is_method_strict()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0272` [ ] `FR-RISK-008` - Return exact requested/normalized size, constraints applied, evidence gaps, fallback disclosure, and no approval claim.

- **Execution domain:** `Risk`.
- **Execution position:** `272` of `675`.
- **Cannot start before:** Step `P01-S0271` (`FR-RISK-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 498); matrix row `501`.
- **Source responsibility:** Return exact requested/normalized size, constraints applied, evidence gaps, fallback disclosure, and no approval claim.
- **Source class / function / method:** `PositionSizingResult`
- **Source side effects:** None
- **Source raises:** `ValidationError`: non-finite result
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_sizing()`<br>**Unit:** `test_results.py::test_sizing_result_cannot_claim_approval()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0273` [ ] `FR-RISK-009` - Validate and review AllocationReviewRequest v1 without constructing or applying a Portfolio allocation.

- **Execution domain:** `Risk`.
- **Execution position:** `273` of `675`.
- **Cannot start before:** Step `P01-S0272` (`FR-RISK-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 499); matrix row `502`.
- **Source responsibility:** Validate and review `AllocationReviewRequest v1` without constructing or applying a Portfolio allocation.
- **Source class / function / method:** `review_allocation_proposal`
- **Source side effects:** None
- **Source raises:** Structured rejection on missing/stale/incompatible evidence
- **Source usage / test:** **Verification:** `SYS-WF-007`/`SYS-WF-008` compatibility tests.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0274` [ ] `FR-RISK-010` - Validate StrategyOperationalEligibilityRequest v1 for an exact registered strategy/version and scope.

- **Execution domain:** `Risk`.
- **Execution position:** `274` of `675`.
- **Cannot start before:** Step `P01-S0273` (`FR-RISK-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 500); matrix row `503`.
- **Source responsibility:** Validate `StrategyOperationalEligibilityRequest v1` for an exact registered strategy/version and scope.
- **Source class / function / method:** `review_strategy_admission`
- **Source side effects:** None
- **Source raises:** Structured rejection on registration/evidence/policy failure
- **Source usage / test:** **Verification:** `SYS-WF-006` compatibility test.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0275` [ ] `FR-RISK-011` - Return classified volatility/liquidity/correlation/drawdown/crisis/news/session states, transition evidence, modifiers, and missingness.

- **Execution domain:** `Risk`.
- **Execution position:** `275` of `675`.
- **Cannot start before:** Step `P01-S0274` (`FR-RISK-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 501); matrix row `504`.
- **Source responsibility:** Return classified volatility/liquidity/correlation/drawdown/crisis/news/session states, transition evidence, modifiers, and missingness.
- **Source class / function / method:** `RegimeAssessment`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid regime value
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_regime()`<br>**Unit:** `test_results.py::test_regime_assessment_carries_transition()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0276` [ ] `FR-RISK-012` - Define a bounded immutable advisory scenario with deterministic shocks and optional explicit seed.

- **Execution domain:** `Risk`.
- **Execution position:** `276` of `675`.
- **Cannot start before:** Step `P01-S0275` (`FR-RISK-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 502); matrix row `505`.
- **Source responsibility:** Define a bounded immutable advisory scenario with deterministic shocks and optional explicit seed.
- **Source class / function / method:** `ScenarioDefinition`
- **Source side effects:** None
- **Source raises:** `ValidationError`: unsupported/non-finite shock or unseeded randomness
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_requests_scenario()`<br>**Unit:** `test_requests.py::test_scenario_requires_seed_if_randomized()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0277` [ ] `FR-RISK-013` - Return baseline/projected risk comparison and state that the output is advisory and not approved.

- **Execution domain:** `Risk`.
- **Execution position:** `277` of `675`.
- **Cannot start before:** Step `P01-S0276` (`FR-RISK-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 503); matrix row `506`.
- **Source responsibility:** Return baseline/projected risk comparison and state that the output is advisory and not approved.
- **Source class / function / method:** `ScenarioResult`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid projection
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_scenario()`<br>**Unit:** `test_results.py::test_scenario_result_is_advisory()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0278` [ ] `FR-RISK-014` - Implement RiskDecision v1 with verdict, approved size, ordered checks, primary/composite reasons, provenance, expiry, concurrency disclosure, and optional to...

- **Execution domain:** `Risk`.
- **Execution position:** `278` of `675`.
- **Cannot start before:** Step `P01-S0277` (`FR-RISK-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 504); matrix row `507`.
- **Source responsibility:** Implement `RiskDecision` v1 with verdict, approved size, ordered checks, primary/composite reasons, provenance, expiry, concurrency disclosure, and optional token.
- **Source class / function / method:** `RiskDecisionPackage`
- **Source side effects:** None
- **Source raises:** `ValidationError`: inconsistent verdict/token or missing provenance
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_decision()`<br>**Unit:** `test_results.py::test_decision_package_invariants()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0279` [ ] `FR-RISK-015` - Carry signed token scope, decision/config hashes, approver, expiry, nonce, schema version, and no secret key.

- **Execution domain:** `Risk`.
- **Execution position:** `279` of `675`.
- **Cannot start before:** Step `P01-S0278` (`FR-RISK-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 505); matrix row `508`.
- **Source responsibility:** Carry signed token scope, decision/config hashes, approver, expiry, nonce, schema version, and no secret key.
- **Source class / function / method:** `RiskApprovalToken`
- **Source side effects:** None
- **Source raises:** `ValidationError`: incomplete or non-UTC token
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_token()`<br>**Unit:** `test_results.py::test_token_contract_has_required_bindings()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0280` [ ] `FR-RISK-016` - Implement KillSwitchCommand v1 with action, explicit scope level, applicable portfolio/strategy/symbol identifiers, reason, UTC timestamp, request/workflow/c...

- **Execution domain:** `Risk`.
- **Execution position:** `280` of `675`.
- **Cannot start before:** Step `P01-S0279` (`FR-RISK-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 506); matrix row `509`.
- **Source responsibility:** Implement `KillSwitchCommand v1` with action, explicit scope level, applicable portfolio/strategy/symbol identifiers, reason, UTC timestamp, request/workflow/correlation IDs, and schema identity. Principal authorization remains in the separate `AuthContext`; clearance requires a separate matching current `ApprovalAttestation`.
- **Source class / function / method:** `KillSwitchCommand`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid action, scope, identifiers, time, or trace identity
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_requests_kill_switch()`<br>**Unit:** `test_requests.py::test_kill_switch_command_requires_scope_and_reason()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0281` [ ] `FR-RISK-017` - Implement KillSwitchState v1 with scope, active/unknown state, reason, version, and UTC update time.

- **Execution domain:** `Risk`.
- **Execution position:** `281` of `675`.
- **Cannot start before:** Step `P01-S0280` (`FR-RISK-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 507); matrix row `510`.
- **Source responsibility:** Implement `KillSwitchState` v1 with scope, active/unknown state, reason, version, and UTC update time.
- **Source class / function / method:** `KillSwitchState`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid transition data
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_kill_switch()`<br>**Unit:** `test_results.py::test_kill_switch_unknown_is_representable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0282` [ ] `FR-RISK-018` - Carry canonical redacted audit payload, evidence/config/decision provenance, sequence, previous hash, and record hash.

- **Execution domain:** `Risk`.
- **Execution position:** `282` of `675`.
- **Cannot start before:** Step `P01-S0281` (`FR-RISK-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 508); matrix row `511`.
- **Source responsibility:** Carry canonical redacted audit payload, evidence/config/decision provenance, sequence, previous hash, and record hash.
- **Source class / function / method:** `RiskAuditRecord`
- **Source side effects:** None
- **Source raises:** `ValidationError`: secret-like field, invalid hash, or incomplete provenance
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_audit()`<br>**Unit:** `test_results.py::test_audit_record_redacts_secrets()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0283` [ ] `FR-RISK-019` - Carry Markdown or exact JSON summary with separated evidence, assumptions, warnings, decision, and recommendations.

- **Execution domain:** `Risk`.
- **Execution position:** `283` of `675`.
- **Cannot start before:** Step `P01-S0282` (`FR-RISK-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 509); matrix row `512`.
- **Source responsibility:** Carry Markdown or exact JSON summary with separated evidence, assumptions, warnings, decision, and recommendations.
- **Source class / function / method:** `RiskReport`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid format or false approval state
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_report()`<br>**Unit:** `test_results.py::test_report_contract_separates_sections()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0284` [ ] `FR-RISK-020` - Return token validity, consumption state, reason code, and audit reference without exposing secrets.

- **Execution domain:** `Risk`.
- **Execution position:** `284` of `675`.
- **Cannot start before:** Step `P01-S0283` (`FR-RISK-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 510); matrix row `513`.
- **Source responsibility:** Return token validity, consumption state, reason code, and audit reference without exposing secrets.
- **Source class / function / method:** `ApprovalValidationResult`
- **Source side effects:** None
- **Source raises:** `ValidationError`: inconsistent valid/reason state
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_token_validation()`<br>**Unit:** `test_results.py::test_validation_result_invariants()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0285` [ ] `FR-RISK-021` - Raise one redacted domain exception carrying a RiskErrorCode and safe details for boundary mapping.

- **Execution domain:** `Risk`.
- **Execution position:** `285` of `675`.
- **Cannot start before:** Step `P01-S0284` (`FR-RISK-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.1 `contracts/` — Versioned Contracts and Deterministic Errors > Functional requirements`` (line 511); matrix row `514`.
- **Source responsibility:** Raise one redacted domain exception carrying a `RiskErrorCode` and safe details for boundary mapping.
- **Source class / function / method:** `RiskDomainError(code: RiskErrorCode, details: str)`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_errors_domain_error()`<br>**Unit:** `test_errors.py::test_domain_error_redacts_details()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0286` [ ] `P-RISK-002` - config feature/component (provisional)

- **Execution domain:** `Risk`.
- **Execution position:** `286` of `675`.
- **Cannot start before:** Step `P01-S0285` (`FR-RISK-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.2` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RISK-002`); matrix row `539`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RISK-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0287` [ ] `FR-RISK-023` - Load only the selected YAML profile from the bounded root and fail closed on missing/invalid live configuration.

- **Execution domain:** `Risk`.
- **Execution position:** `287` of `675`.
- **Cannot start before:** Step `P01-S0286` (`P-RISK-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.2 `config/` — Risk Profiles and Stable Configuration > Configuration and Limits Manifest`` (line 546); matrix row `515`.
- **Source responsibility:** Load only the selected YAML profile from the bounded root and fail closed on missing/invalid live configuration.
- **Source class / function / method:** `load_risk_config(profile: str, config_root: Path) -> RiskConfig`
- **Source side effects:** Read-only
- **Source raises:** `RiskDomainError(INVALID_RISK_CONFIG)`: file/schema/path failure
- **Source usage / test:** **Usage:** `test_usage_config.py::test_usage_profiles_load()`<br>**Unit:** `test_profiles.py::test_missing_live_profile_fails_closed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0288` [ ] `FR-RISK-025` - Build an immutable snapshot containing pending-order-aware gross/net exposure by dimension, account-currency conversions, drawdown/loss state, margin/leverag...

- **Execution domain:** `Risk`.
- **Execution position:** `288` of `675`.
- **Cannot start before:** Step `P01-S0287` (`FR-RISK-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.3 `portfolio/` — Evidence Normalization and Portfolio Risk Snapshot > Configuration and Limits Manifest`` (line 580); matrix row `516`.
- **Source responsibility:** Build an immutable snapshot containing pending-order-aware gross/net exposure by dimension, account-currency conversions, drawdown/loss state, margin/leverage, volatility, historical VaR/CVaR, pair/portfolio correlation, incremental contribution, assumptions, coverage, and explicit gaps.
- **Source class / function / method:** `build_portfolio_risk_snapshot(state: PortfolioState, config: RiskConfig, *, now: datetime) -> PortfolioRiskSnapshot`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(INVALID_PORTFOLIO_STATE, MISSING_EVIDENCE, SNAPSHOT_BUILD_FAILED)`: corresponding condition
- **Source usage / test:** **Usage:** `tests/risk/usage/test_usage_portfolio.py::test_usage_snapshot_build()`<br>**Unit:** `tests/risk/unit/test_snapshot.py::test_snapshot_includes_pending_and_conversion_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0289` [ ] `P-RISK-004` - sizing feature/component (provisional)

- **Execution domain:** `Risk`.
- **Execution position:** `289` of `675`.
- **Cannot start before:** Step `P01-S0288` (`FR-RISK-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.4` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RISK-004`); matrix row `540`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RISK-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0290` [ ] `FR-RISK-026` - Calculate fixed-lot, fixed-risk, milestone, fractional-Kelly, volatility, or fixed-fractional size; enforce stop/equity/evidence rules; disclose fallback/cor...

- **Execution domain:** `Risk`.
- **Execution position:** `290` of `675`.
- **Cannot start before:** Step `P01-S0289` (`P-RISK-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.4 `sizing/` — Position Sizing Recommendations > Configuration and Limits Manifest`` (line 613); matrix row `517`.
- **Source responsibility:** Calculate fixed-lot, fixed-risk, milestone, fractional-Kelly, volatility, or fixed-fractional size; enforce stop/equity/evidence rules; disclose fallback/correlation adjustment; normalize against explicit broker and risk constraints; return no non-zero failure fallback and no approval.
- **Source class / function / method:** `calculate_position_size(request: PositionSizingRequest, snapshot: PortfolioRiskSnapshot, config: RiskConfig) -> PositionSizingResult`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(MISSING_STOP_LOSS, INSUFFICIENT_VOLATILITY_EVIDENCE, INSUFFICIENT_K_EVIDENCE, CALCULATION_FAILED)`: corresponding condition
- **Source usage / test:** **Usage:** `tests/risk/usage/test_usage_sizing.py::test_usage_calculator_position_size()`<br>**Unit:** `tests/risk/unit/test_calculator.py::test_all_six_methods_and_no_point_one_fallback()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0291` [ ] `P-RISK-005` - policy feature/component (provisional)

- **Execution domain:** `Risk`.
- **Execution position:** `291` of `675`.
- **Cannot start before:** Step `P01-S0290` (`FR-RISK-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.5` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RISK-005`); matrix row `541`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RISK-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0292` [ ] `FR-RISK-027` - Evaluate daily/total loss, drawdown state, consistency, exposure/concentration, margin/leverage, historical tail risk, correlation, and freshness in determin...

- **Execution domain:** `Risk`.
- **Execution position:** `292` of `675`.
- **Cannot start before:** Step `P01-S0291` (`P-RISK-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.5 `policy/` — Limits, Market Context, Admission, and Allocation Gates > Configuration and Limits Manifest`` (line 649); matrix row `518`.
- **Source responsibility:** Evaluate daily/total loss, drawdown state, consistency, exposure/concentration, margin/leverage, historical tail risk, correlation, and freshness in deterministic precedence, returning primary and composite failures.
- **Source class / function / method:** `evaluate_portfolio_limits(snapshot: PortfolioRiskSnapshot, config: RiskConfig) -> tuple[RiskLimitResult, ...]`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(INVALID_RISK_CONFIG, MISSING_EVIDENCE, LIMIT_FAILED)`
- **Source usage / test:** **Usage:** `tests/risk/usage/test_usage_policy.py::test_usage_limits_portfolio()`<br>**Unit:** `tests/risk/unit/test_limits.py::test_limit_order_and_composite_failures()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-027`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0293` [ ] `FR-RISK-029` - Produce and persist StrategyOperationalEligibilityDecision v1 with exact scope, conditions, evidence/policy lineage, issue/expiry, and suspension semantics; ...

- **Execution domain:** `Risk`.
- **Execution position:** `293` of `675`.
- **Cannot start before:** Step `P01-S0292` (`FR-RISK-027`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.5 `policy/` — Limits, Market Context, Admission, and Allocation Gates > Configuration and Limits Manifest`` (line 651); matrix row `519`.
- **Source responsibility:** Produce and persist `StrategyOperationalEligibilityDecision v1` with exact scope, conditions, evidence/policy lineage, issue/expiry, and suspension semantics; never mutate Strategy state.
- **Source class / function / method:** `review_strategy_admission`
- **Source side effects:** Risk decision/audit stores
- **Source raises:** Structured fail-closed error
- **Source usage / test:** **Verification:** eligibility contract and persistence tests.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-029`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0294` [ ] `FR-RISK-030` - Produce AllocationRiskDecision v1, enforce caps, and atomically activate the authoritative risk-budget projection only for the exact approved Portfolio version.

- **Execution domain:** `Risk`.
- **Execution position:** `294` of `675`.
- **Cannot start before:** Step `P01-S0293` (`FR-RISK-029`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.5 `policy/` — Limits, Market Context, Admission, and Allocation Gates > Configuration and Limits Manifest`` (line 652); matrix row `520`.
- **Source responsibility:** Produce `AllocationRiskDecision v1`, enforce caps, and atomically activate the authoritative risk-budget projection only for the exact approved Portfolio version.
- **Source class / function / method:** `review_allocation_proposal`, budget activation API
- **Source side effects:** Risk budget/audit stores
- **Source raises:** Version/expiry/kill-switch conflict blocks activation
- **Source usage / test:** **Verification:** allocation and concurrency tests.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0295` [ ] `FR-RISK-031` - Classify volatility, liquidity, correlation, drawdown, crisis, news, and session regimes; record deterministic transitions/evidence; return only equal-or-str...

- **Execution domain:** `Risk`.
- **Execution position:** `295` of `675`.
- **Cannot start before:** Step `P01-S0294` (`FR-RISK-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.6 `regimes/` — Regime Assessment and Limit Tightening > Configuration and Limits Manifest`` (line 679); matrix row `521`.
- **Source responsibility:** Classify volatility, liquidity, correlation, drawdown, crisis, news, and session regimes; record deterministic transitions/evidence; return only equal-or-stricter modifiers; fail closed on required missing/unknown live evidence.
- **Source class / function / method:** `assess_risk_regime(snapshot: PortfolioRiskSnapshot, evidence: MarketContextEvidence, config: RiskConfig, *, now: datetime) -> RegimeAssessment`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(MISSING_EVIDENCE, STALE_EVIDENCE, CALCULATION_FAILED)`
- **Source usage / test:** **Usage:** `tests/risk/usage/test_usage_regimes.py::test_usage_assessment_regime()`<br>**Unit:** `tests/risk/unit/test_assessment.py::test_high_risk_modifiers_only_tighten()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0296` [ ] `P-RISK-007` - audit feature/component (provisional)

- **Execution domain:** `Risk`.
- **Execution position:** `296` of `675`.
- **Cannot start before:** Step `P01-S0295` (`FR-RISK-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.7` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RISK-007`); matrix row `542`
- **Source type:** Testing
- **Source responsibility:** Every public symbol has one usage example and unit coverage; every collaborative workflow has an integration test; package coverage is at least 80%.
- **Source verification:** Test/traceability audit
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RISK-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0297` [ ] `FR-RISK-033` - Redact, canonicalize, hash, and durably append a material record with previous-hash continuity.

- **Execution domain:** `Risk`.
- **Execution position:** `297` of `675`.
- **Cannot start before:** Step `P01-S0296` (`P-RISK-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.7 `audit/` — Tamper-Evident Risk Audit Boundary > Configuration and Limits Manifest`` (line 710); matrix row `522`.
- **Source responsibility:** Redact, canonicalize, hash, and durably append a material record with previous-hash continuity.
- **Source class / function / method:** `RiskAuditChain.append(record: RiskAuditRecord) -> RiskAuditRecord`
- **Source side effects:** Persistence write
- **Source raises:** `RiskDomainError(STORAGE_ERROR)`: partial/unavailable/permission failure
- **Source usage / test:** **Usage:** `test_usage_audit.py::test_usage_chain_append()`<br>**Unit:** `test_chain.py::test_append_hashes_and_fails_closed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-033`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0298` [ ] `FR-RISK-034` - Verify genesis, sequence, previous hash, and record hash; identify tamper deterministically.

- **Execution domain:** `Risk`.
- **Execution position:** `298` of `675`.
- **Cannot start before:** Step `P01-S0297` (`FR-RISK-033`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.7 `audit/` — Tamper-Evident Risk Audit Boundary > Configuration and Limits Manifest`` (line 711); matrix row `523`.
- **Source responsibility:** Verify genesis, sequence, previous hash, and record hash; identify tamper deterministically.
- **Source class / function / method:** `RiskAuditChain.verify(records: Sequence[RiskAuditRecord]) -> bool`
- **Source side effects:** Read-only
- **Source raises:** `RiskDomainError(AUDIT_CHAIN_TAMPER_DETECTED, STORAGE_ERROR)`
- **Source usage / test:** **Usage:** `test_usage_audit.py::test_usage_chain_verify()`<br>**Unit:** `test_chain.py::test_verify_detects_tamper()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-034`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0299` [ ] `P-RISK-008` - approvals feature/component (provisional)

- **Execution domain:** `Risk`.
- **Execution position:** `299` of `675`.
- **Cannot start before:** Step `P01-S0298` (`FR-RISK-034`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.8` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RISK-008`); matrix row `543`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RISK-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0300` [ ] `FR-RISK-036` - Validate Risk-owned, UI/API-produced ApprovalAttestation v1, then issue a tamper-evident token only for an eligible decision, binding request/workflow/action...

- **Execution domain:** `Risk`.
- **Execution position:** `300` of `675`.
- **Cannot start before:** Step `P01-S0299` (`P-RISK-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.8 `approvals/` — Durable Approval-Token Lifecycle > Configuration and Limits Manifest`` (line 742); matrix row `524`.
- **Source responsibility:** Validate Risk-owned, UI/API-produced `ApprovalAttestation v1`, then issue a tamper-evident token only for an eligible decision, binding request/workflow/action/account/strategy/symbol/config/decision/approver/expiry/nonce and writing audit/state durably.
- **Source class / function / method:** `ApprovalTokenService.issue(decision: RiskDecisionPackage, attestation: ApprovalAttestation, *, now: datetime) -> RiskApprovalToken`
- **Source side effects:** Persistence write
- **Source raises:** `RiskDomainError(APPROVAL_REQUIRED, PERMISSION_DENIED, STORAGE_ERROR)`
- **Source usage / test:** **Usage:** `test_usage_approvals.py::test_usage_tokens_issue()`<br>**Unit:** `test_tokens.py::test_issue_requires_valid_ui_approval_attestation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-036`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0301` [ ] `FR-RISK-037` - Atomically verify schema/signature/scope/hashes/attestation/time/revocation/nonce, reserve token + workflow + action scope + expiry, persist single-use consu...

- **Execution domain:** `Risk`.
- **Execution position:** `301` of `675`.
- **Cannot start before:** Step `P01-S0300` (`FR-RISK-036`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.8 `approvals/` — Durable Approval-Token Lifecycle > Configuration and Limits Manifest`` (line 743); matrix row `525`.
- **Source responsibility:** Atomically verify schema/signature/scope/hashes/attestation/time/revocation/nonce, reserve token + workflow + action scope + expiry, persist single-use consumption before live success, and audit the result.
- **Source class / function / method:** `ApprovalTokenService.validate_reserve_and_consume(token: RiskApprovalToken, attestation: ApprovalAttestation, expected: Mapping[str, str], *, now: datetime) -> ApprovalValidationResult`
- **Source side effects:** Persistence write
- **Source raises:** `RiskDomainError(APPROVAL_TOKEN_INVALID, APPROVAL_TOKEN_EXPIRED, APPROVAL_TOKEN_REVOKED, APPROVAL_TOKEN_CONSUMED, PENDING_APPROVAL_DOUBLE_SPEND_BLOCKED, CONFIG_VERSION_MISMATCH, STORAGE_ERROR)`
- **Source usage / test:** **Usage:** `test_usage_approvals.py::test_usage_tokens_validate()`<br>**Unit:** `test_tokens.py::test_concurrent_reservation_succeeds_once()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-037`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0302` [ ] `FR-RISK-038` - Revoke every outstanding token intersecting an activated global/portfolio/strategy/symbol scope and write a material audit event.

- **Execution domain:** `Risk`.
- **Execution position:** `302` of `675`.
- **Cannot start before:** Step `P01-S0301` (`FR-RISK-037`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.8 `approvals/` — Durable Approval-Token Lifecycle > Configuration and Limits Manifest`` (line 744); matrix row `526`.
- **Source responsibility:** Revoke every outstanding token intersecting an activated global/portfolio/strategy/symbol scope and write a material audit event.
- **Source class / function / method:** `ApprovalTokenService.revoke_scope(scope: Mapping[str, str], reason: str, *, now: datetime) -> int`
- **Source side effects:** Persistence write
- **Source raises:** `RiskDomainError(STORAGE_ERROR, PERMISSION_DENIED)`
- **Source usage / test:** **Usage:** `test_usage_approvals.py::test_usage_tokens_revoke_scope()`<br>**Unit:** `test_tokens.py::test_kill_switch_revokes_affected_scope()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-038`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0303` [ ] `P-RISK-009` - decisions feature/component (provisional)

- **Execution domain:** `Risk`.
- **Execution position:** `303` of `675`.
- **Cannot start before:** Step `P01-S0302` (`FR-RISK-038`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.9` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RISK-009`); matrix row `544`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RISK-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0304` [ ] `FR-RISK-039` - Own immutable config plus injected token, audit, clock, and optional configured concurrency protection dependencies.

- **Execution domain:** `Risk`.
- **Execution position:** `304` of `675`.
- **Cannot start before:** Step `P01-S0303` (`P-RISK-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.9 `decisions/` — Canonical Governor, Validity, and Kill Switch > Configuration and Limits Manifest`` (line 778); matrix row `527`.
- **Source responsibility:** Own immutable config plus injected token, audit, clock, and optional configured concurrency protection dependencies.
- **Source class / function / method:** `RiskGovernor(config: RiskConfig, approvals: ApprovalTokenService, audit: RiskAuditChain, clock: Callable[[], datetime], capacity_guard: _CapacityGuard | None = None)`
- **Source side effects:** Local state mutation
- **Source raises:** `RiskDomainError(INVALID_RISK_CONFIG)`
- **Source usage / test:** **Usage:** `tests/risk/usage/test_usage_decisions.py::test_usage_governor_create()`<br>**Unit:** `tests/risk/unit/test_governor.py::test_governor_requires_live_dependencies()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-039`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0305` [ ] `FR-RISK-040` - Validate and review one proposed trade in fixed precedence, include regime/projected risks/final capped size/concurrency disclosure, attach token only when e...

- **Execution domain:** `Risk`.
- **Execution position:** `305` of `675`.
- **Cannot start before:** Step `P01-S0304` (`FR-RISK-039`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.9 `decisions/` — Canonical Governor, Validity, and Kill Switch > Configuration and Limits Manifest`` (line 779); matrix row `528`.
- **Source responsibility:** Validate and review one proposed trade in fixed precedence, include regime/projected risks/final capped size/concurrency disclosure, attach token only when eligible, and audit the decision.
- **Source class / function / method:** `RiskGovernor.review_trade_risk(proposal: ProposedTrade, snapshot: PortfolioRiskSnapshot, market: MarketContextEvidence, regime: RegimeAssessment, auth: AuthContext, *, now: datetime) -> RiskDecisionPackage`
- **Source side effects:** Persistence write
- **Source raises:** `RiskDomainError(GOVERNOR_DECISION_FAILED, STORAGE_ERROR)`
- **Source usage / test:** **Usage:** `test_usage_decisions.py::test_usage_governor_trade_review()`<br>**Unit:** `test_governor.py::test_trade_review_truth_table_and_precedence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-040`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0306` [ ] `FR-RISK-041` - Evaluate current portfolio compliance and return a remediation recommendation without changing execution state.

- **Execution domain:** `Risk`.
- **Execution position:** `306` of `675`.
- **Cannot start before:** Step `P01-S0305` (`FR-RISK-040`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.9 `decisions/` — Canonical Governor, Validity, and Kill Switch > Configuration and Limits Manifest`` (line 780); matrix row `529`.
- **Source responsibility:** Evaluate current portfolio compliance and return a remediation recommendation without changing execution state.
- **Source class / function / method:** `RiskGovernor.run_portfolio_risk_governor(snapshot: PortfolioRiskSnapshot, market: MarketContextEvidence, regime: RegimeAssessment, auth: AuthContext, *, now: datetime) -> RiskDecisionPackage`
- **Source side effects:** Persistence write
- **Source raises:** `RiskDomainError(GOVERNOR_DECISION_FAILED, STORAGE_ERROR)`
- **Source usage / test:** **Usage:** `test_usage_decisions.py::test_usage_governor_portfolio()`<br>**Unit:** `test_governor.py::test_portfolio_governor_no_execution_mutation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-041`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0307` [ ] `FR-RISK-042` - Compare proposal/evidence/config/time with a prior decision and invalidate material changes, expiry, skew, stale state, config mismatch, or reconciliation ex...

- **Execution domain:** `Risk`.
- **Execution position:** `307` of `675`.
- **Cannot start before:** Step `P01-S0306` (`FR-RISK-041`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.9 `decisions/` — Canonical Governor, Validity, and Kill Switch > Configuration and Limits Manifest`` (line 781); matrix row `530`.
- **Source responsibility:** Compare proposal/evidence/config/time with a prior decision and invalidate material changes, expiry, skew, stale state, config mismatch, or reconciliation expiry.
- **Source class / function / method:** `revalidate_risk_decision(decision: RiskDecisionPackage, proposal: ProposedTrade, snapshot: PortfolioRiskSnapshot, config: RiskConfig, *, now: datetime) -> ApprovalValidationResult`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(STALE_EVIDENCE, CONFIG_VERSION_MISMATCH, IN_FLIGHT_RECONCILIATION_EXPIRED)`
- **Source usage / test:** **Usage:** `test_usage_decisions.py::test_usage_validity_revalidate()`<br>**Unit:** `tests/risk/unit/test_validity.py::test_material_change_invalidates()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-042`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0308` [ ] `FR-RISK-043` - Apply authorized activation/clearance under global > portfolio > strategy > symbol precedence, revoke affected approvals on activation, and never mutate exec...

- **Execution domain:** `Risk`.
- **Execution position:** `308` of `675`.
- **Cannot start before:** Step `P01-S0307` (`FR-RISK-042`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.9 `decisions/` — Canonical Governor, Validity, and Kill Switch > Configuration and Limits Manifest`` (line 782); matrix row `531`.
- **Source responsibility:** Apply authorized activation/clearance under `global > portfolio > strategy > symbol` precedence, revoke affected approvals on activation, and never mutate execution controls. Activation requires an authorized `AuthContext`; clearance additionally requires a matching current `ApprovalAttestation v1`.
- **Source class / function / method:** `apply_kill_switch_command(command: KillSwitchCommand, current: KillSwitchState, auth: AuthContext, approvals: ApprovalTokenService, audit: RiskAuditChain, *, attestation: ApprovalAttestation | None = None, now: datetime) -> KillSwitchState`
- **Source side effects:** Persistence write
- **Source raises:** `RiskDomainError(PERMISSION_DENIED, POLICY_BLOCKED, STORAGE_ERROR)`
- **Source usage / test:** **Usage:** `test_usage_decisions.py::test_usage_kill_switch_apply()`<br>**Unit:** `tests/risk/unit/test_kill_switch.py::test_child_clear_cannot_override_active_parent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-043`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0309` [ ] `FR-RISK-044` - Return deterministic block/recovery eligibility; active or unknown applicable state blocks live risk increase, and recovery requires all applicable scopes in...

- **Execution domain:** `Risk`.
- **Execution position:** `309` of `675`.
- **Cannot start before:** Step `P01-S0308` (`FR-RISK-043`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.9 `decisions/` — Canonical Governor, Validity, and Kill Switch > Configuration and Limits Manifest`` (line 783); matrix row `532`.
- **Source responsibility:** Return deterministic block/recovery eligibility; active or unknown applicable state blocks live risk increase, and recovery requires all applicable scopes inactive plus Trading reconciliation.
- **Source class / function / method:** `check_risk_kill_switch(states: Sequence[KillSwitchState], scope: Mapping[str, str], *, reconciled: bool, now: datetime) -> RiskDecisionPackage`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(KILL_SWITCH_ACTIVE, KILL_SWITCH_UNKNOWN, POLICY_BLOCKED)`
- **Source usage / test:** **Usage:** `test_usage_decisions.py::test_usage_kill_switch_check()`<br>**Unit:** `test_kill_switch.py::test_recovery_requires_clear_hierarchy_and_reconciliation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-044`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0310` [ ] `FR-RISK-045` - Deterministically apply bounded scenarios to immutable snapshot evidence, return baseline/projected risk differences, preserve explicit seed, and mark every ...

- **Execution domain:** `Risk`.
- **Execution position:** `310` of `675`.
- **Cannot start before:** Step `P01-S0309` (`FR-RISK-044`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.10 `scenarios/` — Advisory Scenario and What-If Analysis > Configuration and Limits Manifest`` (line 809); matrix row `533`.
- **Source responsibility:** Deterministically apply bounded scenarios to immutable snapshot evidence, return baseline/projected risk differences, preserve explicit seed, and mark every result advisory.
- **Source class / function / method:** `run_risk_scenario_analysis(snapshot: PortfolioRiskSnapshot, scenarios: Sequence[ScenarioDefinition], config: RiskConfig, *, now: datetime) -> tuple[ScenarioResult, ...]`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(PAYLOAD_TOO_LARGE, CALCULATION_FAILED)`
- **Source usage / test:** **Usage:** `tests/risk/usage/test_usage_scenarios.py::test_usage_analysis_scenarios()`<br>**Unit:** `tests/risk/unit/test_analysis.py::test_analysis_is_immutable_and_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-045`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0311` [ ] `FR-RISK-046` - Render evidence, calculations, assumptions, warnings, decision, and recommendations separately; show primary failure first; never claim live approval without...

- **Execution domain:** `Risk`.
- **Execution position:** `311` of `675`.
- **Cannot start before:** Step `P01-S0310` (`FR-RISK-045`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` module `4.11` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 4. Module and Requirement Specifications > 4.11 `reporting/` — Focused Risk Decision Summaries > Configuration and Limits Manifest`` (line 835); matrix row `534`.
- **Source responsibility:** Render evidence, calculations, assumptions, warnings, decision, and recommendations separately; show primary failure first; never claim live approval without valid decision/token evidence.
- **Source class / function / method:** `generate_risk_report(source: PortfolioRiskSnapshot | RiskDecisionPackage | Sequence[ScenarioResult], format: Literal["markdown", "json"]) -> RiskReport`
- **Source side effects:** None
- **Source raises:** `RiskDomainError(REPORT_GENERATION_FAILED)`
- **Source usage / test:** **Usage:** `tests/risk/usage/test_usage_reporting.py::test_usage_reports_generate()`<br>**Unit:** `tests/risk/unit/test_reports.py::test_report_has_no_false_approval_claim()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RISK-046`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0312` [ ] `WF-RISK-002` - Calculate position size

- **Execution domain:** `Risk`.
- **Execution position:** `312` of `675`.
- **Cannot start before:** Step `P01-S0311` (`FR-RISK-046`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 3. Workflows > Workflow scope values`` (line 266); matrix row `545`.
- **Source scope:** Cross-domain
- **Source workflow:** Calculate position size
- **Source requirement sequence:** `FR-RISK-007 → FR-RISK-008 → FR-RISK-026`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-RISK-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0313` [ ] `WF-RISK-004` - Review proposed trade risk

- **Execution domain:** `Risk`.
- **Execution position:** `313` of `675`.
- **Cannot start before:** Step `P01-S0312` (`WF-RISK-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 3. Workflows > Workflow scope values`` (line 268); matrix row `546`.
- **Source scope:** Cross-domain
- **Source workflow:** Review proposed trade risk
- **Source requirement sequence:** `FR-RISK-006 → FR-RISK-027 → FR-RISK-031 → FR-RISK-040`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-RISK-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0314` [ ] `WF-RISK-008` - Validate approval token

- **Execution domain:** `Risk`.
- **Execution position:** `314` of `675`.
- **Cannot start before:** Step `P01-S0313` (`WF-RISK-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 3. Workflows > Workflow scope values`` (line 272); matrix row `547`.
- **Source scope:** Cross-domain
- **Source workflow:** Validate approval token
- **Source requirement sequence:** `FR-RISK-015 → FR-RISK-020 → FR-RISK-037`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-RISK-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0315` [ ] `WF-RISK-009` - Apply or check kill-switch state

- **Execution domain:** `Risk`.
- **Execution position:** `315` of `675`.
- **Cannot start before:** Step `P01-S0314` (`WF-RISK-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 3. Workflows > Workflow scope values`` (line 273); matrix row `548`.
- **Source scope:** Cross-domain
- **Source workflow:** Apply/check kill-switch state
- **Source requirement sequence:** `FR-RISK-016 → FR-RISK-043 → FR-RISK-017 → FR-RISK-044`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-RISK-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0316` [ ] `WF-RISK-012` - Persist risk audit and token state

- **Execution domain:** `Risk`.
- **Execution position:** `316` of `675`.
- **Cannot start before:** Step `P01-S0315` (`WF-RISK-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Risk` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/risk/README.md` - ``Risk > 3. Workflows > Workflow scope values`` (line 276); matrix row `549`.
- **Source scope:** Cross-domain
- **Source workflow:** Persist risk audit and token state
- **Source requirement sequence:** `FR-RISK-018 → FR-RISK-033 → FR-RISK-037`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-RISK-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 8 - Trading

Implement `Trading` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0317` [ ] `P-TRD-001` - contracts feature/component (provisional)

- **Execution domain:** `Trading`.
- **Execution position:** `317` of `675`.
- **Cannot start before:** Step `P01-S0316` (`WF-RISK-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-TRD-001`); matrix row `634`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-TRD-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0318` [ ] `P-TRD-002` - state feature/component (provisional)

- **Execution domain:** `Trading`.
- **Execution position:** `318` of `675`.
- **Cannot start before:** Step `P01-S0317` (`P-TRD-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` module `4.2` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-TRD-002`); matrix row `635`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-TRD-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0319` [ ] `P-TRD-003` - validation feature/component (provisional)

- **Execution domain:** `Trading`.
- **Execution position:** `319` of `675`.
- **Cannot start before:** Step `P01-S0318` (`P-TRD-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` module `4.3` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-TRD-003`); matrix row `636`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-TRD-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0320` [ ] `P-TRD-004` - routing feature/component (provisional)

- **Execution domain:** `Trading`.
- **Execution position:** `320` of `675`.
- **Cannot start before:** Step `P01-S0319` (`P-TRD-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` module `4.4` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-TRD-004`); matrix row `637`
- **Source capability:** `CAP-TRD-023`
- **Source final destination:** `routing/responses.py`; external rate verdicts, no local policy engine
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-TRD-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0321` [ ] `P-TRD-005` - reconciliation feature/component (provisional)

- **Execution domain:** `Trading`.
- **Execution position:** `321` of `675`.
- **Cannot start before:** Step `P01-S0320` (`P-TRD-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` module `4.5` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-TRD-005`); matrix row `638`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-TRD-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0322` [ ] `P-TRD-007` - live feature/component (provisional)

- **Execution domain:** `Trading`.
- **Execution position:** `322` of `675`.
- **Cannot start before:** Step `P01-S0321` (`P-TRD-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` module `4.7` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-TRD-007`); matrix row `639`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-TRD-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0323` [ ] `P-TRD-008` - actions feature/component (provisional)

- **Execution domain:** `Trading`.
- **Execution position:** `323` of `675`.
- **Cannot start before:** Step `P01-S0322` (`P-TRD-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` module `4.8` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-TRD-008`); matrix row `640`
- **Source capability:** `CAP-TRD-015`
- **Source final destination:** `actions/controls.py`, `actions/emergency.py`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-TRD-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0324` [ ] `P-TRD-009` - reporting feature/component (provisional)

- **Execution domain:** `Trading`.
- **Execution position:** `324` of `675`.
- **Cannot start before:** Step `P01-S0323` (`P-TRD-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` module `4.9` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-TRD-009`); matrix row `641`
- **Source capability:** `CAP-TRD-019`
- **Source final destination:** `reporting/evidence.py`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-TRD-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0325` [ ] `FR-TRD-001` - expose only sim, paper, and live action routes; package-only is a side-effect mode, not a route.

- **Execution domain:** `Trading`.
- **Execution position:** `325` of `675`.
- **Cannot start before:** Step `P01-S0324` (`P-TRD-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Canonical contract family`` (line 513); matrix row `579`.
- **Source responsibility:** The system shall expose only `sim`, `paper`, and `live` action routes; package-only is a side-effect mode, not a route.
- **Source class / function / method:** `TradingRoute: type[StrEnum]`
- **Source side effects:** None
- **Source raises:** `TradingError`: unknown route
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_models_trading_route()`<br>**Unit:** `tests/trading/unit/contracts/test_models.py::test_trading_route_rejects_unknown()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0326` [ ] `FR-TRD-002` - validate one immutable canonical request with route, action, trace, authority, approval, Risk, idempotency, and UTC evidence.

- **Execution domain:** `Trading`.
- **Execution position:** `326` of `675`.
- **Cannot start before:** Step `P01-S0325` (`FR-TRD-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Canonical contract family`` (line 514); matrix row `580`.
- **Source responsibility:** The system shall validate one immutable canonical request with route, action, trace, authority, approval, Risk, idempotency, and UTC evidence.
- **Source class / function / method:** `TradingRequest`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid/missing field or unsafe numeric/time value
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_models_trading_request()`<br>**Unit:** `tests/trading/unit/contracts/test_models.py::test_trading_request_requires_governed_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0327` [ ] `FR-TRD-003` - return one finite JSON-safe envelope distinguishing packaging, rejection, block, send, fill, cancellation, unknown outcome, and error.

- **Execution domain:** `Trading`.
- **Execution position:** `327` of `675`.
- **Cannot start before:** Step `P01-S0326` (`FR-TRD-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Canonical contract family`` (line 515); matrix row `581`.
- **Source responsibility:** The system shall return one finite JSON-safe envelope distinguishing packaging, rejection, block, send, fill, cancellation, unknown outcome, and error.
- **Source class / function / method:** `StandardTradingEnvelope`
- **Source side effects:** None
- **Source raises:** `TradingError`: nonconforming envelope
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_models_standard_envelope()`<br>**Unit:** `tests/trading/unit/contracts/test_models.py::test_standard_envelope_status_contract()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0328` [ ] `FR-TRD-004` - expose OrderIntent v1 exactly as defined in Section 1 and preserve Risk-approved size.

- **Execution domain:** `Trading`.
- **Execution position:** `328` of `675`.
- **Cannot start before:** Step `P01-S0327` (`FR-TRD-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Canonical contract family`` (line 516); matrix row `582`.
- **Source responsibility:** The system shall expose `OrderIntent v1` exactly as defined in Section 1 and preserve Risk-approved size.
- **Source class / function / method:** `OrderIntent`
- **Source side effects:** None
- **Source raises:** `TradingError`: size or lineage mismatch
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_models_order_intent()`<br>**Unit:** `tests/trading/unit/contracts/test_models.py::test_order_intent_cannot_exceed_risk_size()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0329` [ ] `FR-TRD-005` - expose immutable ExecutionReceipt v1 with authority, status, fill, retry, and reconciliation evidence.

- **Execution domain:** `Trading`.
- **Execution position:** `329` of `675`.
- **Cannot start before:** Step `P01-S0328` (`FR-TRD-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Canonical contract family`` (line 517); matrix row `583`.
- **Source responsibility:** The system shall expose immutable `ExecutionReceipt v1` with authority, status, fill, retry, and reconciliation evidence.
- **Source class / function / method:** `ExecutionReceipt`
- **Source side effects:** None
- **Source raises:** `TradingError`: malformed receipt
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_models_execution_receipt()`<br>**Unit:** `tests/trading/unit/contracts/test_models.py::test_receipt_requires_authority_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0330` [ ] `FR-TRD-006` - expose TradeRecord v1 without deriving Analytics metrics or hiding unreconciled state.

- **Execution domain:** `Trading`.
- **Execution position:** `330` of `675`.
- **Cannot start before:** Step `P01-S0329` (`FR-TRD-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `models.py` — Canonical contract family`` (line 518); matrix row `584`.
- **Source responsibility:** The system shall expose `TradeRecord v1` without deriving Analytics metrics or hiding unreconciled state.
- **Source class / function / method:** `TradeRecord`
- **Source side effects:** None
- **Source raises:** `TradingError`: inconsistent fill/authority state
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_models_trade_record()`<br>**Unit:** `tests/trading/unit/contracts/test_models.py::test_trade_record_flags_unreconciled_state()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0331` [ ] `FR-TRD-007` - expose one finite Trading exception carrying a registered code and redacted trace context.

- **Execution domain:** `Trading`.
- **Execution position:** `331` of `675`.
- **Cannot start before:** Step `P01-S0330` (`FR-TRD-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `errors.py` — Error taxonomy and redaction`` (line 524); matrix row `585`.
- **Source responsibility:** The system shall expose one finite Trading exception carrying a registered code and redacted trace context.
- **Source class / function / method:** `TradingError`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_errors_trading_error()`<br>**Unit:** `tests/trading/unit/contracts/test_errors.py::test_trading_error_rejects_unknown_code()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0332` [ ] `FR-TRD-008` - map validation, permission, persistence, timeout, provider, and unknown failures into the canonical envelope without raw exceptions.

- **Execution domain:** `Trading`.
- **Execution position:** `332` of `675`.
- **Cannot start before:** Step `P01-S0331` (`FR-TRD-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `errors.py` — Error taxonomy and redaction`` (line 525); matrix row `586`.
- **Source responsibility:** The system shall map validation, permission, persistence, timeout, provider, and unknown failures into the canonical envelope without raw exceptions.
- **Source class / function / method:** `map_trading_error(error: Exception, context: Mapping[str, JsonValue]) -> StandardTradingEnvelope`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_errors_map_trading_error()`<br>**Unit:** `tests/trading/unit/contracts/test_errors.py::test_map_trading_error_redacts_provider_exception()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0333` [ ] `FR-TRD-009` - recursively redact secrets before any log, error, event, metric, or returned payload.

- **Execution domain:** `Trading`.
- **Execution position:** `333` of `675`.
- **Cannot start before:** Step `P01-S0332` (`FR-TRD-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `errors.py` — Error taxonomy and redaction`` (line 526); matrix row `587`.
- **Source responsibility:** The system shall recursively redact secrets before any log, error, event, metric, or returned payload.
- **Source class / function / method:** `redact_trading_payload(payload: JsonValue) -> JsonValue`
- **Source side effects:** None
- **Source raises:** `TradingError`: payload is not JSON-safe
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_errors_redact_trading_payload()`<br>**Unit:** `tests/trading/unit/contracts/test_errors.py::test_redaction_is_recursive_and_case_insensitive()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0334` [ ] `FR-TRD-010` - return the exact stable Python API with routes, schemas, side effects, approvals, idempotency, statuses, errors, and stability metadata.

- **Execution domain:** `Trading`.
- **Execution position:** `334` of `675`.
- **Cannot start before:** Step `P01-S0333` (`FR-TRD-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `registry.py` — Public API catalog`` (line 532); matrix row `588`.
- **Source responsibility:** The system shall return the exact stable Python API with routes, schemas, side effects, approvals, idempotency, statuses, errors, and stability metadata.
- **Source class / function / method:** `get_public_contracts() -> tuple[Mapping[str, JsonValue], ...]`
- **Source side effects:** None
- **Source raises:** `TradingError`: catalog conflicts with exports
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_registry_get_public_contracts()`<br>**Unit:** `tests/trading/unit/contracts/test_registry.py::test_public_catalog_matches_exports()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0335` [ ] `FR-TRD-012` - create a non-executable action draft that cannot call a route authority.

- **Execution domain:** `Trading`.
- **Execution position:** `335` of `675`.
- **Cannot start before:** Step `P01-S0334` (`FR-TRD-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `registry.py` — Public API catalog`` (line 533); matrix row `589`.
- **Source responsibility:** The system shall create a non-executable action draft that cannot call a route authority.
- **Source class / function / method:** `create_trading_action_draft(request: Mapping[str, JsonValue]) -> StandardTradingEnvelope`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid draft
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_contracts.py::test_usage_registry_create_draft()`<br>**Unit:** `tests/trading/unit/contracts/test_registry.py::test_create_draft_has_no_side_effect()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0336` [ ] `FR-TRD-037` - represent send attempts, receipts, fills, reconciliation transitions, and incidents as versioned, redacted events.

- **Execution domain:** `Trading`.
- **Execution position:** `336` of `675`.
- **Cannot start before:** Step `P01-S0335` (`FR-TRD-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 569); matrix row `613`.
- **Source responsibility:** The system shall represent send attempts, receipts, fills, reconciliation transitions, and incidents as versioned, redacted events.
- **Source class / function / method:** `TradingEvent`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid event/version
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_events_trading_event()`<br>**Unit:** `tests/trading/unit/state/test_events.py::test_event_requires_trace_and_utc_time()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-037`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0337` [ ] `FR-TRD-038` - expose only minimal injected operations for idempotency, append, projection reads/writes, and reconciliation evidence.

- **Execution domain:** `Trading`.
- **Execution position:** `337` of `675`.
- **Cannot start before:** Step `P01-S0336` (`FR-TRD-037`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 570); matrix row `614`.
- **Source responsibility:** The system shall expose only minimal injected operations for idempotency, append, projection reads/writes, and reconciliation evidence.
- **Source class / function / method:** `TradingStateStore`
- **Source side effects:** Read-only; persistence write
- **Source raises:** `TradingError`: store failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_stores_trading_state_store()`<br>**Unit:** `tests/trading/unit/state/test_stores.py::test_store_contract_failure_is_visible()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-038`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0338` [ ] `FR-TRD-039` - reserve a caller-supplied key against versioned canonical SHA-256 material before send and reject different-material reuse.

- **Execution domain:** `Trading`.
- **Execution position:** `338` of `675`.
- **Cannot start before:** Step `P01-S0337` (`FR-TRD-038`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 571); matrix row `615`.
- **Source responsibility:** The system shall reserve a caller-supplied key against versioned canonical SHA-256 material before send and reject different-material reuse.
- **Source class / function / method:** `reserve_idempotency(request: TradingRequest, store: TradingStateStore) -> IdempotencyReservation`
- **Source side effects:** Persistence write
- **Source raises:** `TradingError`: missing key, conflict, or write failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_idempotency_reserve()`<br>**Unit:** `tests/trading/unit/state/test_idempotency.py::test_same_key_different_material_rejected()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-039`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0339` [ ] `FR-TRD-042` - provide additive Trading migration definitions for execution-owned state without opening a database.

- **Execution domain:** `Trading`.
- **Execution position:** `339` of `675`.
- **Cannot start before:** Step `P01-S0338` (`FR-TRD-039`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 574); matrix row `616`.
- **Source responsibility:** The system shall provide additive Trading migration definitions for execution-owned state without opening a database.
- **Source class / function / method:** `get_trading_migrations() -> tuple[MigrationDefinition, ...]`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid/non-additive definition
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_migrations_get_migrations()`<br>**Unit:** `tests/trading/unit/state/test_migrations.py::test_migrations_are_additive_and_ordered()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-042`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0340` [ ] `FR-TRD-051` - The store shall reserve one caller key against canonical material and return the existing/new/conflict decision atomically.

- **Execution domain:** `Trading`.
- **Execution position:** `340` of `675`.
- **Cannot start before:** Step `P01-S0339` (`FR-TRD-042`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 575); matrix row `624`.
- **Source responsibility:** The store shall reserve one caller key against canonical material and return the existing/new/conflict decision atomically.
- **Source class / function / method:** `TradingStateStore.reserve_idempotency(key: str, material_hash: str, material_version: str, expires_at: datetime) -> IdempotencyReservation`
- **Source side effects:** Persistence write
- **Source raises:** `TradingError`: conflict or write failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_stores_reserve_idempotency()`<br>**Unit:** `tests/trading/unit/state/test_stores.py::test_reserve_idempotency_is_atomic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-051`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0341` [ ] `FR-TRD-052` - The store shall append one versioned event without rewriting prior events.

- **Execution domain:** `Trading`.
- **Execution position:** `341` of `675`.
- **Cannot start before:** Step `P01-S0340` (`FR-TRD-051`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 576); matrix row `625`.
- **Source responsibility:** The store shall append one versioned event without rewriting prior events.
- **Source class / function / method:** `TradingStateStore.append_event(event: TradingEvent) -> None`
- **Source side effects:** Persistence write
- **Source raises:** `TradingError`: append/version failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_stores_append_event()`<br>**Unit:** `tests/trading/unit/state/test_stores.py::test_append_event_is_append_only()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-052`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0342` [ ] `FR-TRD-053` - The store shall load the latest projection for an exact route/tenant/authority scope.

- **Execution domain:** `Trading`.
- **Execution position:** `342` of `675`.
- **Cannot start before:** Step `P01-S0341` (`FR-TRD-052`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 577); matrix row `626`.
- **Source responsibility:** The store shall load the latest projection for an exact route/tenant/authority scope.
- **Source class / function / method:** `TradingStateStore.load_projection(scope: tuple[TradingRoute, str, str]) -> TradingProjection | None`
- **Source side effects:** Read-only
- **Source raises:** `TradingError`: read or schema failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_stores_load_projection()`<br>**Unit:** `tests/trading/unit/state/test_stores.py::test_load_projection_is_scope_isolated()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-053`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0343` [ ] `FR-TRD-054` - The store shall save a projection only when the expected optimistic version matches.

- **Execution domain:** `Trading`.
- **Execution position:** `343` of `675`.
- **Cannot start before:** Step `P01-S0342` (`FR-TRD-053`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 578); matrix row `627`.
- **Source responsibility:** The store shall save a projection only when the expected optimistic version matches.
- **Source class / function / method:** `TradingStateStore.save_projection(projection: TradingProjection, expected_version: int) -> None`
- **Source side effects:** Persistence write
- **Source raises:** `TradingError`: stale version or write failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_stores_save_projection()`<br>**Unit:** `tests/trading/unit/state/test_stores.py::test_save_projection_rejects_stale_version()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-054`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0344` [ ] `FR-TRD-055` - The store shall return every unresolved send attempt for an exact authority/conflict scope.

- **Execution domain:** `Trading`.
- **Execution position:** `344` of `675`.
- **Cannot start before:** Step `P01-S0343` (`FR-TRD-054`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 579); matrix row `628`.
- **Source responsibility:** The store shall return every unresolved send attempt for an exact authority/conflict scope.
- **Source class / function / method:** `TradingStateStore.load_unresolved_attempts(scope: tuple[TradingRoute, str, str]) -> tuple[TradingEvent, ...]`
- **Source side effects:** Read-only
- **Source raises:** `TradingError`: read or schema failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_stores_load_unresolved_attempts()`<br>**Unit:** `tests/trading/unit/state/test_stores.py::test_unresolved_attempts_are_scope_isolated()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-055`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0345` [ ] `FR-TRD-057` - expose an immutable reservation result distinguishing new, duplicate-completed, duplicate-active, conflict, and reconciliation-required states.

- **Execution domain:** `Trading`.
- **Execution position:** `345` of `675`.
- **Cannot start before:** Step `P01-S0344` (`FR-TRD-055`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 580); matrix row `630`.
- **Source responsibility:** The system shall expose an immutable reservation result distinguishing new, duplicate-completed, duplicate-active, conflict, and reconciliation-required states.
- **Source class / function / method:** `IdempotencyReservation`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid reservation state
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_idempotency_reservation()`<br>**Unit:** `tests/trading/unit/state/test_idempotency.py::test_reservation_states_are_finite()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-057`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0346` [ ] `FR-TRD-058` - expose a route/tenant-scoped order, position, fill, receipt, and authority projection with optimistic version.

- **Execution domain:** `Trading`.
- **Execution position:** `346` of `675`.
- **Cannot start before:** Step `P01-S0345` (`FR-TRD-057`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > State requirements`` (line 581); matrix row `631`.
- **Source responsibility:** The system shall expose a route/tenant-scoped order, position, fill, receipt, and authority projection with optimistic version.
- **Source class / function / method:** `TradingProjection`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid projection/version
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_state.py::test_usage_projections_trading_projection()`<br>**Unit:** `tests/trading/unit/state/test_projections.py::test_projection_requires_scope_and_version()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-058`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0347` [ ] `FR-TRD-024` - validate symbol, action, Decimal volume/price/stops, instrument limits, margin evidence, tickets, and operation preconditions before route s...

- **Execution domain:** `Trading`.
- **Execution position:** `347` of `675`.
- **Cannot start before:** Step `P01-S0346` (`FR-TRD-058`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Validation requirements`` (line 616); matrix row `601`.
- **Source responsibility:** The system shall validate symbol, action, Decimal volume/price/stops, instrument limits, margin evidence, tickets, and operation preconditions before route selection.
- **Source class / function / method:** `validate_order_request(request: TradingRequest, account_state: AccountStateSnapshot) -> TradingRequest`
- **Source side effects:** None
- **Source raises:** `TradingError`: validation failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_validation.py::test_usage_orders_validate_order_request()`<br>**Unit:** `tests/trading/unit/validation/test_orders.py::test_invalid_order_never_reaches_authority()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0348` [ ] `FR-TRD-026` - return timestamped account/symbol/quote/permission/authority facts or explicit unavailable/stale failures.

- **Execution domain:** `Trading`.
- **Execution position:** `348` of `675`.
- **Cannot start before:** Step `P01-S0347` (`FR-TRD-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Validation requirements`` (line 617); matrix row `603`.
- **Source responsibility:** The system shall return timestamped account/symbol/quote/permission/authority facts or explicit unavailable/stale failures.
- **Source class / function / method:** `get_route_snapshot(request: TradingRequest, source: Callable[[TradingRoute, str | None], Mapping[str, JsonValue]]) -> RouteSnapshot`
- **Source side effects:** Read-only
- **Source raises:** `TradingError`: unavailable, stale, or malformed source
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_validation.py::test_usage_snapshots_get_route_snapshot()`<br>**Unit:** `tests/trading/unit/validation/test_snapshots.py::test_snapshot_never_substitutes_neutral_defaults()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0349` [ ] `FR-TRD-027` - aggregate all required checks and return a bounded pass/fail assessment with evidence references.

- **Execution domain:** `Trading`.
- **Execution position:** `349` of `675`.
- **Cannot start before:** Step `P01-S0348` (`FR-TRD-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Validation requirements`` (line 618); matrix row `604`.
- **Source responsibility:** The system shall aggregate all required checks and return a bounded pass/fail assessment with evidence references.
- **Source class / function / method:** `assess_execution_readiness(request: TradingRequest, snapshot: RouteSnapshot, risk_decision: RiskDecision, kill_switch_state: KillSwitchState, action_policy: Mapping[str, JsonValue]) -> ReadinessAssessment`
- **Source side effects:** None
- **Source raises:** `TradingError`: missing required policy/threshold
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_validation.py::test_usage_readiness_assess()`<br>**Unit:** `tests/trading/unit/validation/test_readiness.py::test_readiness_fails_on_any_missing_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-027`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0350` [ ] `FR-TRD-028` - construct a deterministic plan and canonical idempotency material without side effects.

- **Execution domain:** `Trading`.
- **Execution position:** `350` of `675`.
- **Cannot start before:** Step `P01-S0349` (`FR-TRD-027`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Validation requirements`` (line 619); matrix row `605`.
- **Source responsibility:** The system shall construct a deterministic plan and canonical idempotency material without side effects.
- **Source class / function / method:** `build_execution_plan(request: TradingRequest, readiness: ReadinessAssessment) -> OrderIntent`
- **Source side effects:** None
- **Source raises:** `TradingError`: readiness failed or material noncanonical
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_validation.py::test_usage_plans_build_execution_plan()`<br>**Unit:** `tests/trading/unit/validation/test_plans.py::test_plan_is_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0351` [ ] `FR-TRD-030` - classify malformed success, timeout, and ambiguous/rate-limited mutation conservatively with retry delay/safety evidence.

- **Execution domain:** `Trading`.
- **Execution position:** `351` of `675`.
- **Cannot start before:** Step `P01-S0350` (`FR-TRD-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Routing requirements`` (line 656); matrix row `606`.
- **Source responsibility:** The system shall classify malformed success, timeout, and ambiguous/rate-limited mutation conservatively with retry delay/safety evidence.
- **Source class / function / method:** `classify_authority_response(raw: JsonValue, capability: Mapping[str, JsonValue]) -> ExecutionReceipt`
- **Source side effects:** None
- **Source raises:** `TradingError`: response cannot be safely represented
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_routing.py::test_usage_responses_classify()`<br>**Unit:** `tests/trading/unit/routing/test_responses.py::test_malformed_success_is_unknown_outcome()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0352` [ ] `FR-TRD-031` - dispatch exactly one approved intent to Simulation for sim or Brokers' BrokerAdapter mutation operations for paper/live.

- **Execution domain:** `Trading`.
- **Execution position:** `352` of `675`.
- **Cannot start before:** Step `P01-S0351` (`FR-TRD-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Routing requirements`` (line 657); matrix row `607`.
- **Source responsibility:** The system shall dispatch exactly one approved intent to Simulation for sim or Brokers' `BrokerAdapter` mutation operations for paper/live.
- **Source class / function / method:** `dispatch_order_intent(intent: OrderIntent, broker_adapter: BrokerAdapter | None, simulation_dispatch: Callable[[OrderIntent], ExecutionReceipt] | None) -> ExecutionReceipt`
- **Source side effects:** Broker mutation or external Simulation mutation; persistence write
- **Source raises:** `TradingError`: authority unavailable, gate absent, pre-write failure, timeout, or unsafe response
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_routing.py::test_usage_dispatcher_dispatch()`<br>**Unit:** `tests/trading/unit/routing/test_dispatcher.py::test_dispatch_has_single_mutation_boundary()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0353` [ ] `FR-TRD-043` - expose normalized account/order/position/time authority evidence without provider objects.

- **Execution domain:** `Trading`.
- **Execution position:** `353` of `675`.
- **Cannot start before:** Step `P01-S0352` (`FR-TRD-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Reconciliation requirements`` (line 690); matrix row `617`.
- **Source responsibility:** The system shall expose normalized account/order/position/time authority evidence without provider objects.
- **Source class / function / method:** `AuthoritySnapshot`
- **Source side effects:** None
- **Source raises:** `TradingError`: unavailable/malformed snapshot
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_reconciliation.py::test_usage_snapshots_authority_snapshot()`<br>**Unit:** `tests/trading/unit/reconciliation/test_snapshots.py::test_snapshot_is_json_safe()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-043`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0354` [ ] `FR-TRD-044` - deterministically report missing, extra, mismatched, and stale records without claiming resolution.

- **Execution domain:** `Trading`.
- **Execution position:** `354` of `675`.
- **Cannot start before:** Step `P01-S0353` (`FR-TRD-043`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Reconciliation requirements`` (line 691); matrix row `618`.
- **Source responsibility:** The system shall deterministically report missing, extra, mismatched, and stale records without claiming resolution.
- **Source class / function / method:** `compare_authority_state(authority: AuthoritySnapshot, internal: TradingProjection) -> ReconciliationReport`
- **Source side effects:** None
- **Source raises:** `TradingError`: incompatible evidence/version
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_reconciliation.py::test_usage_compare_authority_state()`<br>**Unit:** `tests/trading/unit/reconciliation/test_compare.py::test_unresolved_mismatch_stays_unresolved()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-044`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0355` [ ] `FR-TRD-045` - lock retry, persist evidence, prefer route authority truth, and release only after an approved transition resolves.

- **Execution domain:** `Trading`.
- **Execution position:** `355` of `675`.
- **Cannot start before:** Step `P01-S0354` (`FR-TRD-044`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Reconciliation requirements`` (line 692); matrix row `619`.
- **Source responsibility:** The system shall lock retry, persist evidence, prefer route authority truth, and release only after an approved transition resolves.
- **Source class / function / method:** `resolve_unknown_outcome(receipt: ExecutionReceipt, store: TradingStateStore, snapshot_source: Callable[[TradingRoute], AuthoritySnapshot]) -> AuthorityResolution`
- **Source side effects:** Read-only; persistence write
- **Source raises:** `TradingError`: persistence, snapshot, or unresolved authority failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_reconciliation.py::test_usage_authority_resolve_unknown()`<br>**Unit:** `tests/trading/unit/reconciliation/test_authority.py::test_unknown_outcome_cannot_blind_retry()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-045`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0356` [ ] `FR-TRD-061` - expose a deterministic comparison result with discrepancy classes, severity, evidence references, and unresolved status.

- **Execution domain:** `Trading`.
- **Execution position:** `356` of `675`.
- **Cannot start before:** Step `P01-S0355` (`FR-TRD-045`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Reconciliation requirements`` (line 693); matrix row `632`.
- **Source responsibility:** The system shall expose a deterministic comparison result with discrepancy classes, severity, evidence references, and unresolved status.
- **Source class / function / method:** `ReconciliationReport`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid report
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_reconciliation.py::test_usage_compare_reconciliation_report()`<br>**Unit:** `tests/trading/unit/reconciliation/test_compare.py::test_report_cannot_claim_false_resolution()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-061`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0357` [ ] `FR-TRD-062` - expose the approved authority transition, retry decision, incident reference, and remaining unresolved scope.

- **Execution domain:** `Trading`.
- **Execution position:** `357` of `675`.
- **Cannot start before:** Step `P01-S0356` (`FR-TRD-061`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Reconciliation requirements`` (line 694); matrix row `633`.
- **Source responsibility:** The system shall expose the approved authority transition, retry decision, incident reference, and remaining unresolved scope.
- **Source class / function / method:** `AuthorityResolution`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid/unapproved transition
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_reconciliation.py::test_usage_authority_resolution()`<br>**Unit:** `tests/trading/unit/reconciliation/test_authority.py::test_resolution_requires_approved_transition()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-062`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0358` [ ] `FR-TRD-046` - represent focused health, dependency, staleness, timeout, latency, cost, and incident evidence in a Trading-owned contract.

- **Execution domain:** `Trading`.
- **Execution position:** `358` of `675`.
- **Cannot start before:** Step `P01-S0357` (`FR-TRD-062`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Monitoring requirements`` (line 727); matrix row `620`.
- **Source responsibility:** The system shall represent focused health, dependency, staleness, timeout, latency, cost, and incident evidence in a Trading-owned contract.
- **Source class / function / method:** `OperationalEvent`
- **Source side effects:** None
- **Source raises:** `TradingError`: invalid/unredacted event
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_monitoring.py::test_usage_events_operational_event()`<br>**Unit:** `tests/trading/unit/monitoring/test_events.py::test_event_has_trace_and_severity()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-046`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0359` [ ] `FR-TRD-048` - publish redacted runtime evidence through an injected composition sink without importing Data or UI/API and without hiding delivery failure.

- **Execution domain:** `Trading`.
- **Execution position:** `359` of `675`.
- **Cannot start before:** Step `P01-S0358` (`FR-TRD-046`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Monitoring requirements`` (line 729); matrix row `621`.
- **Source responsibility:** The system shall publish redacted runtime evidence through an injected composition sink without importing Data or UI/API and without hiding delivery failure.
- **Source class / function / method:** `emit_runtime_event(event: OperationalEvent, sink: Callable[[OperationalEvent], None]) -> None`
- **Source side effects:** Event publication
- **Source raises:** `TradingError`: sink failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_monitoring.py::test_usage_events_emit_runtime_event()`<br>**Unit:** `tests/trading/unit/monitoring/test_events.py::test_event_delivery_failure_is_incident()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-048`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0360` [ ] `FR-TRD-032` - use one stateful lifecycle object for admission, startup evidence, recovery lock, in-flight work, and shutdown.

- **Execution domain:** `Trading`.
- **Execution position:** `360` of `675`.
- **Cannot start before:** Step `P01-S0359` (`FR-TRD-048`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Live requirements`` (line 766); matrix row `608`.
- **Source responsibility:** The system shall use one stateful lifecycle object for admission, startup evidence, recovery lock, in-flight work, and shutdown.
- **Source class / function / method:** `LiveSession`
- **Source side effects:** Local state mutation
- **Source raises:** `TradingError`: invalid construction/evidence
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_live.py::test_usage_session_live_session()`<br>**Unit:** `tests/trading/unit/live/test_session.py::test_session_starts_package_only()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-032`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0361` [ ] `FR-TRD-033` - validate config/security, bind opaque Data authority, and complete startup reconciliation before enabling mutation.

- **Execution domain:** `Trading`.
- **Execution position:** `361` of `675`.
- **Cannot start before:** Step `P01-S0360` (`FR-TRD-032`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Live requirements`` (line 767); matrix row `609`.
- **Source responsibility:** The system shall validate config/security, bind opaque Data authority, and complete startup reconciliation before enabling mutation.
- **Source class / function / method:** `LiveSession.start(config: Mapping[str, JsonValue], evidence: Mapping[str, JsonValue]) -> StandardTradingEnvelope`
- **Source side effects:** Read-only; local state mutation; persistence write
- **Source raises:** `TradingError`: invalid config, unsafe adapter, reconciliation failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_live.py::test_usage_session_start()`<br>**Unit:** `tests/trading/unit/live/test_session.py::test_start_never_enables_before_reconciliation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-033`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0362` [ ] `FR-TRD-034` - return the actual session mode, admission, authority, health, reconciliation, and unresolved-work state.

- **Execution domain:** `Trading`.
- **Execution position:** `362` of `675`.
- **Cannot start before:** Step `P01-S0361` (`FR-TRD-033`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Live requirements`` (line 768); matrix row `610`.
- **Source responsibility:** The system shall return the actual session mode, admission, authority, health, reconciliation, and unresolved-work state.
- **Source class / function / method:** `LiveSession.status() -> StandardTradingEnvelope`
- **Source side effects:** Read-only
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_live.py::test_usage_session_status()`<br>**Unit:** `tests/trading/unit/live/test_session.py::test_status_never_overstates_readiness()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-034`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0363` [ ] `FR-TRD-035` - stop admission, drain/mark work, flush evidence, reconcile, and report every incomplete shutdown step.

- **Execution domain:** `Trading`.
- **Execution position:** `363` of `675`.
- **Cannot start before:** Step `P01-S0362` (`FR-TRD-034`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Live requirements`` (line 769); matrix row `611`.
- **Source responsibility:** The system shall stop admission, drain/mark work, flush evidence, reconcile, and report every incomplete shutdown step.
- **Source class / function / method:** `LiveSession.stop() -> StandardTradingEnvelope`
- **Source side effects:** Local state mutation; persistence write
- **Source raises:** `TradingError`: shutdown dependency failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_live.py::test_usage_session_stop()`<br>**Unit:** `tests/trading/unit/live/test_session.py::test_stop_reports_flush_and_reconciliation_failure()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-035`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0364` [ ] `FR-TRD-036` - enforce the canonical mandatory gate order and prohibit passthrough Risk or caller-declared emergency authority.

- **Execution domain:** `Trading`.
- **Execution position:** `364` of `675`.
- **Cannot start before:** Step `P01-S0363` (`FR-TRD-035`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Live requirements`` (line 770); matrix row `612`.
- **Source responsibility:** The system shall enforce the canonical mandatory gate order and prohibit passthrough Risk or caller-declared emergency authority.
- **Source class / function / method:** `evaluate_live_gate(request: TradingRequest, evidence: Mapping[str, JsonValue]) -> StandardTradingEnvelope`
- **Source side effects:** Read-only; persistence write; event publication
- **Source raises:** `TradingError`: first failed mandatory gate or audit-write failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_live.py::test_usage_gates_evaluate_live_gate()`<br>**Unit:** `tests/trading/unit/live/test_gates.py::test_real_risk_decision_is_mandatory()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-036`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0365` [ ] `FR-TRD-013` - submit one validated Risk-approved order through the selected route.

- **Execution domain:** `Trading`.
- **Execution position:** `365` of `675`.
- **Cannot start before:** Step `P01-S0364` (`FR-TRD-036`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 805); matrix row `590`.
- **Source responsibility:** The system shall submit one validated Risk-approved order through the selected route.
- **Source class / function / method:** `submit_order(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** External Simulation mutation or broker mutation; persistence write
- **Source raises:** `TradingError`: validation, gate, authority, or response failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_orders_submit_order()`<br>**Unit:** `tests/trading/unit/actions/test_orders.py::test_submit_order_route_parity()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0366` [ ] `FR-TRD-014` - modify only the approved identity/scope with optimistic version and caller idempotency.

- **Execution domain:** `Trading`.
- **Execution position:** `366` of `675`.
- **Cannot start before:** Step `P01-S0365` (`FR-TRD-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 806); matrix row `591`.
- **Source responsibility:** The system shall modify only the approved identity/scope with optimistic version and caller idempotency.
- **Source class / function / method:** `modify_order(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** External Simulation mutation or broker mutation; persistence write
- **Source raises:** `TradingError`: stale version/scope/gate failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_orders_modify_order()`<br>**Unit:** `tests/trading/unit/actions/test_orders.py::test_modify_order_rejects_stale_version()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0367` [ ] `FR-TRD-015` - cancel one pending order after normal gates.

- **Execution domain:** `Trading`.
- **Execution position:** `367` of `675`.
- **Cannot start before:** Step `P01-S0366` (`FR-TRD-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 807); matrix row `592`.
- **Source responsibility:** The system shall cancel one pending order after normal gates.
- **Source class / function / method:** `cancel_order(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** External Simulation mutation or broker mutation; persistence write
- **Source raises:** `TradingError`: order/gate/authority failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_orders_cancel_order()`<br>**Unit:** `tests/trading/unit/actions/test_orders.py::test_cancel_order_is_idempotent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0368` [ ] `FR-TRD-016` - close a position fully or partially with correct netting/hedging identity.

- **Execution domain:** `Trading`.
- **Execution position:** `368` of `675`.
- **Cannot start before:** Step `P01-S0367` (`FR-TRD-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 808); matrix row `593`.
- **Source responsibility:** The system shall close a position fully or partially with correct netting/hedging identity.
- **Source class / function / method:** `close_position(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** External Simulation mutation or broker mutation; persistence write
- **Source raises:** `TradingError`: identity/volume/gate failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_positions_close_position()`<br>**Unit:** `tests/trading/unit/actions/test_positions.py::test_partial_close_preserves_position_identity()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0369` [ ] `FR-TRD-017` - modify only approved stop-loss/take-profit scope.

- **Execution domain:** `Trading`.
- **Execution position:** `369` of `675`.
- **Cannot start before:** Step `P01-S0368` (`FR-TRD-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 809); matrix row `594`.
- **Source responsibility:** The system shall modify only approved stop-loss/take-profit scope.
- **Source class / function / method:** `modify_position(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** External Simulation mutation or broker mutation; persistence write
- **Source raises:** `TradingError`: stop geometry/scope/gate failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_positions_modify_position()`<br>**Unit:** `tests/trading/unit/actions/test_positions.py::test_modify_position_rejects_unapproved_field()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0370` [ ] `FR-TRD-018` - reduce, never increase, exposure and execute exactly the Risk-approved reduction.

- **Execution domain:** `Trading`.
- **Execution position:** `370` of `675`.
- **Cannot start before:** Step `P01-S0369` (`FR-TRD-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 810); matrix row `595`.
- **Source responsibility:** The system shall reduce, never increase, exposure and execute exactly the Risk-approved reduction.
- **Source class / function / method:** `reduce_exposure(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** External Simulation mutation or broker mutation; persistence write
- **Source raises:** `TradingError`: increase/scope/gate failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_positions_reduce_exposure()`<br>**Unit:** `tests/trading/unit/actions/test_positions.py::test_reduce_exposure_cannot_increase()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0371` [ ] `FR-TRD-019` - pause runtime admission without changing strategy lifecycle governance.

- **Execution domain:** `Trading`.
- **Execution position:** `371` of `675`.
- **Cannot start before:** Step `P01-S0370` (`FR-TRD-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 811); matrix row `596`.
- **Source responsibility:** The system shall pause runtime admission without changing strategy lifecycle governance.
- **Source class / function / method:** `pause_strategy(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** Local state mutation; persistence write
- **Source raises:** `TradingError`: policy/approval failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_controls_pause_strategy()`<br>**Unit:** `tests/trading/unit/actions/test_controls.py::test_pause_does_not_promote_strategy()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0372` [ ] `FR-TRD-020` - resume only after a valid Risk-owned ActionPolicyVerdict, all applicable global > portfolio > strategy > symbol kill-switch scopes are inact...

- **Execution domain:** `Trading`.
- **Execution position:** `372` of `675`.
- **Cannot start before:** Step `P01-S0371` (`FR-TRD-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 812); matrix row `597`.
- **Source responsibility:** The system shall resume only after a valid Risk-owned `ActionPolicyVerdict`, all applicable `global > portfolio > strategy > symbol` kill-switch scopes are inactive, and reconciliation is ready.
- **Source class / function / method:** `resume_strategy(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** Local state mutation; persistence write
- **Source raises:** `TradingError`: blocking state, invalid verdict, or reconciliation failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_controls_resume_strategy()`<br>**Unit:** `tests/trading/unit/actions/test_controls.py::test_resume_requires_cleared_hierarchy_and_reconciliation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0373` [ ] `FR-TRD-025` - synchronize projections from route truth without mutating route orders or positions.

- **Execution domain:** `Trading`.
- **Execution position:** `373` of `675`.
- **Cannot start before:** Step `P01-S0372` (`FR-TRD-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 813); matrix row `602`.
- **Source responsibility:** The system shall synchronize projections from route truth without mutating route orders or positions.
- **Source class / function / method:** `sync_positions(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** Read-only; persistence write
- **Source raises:** `TradingError`: source/persistence failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_controls_sync_positions()`<br>**Unit:** `tests/trading/unit/actions/test_controls.py::test_sync_is_route_read_only()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0374` [ ] `FR-TRD-021` - request a scoped Risk-owned kill-switch transition only with a compatible ActionPolicyVerdict; request text cannot create emergency authority.

- **Execution domain:** `Trading`.
- **Execution position:** `374` of `675`.
- **Cannot start before:** Step `P01-S0373` (`FR-TRD-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 814); matrix row `598`.
- **Source responsibility:** The system shall request a scoped Risk-owned kill-switch transition only with a compatible `ActionPolicyVerdict`; request text cannot create emergency authority.
- **Source class / function / method:** `trigger_kill_switch(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** Persistence write; event publication
- **Source raises:** `TradingError`: policy/approval/state failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_controls_trigger_kill_switch()`<br>**Unit:** `tests/trading/unit/actions/test_controls.py::test_request_text_cannot_self_classify_emergency()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0375` [ ] `FR-TRD-022` - clear a switch only through Risk-authorized clearance; an inactive child cannot override an active parent, and resume requires reconciliatio...

- **Execution domain:** `Trading`.
- **Execution position:** `375` of `675`.
- **Cannot start before:** Step `P01-S0374` (`FR-TRD-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 815); matrix row `599`.
- **Source responsibility:** The system shall clear a switch only through Risk-authorized clearance; an inactive child cannot override an active parent, and resume requires reconciliation readiness.
- **Source class / function / method:** `clear_kill_switch(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** Persistence write; event publication
- **Source raises:** `TradingError`: clearance/approval/hierarchy/readiness failure
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_controls_clear_kill_switch()`<br>**Unit:** `tests/trading/unit/actions/test_controls.py::test_clear_cannot_override_active_parent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-022`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0376` [ ] `FR-TRD-023` - mass-cancel pending or otherwise cancellable orders through normal gates, return every child result, and never claim cancellation for uncert...

- **Execution domain:** `Trading`.
- **Execution position:** `376` of `675`.
- **Cannot start before:** Step `P01-S0375` (`FR-TRD-022`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 816); matrix row `600`.
- **Source responsibility:** The system shall mass-cancel pending or otherwise cancellable orders through normal gates, return every child result, and never claim cancellation for uncertain or already-filled work.
- **Source class / function / method:** `cancel_all_orders(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** External Simulation mutation or broker mutation; persistence write
- **Source raises:** `TradingError`: gate/authority failure; partial/uncertain results retained
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_emergency_cancel_all()`<br>**Unit:** `tests/trading/unit/actions/test_emergency.py::test_cancel_all_preserves_uncertain_results()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0377` [ ] `FR-TRD-050` - mass-close positions through normal gates and return every child result.

- **Execution domain:** `Trading`.
- **Execution position:** `377` of `675`.
- **Cannot start before:** Step `P01-S0376` (`FR-TRD-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 817); matrix row `623`.
- **Source responsibility:** The system shall mass-close positions through normal gates and return every child result.
- **Source class / function / method:** `close_all_positions(request: TradingRequest, deps: TradingDependencies) -> StandardTradingEnvelope`
- **Source side effects:** External Simulation mutation or broker mutation; persistence write
- **Source raises:** `TradingError`: gate/authority failure; partial results retained
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_emergency_close_all()`<br>**Unit:** `tests/trading/unit/actions/test_emergency.py::test_close_all_reports_partial_completion()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-050`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0378` [ ] `FR-TRD-056` - expose one immutable injected dependency container without resolving secrets or creating route/store dependencies at import time.

- **Execution domain:** `Trading`.
- **Execution position:** `378` of `675`.
- **Cannot start before:** Step `P01-S0377` (`FR-TRD-050`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Action requirements`` (line 818); matrix row `629`.
- **Source responsibility:** The system shall expose one immutable injected dependency container without resolving secrets or creating route/store dependencies at import time.
- **Source class / function / method:** `TradingDependencies`
- **Source side effects:** None
- **Source raises:** `TradingError`: required dependency missing or route-incompatible
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_actions.py::test_usage_dependencies_trading_dependencies()`<br>**Unit:** `tests/trading/unit/actions/test_dependencies.py::test_dependencies_have_no_import_side_effect()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-056`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0379` [ ] `FR-TRD-049` - package receipts, factual costs, readiness, reconciliation, incidents, warnings, and unresolved actions without calculating performance/TCA.

- **Execution domain:** `Trading`.
- **Execution position:** `379` of `675`.
- **Cannot start before:** Step `P01-S0378` (`FR-TRD-056`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `evidence.py` — Execution and reconciliation evidence`` (line 847); matrix row `622`.
- **Source responsibility:** The system shall package receipts, factual costs, readiness, reconciliation, incidents, warnings, and unresolved actions without calculating performance/TCA.
- **Source class / function / method:** `build_trading_report(request: TradingRequest, store: TradingStateStore) -> StandardTradingEnvelope`
- **Source side effects:** Read-only
- **Source raises:** `TradingError`: missing/inconsistent evidence
- **Source usage / test:** **Usage:** `tests/trading/usage/test_usage_reporting.py::test_usage_evidence_build_trading_report()`<br>**Unit:** `tests/trading/unit/reporting/test_evidence.py::test_report_does_not_compute_analytics_metrics()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-TRD-049`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0380` [ ] `WF-TRD-001` - Validate and package a route-aware action

- **Execution domain:** `Trading`.
- **Execution position:** `380` of `675`.
- **Cannot start before:** Step `P01-S0379` (`FR-TRD-049`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 3. Workflows > Workflow scope values`` (line 258); matrix row `642`.
- **Source scope:** Internal
- **Source workflow:** Validate and package a route-aware action
- **Source requirement sequence:** `FR-TRD-024 → FR-TRD-027 → FR-TRD-028`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-TRD-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0381` [ ] `WF-TRD-002` - Execute a simulation-route action

- **Execution domain:** `Trading`.
- **Execution position:** `381` of `675`.
- **Cannot start before:** Step `P01-S0380` (`WF-TRD-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 3. Workflows > Workflow scope values`` (line 259); matrix row `643`.
- **Source scope:** Cross-domain
- **Source workflow:** Execute a simulation-route action
- **Source requirement sequence:** `FR-TRD-013 → FR-TRD-031`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-TRD-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0382` [ ] `WF-TRD-003` - Start and enable a live session

- **Execution domain:** `Trading`.
- **Execution position:** `382` of `675`.
- **Cannot start before:** Step `P01-S0381` (`WF-TRD-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 3. Workflows > Workflow scope values`` (line 260); matrix row `644`.
- **Source scope:** Cross-domain
- **Source workflow:** Start and enable a live session
- **Source requirement sequence:** `FR-TRD-033 → FR-TRD-034`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-TRD-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0383` [ ] `WF-TRD-004` - Gate and dispatch a live action

- **Execution domain:** `Trading`.
- **Execution position:** `383` of `675`.
- **Cannot start before:** Step `P01-S0382` (`WF-TRD-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 3. Workflows > Workflow scope values`` (line 261); matrix row `645`.
- **Source scope:** Cross-domain
- **Source workflow:** Gate and dispatch a live action
- **Source requirement sequence:** `FR-TRD-036 → FR-TRD-013 → FR-TRD-031`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-TRD-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0384` [ ] `WF-TRD-006` - Read route facts and aggregate readiness

- **Execution domain:** `Trading`.
- **Execution position:** `384` of `675`.
- **Cannot start before:** Step `P01-S0383` (`WF-TRD-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 3. Workflows > Workflow scope values`` (line 263); matrix row `646`.
- **Source scope:** Cross-domain
- **Source workflow:** Read route facts and aggregate readiness
- **Source requirement sequence:** `FR-TRD-026 → FR-TRD-027`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-TRD-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0385` [ ] `WF-TRD-008` - Persist execution evidence and recover state

- **Execution domain:** `Trading`.
- **Execution position:** `385` of `675`.
- **Cannot start before:** Step `P01-S0384` (`WF-TRD-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 3. Workflows > Workflow scope values`` (line 265); matrix row `647`.
- **Source scope:** Cross-domain
- **Source workflow:** Persist evidence and recover state
- **Source requirement sequence:** `FR-TRD-037 → FR-TRD-042`, `FR-TRD-051 → FR-TRD-055`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-TRD-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0386` [ ] `WF-TRD-011` - Build execution and reconciliation evidence

- **Execution domain:** `Trading`.
- **Execution position:** `386` of `675`.
- **Cannot start before:** Step `P01-S0385` (`WF-TRD-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 3. Workflows > Workflow scope values`` (line 268); matrix row `648`.
- **Source scope:** Cross-domain
- **Source workflow:** Build execution/reconciliation evidence
- **Source requirement sequence:** `FR-TRD-049`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-TRD-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0387` [ ] `WF-TRD-012` - Accept a governed upstream request

- **Execution domain:** `Trading`.
- **Execution position:** `387` of `675`.
- **Cannot start before:** Step `P01-S0386` (`WF-TRD-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Trading` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/trading/README.md` - ``Trading > 3. Workflows > Workflow scope values`` (line 269); matrix row `649`.
- **Source scope:** Cross-domain
- **Source workflow:** Accept governed upstream request
- **Source requirement sequence:** `FR-TRD-003 → FR-TRD-024`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-TRD-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 9 - Simulation

Implement `Simulation` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0388` [ ] `P-SIM-001` - validation feature/component (provisional)

- **Execution domain:** `Simulation`.
- **Execution position:** `388` of `675`.
- **Cannot start before:** Step `P01-S0387` (`WF-TRD-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-SIM-001`); matrix row `717`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-SIM-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0389` [ ] `P-SIM-005` - execution feature/component (provisional)

- **Execution domain:** `Simulation`.
- **Execution position:** `389` of `675`.
- **Cannot start before:** Step `P01-S0388` (`P-SIM-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` module `4.5` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-SIM-005`); matrix row `718`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-SIM-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0390` [ ] `P-SIM-006` - reporting feature/component (provisional)

- **Execution domain:** `Simulation`.
- **Execution position:** `390` of `675`.
- **Cannot start before:** Step `P01-S0389` (`P-SIM-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` module `4.6` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-SIM-006`); matrix row `719`
- **Source setting / limit:** Secret redaction policy
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-SIM-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0391` [ ] `P-SIM-007` - run feature/component (provisional)

- **Execution domain:** `Simulation`.
- **Execution position:** `391` of `675`.
- **Cannot start before:** Step `P01-S0390` (`P-SIM-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` module `4.7` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-SIM-007`); matrix row `720`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-SIM-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0392` [ ] `FR-SIM-001` - validate authentication-relevant request structure, registered strategy references, Data references, broker-profile references, trace identi...

- **Execution domain:** `Simulation`.
- **Execution position:** `392` of `675`.
- **Cannot start before:** Step `P01-S0391` (`P-SIM-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `validate.py` — Boundary and Quality Validation`` (line 489); matrix row `695`.
- **Source responsibility:** The system shall validate authentication-relevant request structure, registered strategy references, Data references, broker-profile references, trace identifiers, and deterministic serialization before any import or execution.
- **Source class / function / method:** `validate_run_inputs(payload: Mapping[str, object]) -> None`
- **Source side effects:** Read-only
- **Source raises:** `SimulationError`: `SIM_INVALID_CONFIG` for malformed evidence; `SIM_ARBITRARY_CODE_REJECTED` for raw code/path input
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_validation.py::test_usage_validate_run_inputs()`<br>**Unit:** `tests/simulator/unit/test_validate.py::test_validate_run_inputs_rejects_raw_code()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0393` [ ] `FR-SIM-002` - verify manifest checksum, required schema, UTC monotonic timestamps, uniqueness, OHLC consistency, bid/ask spread, staleness, availability m...

- **Execution domain:** `Simulation`.
- **Execution position:** `393` of `675`.
- **Cannot start before:** Step `P01-S0392` (`FR-SIM-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `validate.py` — Boundary and Quality Validation`` (line 490); matrix row `696`.
- **Source responsibility:** The system shall verify manifest checksum, required schema, UTC monotonic timestamps, uniqueness, OHLC consistency, bid/ask spread, staleness, availability metadata, and requested coverage, blocking severe failures before execution.
- **Source class / function / method:** `validate_market_data(dataset: MarketDataset) -> None`
- **Source side effects:** Read-only
- **Source raises:** `SimulationError`: exact `SIM_DATA_*` code for the detected severe condition
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_validation.py::test_usage_validate_market_data()`<br>**Unit:** `tests/simulator/unit/test_validate.py::test_validate_market_data_blocks_invalid_ohlc()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0394` [ ] `FR-SIM-003` - permit only approved FX scope or explicit FAST_RESEARCH, rejecting unsupported assets, features, service mode, and canonical claims from app...

- **Execution domain:** `Simulation`.
- **Execution position:** `394` of `675`.
- **Cannot start before:** Step `P01-S0393` (`FR-SIM-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `validate.py` — Boundary and Quality Validation`` (line 491); matrix row `697`.
- **Source responsibility:** The system shall permit only approved FX scope or explicit `FAST_RESEARCH`, rejecting unsupported assets, features, service mode, and canonical claims from approximation.
- **Source class / function / method:** `validate_phase_one_scope(payload: Mapping[str, object]) -> None`
- **Source side effects:** Read-only
- **Source raises:** `SimulationError`: `UNSUPPORTED_OPERATION` or the specific approved `SIM_UNSUPPORTED_*` code
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_validation.py::test_usage_validate_phase_one_scope()`<br>**Unit:** `tests/simulator/unit/test_validate.py::test_validate_phase_one_scope_rejects_unsupported_asset()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0395` [ ] `FR-SIM-005` - transform approved FX bars or real ticks into a stable, strictly ordered bid/ask tick tuple whose identity is reproducible from data, model,...

- **Execution domain:** `Simulation`.
- **Execution position:** `395` of `675`.
- **Cannot start before:** Step `P01-S0394` (`FR-SIM-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `timeline.py` — Tick Construction and Timing`` (line 535); matrix row `698`.
- **Source responsibility:** The system shall transform approved FX bars or real ticks into a stable, strictly ordered bid/ask tick tuple whose identity is reproducible from data, model, and seed.
- **Source class / function / method:** `build_tick_timeline(dataset: MarketDataset, model: str, seed: int | None) -> tuple[Tick, ...]`
- **Source side effects:** Read-only
- **Source raises:** `SimulationError`: `SIM_SYNTHETIC_TICK_GENERATION_FAILED`, `SIM_SPREAD_MISSING`, or `SIM_UNSUPPORTED_TICK_MODEL`
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_timeline.py::test_usage_build_tick_timeline()`<br>**Unit:** `tests/simulator/unit/test_timeline.py::test_build_tick_timeline_is_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0396` [ ] `FR-SIM-006` - reject a strategy intent whose evidence became available after its execution time and enforce previous-closed-bar visibility by default.

- **Execution domain:** `Simulation`.
- **Execution position:** `396` of `675`.
- **Cannot start before:** Step `P01-S0395` (`FR-SIM-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `timeline.py` — Tick Construction and Timing`` (line 536); matrix row `699`.
- **Source responsibility:** The system shall reject a strategy intent whose evidence became available after its execution time and enforce previous-closed-bar visibility by default.
- **Source class / function / method:** `validate_intent_timing(intent_available_at: datetime, execution_time: datetime) -> None`
- **Source side effects:** None
- **Source raises:** `SimulationError`: `SIM_LOOKAHEAD_DETECTED` or `SIM_FEATURE_LOOKAHEAD_DETECTED`
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_timeline.py::test_usage_validate_intent_timing()`<br>**Unit:** `tests/simulator/unit/test_timeline.py::test_validate_intent_timing_blocks_lookahead()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0397` [ ] `FR-SIM-010` - Apply only supplied fresh FXConversionEvidence v1 to conversion-dependent accounting; never choose/synthesize a path or fetch a rate.

- **Execution domain:** `Simulation`.
- **Execution position:** `397` of `675`.
- **Cannot start before:** Step `P01-S0396` (`FR-SIM-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `calculations.py` — Stateless Accounting Calculations`` (line 577); matrix row `700`.
- **Source responsibility:** Apply only supplied fresh `FXConversionEvidence v1` to conversion-dependent accounting; never choose/synthesize a path or fetch a rate.
- **Source class / function / method:** `validate_fx_evidence`, accounting conversion helper
- **Source side effects:** None
- **Source raises:** `SimulationError`: `SIM_FX_EVIDENCE_UNAVAILABLE` / stale/incompatible evidence
- **Source usage / test:** **Verification:** `tests/simulator/unit/test_validate.py::test_cross_currency_run_requires_fresh_registered_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0398` [ ] `FR-SIM-014` - append one event with the next monotonic sequence and hash-chain link before the corresponding governed state transition is considered durable.

- **Execution domain:** `Simulation`.
- **Execution position:** `398` of `675`.
- **Cannot start before:** Step `P01-S0397` (`FR-SIM-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `writer.py` — Streaming Journal Persistence`` (line 629); matrix row `701`.
- **Source responsibility:** The system shall append one event with the next monotonic sequence and hash-chain link before the corresponding governed state transition is considered durable.
- **Source class / function / method:** `JournalWriter.append(event: JournalEvent) -> None`
- **Source side effects:** Persistence write
- **Source raises:** `SimulationError`: `SIM_PERSISTENCE_FAILED` on write, flush, lock, or continuity failure
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_journal.py::test_usage_journal_append()`<br>**Unit:** `tests/simulator/unit/test_journal_writer.py::test_append_fails_closed_on_write_error()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0399` [ ] `FR-SIM-016` - validate schema, sequence, hash chain, config/data/engine identities, and invariants while reconstructing state through an injected determin...

- **Execution domain:** `Simulation`.
- **Execution position:** `399` of `675`.
- **Cannot start before:** Step `P01-S0398` (`FR-SIM-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `replay.py` — Replay and Idempotency`` (line 636); matrix row `702`.
- **Source responsibility:** The system shall validate schema, sequence, hash chain, config/data/engine identities, and invariants while reconstructing state through an injected deterministic reducer.
- **Source class / function / method:** `replay_journal(path: Path, reducer: Callable[[Mapping[str, object], JournalEvent], Mapping[str, object]]) -> Mapping[str, object]`
- **Source side effects:** Read-only
- **Source raises:** `SimulationError`: `SIM_CHECKPOINT_INCOMPATIBLE`, `SIM_PERSISTENCE_FAILED`, or `SIM_ACCOUNT_INVARIANT_BROKEN`
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_journal.py::test_usage_replay_journal()`<br>**Unit:** `tests/simulator/unit/test_replay.py::test_replay_rejects_hash_break()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0400` [ ] `FR-SIM-018` - derive an executable bid/ask price from the current tick and approved spread/slippage model without using future ticks.

- **Execution domain:** `Simulation`.
- **Execution position:** `400` of `675`.
- **Cannot start before:** Step `P01-S0399` (`FR-SIM-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `pricing.py` — Execution Pricing`` (line 677); matrix row `703`.
- **Source responsibility:** The system shall derive an executable bid/ask price from the current tick and approved spread/slippage model without using future ticks.
- **Source class / function / method:** `price_order(intent: OrderIntent, tick: Tick, model: Mapping[str, object]) -> Decimal`
- **Source side effects:** None
- **Source raises:** `SimulationError`: `SIM_INVALID_PRICE`, `SIM_SPREAD_MISSING`, `SIM_SLIPPAGE_EXCEEDED`, or unsupported model code
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_execution.py::test_usage_price_order()`<br>**Unit:** `tests/simulator/unit/test_pricing.py::test_price_order_uses_side_correct_bid_ask()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0401` [ ] `FR-SIM-019` - deterministically match supported FX market and pending intents using configured trigger, gap, liquidity, FOK/IOC, and same-tick priority ru...

- **Execution domain:** `Simulation`.
- **Execution position:** `401` of `675`.
- **Cannot start before:** Step `P01-S0400` (`FR-SIM-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `matching.py` — Order Matching`` (line 683); matrix row `704`.
- **Source responsibility:** The system shall deterministically match supported FX market and pending intents using configured trigger, gap, liquidity, FOK/IOC, and same-tick priority rules, explicitly recording partial or cancelled remainder outcomes.
- **Source class / function / method:** `match_order(intent: OrderIntent, tick: Tick, state: Mapping[str, object]) -> Mapping[str, object]`
- **Source side effects:** None
- **Source raises:** `SimulationError`: specific matching, liquidity, gap, market-hours, or fill-policy `SIM_*` code
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_execution.py::test_usage_match_order()`<br>**Unit:** `tests/simulator/unit/test_matching.py::test_match_order_journals_ioc_remainder()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0402` [ ] `FR-SIM-020` - process one canonical tick at a time, enforce timing and state transitions, apply fills through the ledger, append journal events, and retur...

- **Execution domain:** `Simulation`.
- **Execution position:** `402` of `675`.
- **Cannot start before:** Step `P01-S0401` (`FR-SIM-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `engine.py` — Canonical Tick Engine`` (line 689); matrix row `705`.
- **Source responsibility:** The system shall process one canonical tick at a time, enforce timing and state transitions, apply fills through the ledger, append journal events, and return immutable execution outcomes.
- **Source class / function / method:** `EventDrivenExecutionEngine.execute_tick(tick: Tick) -> tuple[Mapping[str, object], ...]`
- **Source side effects:** Local state mutation; event publication; persistence write
- **Source raises:** `SimulationError`: exact validation, execution, accounting, invariant, or persistence code
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_execution.py::test_usage_engine_execute_tick()`<br>**Unit:** `tests/simulator/unit/test_engine.py::test_execute_tick_is_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0403` [ ] `FR-SIM-021` - accept only a Trading-owned OrderIntent for route sim, preserve its final approved volume, and submit it to the active simulation engine wit...

- **Execution domain:** `Simulation`.
- **Execution position:** `403` of `675`.
- **Cannot start before:** Step `P01-S0402` (`FR-SIM-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `trader.py` — Simulation-Scoped Trader Facade`` (line 695); matrix row `706`.
- **Source responsibility:** The system shall accept only a Trading-owned `OrderIntent` for route `sim`, preserve its final approved volume, and submit it to the active simulation engine without any broker call.
- **Source class / function / method:** `SimTrader.submit_order(intent: OrderIntent) -> Mapping[str, object]`
- **Source side effects:** Local state mutation; event publication; persistence write
- **Source raises:** `SimulationError`: `SIM_INVALID_CONFIG`, `SIM_INVALID_VOLUME`, or matching/accounting code
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_execution.py::test_usage_sim_trader_submit_order()`<br>**Unit:** `tests/simulator/unit/test_trader.py::test_submit_order_never_calls_live_adapter()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0404` [ ] `FR-SIM-022` - close an existing simulated position by approved quantity using the current canonical tick and journal the resulting fill.

- **Execution domain:** `Simulation`.
- **Execution position:** `404` of `675`.
- **Cannot start before:** Step `P01-S0403` (`FR-SIM-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `trader.py` — Simulation-Scoped Trader Facade`` (line 696); matrix row `707`.
- **Source responsibility:** The system shall close an existing simulated position by approved quantity using the current canonical tick and journal the resulting fill.
- **Source class / function / method:** `SimTrader.close_position(position_id: str, quantity: Decimal) -> Mapping[str, object]`
- **Source side effects:** Local state mutation; event publication; persistence write
- **Source raises:** `SimulationError`: `SIM_POSITION_NOT_FOUND` or `SIM_INVALID_VOLUME`
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_execution.py::test_usage_sim_trader_close_position()`<br>**Unit:** `tests/simulator/unit/test_trader.py::test_close_position_rejects_unknown_position()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-022`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0405` [ ] `FR-SIM-023` - expose immutable read-only orders, positions, pending orders, deals, and account state for the current run without leaking mutable engine ob...

- **Execution domain:** `Simulation`.
- **Execution position:** `405` of `675`.
- **Cannot start before:** Step `P01-S0404` (`FR-SIM-022`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `trader.py` — Simulation-Scoped Trader Facade`` (line 697); matrix row `708`.
- **Source responsibility:** The system shall expose immutable read-only orders, positions, pending orders, deals, and account state for the current run without leaking mutable engine objects.
- **Source class / function / method:** `SimTrader.snapshot() -> Mapping[str, object]`
- **Source side effects:** Read-only
- **Source raises:** `SimulationError`: `SIM_ACCOUNT_INVARIANT_BROKEN` when state cannot be verified
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_execution.py::test_usage_sim_trader_snapshot()`<br>**Unit:** `tests/simulator/unit/test_trader.py::test_snapshot_cannot_mutate_engine_state()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0406` [ ] `FR-SIM-024` - expose SimulationResult v1 with separate compatibility/schema identity, reproducibility identities, completed status, fills/journal/artifact...

- **Execution domain:** `Simulation`.
- **Execution position:** `406` of `675`.
- **Cannot start before:** Step `P01-S0405` (`FR-SIM-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `contracts.py` — Result Contracts`` (line 735); matrix row `709`.
- **Source responsibility:** The system shall expose `SimulationResult` v1 with separate compatibility/schema identity, reproducibility identities, completed status, fills/journal/artifact references, fixed-precision accounting totals, diagnostics, and realism disclosures, and shall reject incomplete publication.
- **Source class / function / method:** `SimulationResult(contract_version: Literal["v1"], schema_id: Literal["simulation.result.v1"], run_id: str, request_hash: str, config_hash: str, data_hash: str, engine_version: str, status: Literal["completed"], journal_ref: str, artifact_manifest: ArtifactManifest, fills: tuple[Mapping[str, object], ...], accounting: Mapping[str, Decimal], diagnostics: tuple[str, ...], realism: Mapping[str, object])`
- **Source side effects:** None
- **Source raises:** `ValueError`: missing identity/artifact, non-final status, unsafe metadata, or invalid monetary value
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_reporting.py::test_usage_simulation_result()`<br>**Unit:** `tests/simulator/unit/test_reporting_contracts.py::test_result_rejects_incomplete_status()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0407` [ ] `FR-SIM-025` - expose a versioned manifest entry for every canonical artifact with relative path, media type, size, SHA-256 checksum, schema version, and c...

- **Execution domain:** `Simulation`.
- **Execution position:** `407` of `675`.
- **Cannot start before:** Step `P01-S0406` (`FR-SIM-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `contracts.py` — Result Contracts`` (line 736); matrix row `710`.
- **Source responsibility:** The system shall expose a versioned manifest entry for every canonical artifact with relative path, media type, size, SHA-256 checksum, schema version, and creation time.
- **Source class / function / method:** `ArtifactManifest(artifacts: tuple[Mapping[str, object], ...], created_at: datetime, schema_version: str = "v1")`
- **Source side effects:** None
- **Source raises:** `ValueError`: absolute/unsafe path, invalid checksum, missing canonical artifact, or unsupported version
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_reporting.py::test_usage_artifact_manifest()`<br>**Unit:** `tests/simulator/unit/test_reporting_contracts.py::test_manifest_rejects_unsafe_path()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0408` [ ] `FR-SIM-026` - read completed canonical artifacts, verify containment and size, calculate checksums, and return a stable manifest without publishing tempor...

- **Execution domain:** `Simulation`.
- **Execution position:** `408` of `675`.
- **Cannot start before:** Step `P01-S0407` (`FR-SIM-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `artifacts.py` — Artifact Manifest Assembly`` (line 742); matrix row `711`.
- **Source responsibility:** The system shall read completed canonical artifacts, verify containment and size, calculate checksums, and return a stable manifest without publishing temporary files.
- **Source class / function / method:** `build_artifact_manifest(artifact_root: Path, paths: Sequence[Path]) -> ArtifactManifest`
- **Source side effects:** Read-only
- **Source raises:** `SimulationError`: `SIM_PERSISTENCE_FAILED` for missing, unsafe, unreadable, or changed artifacts
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_reporting.py::test_usage_build_artifact_manifest()`<br>**Unit:** `tests/simulator/unit/test_artifacts.py::test_manifest_rejects_path_escape()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0409` [ ] `FR-SIM-027` - serialize a SimulationResult to deterministic canonical JSON with execution/accounting diagnostics and realism/data-quality disclosures, exc...

- **Execution domain:** `Simulation`.
- **Execution position:** `409` of `675`.
- **Cannot start before:** Step `P01-S0408` (`FR-SIM-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `reports.py` — Canonical Reports`` (line 748); matrix row `712`.
- **Source responsibility:** The system shall serialize a `SimulationResult` to deterministic canonical JSON with execution/accounting diagnostics and realism/data-quality disclosures, excluding Analytics-owned metric formulas.
- **Source class / function / method:** `build_json_report(result: SimulationResult) -> str`
- **Source side effects:** None
- **Source raises:** `SimulationError`: `SIM_INTERNAL_ERROR` if canonical serialization fails
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_reporting.py::test_usage_build_json_report()`<br>**Unit:** `tests/simulator/unit/test_reports.py::test_json_report_is_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-027`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0410` [ ] `FR-SIM-028` - render a deterministic Markdown execution report with assumptions, limitations, costs, fills, rejections, data quality, and artifact identit...

- **Execution domain:** `Simulation`.
- **Execution position:** `410` of `675`.
- **Cannot start before:** Step `P01-S0409` (`FR-SIM-027`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `reports.py` — Canonical Reports`` (line 749); matrix row `713`.
- **Source responsibility:** The system shall render a deterministic Markdown execution report with assumptions, limitations, costs, fills, rejections, data quality, and artifact identities, excluding external distribution claims.
- **Source class / function / method:** `build_markdown_report(result: SimulationResult) -> str`
- **Source side effects:** None
- **Source raises:** `SimulationError`: `SIM_INTERNAL_ERROR` when required evidence is absent
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_reporting.py::test_usage_build_markdown_report()`<br>**Unit:** `tests/simulator/unit/test_reports.py::test_markdown_report_discloses_shortcuts()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0411` [ ] `FR-SIM-029` - expose the exact docs/PROJECT.md 5 request for one synchronous bounded FX run, with separate contract version/schema ID, immutable Strateg...

- **Execution domain:** `Simulation`.
- **Execution position:** `411` of `675`.
- **Cannot start before:** Step `P01-S0410` (`FR-SIM-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `contracts.py` — Backtest Request Contract`` (line 791); matrix row `714`.
- **Source responsibility:** The system shall expose the exact `docs/PROJECT.md` §5 request for one synchronous bounded FX run, with separate contract version/schema ID, immutable Strategy/Data/Simulation/Risk references, JSON-safe parameters, symbol/timeframe/UTC range, positive initial balance, trace IDs, simulation profile/route, config hash, and no raw code/provider objects/inline data.
- **Source class / function / method:** `SimulationBacktestRequestV1`
- **Source side effects:** None
- **Source raises:** `ValueError`: missing/unknown field, invalid range/balance/mode/reference/version, non-deterministic value, or unsafe metadata
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_run.py::test_usage_backtest_request()`<br>**Unit:** `tests/simulator/unit/test_run_contracts.py::test_request_matches_adr_0014_exactly()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-029`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0412` [ ] `FR-SIM-030` - authenticate, deduplicate, validate, execute, journal, report, persist, and return one deterministic canonical FX run, never publishing a pa...

- **Execution domain:** `Simulation`.
- **Execution position:** `412` of `675`.
- **Cannot start before:** Step `P01-S0411` (`FR-SIM-029`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `orchestrator.py` — Official Backtest`` (line 797); matrix row `715`.
- **Source responsibility:** The system shall authenticate, deduplicate, validate, execute, journal, report, persist, and return one deterministic canonical FX run, never publishing a partial completed result.
- **Source class / function / method:** `run_backtest(request: SimulationBacktestRequestV1, auth_context: AuthContext, request_id: str | None = None) -> SimulationResult`
- **Source side effects:** Read-only external-domain calls; local state mutation; persistence write; event publication
- **Source raises:** `SimulationError`: controlled validation, execution, journal, reporting, or persistence failure
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_run.py::test_usage_run_backtest()`<br>**Unit:** `tests/simulator/unit/test_orchestrator.py::test_run_backtest_maps_internal_failure()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0413` [ ] `FR-SIM-031` - run an explicitly requested approximation only when enabled, mark every output canonical=false, disclose assumptions, and prohibit canonical...

- **Execution domain:** `Simulation`.
- **Execution position:** `413` of `675`.
- **Cannot start before:** Step `P01-S0412` (`FR-SIM-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `research.py` — Fast Research Approximation`` (line 803); matrix row `716`.
- **Source responsibility:** The system shall run an explicitly requested approximation only when enabled, mark every output `canonical=false`, disclose assumptions, and prohibit canonical fills, promotion evidence, and reports.
- **Source class / function / method:** `run_fast_research(request: SimulationBacktestRequestV1, auth_context: AuthContext, request_id: str | None = None) -> SimulationResult`
- **Source side effects:** Read-only; optional local diagnostic artifact write
- **Source raises:** `SimulationError`: controlled failure
- **Source usage / test:** **Usage:** `tests/simulator/usage/test_usage_run.py::test_usage_run_fast_research()`<br>**Unit:** `tests/simulator/unit/test_research.py::test_fast_research_cannot_claim_canonical()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-SIM-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0414` [ ] `WF-SIM-001` - Official FX Backtest

- **Execution domain:** `Simulation`.
- **Execution position:** `414` of `675`.
- **Cannot start before:** Step `P01-S0413` (`FR-SIM-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 3. Workflows > Workflow scope values`` (line 250); matrix row `721`.
- **Source scope:** Cross-domain
- **Source workflow:** Official FX backtest
- **Source requirement sequence:** `FR-SIM-029 → FR-SIM-001 → FR-SIM-002 → FR-SIM-003 → FR-SIM-005 → FR-SIM-006 → FR-SIM-020 → FR-SIM-024 → FR-SIM-026 → FR-SIM-027 → FR-SIM-028 → FR-SIM-030`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-SIM-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0415` [ ] `WF-SIM-002` - Simulation Trader Operations

- **Execution domain:** `Simulation`.
- **Execution position:** `415` of `675`.
- **Cannot start before:** Step `P01-S0414` (`WF-SIM-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 3. Workflows > Workflow scope values`` (line 251); matrix row `722`.
- **Source scope:** Cross-domain
- **Source workflow:** Simulation Trader operations
- **Source requirement sequence:** `FR-SIM-018 → FR-SIM-019 → FR-SIM-021 → FR-SIM-022/023 → FR-SIM-014`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-SIM-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0416` [ ] `CAP-SIM-001` - Typed public API and versioned contracts

- **Execution domain:** `Simulation`.
- **Execution position:** `416` of `675`.
- **Cannot start before:** Step `P01-S0415` (`WF-SIM-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Approved capability traceability`` (line 448); matrix row `693`.
- **Source final destination:** `run/`: `FR-SIM-029`, `FR-SIM-030`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-SIM-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0417` [ ] `CAP-SIM-004` - Canonical FX execution, matching, realism

- **Execution domain:** `Simulation`.
- **Execution position:** `417` of `675`.
- **Cannot start before:** Step `P01-S0416` (`CAP-SIM-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Simulation` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/simulator/README.md` - ``Simulation > 4. Module and Requirement Specifications > Approved capability traceability`` (line 451); matrix row `694`.
- **Source final destination:** `timeline/`, `execution/`: `FR-SIM-005`, `FR-SIM-018`–`FR-SIM-020`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-SIM-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 10 - Analytics

Implement `Analytics` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0418` [ ] `P-ANLT-001` - contracts feature/component (provisional)

- **Execution domain:** `Analytics`.
- **Execution position:** `418` of `675`.
- **Cannot start before:** Step `P01-S0417` (`CAP-SIM-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-ANLT-001`); matrix row `800`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-ANLT-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0419` [ ] `FR-ANLT-001` - expose one base exception for direct Analytics feature APIs.

- **Execution domain:** `Analytics`.
- **Execution position:** `419` of `675`.
- **Cannot start before:** Step `P01-S0418` (`P-ANLT-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `errors.py` — Public Error Taxonomy`` (line 500); matrix row `764`.
- **Source responsibility:** The system shall expose one base exception for direct Analytics feature APIs.
- **Source class / function / method:** `AnalyticsError`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_errors_analytics_error()`<br>**Unit:** `tests/analytics/unit/test_errors.py::test_analytics_error_is_domain_base()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0420` [ ] `FR-ANLT-002` - distinguish invalid, missing, incompatible, or unsafe analytics evidence from execution failures.

- **Execution domain:** `Analytics`.
- **Execution position:** `420` of `675`.
- **Cannot start before:** Step `P01-S0419` (`FR-ANLT-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `errors.py` — Public Error Taxonomy`` (line 501); matrix row `765`.
- **Source responsibility:** The system shall distinguish invalid, missing, incompatible, or unsafe analytics evidence from execution failures.
- **Source class / function / method:** `AnalyticsValidationError`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_errors_validation_error()`<br>**Unit:** `tests/analytics/unit/test_errors.py::test_validation_error_is_analytics_error()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0421` [ ] `FR-ANLT-003` - convert a controlled exception into a bounded, redacted error payload without exposing provider exceptions or secrets.

- **Execution domain:** `Analytics`.
- **Execution position:** `421` of `675`.
- **Cannot start before:** Step `P01-S0420` (`FR-ANLT-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `errors.py` — Public Error Taxonomy`` (line 502); matrix row `766`.
- **Source responsibility:** The system shall convert a controlled exception into a bounded, redacted error payload without exposing provider exceptions or secrets.
- **Source class / function / method:** `to_analytics_error_payload(error: Exception) -> dict[str, object]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_errors_error_payload()`<br>**Unit:** `tests/analytics/unit/test_errors.py::test_error_payload_is_bounded_and_redacted()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0422` [ ] `FR-ANLT-004` - represent an adapted upstream result with source version, IDs, phase, UTC window, currency, strategy, symbols, timeframe, trades, curves, be...

- **Execution domain:** `Analytics`.
- **Execution position:** `422` of `675`.
- **Cannot start before:** Step `P01-S0421` (`FR-ANLT-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 508); matrix row `767`.
- **Source responsibility:** The system shall represent an adapted upstream result with source version, IDs, phase, UTC window, currency, strategy, symbols, timeframe, trades, curves, benchmark evidence, quality metadata, and lineage.
- **Source class / function / method:** `TradingResult`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: required canonical evidence is absent or inconsistent
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_trading_result()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_trading_result_rejects_missing_identity()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0423` [ ] `FR-ANLT-005` - represent one metric as a finite calculated/undefined/skipped value with unit, confidence, warnings, and source context.

- **Execution domain:** `Analytics`.
- **Execution position:** `423` of `675`.
- **Cannot start before:** Step `P01-S0422` (`FR-ANLT-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 509); matrix row `768`.
- **Source responsibility:** The system shall represent one metric as a finite calculated/undefined/skipped value with unit, confidence, warnings, and source context.
- **Source class / function / method:** `MetricEvidence`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: value is non-finite or status/unit is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_metric_evidence()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_metric_evidence_rejects_infinity()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0424` [ ] `FR-ANLT-006` - represent one report section with approved criticality, ordered metrics, status, warnings, and failure/skipped reason.

- **Execution domain:** `Analytics`.
- **Execution position:** `424` of `675`.
- **Cannot start before:** Step `P01-S0423` (`FR-ANLT-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 510); matrix row `769`.
- **Source responsibility:** The system shall represent one report section with approved criticality, ordered metrics, status, warnings, and failure/skipped reason.
- **Source class / function / method:** `SectionEvidence`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: status conflicts with included evidence
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_section_evidence()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_section_evidence_requires_reason_when_skipped()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0425` [ ] `FR-ANLT-007` - represent a bounded warning with code, severity, affected section, source context, and detail.

- **Execution domain:** `Analytics`.
- **Execution position:** `425` of `675`.
- **Cannot start before:** Step `P01-S0424` (`FR-ANLT-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 511); matrix row `770`.
- **Source responsibility:** The system shall represent a bounded warning with code, severity, affected section, source context, and detail.
- **Source class / function / method:** `AnalyticsWarning`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: code/severity is uncataloged
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_warning()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_warning_uses_cataloged_severity()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0426` [ ] `FR-ANLT-008` - represent a quality flag separately from metrics and governance decisions, including blocker semantics and source evidence.

- **Execution domain:** `Analytics`.
- **Execution position:** `426` of `675`.
- **Cannot start before:** Step `P01-S0425` (`FR-ANLT-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 512); matrix row `771`.
- **Source responsibility:** The system shall represent a quality flag separately from metrics and governance decisions, including blocker semantics and source evidence.
- **Source class / function / method:** `QualityFlag`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: flag type/code is uncataloged
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_quality_flag()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_quality_flag_cannot_claim_governance_decision()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0427` [ ] `FR-ANLT-009` - preserve bounded source IDs, versions, configuration sources, inherited currency, and transformation history.

- **Execution domain:** `Analytics`.
- **Execution position:** `427` of `675`.
- **Cannot start before:** Step `P01-S0426` (`FR-ANLT-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 513); matrix row `772`.
- **Source responsibility:** The system shall preserve bounded source IDs, versions, configuration sources, inherited currency, and transformation history.
- **Source class / function / method:** `Lineage`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: required lineage is missing or unsafe
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_lineage()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_lineage_preserves_source_versions()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0428` [ ] `FR-ANLT-010` - hold SHA-256 hashes for input, configuration, trade ledger, equity curve, optional benchmark, and final report evidence.

- **Execution domain:** `Analytics`.
- **Execution position:** `428` of `675`.
- **Cannot start before:** Step `P01-S0427` (`FR-ANLT-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 514); matrix row `773`.
- **Source responsibility:** The system shall hold SHA-256 hashes for input, configuration, trade ledger, equity curve, optional benchmark, and final report evidence.
- **Source class / function / method:** `ReproducibilityHashes`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: a required hash is absent or malformed
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_hashes()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_reproducibility_hashes_require_sha256()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0429` [ ] `FR-ANLT-011` - expose the owned PerformanceReport v1 cross-domain contract with ordered sections, caveats, lineage, hashes, precision metadata, and non_bin...

- **Execution domain:** `Analytics`.
- **Execution position:** `429` of `675`.
- **Cannot start before:** Step `P01-S0428` (`FR-ANLT-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 515); matrix row `774`.
- **Source responsibility:** The system shall expose the owned `PerformanceReport v1` cross-domain contract with ordered sections, caveats, lineage, hashes, precision metadata, and `non_binding=true`.
- **Source class / function / method:** `PerformanceReport`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: contract, finite-output, or section invariants fail
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_performance_report()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_performance_report_matches_v1_contract()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0430` [ ] `FR-ANLT-012` - represent real portfolio aggregation with component lineage, base currency, FX evidence, blocker flags, and no fabricated aggregate values.

- **Execution domain:** `Analytics`.
- **Execution position:** `430` of `675`.
- **Cannot start before:** Step `P01-S0429` (`FR-ANLT-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 516); matrix row `775`.
- **Source responsibility:** The system shall represent real portfolio aggregation with component lineage, base currency, FX evidence, blocker flags, and no fabricated aggregate values.
- **Source class / function / method:** `PortfolioPerformanceReport`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: component schema/currency/FX evidence is incompatible
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_portfolio_report()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_portfolio_report_requires_fx_lineage()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0431` [ ] `FR-ANLT-013` - represent versioned finite chart/table payloads, section statuses, warnings, units, and truncation metadata without UI rendering logic.

- **Execution domain:** `Analytics`.
- **Execution position:** `431` of `675`.
- **Cannot start before:** Step `P01-S0430` (`FR-ANLT-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 517); matrix row `776`.
- **Source responsibility:** The system shall represent versioned finite chart/table payloads, section statuses, warnings, units, and truncation metadata without UI rendering logic.
- **Source class / function / method:** `DashboardPayload`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: payload is non-finite or exceeds approved limits
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_dashboard_payload()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_dashboard_payload_is_json_safe()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0432` [ ] `FR-ANLT-014` - represent report-derived facts, score inputs, warnings, and recommendation context while explicitly excluding governance authority.

- **Execution domain:** `Analytics`.
- **Execution position:** `432` of `675`.
- **Cannot start before:** Step `P01-S0431` (`FR-ANLT-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `models.py` — Immutable Canonical Contracts`` (line 518); matrix row `777`.
- **Source responsibility:** The system shall represent report-derived facts, score inputs, warnings, and recommendation context while explicitly excluding governance authority.
- **Source class / function / method:** `StrategyQualityEvidence`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: evidence claims approval/promotion authority
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_models_strategy_quality()`<br>**Unit:** `tests/analytics/unit/test_models.py::test_strategy_quality_is_non_binding()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0433` [ ] `FR-ANLT-016` - expose an authoritative definition for every metric used by a report, dashboard, warning, or quality flag.

- **Execution domain:** `Analytics`.
- **Execution position:** `433` of `675`.
- **Cannot start before:** Step `P01-S0432` (`FR-ANLT-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `catalogs.py` — Static Catalogs and Compatibility`` (line 524); matrix row `778`.
- **Source responsibility:** The system shall expose an authoritative definition for every metric used by a report, dashboard, warning, or quality flag.
- **Source class / function / method:** `METRIC_DEFINITION_CATALOG: Mapping[str, Mapping[str, object]]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_catalogs_metrics()`<br>**Unit:** `tests/analytics/unit/test_catalogs.py::test_every_contract_metric_is_cataloged()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0434` [ ] `FR-ANLT-017` - expose deterministic, separately namespaced warning and quality-flag definitions with bounded details, source-backed status, and blocker mea...

- **Execution domain:** `Analytics`.
- **Execution position:** `434` of `675`.
- **Cannot start before:** Step `P01-S0433` (`FR-ANLT-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `catalogs.py` — Static Catalogs and Compatibility`` (line 525); matrix row `779`.
- **Source responsibility:** The system shall expose deterministic, separately namespaced warning and quality-flag definitions with bounded details, source-backed status, and blocker meaning.
- **Source class / function / method:** `EVIDENCE_CATALOG: Mapping[str, Mapping[str, Mapping[str, object]]]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_catalogs_warnings()`<br>**Unit:** `tests/analytics/unit/test_catalogs.py::test_warning_and_flag_codes_are_unique()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0435` [ ] `FR-ANLT-018` - classify accepted, deprecated, legacy-adapted, unsupported, and future source/report contract versions independently of schema_id.

- **Execution domain:** `Analytics`.
- **Execution position:** `435` of `675`.
- **Cannot start before:** Step `P01-S0434` (`FR-ANLT-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `catalogs.py` — Static Catalogs and Compatibility`` (line 526); matrix row `780`.
- **Source responsibility:** The system shall classify accepted, deprecated, legacy-adapted, unsupported, and future source/report contract versions independently of `schema_id`.
- **Source class / function / method:** `CONTRACT_COMPATIBILITY_MATRIX: Mapping[str, Mapping[str, str]]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_catalogs_contract_compatibility()`<br>**Unit:** `tests/analytics/unit/test_catalogs.py::test_contract_matrix_covers_each_counterparty()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0436` [ ] `FR-ANLT-020` - reject a metric catalog lacking formula, unit, inputs, scale, annualization, sample convention, minimum sample, undefined behavior, evidence...

- **Execution domain:** `Analytics`.
- **Execution position:** `436` of `675`.
- **Cannot start before:** Step `P01-S0435` (`FR-ANLT-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `catalogs.py` — Static Catalogs and Compatibility`` (line 527); matrix row `781`.
- **Source responsibility:** The system shall reject a metric catalog lacking formula, unit, inputs, scale, annualization, sample convention, minimum sample, undefined behavior, evidence type, or fixture.
- **Source class / function / method:** `validate_metric_catalog(catalog: Mapping[str, Mapping[str, object]]) -> None`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: any official metric definition is incomplete
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_catalogs_validate_metrics()`<br>**Unit:** `tests/analytics/unit/test_catalogs.py::test_validate_metric_catalog_requires_formula_policy()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0437` [ ] `FR-ANLT-021` - classify contract_version and reject missing, unsupported, or unsafe future compatibility versions before calculation; schema_id is validate...

- **Execution domain:** `Analytics`.
- **Execution position:** `437` of `675`.
- **Cannot start before:** Step `P01-S0436` (`FR-ANLT-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `catalogs.py` — Static Catalogs and Compatibility`` (line 528); matrix row `782`.
- **Source responsibility:** The system shall classify `contract_version` and reject missing, unsupported, or unsafe future compatibility versions before calculation; `schema_id` is validated separately and never parsed as a version.
- **Source class / function / method:** `validate_contract_version(contract: str, version: str) -> str`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: version cannot be safely consumed
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_catalogs_validate_contract_version()`<br>**Unit:** `tests/analytics/unit/test_catalogs.py::test_validate_contract_version_rejects_future()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0438` [ ] `FR-ANLT-022` - build a catalog-backed warning with deterministic ordering and bounded redacted detail.

- **Execution domain:** `Analytics`.
- **Execution position:** `438` of `675`.
- **Cannot start before:** Step `P01-S0437` (`FR-ANLT-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `evidence.py` — Evidence Construction and Output Safety`` (line 534); matrix row `783`.
- **Source responsibility:** The system shall build a catalog-backed warning with deterministic ordering and bounded redacted detail.
- **Source class / function / method:** `build_warning(code: str, *, section: str, source_context: str, detail: Mapping[str, object]) -> AnalyticsWarning`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: code/detail is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_evidence_build_warning()`<br>**Unit:** `tests/analytics/unit/test_evidence.py::test_build_warning_bounds_detail()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-022`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0439` [ ] `FR-ANLT-023` - build a catalog-backed quality flag that separates evidence from final governance decisions.

- **Execution domain:** `Analytics`.
- **Execution position:** `439` of `675`.
- **Cannot start before:** Step `P01-S0438` (`FR-ANLT-022`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `evidence.py` — Evidence Construction and Output Safety`` (line 535); matrix row `784`.
- **Source responsibility:** The system shall build a catalog-backed quality flag that separates evidence from final governance decisions.
- **Source class / function / method:** `build_quality_flag(code: str, *, section: str, source_context: str, detail: Mapping[str, object]) -> QualityFlag`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: code/detail is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_evidence_build_quality_flag()`<br>**Unit:** `tests/analytics/unit/test_evidence.py::test_quality_flag_does_not_embed_decision()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0440` [ ] `FR-ANLT-025` - convert supported contracts, Decimal, datetime, pandas, and NumPy values to finite JSON-safe values and reject unsupported/non-finite values.

- **Execution domain:** `Analytics`.
- **Execution position:** `440` of `675`.
- **Cannot start before:** Step `P01-S0439` (`FR-ANLT-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.1 `contracts/` — Schemas, Catalogs, and Evidence Safety > `evidence.py` — Evidence Construction and Output Safety`` (line 537); matrix row `785`.
- **Source responsibility:** The system shall convert supported contracts, Decimal, datetime, pandas, and NumPy values to finite JSON-safe values and reject unsupported/non-finite values.
- **Source class / function / method:** `to_json_safe(value: object) -> object`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: value is non-finite or unserializable
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_contracts.py::test_usage_evidence_json_safe()`<br>**Unit:** `tests/analytics/unit/test_evidence.py::test_to_json_safe_rejects_infinity()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0441` [ ] `P-ANLT-002` - adapters feature/component (provisional)

- **Execution domain:** `Analytics`.
- **Execution position:** `441` of `675`.
- **Cannot start before:** Step `P01-S0440` (`FR-ANLT-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.2` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-ANLT-002`); matrix row `801`
- **Source file:** `builder.py`
- **Source responsibility:** Orchestrate canonical report sections, criticality, warnings, lineage, validation, and hashes.
- **Source key exports:** `build_performance_report`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-ANLT-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0442` [ ] `FR-ANLT-027` - deterministically map an approved versioned Trading or Simulation result to TradingResult, preserving IDs, phase, UTC window, currency, stra...

- **Execution domain:** `Analytics`.
- **Execution position:** `442` of `675`.
- **Cannot start before:** Step `P01-S0441` (`P-ANLT-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.2 `adapters/` — Approved Upstream Result Mapping > `results.py` — Canonical Result Adaptation`` (line 594); matrix row `786`.
- **Source responsibility:** The system shall deterministically map an approved versioned Trading or Simulation result to `TradingResult`, preserving IDs, phase, UTC window, currency, strategy, symbols, timeframe, trades, curves, quality metadata, and bounded source metadata without silent field loss.
- **Source class / function / method:** `adapt_trading_result(source: Mapping[str, object], *, source_contract: str) -> TradingResult`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: mapping/version/required field is missing, conflicting, oversized, or incompatible
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_adapters.py::test_usage_results_adapt_trading_result()`<br>**Unit:** `tests/analytics/unit/test_results_adapter.py::test_adapt_trading_result_preserves_required_lineage()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-027`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0443` [ ] `FR-ANLT-028` - calculate closed-trade outcomes, explicit-direction splits, cataloged R-multiples, merged-overlap market presence, streaks, and source conte...

- **Execution domain:** `Analytics`.
- **Execution position:** `443` of `675`.
- **Cannot start before:** Step `P01-S0442` (`FR-ANLT-027`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.3 `metrics/` — Internal Pure Analytical Evidence > Functional requirements`` (line 662); matrix row `787`.
- **Source responsibility:** The system shall calculate closed-trade outcomes, explicit-direction splits, cataloged R-multiples, merged-overlap market presence, streaks, and source context without treating open/placeholders as realized trades.
- **Source class / function / method:** `calculate_trade_evidence(result: TradingResult, *, source_context: str = "all") -> SectionEvidence`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: trade evidence or metric policy is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_metrics.py::test_usage_trades_calculate_trade_evidence()`<br>**Unit:** `tests/analytics/unit/test_trades.py::test_trade_evidence_filters_open_and_placeholder_trades()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0444` [ ] `FR-ANLT-033` - normalize strategy/benchmark timestamps to UTC, restrict the comparison window, resolve duplicates under approved policy, and return determi...

- **Execution domain:** `Analytics`.
- **Execution position:** `444` of `675`.
- **Cannot start before:** Step `P01-S0443` (`FR-ANLT-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.3 `metrics/` — Internal Pure Analytical Evidence > Functional requirements`` (line 667); matrix row `788`.
- **Source responsibility:** The system shall normalize strategy/benchmark timestamps to UTC, restrict the comparison window, resolve duplicates under approved policy, and return deterministic aligned observations.
- **Source class / function / method:** `align_benchmark_series(strategy: Sequence[Mapping[str, object]], benchmark: Sequence[Mapping[str, object]]) -> tuple[tuple[float, ...], tuple[float, ...]]`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: timestamps, duplicates, window, or values are invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_metrics.py::test_usage_benchmarks_align_series()`<br>**Unit:** `tests/analytics/unit/test_benchmarks.py::test_align_benchmark_series_uses_timestamp_intersection()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-033`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0445` [ ] `FR-ANLT-034` - calculate approved benchmark-relative evidence only after alignment and currency checks; non-overlap or zero variance is explicit skipped/un...

- **Execution domain:** `Analytics`.
- **Execution position:** `445` of `675`.
- **Cannot start before:** Step `P01-S0444` (`FR-ANLT-033`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.3 `metrics/` — Internal Pure Analytical Evidence > Functional requirements`` (line 668); matrix row `789`.
- **Source responsibility:** The system shall calculate approved benchmark-relative evidence only after alignment and currency checks; non-overlap or zero variance is explicit skipped/undefined evidence.
- **Source class / function / method:** `calculate_benchmark_evidence(result: TradingResult) -> SectionEvidence`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: required alignment/currency evidence is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_metrics.py::test_usage_benchmarks_calculate_evidence()`<br>**Unit:** `tests/analytics/unit/test_benchmarks.py::test_benchmark_zero_variance_is_undefined()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-034`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0446` [ ] `FR-ANLT-036` - compute real, bounded, seeded bootstrap, permutation, multiple-comparison, and sample diagnostics reproducibly and shall not return fixed pl...

- **Execution domain:** `Analytics`.
- **Execution position:** `446` of `675`.
- **Cannot start before:** Step `P01-S0445` (`FR-ANLT-034`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.3 `metrics/` — Internal Pure Analytical Evidence > Functional requirements`` (line 670); matrix row `790`.
- **Source responsibility:** The system shall compute real, bounded, seeded bootstrap, permutation, multiple-comparison, and sample diagnostics reproducibly and shall not return fixed placeholder evidence.
- **Source class / function / method:** `run_statistical_validation(values: Sequence[float], *, seed: int, bootstrap_iterations: int, permutation_iterations: int, confidence: float, alpha: float) -> SectionEvidence`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: seed/config/sample/limit is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_metrics.py::test_usage_statistics_run_validation()`<br>**Unit:** `tests/analytics/unit/test_statistics.py::test_statistical_validation_is_seed_reproducible()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-036`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0447` [ ] `FR-ANLT-038` - execute approved metric groups in deterministic order and preserve all/long/short/benchmark/cost/statistical source context without exportin...

- **Execution domain:** `Analytics`.
- **Execution position:** `447` of `675`.
- **Cannot start before:** Step `P01-S0446` (`FR-ANLT-036`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.3 `metrics/` — Internal Pure Analytical Evidence > Functional requirements`` (line 672); matrix row `791`.
- **Source responsibility:** The system shall execute approved metric groups in deterministic order and preserve all/long/short/benchmark/cost/statistical source context without exporting individual kernels at package root.
- **Source class / function / method:** `calculate_grouped_evidence(result: TradingResult) -> tuple[SectionEvidence, ...]`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: required group fails or catalog is incomplete
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_metrics.py::test_usage_groups_calculate_grouped_evidence()`<br>**Unit:** `tests/analytics/unit/test_groups.py::test_grouped_evidence_order_is_deterministic()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-038`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0448` [ ] `P-ANLT-004` - reports feature/component (provisional)

- **Execution domain:** `Analytics`.
- **Execution position:** `448` of `675`.
- **Cannot start before:** Step `P01-S0447` (`FR-ANLT-038`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.4` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-ANLT-004`); matrix row `802`
- **Source setting / limit:** Decimal precision standard
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-ANLT-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0449` [ ] `FR-ANLT-039` - compute deterministic SHA-256 input, config, ledger, equity, optional benchmark, and report hashes from canonical JSON while excluding docum...

- **Execution domain:** `Analytics`.
- **Execution position:** `449` of `675`.
- **Cannot start before:** Step `P01-S0448` (`P-ANLT-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.4 `reports/` — Canonical Reporting > Functional requirements`` (line 739); matrix row `792`.
- **Source responsibility:** The system shall compute deterministic SHA-256 input, config, ledger, equity, optional benchmark, and report hashes from canonical JSON while excluding documented nondeterministic fields.
- **Source class / function / method:** `compute_reproducibility_hashes(result: TradingResult, report: PerformanceReport | None = None) -> ReproducibilityHashes`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: required evidence cannot be serialized/hashed
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_reports.py::test_usage_hashes_compute_reproducibility_hashes()`<br>**Unit:** `tests/analytics/unit/test_hashes.py::test_hashes_change_only_for_material_input()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-039`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0450` [ ] `FR-ANLT-040` - serialize a validated report as canonical JSON or one minimal approved human-readable representation without file writes or placeholder form...

- **Execution domain:** `Analytics`.
- **Execution position:** `450` of `675`.
- **Cannot start before:** Step `P01-S0449` (`FR-ANLT-039`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.4 `reports/` — Canonical Reporting > Functional requirements`` (line 740); matrix row `793`.
- **Source responsibility:** The system shall serialize a validated report as canonical JSON or one minimal approved human-readable representation without file writes or placeholder formatters.
- **Source class / function / method:** `serialize_report(report: PerformanceReport, *, format_name: str) -> str`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: format is unsupported or report is unsafe
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_reports.py::test_usage_serialization_serialize_report()`<br>**Unit:** `tests/analytics/unit/test_serialization.py::test_serialize_report_json_is_canonical()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-040`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0451` [ ] `FR-ANLT-041` - aggregate actual compatible component evidence only after schema, base-currency, and caller-supplied FX validation; missing conversion block...

- **Execution domain:** `Analytics`.
- **Execution position:** `451` of `675`.
- **Cannot start before:** Step `P01-S0450` (`FR-ANLT-040`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.4 `reports/` — Canonical Reporting > Functional requirements`` (line 741); matrix row `794`.
- **Source responsibility:** The system shall aggregate actual compatible component evidence only after schema, base-currency, and caller-supplied FX validation; missing conversion blocks affected aggregation.
- **Source class / function / method:** `build_portfolio_performance_report(reports: Sequence[PerformanceReport], *, base_currency: str, fx_evidence: Mapping[str, object] | None) -> PortfolioPerformanceReport`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: schema/currency/FX/component/limit is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_reports.py::test_usage_portfolio_build_report()`<br>**Unit:** `tests/analytics/unit/test_portfolio.py::test_portfolio_builder_never_sums_mixed_currency_raw_pnl()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-041`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0452` [ ] `FR-ANLT-042` - compare schema- and pairing-compatible reports using actual common cataloged metrics, preserving omissions and caveats without mutating eith...

- **Execution domain:** `Analytics`.
- **Execution position:** `452` of `675`.
- **Cannot start before:** Step `P01-S0451` (`FR-ANLT-041`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.4 `reports/` — Canonical Reporting > Functional requirements`` (line 742); matrix row `795`.
- **Source responsibility:** The system shall compare schema- and pairing-compatible reports using actual common cataloged metrics, preserving omissions and caveats without mutating either report.
- **Source class / function / method:** `compare_performance_reports(reference: PerformanceReport, candidate: PerformanceReport) -> SectionEvidence`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: schemas/pairing are incompatible
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_reports.py::test_usage_comparison_compare_reports()`<br>**Unit:** `tests/analytics/unit/test_comparison.py::test_comparison_calculates_real_deltas()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-042`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0453` [ ] `FR-ANLT-043` - build PerformanceReport v1 from approved source evidence, required and optional cataloged sections, deterministic warnings/flags, lineage, p...

- **Execution domain:** `Analytics`.
- **Execution position:** `453` of `675`.
- **Cannot start before:** Step `P01-S0452` (`FR-ANLT-042`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.4 `reports/` — Canonical Reporting > Functional requirements`` (line 743); matrix row `796`.
- **Source responsibility:** The system shall build `PerformanceReport v1` from approved source evidence, required and optional cataloged sections, deterministic warnings/flags, lineage, precision metadata, finite validation, and hashes.
- **Source class / function / method:** `build_performance_report(source: Mapping[str, object], *, source_contract: str, request_id: str, diagnostic_partial_mode: bool = False) -> PerformanceReport`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: request/source/required section/output/limit is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_reports.py::test_usage_builder_build_performance_report()`<br>**Unit:** `tests/analytics/unit/test_builder.py::test_builder_fails_closed_on_required_section()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-043`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0454` [ ] `FR-ANLT-044` - evaluate canonical report facts under owner-approved thresholds, surface sample/drawdown/overfit/profitability caveats, and return non-bindi...

- **Execution domain:** `Analytics`.
- **Execution position:** `454` of `675`.
- **Cannot start before:** Step `P01-S0453` (`FR-ANLT-043`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.5 `scorecards/` — Non-Binding Strategy Quality > `quality.py` — Strategy Quality Evidence`` (line 799); matrix row `797`.
- **Source responsibility:** The system shall evaluate canonical report facts under owner-approved thresholds, surface sample/drawdown/overfit/profitability caveats, and return non-binding evidence without approval, promotion, live-readiness, compliance, risk, or allocation authority.
- **Source class / function / method:** `evaluate_strategy_quality(report: PerformanceReport) -> StrategyQualityEvidence`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: report/thresholds/required score inputs are invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_scorecards.py::test_usage_quality_evaluate_strategy_quality()`<br>**Unit:** `tests/analytics/unit/test_quality.py::test_quality_output_forbids_governance_claims()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-044`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0455` [ ] `P-ANLT-006` - dashboards feature/component (provisional)

- **Execution domain:** `Analytics`.
- **Execution position:** `455` of `675`.
- **Cannot start before:** Step `P01-S0454` (`FR-ANLT-044`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.6` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-ANLT-006`); matrix row `803`
- **Source setting / limit:** UTC-first time policy
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-ANLT-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0456` [ ] `FR-ANLT-045` - deterministically bound a series without exceeding the approved limit, preserving endpoints and approved extrema/trough/high/warning points ...

- **Execution domain:** `Analytics`.
- **Execution position:** `456` of `675`.
- **Cannot start before:** Step `P01-S0455` (`P-ANLT-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.6 `dashboards/` — Bounded Report Projection > Functional requirements`` (line 845); matrix row `798`.
- **Source responsibility:** The system shall deterministically bound a series without exceeding the approved limit, preserving endpoints and approved extrema/trough/high/warning points by defined priority and returning original/returned counts, method, reason, and truncation status.
- **Source class / function / method:** `truncate_series(points: Sequence[Mapping[str, object]], *, max_points: int) -> tuple[tuple[Mapping[str, object], ...], Mapping[str, object]]`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: shape/limit/preservation policy is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_dashboards.py::test_usage_truncation_truncate_series()`<br>**Unit:** `tests/analytics/unit/test_truncation.py::test_truncation_never_exceeds_limit()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-045`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0457` [ ] `FR-ANLT-046` - project only approved PerformanceReport sections into finite versioned summary, equity, drawdown, warning, and quality-flag payloads with un...

- **Execution domain:** `Analytics`.
- **Execution position:** `457` of `675`.
- **Cannot start before:** Step `P01-S0456` (`FR-ANLT-045`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 4. Module and Requirement Specifications > 4.6 `dashboards/` — Bounded Report Projection > Functional requirements`` (line 846); matrix row `799`.
- **Source responsibility:** The system shall project only approved `PerformanceReport` sections into finite versioned summary, equity, drawdown, warning, and quality-flag payloads with units/status metadata and no metric recomputation.
- **Source class / function / method:** `build_dashboard_payload(report: PerformanceReport) -> DashboardPayload`
- **Source side effects:** None
- **Source raises:** `AnalyticsValidationError`: report, class, finite output, or payload limit is invalid
- **Source usage / test:** **Usage:** `tests/analytics/usage/test_usage_dashboards.py::test_usage_payloads_build_dashboard_payload()`<br>**Unit:** `tests/analytics/unit/test_payloads.py::test_dashboard_payload_does_not_recompute_metrics()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-ANLT-046`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0458` [ ] `WF-ANLT-001` - Build Canonical Performance Report

- **Execution domain:** `Analytics`.
- **Execution position:** `458` of `675`.
- **Cannot start before:** Step `P01-S0457` (`FR-ANLT-046`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 3. Workflows > Workflow registry`` (line 255); matrix row `804`.
- **Source scope:** Cross-domain
- **Source workflow:** Build canonical performance report
- **Source requirement sequence:** `FR-ANLT-027 → FR-ANLT-038 → FR-ANLT-043`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-ANLT-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0459` [ ] `WF-ANLT-005` - Build Dashboard Payload

- **Execution domain:** `Analytics`.
- **Execution position:** `459` of `675`.
- **Cannot start before:** Step `P01-S0458` (`WF-ANLT-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 3. Workflows > Workflow registry`` (line 259); matrix row `805`.
- **Source scope:** Cross-domain
- **Source workflow:** Build dashboard payload
- **Source requirement sequence:** `FR-ANLT-045 → FR-ANLT-046`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-ANLT-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0460` [ ] `WF-ANLT-006` - Adapt Upstream Result

- **Execution domain:** `Analytics`.
- **Execution position:** `460` of `675`.
- **Cannot start before:** Step `P01-S0459` (`WF-ANLT-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Analytics` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/analytics/README.md` - ``Analytics > 3. Workflows > Workflow registry`` (line 260); matrix row `806`.
- **Source scope:** Cross-domain
- **Source workflow:** Adapt upstream result
- **Source requirement sequence:** `FR-ANLT-021 → FR-ANLT-027`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-ANLT-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 11 - Optimization

Implement `Optimization` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0461` [ ] `P-OPT-001` - parameters feature/component (provisional)

- **Execution domain:** `Optimization`.
- **Execution position:** `461` of `675`.
- **Cannot start before:** Step `P01-S0460` (`WF-ANLT-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-OPT-001`); matrix row `877`
- **Source public operation:** `detect_overfit_parameters`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-OPT-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0462` [ ] `FR-OPT-001` - model one float, integer, categorical, boolean, fixed, or conditional parameter with validated bounds, step/options, and activation condition.

- **Execution domain:** `Optimization`.
- **Execution position:** `462` of `675`.
- **Cannot start before:** Step `P01-S0461` (`P-OPT-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.1 `parameters/` — Parameter Space and Provenance > `contracts.py` — Parameter Contracts`` (line 450); matrix row `838`.
- **Source responsibility:** The system shall model one float, integer, categorical, boolean, fixed, or conditional parameter with validated bounds, step/options, and activation condition.
- **Source class / function / method:** `ParameterRange(name: str, kind: str, *, minimum: Decimal | None = None, maximum: Decimal | None = None, step: Decimal | None = None, choices: tuple[object, ...] = (), fixed_value: object | None = None, active_when: str | None = None) -> ParameterRange`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: definition is incomplete, contradictory, non-finite, or has invalid bounds/step/options
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_parameters.py::test_usage_contracts_parameter_range()`<br>**Unit:** `tests/optimization/unit/test_parameter_contracts.py::test_parameter_range_rejects_invalid_bounds()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0463` [ ] `FR-OPT-002` - model a uniquely named collection of parameter ranges and constraints without accepting an empty or duplicate definition.

- **Execution domain:** `Optimization`.
- **Execution position:** `463` of `675`.
- **Cannot start before:** Step `P01-S0462` (`FR-OPT-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.1 `parameters/` — Parameter Space and Provenance > `contracts.py` — Parameter Contracts`` (line 451); matrix row `839`.
- **Source responsibility:** The system shall model a uniquely named collection of parameter ranges and constraints without accepting an empty or duplicate definition.
- **Source class / function / method:** `ParameterSpace(parameters: tuple[ParameterRange, ...], constraints: tuple[str, ...] = ()) -> ParameterSpace`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: parameters are empty, duplicate, or structurally invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_parameters.py::test_usage_contracts_parameter_space()`<br>**Unit:** `tests/optimization/unit/test_parameter_contracts.py::test_parameter_space_rejects_duplicate_names()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0464` [ ] `FR-OPT-003` - validate parameter types, conditional cycles, constraints, expansion bounds, and configured limits before candidate generation.

- **Execution domain:** `Optimization`.
- **Execution position:** `464` of `675`.
- **Cannot start before:** Step `P01-S0463` (`FR-OPT-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.1 `parameters/` — Parameter Space and Provenance > `constraints.py` — Validation and Constraints`` (line 457); matrix row `840`.
- **Source responsibility:** The system shall validate parameter types, conditional cycles, constraints, expansion bounds, and configured limits before candidate generation.
- **Source class / function / method:** `validate_parameter_space(space: ParameterSpace, *, max_expansion: int, max_constraints: int) -> None`
- **Source side effects:** None
- **Source raises:** `ValueError`: space is invalid, cyclic, unsafe, unbounded, or exceeds a configured limit
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_parameters.py::test_usage_constraints_validate_parameter_space()`<br>**Unit:** `tests/optimization/unit/test_constraints.py::test_validate_parameter_space_fails_closed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0465` [ ] `FR-OPT-004` - evaluate only allowlisted expression nodes and names, returning false for a valid violated constraint and blocking unsafe expressions.

- **Execution domain:** `Optimization`.
- **Execution position:** `465` of `675`.
- **Cannot start before:** Step `P01-S0464` (`FR-OPT-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.1 `parameters/` — Parameter Space and Provenance > `constraints.py` — Validation and Constraints`` (line 458); matrix row `841`.
- **Source responsibility:** The system shall evaluate only allowlisted expression nodes and names, returning false for a valid violated constraint and blocking unsafe expressions.
- **Source class / function / method:** `evaluate_constraints(parameters: Mapping[str, object], constraints: Sequence[str]) -> bool`
- **Source side effects:** None
- **Source raises:** `ValueError`: expression contains unsafe syntax, unknown names, calls, attributes, or invalid operations
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_parameters.py::test_usage_constraints_evaluate_constraints()`<br>**Unit:** `tests/optimization/unit/test_constraints.py::test_evaluate_constraints_blocks_unsafe_ast()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0466` [ ] `FR-OPT-005` - exclude inactive conditional parameters from execution while retaining the original definition for metadata and audit evidence.

- **Execution domain:** `Optimization`.
- **Execution position:** `466` of `675`.
- **Cannot start before:** Step `P01-S0465` (`FR-OPT-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.1 `parameters/` — Parameter Space and Provenance > `constraints.py` — Validation and Constraints`` (line 459); matrix row `842`.
- **Source responsibility:** The system shall exclude inactive conditional parameters from execution while retaining the original definition for metadata and audit evidence.
- **Source class / function / method:** `get_executable_parameters(parameters: Mapping[str, object], space: ParameterSpace) -> dict[str, object]`
- **Source side effects:** None
- **Source raises:** `ValueError`: a condition is cyclic, ambiguous, or references an unknown parameter
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_parameters.py::test_usage_constraints_get_executable_parameters()`<br>**Unit:** `tests/optimization/unit/test_constraints.py::test_get_executable_parameters_excludes_inactive_values()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0467` [ ] `FR-OPT-006` - compute an order-invariant SHA-256 parameter-space hash from canonical definitions and constraints using normalized decimals.

- **Execution domain:** `Optimization`.
- **Execution position:** `467` of `675`.
- **Cannot start before:** Step `P01-S0466` (`FR-OPT-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.1 `parameters/` — Parameter Space and Provenance > `hashing.py` — Provenance Hashes`` (line 465); matrix row `843`.
- **Source responsibility:** The system shall compute an order-invariant SHA-256 parameter-space hash from canonical definitions and constraints using normalized decimals.
- **Source class / function / method:** `parameter_space_hash(space: ParameterSpace, *, decimal_places: int = 8) -> str`
- **Source side effects:** None
- **Source raises:** `ValueError`: space or precision cannot be canonically represented
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_parameters.py::test_usage_hashing_parameter_space_hash()`<br>**Unit:** `tests/optimization/unit/test_hashing.py::test_parameter_space_hash_is_order_invariant()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0468` [ ] `FR-OPT-007` - compute the candidate source-of-truth hash from strategy/data/cost/realism/objective/engine/module/space provenance and executable parameter...

- **Execution domain:** `Optimization`.
- **Execution position:** `468` of `675`.
- **Cannot start before:** Step `P01-S0467` (`FR-OPT-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.1 `parameters/` — Parameter Space and Provenance > `hashing.py` — Provenance Hashes`` (line 466); matrix row `844`.
- **Source responsibility:** The system shall compute the candidate source-of-truth hash from strategy/data/cost/realism/objective/engine/module/space provenance and executable parameters only.
- **Source class / function / method:** `candidate_hash(*, strategy_hash: str, data_hash: str, cost_model_hash: str, realism_hash: str, objective_hash: str, engine_type: str, engine_version: str, module_version: str, space_hash: str, executable_parameters: Mapping[str, object], decimal_places: int = 8) -> str`
- **Source side effects:** None
- **Source raises:** `ValueError`: required provenance is blank or data is not canonicalizable
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_parameters.py::test_usage_hashing_candidate_hash()`<br>**Unit:** `tests/optimization/unit/test_hashing.py::test_candidate_hash_excludes_inactive_parameters()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0469` [ ] `P-OPT-007` - evidence feature/component (provisional)

- **Execution domain:** `Optimization`.
- **Execution position:** `469` of `675`.
- **Cannot start before:** Step `P01-S0468` (`FR-OPT-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.2` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-OPT-007`); matrix row `880`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-OPT-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0470` [ ] `FR-OPT-010` - calculate the selected enabled core objective from validated outcomes and return explicit unavailable evidence when inputs are insufficient.

- **Execution domain:** `Optimization`.
- **Execution position:** `470` of `675`.
- **Cannot start before:** Step `P01-S0469` (`P-OPT-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.2 `scoring/` — Objectives, Ranking, and Overfit Evidence > `metrics.py` — Objectives and Trial Evidence`` (line 516); matrix row `845`.
- **Source responsibility:** The system shall calculate the selected enabled core objective from validated outcomes and return explicit unavailable evidence when inputs are insufficient.
- **Source class / function / method:** `calculate_candidate_score(outcomes: Sequence[Mapping[str, object]], *, initial_balance: Decimal, objective: ObjectiveName, enabled_objectives: frozenset[ObjectiveName]) -> CandidateScore`
- **Source side effects:** None
- **Source raises:** `ValueError`: balance, outcome, timestamp, numeric value, or objective is invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_scoring.py::test_usage_metrics_calculate_candidate_score()`<br>**Unit:** `tests/optimization/unit/test_metrics.py::test_calculate_candidate_score_rejects_unknown_objective()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0471` [ ] `FR-OPT-011` - calculate Deflated Sharpe evidence only from validated Sharpe, sample moments, sample count, and nominal-trial evidence.

- **Execution domain:** `Optimization`.
- **Execution position:** `471` of `675`.
- **Cannot start before:** Step `P01-S0470` (`FR-OPT-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.2 `scoring/` — Objectives, Ranking, and Overfit Evidence > `metrics.py` — Objectives and Trial Evidence`` (line 517); matrix row `846`.
- **Source responsibility:** The system shall calculate Deflated Sharpe evidence only from validated Sharpe, sample moments, sample count, and nominal-trial evidence.
- **Source class / function / method:** `calculate_deflated_sharpe(*, sharpe: float, variance: float, skewness: float, kurtosis: float, sample_count: int, nominal_trials: int) -> float | None`
- **Source side effects:** None
- **Source raises:** `ValueError`: input is non-finite, outside its valid domain, or contradictory
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_scoring.py::test_usage_metrics_calculate_deflated_sharpe()`<br>**Unit:** `tests/optimization/unit/test_metrics.py::test_calculate_deflated_sharpe_handles_insufficient_data()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0472` [ ] `FR-OPT-012` - count unique executable candidate hashes after constraint rejection and deduplication and label the count nominal rather than independent.

- **Execution domain:** `Optimization`.
- **Execution position:** `472` of `675`.
- **Cannot start before:** Step `P01-S0471` (`FR-OPT-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.2 `scoring/` — Objectives, Ranking, and Overfit Evidence > `metrics.py` — Objectives and Trial Evidence`` (line 518); matrix row `847`.
- **Source responsibility:** The system shall count unique executable candidate hashes after constraint rejection and deduplication and label the count nominal rather than independent.
- **Source class / function / method:** `count_nominal_trials(candidate_hashes: Sequence[str]) -> int`
- **Source side effects:** None
- **Source raises:** `ValueError`: a candidate hash is blank or malformed
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_scoring.py::test_usage_metrics_count_nominal_trials()`<br>**Unit:** `tests/optimization/unit/test_metrics.py::test_count_nominal_trials_deduplicates_hashes()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0473` [ ] `FR-OPT-013` - rank candidates by score descending, present trade count descending, then candidate hash ascending without mutating input records.

- **Execution domain:** `Optimization`.
- **Execution position:** `473` of `675`.
- **Cannot start before:** Step `P01-S0472` (`FR-OPT-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.2 `scoring/` — Objectives, Ranking, and Overfit Evidence > `ranking.py` — Deterministic Selection`` (line 524); matrix row `848`.
- **Source responsibility:** The system shall rank candidates by score descending, present trade count descending, then candidate hash ascending without mutating input records.
- **Source class / function / method:** `rank_candidates(candidates: Sequence[CandidateScore]) -> tuple[CandidateScore, ...]`
- **Source side effects:** None
- **Source raises:** `ValueError`: a required candidate hash or available score is invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_scoring.py::test_usage_ranking_rank_candidates()`<br>**Unit:** `tests/optimization/unit/test_ranking.py::test_rank_candidates_uses_canonical_tie_breakers()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0474` [ ] `FR-OPT-015` - assess IS/OOS degradation, DSR availability, nominal-trial caveats, trade-count adequacy, and cost/MC evidence; PBO and topology gates are n...

- **Execution domain:** `Optimization`.
- **Execution position:** `474` of `675`.
- **Cannot start before:** Step `P01-S0473` (`FR-OPT-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.2 `scoring/` — Objectives, Ranking, and Overfit Evidence > `overfit.py` — Overfit Evidence`` (line 531); matrix row `849`.
- **Source responsibility:** The system shall assess IS/OOS degradation, DSR availability, nominal-trial caveats, trade-count adequacy, and cost/MC evidence; PBO and topology gates are not part of the design.
- **Source class / function / method:** `assess_overfit_evidence(*, in_sample: CandidateScore, out_of_sample: CandidateScore, nominal_trials: int, deflated_sharpe: float | None, minimum_trade_count: int, extra_evidence: Mapping[str, object] | None = None) -> dict[str, object]`
- **Source side effects:** None
- **Source raises:** `ValueError`: scores, threshold, or trial evidence is missing or contradictory
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_scoring.py::test_usage_overfit_assess_overfit_evidence()`<br>**Unit:** `tests/optimization/unit/test_overfit.py::test_assess_overfit_evidence_reports_insufficient_data()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0475` [ ] `P-OPT-003` - execution feature/component (provisional)

- **Execution domain:** `Optimization`.
- **Execution position:** `475` of `675`.
- **Cannot start before:** Step `P01-S0474` (`FR-OPT-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.3` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-OPT-003`); matrix row `878`
- **Source setting / limit:** `DATABASE_URL` / `DATA_DIR`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-OPT-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0476` [ ] `FR-OPT-016` - expose a domain exception carrying a deterministic OPT_* code and redacted details for internal execution failures.

- **Execution domain:** `Optimization`.
- **Execution position:** `476` of `675`.
- **Cannot start before:** Step `P01-S0475` (`P-OPT-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.3 `execution/` — Simulation Adapter Boundary > `contracts.py` — Execution Contracts`` (line 572); matrix row `850`.
- **Source responsibility:** The system shall expose a domain exception carrying a deterministic `OPT_*` code and redacted details for internal execution failures.
- **Source class / function / method:** `OptimizationExecutionError(message: str, *, code: str, details: Mapping[str, object] | None = None) -> OptimizationExecutionError`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_execution.py::test_usage_contracts_optimization_execution_error()`<br>**Unit:** `tests/optimization/unit/test_execution_contracts.py::test_execution_error_requires_opt_code()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0477` [ ] `FR-OPT-017` - define a versioned protocol implemented by Simulation and accepting only an optimization execution request.

- **Execution domain:** `Optimization`.
- **Execution position:** `477` of `675`.
- **Cannot start before:** Step `P01-S0476` (`FR-OPT-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.3 `execution/` — Simulation Adapter Boundary > `contracts.py` — Execution Contracts`` (line 573); matrix row `851`.
- **Source responsibility:** The system shall define a versioned protocol implemented by Simulation and accepting only an optimization execution request.
- **Source class / function / method:** `BacktestExecutionAdapter.execute(request: BacktestExecutionRequest) -> EngineOptimizationResult`
- **Source side effects:** External API call
- **Source raises:** `OptimizationExecutionError`: Simulation cannot execute or convert the request
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_execution.py::test_usage_contracts_backtest_execution_adapter()`<br>**Unit:** `tests/optimization/unit/test_execution_contracts.py::test_adapter_protocol_contract()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0478` [ ] `FR-OPT-018` - model one executable candidate with approved strategy reference, MarketDataset reference/provenance, executable parameters, seed, costs, rea...

- **Execution domain:** `Optimization`.
- **Execution position:** `478` of `675`.
- **Cannot start before:** Step `P01-S0477` (`FR-OPT-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.3 `execution/` — Simulation Adapter Boundary > `contracts.py` — Execution Contracts`` (line 574); matrix row `852`.
- **Source responsibility:** The system shall model one executable candidate with approved strategy reference, `MarketDataset` reference/provenance, executable parameters, seed, costs, realism, engine identity, and trace context.
- **Source class / function / method:** `BacktestExecutionRequest(strategy_ref: str, market_data_ref: str, executable_parameters: Mapping[str, object], candidate_hash: str, seed: int, cost_model_hash: str, realism_hash: str, engine_type: str, required_adapter_version: str, request_id: str | None = None) -> BacktestExecutionRequest`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: required execution or provenance data is missing/invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_execution.py::test_usage_contracts_backtest_execution_request()`<br>**Unit:** `tests/optimization/unit/test_execution_contracts.py::test_execution_request_rejects_missing_provenance()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0479` [ ] `FR-OPT-019` - expose an optimization-facing immutable result converted from SimulationResult without leaking simulator internals.

- **Execution domain:** `Optimization`.
- **Execution position:** `479` of `675`.
- **Cannot start before:** Step `P01-S0478` (`FR-OPT-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.3 `execution/` — Simulation Adapter Boundary > `contracts.py` — Execution Contracts`` (line 575); matrix row `853`.
- **Source responsibility:** The system shall expose an optimization-facing immutable result converted from `SimulationResult` without leaking simulator internals.
- **Source class / function / method:** `EngineOptimizationResult(candidate_hash: str, score_inputs: Mapping[str, object], trade_count: int, equity_points: tuple[Decimal, ...], processed_records: int, engine_version: str, realism_hash: str, warnings: tuple[str, ...] = ()) -> EngineOptimizationResult`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: result is incomplete, inconsistent, non-finite, or contains provider objects
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_execution.py::test_usage_contracts_engine_optimization_result()`<br>**Unit:** `tests/optimization/unit/test_execution_contracts.py::test_engine_result_rejects_inconsistent_candidate_hash()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0480` [ ] `FR-OPT-020` - validate adapter version, engine type, deterministic seed, cost/realism evidence, strategy compatibility, and required data before invoking ...

- **Execution domain:** `Optimization`.
- **Execution position:** `480` of `675`.
- **Cannot start before:** Step `P01-S0479` (`FR-OPT-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.3 `execution/` — Simulation Adapter Boundary > `adapter.py` — Candidate Execution`` (line 581); matrix row `854`.
- **Source responsibility:** The system shall validate adapter version, engine type, deterministic seed, cost/realism evidence, strategy compatibility, and required data before invoking Simulation once.
- **Source class / function / method:** `execute_candidate(request: BacktestExecutionRequest, adapter: BacktestExecutionAdapter, *, deterministic_only: bool = True) -> EngineOptimizationResult`
- **Source side effects:** External API call
- **Source raises:** `OptimizationExecutionError`: adapter mismatch, unsupported realism, invalid request, unavailable engine, symbol setup failure, or candidate execution failure
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_execution.py::test_usage_adapter_execute_candidate()`<br>**Unit:** `tests/optimization/unit/test_adapter.py::test_execute_candidate_fails_closed_on_version_mismatch()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0481` [ ] `P-OPT-004` - search feature/component (provisional)

- **Execution domain:** `Optimization`.
- **Execution position:** `481` of `675`.
- **Cannot start before:** Step `P01-S0480` (`FR-OPT-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-OPT-004`); matrix row `879`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-OPT-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0482` [ ] `FR-OPT-021` - support only grid and random search.

- **Execution domain:** `Optimization`.
- **Execution position:** `482` of `675`.
- **Cannot start before:** Step `P01-S0481` (`P-OPT-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.4 `search/` — Bounded Candidate Search > `contracts.py` — Search Contracts`` (line 626); matrix row `855`.
- **Source responsibility:** The system shall support only `grid` and `random` search.
- **Source class / function / method:** `SearchMethod: type[StrEnum]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_search.py::test_usage_contracts_search_method()`<br>**Unit:** `tests/optimization/unit/test_search_contracts.py::test_search_method_allows_only_grid_and_random()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0483` [ ] `FR-OPT-022` - model a bounded search with strategy/data provenance, parameter space, objective, method, seed, and approved resource caps.

- **Execution domain:** `Optimization`.
- **Execution position:** `483` of `675`.
- **Cannot start before:** Step `P01-S0482` (`FR-OPT-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.4 `search/` — Bounded Candidate Search > `contracts.py` — Search Contracts`` (line 627); matrix row `856`.
- **Source responsibility:** The system shall model a bounded search with strategy/data provenance, parameter space, objective, method, seed, and approved resource caps.
- **Source class / function / method:** `SearchRequest(strategy_ref: str, market_data_ref: str, parameter_space: ParameterSpace, objective: ObjectiveName, method: SearchMethod, seed: int | None, max_candidates: int, max_runtime_seconds: float, provenance: Mapping[str, str], request_id: str | None = None) -> SearchRequest`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: required field/cap/provenance is missing or invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_search.py::test_usage_contracts_search_request()`<br>**Unit:** `tests/optimization/unit/test_search_contracts.py::test_search_request_requires_seed_for_random()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-022`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0484` [ ] `FR-OPT-023` - represent one accepted, rejected, or failed candidate with executable parameters, hash, score/evidence, and structured reason.

- **Execution domain:** `Optimization`.
- **Execution position:** `484` of `675`.
- **Cannot start before:** Step `P01-S0483` (`FR-OPT-022`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.4 `search/` — Bounded Candidate Search > `contracts.py` — Search Contracts`` (line 628); matrix row `857`.
- **Source responsibility:** The system shall represent one accepted, rejected, or failed candidate with executable parameters, hash, score/evidence, and structured reason.
- **Source class / function / method:** `CandidateResult(candidate_hash: str, executable_parameters: Mapping[str, object], state: str, score: CandidateScore | None, reason_code: str | None = None, evidence: Mapping[str, object] | None = None) -> CandidateResult`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: state and score/reason fields are inconsistent
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_search.py::test_usage_contracts_candidate_result()`<br>**Unit:** `tests/optimization/unit/test_search_contracts.py::test_candidate_result_requires_failure_reason()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0485` [ ] `FR-OPT-024` - represent a completed bounded search with deterministic candidate order, best candidate, runtime, objective, method, provenance, and warnings.

- **Execution domain:** `Optimization`.
- **Execution position:** `485` of `675`.
- **Cannot start before:** Step `P01-S0484` (`FR-OPT-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.4 `search/` — Bounded Candidate Search > `contracts.py` — Search Contracts`` (line 629); matrix row `858`.
- **Source responsibility:** The system shall represent a completed bounded search with deterministic candidate order, best candidate, runtime, objective, method, provenance, and warnings.
- **Source class / function / method:** `SearchSummary(search_id: str, request_hash: str, method: SearchMethod, objective: ObjectiveName, candidates: tuple[CandidateResult, ...], best_candidate_hash: str | None, runtime_ms: float, warnings: tuple[str, ...]) -> SearchSummary`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: ordering, best candidate, runtime, or provenance is inconsistent
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_search.py::test_usage_contracts_search_summary()`<br>**Unit:** `tests/optimization/unit/test_search_contracts.py::test_search_summary_validates_best_candidate()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0486` [ ] `FR-OPT-025` - lazily yield valid grid candidates without materializing the full Cartesian product and shall stop before exceeding the configured cap.

- **Execution domain:** `Optimization`.
- **Execution position:** `486` of `675`.
- **Cannot start before:** Step `P01-S0485` (`FR-OPT-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.4 `search/` — Bounded Candidate Search > `grid.py`, `random.py`, and `sweep.py` — Search Behavior`` (line 635); matrix row `859`.
- **Source responsibility:** The system shall lazily yield valid grid candidates without materializing the full Cartesian product and shall stop before exceeding the configured cap.
- **Source class / function / method:** `iter_grid_candidates(request: SearchRequest) -> Iterator[dict[str, object]]`
- **Source side effects:** None
- **Source raises:** `ValueError`: method is not grid, space is invalid, yields zero candidates, or exceeds cap
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_search.py::test_usage_grid_iter_grid_candidates()`<br>**Unit:** `tests/optimization/unit/test_grid.py::test_iter_grid_candidates_is_lazy_and_bounded()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0487` [ ] `FR-OPT-026` - generate unique pseudo-random candidates deterministically from validated ranges and a required seed, without claiming Sobol or LHS behavior.

- **Execution domain:** `Optimization`.
- **Execution position:** `487` of `675`.
- **Cannot start before:** Step `P01-S0486` (`FR-OPT-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.4 `search/` — Bounded Candidate Search > `grid.py`, `random.py`, and `sweep.py` — Search Behavior`` (line 636); matrix row `860`.
- **Source responsibility:** The system shall generate unique pseudo-random candidates deterministically from validated ranges and a required seed, without claiming Sobol or LHS behavior.
- **Source class / function / method:** `sample_random_candidates(request: SearchRequest) -> tuple[dict[str, object], ...]`
- **Source side effects:** Local state mutation
- **Source raises:** `ValueError`: method/seed/distribution is invalid, uniqueness cannot satisfy cap, or no candidate is valid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_search.py::test_usage_random_sample_random_candidates()`<br>**Unit:** `tests/optimization/unit/test_random_search.py::test_sample_random_candidates_is_seeded()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0488` [ ] `FR-OPT-027` - evaluate each unique valid candidate through the injected adapter, retain structured failures, score successful results, and return determin...

- **Execution domain:** `Optimization`.
- **Execution position:** `488` of `675`.
- **Cannot start before:** Step `P01-S0487` (`FR-OPT-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.4 `search/` — Bounded Candidate Search > `grid.py`, `random.py`, and `sweep.py` — Search Behavior`` (line 637); matrix row `861`.
- **Source responsibility:** The system shall evaluate each unique valid candidate through the injected adapter, retain structured failures, score successful results, and return deterministic ranking within caps.
- **Source class / function / method:** `run_bounded_search(request: SearchRequest, adapter: BacktestExecutionAdapter, *, enabled_objectives: frozenset[ObjectiveName]) -> SearchSummary`
- **Source side effects:** External API call
- **Source raises:** `ValueError`: required policy/cap/objective is missing; `OptimizationExecutionError`: adapter workflow cannot proceed
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_search.py::test_usage_sweep_run_bounded_search()`<br>**Unit:** `tests/optimization/unit/test_sweep.py::test_run_bounded_search_preserves_failed_candidates()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-027`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0489` [ ] `FR-OPT-028` - return the first N candidates from an already deterministic summary without dataframe conversion or input mutation.

- **Execution domain:** `Optimization`.
- **Execution position:** `489` of `675`.
- **Cannot start before:** Step `P01-S0488` (`FR-OPT-027`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.4 `search/` — Bounded Candidate Search > `grid.py`, `random.py`, and `sweep.py` — Search Behavior`` (line 638); matrix row `862`.
- **Source responsibility:** The system shall return the first N candidates from an already deterministic summary without dataframe conversion or input mutation.
- **Source class / function / method:** `select_top_candidates(summary: SearchSummary, limit: int) -> tuple[CandidateResult, ...]`
- **Source side effects:** None
- **Source raises:** `ValueError`: limit is not positive
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_search.py::test_usage_sweep_select_top_candidates()`<br>**Unit:** `tests/optimization/unit/test_sweep.py::test_select_top_candidates_preserves_order()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0490` [ ] `FR-OPT-032` - represent fold results, selected parameters, train/OOS scores, degradation, pass rate, drift, retention, WFE, and leakage evidence.

- **Execution domain:** `Optimization`.
- **Execution position:** `490` of `675`.
- **Cannot start before:** Step `P01-S0489` (`FR-OPT-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.5 `validation/` — Time-Series Splits and Walk-Forward > `contracts.py` — Validation Contracts`` (line 684); matrix row `863`.
- **Source responsibility:** The system shall represent fold results, selected parameters, train/OOS scores, degradation, pass rate, drift, retention, WFE, and leakage evidence.
- **Source class / function / method:** `WalkForwardResult(folds: tuple[Mapping[str, object], ...], fold_pass_rate: float | None, parameter_drift_score: float | None, oos_retention_score: float | None, walk_forward_efficiency: float | None, status: str, warnings: tuple[str, ...]) -> WalkForwardResult`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: evidence is non-finite or inconsistent with fold results
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_validation.py::test_usage_contracts_walk_forward_result()`<br>**Unit:** `tests/optimization/unit/test_validation_contracts.py::test_walk_forward_result_rejects_invalid_rate()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-032`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0491` [ ] `FR-OPT-033` - construct deterministic folds in chronological order, enforce purge/embargo, and reject insufficient or invalid ranges.

- **Execution domain:** `Optimization`.
- **Execution position:** `491` of `675`.
- **Cannot start before:** Step `P01-S0490` (`FR-OPT-032`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.5 `validation/` — Time-Series Splits and Walk-Forward > `splits.py` and `walk_forward.py` — Validation Behavior`` (line 690); matrix row `864`.
- **Source responsibility:** The system shall construct deterministic folds in chronological order, enforce purge/embargo, and reject insufficient or invalid ranges.
- **Source class / function / method:** `build_time_series_splits(request: WalkForwardRequest) -> tuple[TimeSeriesSplit, ...]`
- **Source side effects:** None
- **Source raises:** `ValueError`: dates/windows are invalid, data is insufficient, folds overlap, or minimum fold count is not met
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_validation.py::test_usage_splits_build_time_series_splits()`<br>**Unit:** `tests/optimization/unit/test_splits.py::test_build_time_series_splits_enforces_trade_duration_embargo()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-033`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0492` [ ] `FR-OPT-038` - model explicit spread, slippage, commission, or skip-trade stress without hidden unit conversion.

- **Execution domain:** `Optimization`.
- **Execution position:** `492` of `675`.
- **Cannot start before:** Step `P01-S0491` (`FR-OPT-033`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.6 `robustness/` — Monte Carlo and Stress Analysis > `contracts.py` — Robustness Contracts`` (line 739); matrix row `865`.
- **Source responsibility:** The system shall model explicit spread, slippage, commission, or skip-trade stress without hidden unit conversion.
- **Source class / function / method:** `ExecutionStressRequest(kind: str, value: Decimal, seed: int | None = None) -> ExecutionStressRequest`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: kind/value/seed is invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_robustness.py::test_usage_contracts_execution_stress_request()`<br>**Unit:** `tests/optimization/unit/test_robustness_contracts.py::test_execution_stress_request_requires_seed_for_skip()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-038`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0493` [ ] `FR-OPT-039` - run the selected core Monte Carlo method with deterministic run/candidate/phase sub-seeds and within the approved cap.

- **Execution domain:** `Optimization`.
- **Execution position:** `493` of `675`.
- **Cannot start before:** Step `P01-S0492` (`FR-OPT-038`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.6 `robustness/` — Monte Carlo and Stress Analysis > `monte_carlo.py`, `stress.py`, and `assessment.py` — Robustness Behavior`` (line 745); matrix row `866`.
- **Source responsibility:** The system shall run the selected core Monte Carlo method with deterministic run/candidate/phase sub-seeds and within the approved cap.
- **Source class / function / method:** `run_monte_carlo(request: MonteCarloRequest, *, max_simulations: int) -> MonteCarloResult`
- **Source side effects:** Local state mutation
- **Source raises:** `ValueError`: request/cap/method is invalid or exceeded
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_robustness.py::test_usage_monte_carlo_run_monte_carlo()`<br>**Unit:** `tests/optimization/unit/test_monte_carlo.py::test_run_monte_carlo_repeats_with_same_seed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-039`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0494` [ ] `FR-OPT-040` - calculate the fraction of supplied drawdowns or equity paths that cross an explicit ruin threshold.

- **Execution domain:** `Optimization`.
- **Execution position:** `494` of `675`.
- **Cannot start before:** Step `P01-S0493` (`FR-OPT-039`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.6 `robustness/` — Monte Carlo and Stress Analysis > `monte_carlo.py`, `stress.py`, and `assessment.py` — Robustness Behavior`` (line 746); matrix row `867`.
- **Source responsibility:** The system shall calculate the fraction of supplied drawdowns or equity paths that cross an explicit ruin threshold.
- **Source class / function / method:** `calculate_probability_of_ruin(values: Sequence[Decimal], *, ruin_threshold: Decimal) -> float`
- **Source side effects:** None
- **Source raises:** `ValueError`: values are empty/non-finite or threshold is invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_robustness.py::test_usage_monte_carlo_calculate_probability_of_ruin()`<br>**Unit:** `tests/optimization/unit/test_monte_carlo.py::test_calculate_probability_of_ruin_known_fixture()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-040`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0495` [ ] `FR-OPT-042` - simulate compounding outcomes from validated win rate, reward/risk, risk per trade, trade count, path count, balance, and seed without claim...

- **Execution domain:** `Optimization`.
- **Execution position:** `495` of `675`.
- **Cannot start before:** Step `P01-S0494` (`FR-OPT-040`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.6 `robustness/` — Monte Carlo and Stress Analysis > `monte_carlo.py`, `stress.py`, and `assessment.py` — Robustness Behavior`` (line 748); matrix row `868`.
- **Source responsibility:** The system shall simulate compounding outcomes from validated win rate, reward/risk, risk per trade, trade count, path count, balance, and seed without claiming certainty.
- **Source class / function / method:** `run_parametric_simulation(*, win_rate: Decimal, reward_risk: Decimal, risk_per_trade: Decimal, trade_count: int, simulations: int, initial_balance: Decimal, seed: int, max_simulations: int) -> MonteCarloResult`
- **Source side effects:** Local state mutation
- **Source raises:** `ValueError`: probability/risk/count/balance/cap is invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_robustness.py::test_usage_monte_carlo_run_parametric_simulation()`<br>**Unit:** `tests/optimization/unit/test_monte_carlo.py::test_parametric_simulation_handles_all_win_and_all_loss()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-042`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0496` [ ] `FR-OPT-043` - return copied stressed outcomes for an explicit execution-cost or skip-trade assumption without mutating inputs.

- **Execution domain:** `Optimization`.
- **Execution position:** `496` of `675`.
- **Cannot start before:** Step `P01-S0495` (`FR-OPT-042`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.6 `robustness/` — Monte Carlo and Stress Analysis > `monte_carlo.py`, `stress.py`, and `assessment.py` — Robustness Behavior`` (line 749); matrix row `869`.
- **Source responsibility:** The system shall return copied stressed outcomes for an explicit execution-cost or skip-trade assumption without mutating inputs.
- **Source class / function / method:** `apply_execution_cost_stress(outcomes: Sequence[Mapping[str, object]], request: ExecutionStressRequest) -> tuple[dict[str, object], ...]`
- **Source side effects:** Local state mutation
- **Source raises:** `ValueError`: outcomes, units, stress value, or required seed is invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_robustness.py::test_usage_stress_apply_execution_cost_stress()`<br>**Unit:** `tests/optimization/unit/test_stress.py::test_apply_execution_cost_stress_does_not_mutate_input()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-043`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0497` [ ] `FR-OPT-044` - combine only supplied/applicable MC and stress checks into a robustness percentage, warnings, and evidence-availability summary.

- **Execution domain:** `Optimization`.
- **Execution position:** `497` of `675`.
- **Cannot start before:** Step `P01-S0496` (`FR-OPT-043`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.6 `robustness/` — Monte Carlo and Stress Analysis > `monte_carlo.py`, `stress.py`, and `assessment.py` — Robustness Behavior`` (line 750); matrix row `870`.
- **Source responsibility:** The system shall combine only supplied/applicable MC and stress checks into a robustness percentage, warnings, and evidence-availability summary.
- **Source class / function / method:** `assess_strategy_robustness(*, monte_carlo: MonteCarloResult | None, stress_checks: Sequence[Mapping[str, object]], additional_evidence: Mapping[str, object] | None = None) -> dict[str, object]`
- **Source side effects:** None
- **Source raises:** `ValueError`: check records are empty, malformed, contradictory, or non-finite
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_robustness.py::test_usage_assessment_assess_strategy_robustness()`<br>**Unit:** `tests/optimization/unit/test_assessment.py::test_assess_strategy_robustness_reports_missing_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-044`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0498` [ ] `FR-OPT-045` - use only ready_for_risk_review, validation_needed, research_only, rejected, or failed as synchronous final decisions; no background-job canc...

- **Execution domain:** `Optimization`.
- **Execution position:** `498` of `675`.
- **Cannot start before:** Step `P01-S0497` (`FR-OPT-044`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.7 `evidence/` — Versioned Results and Handoffs > `contracts.py` — Evidence Contracts`` (line 792); matrix row `871`.
- **Source responsibility:** The system shall use only `ready_for_risk_review`, `validation_needed`, `research_only`, `rejected`, or `failed` as synchronous final decisions; no background-job cancellation lifecycle exists.
- **Source class / function / method:** `FinalDecision: type[StrEnum]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_evidence.py::test_usage_contracts_final_decision()`<br>**Unit:** `tests/optimization/unit/test_evidence_contracts.py::test_final_decision_values_are_canonical()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-045`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0499` [ ] `FR-OPT-046` - model the supplied search, WFA, MC, robustness, warnings, audit references, chart data, and optional externally owned evidence needed for as...

- **Execution domain:** `Optimization`.
- **Execution position:** `499` of `675`.
- **Cannot start before:** Step `P01-S0498` (`FR-OPT-045`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.7 `evidence/` — Versioned Results and Handoffs > `contracts.py` — Evidence Contracts`` (line 793); matrix row `872`.
- **Source responsibility:** The system shall model the supplied search, WFA, MC, robustness, warnings, audit references, chart data, and optional externally owned evidence needed for assembly.
- **Source class / function / method:** `EvidenceAssemblyRequest(search: SearchSummary, walk_forward: WalkForwardResult | None = None, monte_carlo: MonteCarloResult | None = None, robustness: Mapping[str, object] | None = None, analytics_evidence: Mapping[str, object] | None = None, risk_evidence: Mapping[str, object] | None = None, chart_data: Mapping[str, object] | None = None, audit_references: tuple[str, ...] = ()) -> EvidenceAssemblyRequest`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: evidence is malformed, non-JSON-safe, or contradicts source results
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_evidence.py::test_usage_contracts_evidence_assembly_request()`<br>**Unit:** `tests/optimization/unit/test_evidence_contracts.py::test_evidence_request_rejects_non_json_data()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-046`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0500` [ ] `FR-OPT-047` - define advisory OptimizationResult v1 with separate compatibility/schema identity, search ID, reproducibility hash, ranked candidates, diagn...

- **Execution domain:** `Optimization`.
- **Execution position:** `500` of `675`.
- **Cannot start before:** Step `P01-S0499` (`FR-OPT-046`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.7 `evidence/` — Versioned Results and Handoffs > `contracts.py` — Evidence Contracts`` (line 794); matrix row `873`.
- **Source responsibility:** The system shall define advisory `OptimizationResult v1` with separate compatibility/schema identity, search ID, reproducibility hash, ranked candidates, diagnostics, warnings, chart-ready data, audit references, and final decision, without trade or Strategy-mutation authority.
- **Source class / function / method:** `OptimizationResult(contract_version: Literal["v1"], schema_id: Literal["optimization.result.v1"], search_id: str, reproducibility_hash: str, ranked_candidates: tuple[Mapping[str, object], ...], diagnostics: Mapping[str, object], warnings: tuple[str, ...], chart_data: Mapping[str, object], audit_references: tuple[str, ...], final_decision: FinalDecision) -> OptimizationResult`
- **Source side effects:** None
- **Source raises:** `pydantic.ValidationError`: owned contract is incomplete, non-canonical, or claims authority
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_evidence.py::test_usage_contracts_optimization_result()`<br>**Unit:** `tests/optimization/unit/test_evidence_contracts.py::test_optimization_result_is_advisory()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-047`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0501` [ ] `FR-OPT-048` - assemble versioned baseline evidence and reproducibility hash from supplied results without recomputing metrics or external decisions.

- **Execution domain:** `Optimization`.
- **Execution position:** `501` of `675`.
- **Cannot start before:** Step `P01-S0500` (`FR-OPT-047`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.7 `evidence/` — Versioned Results and Handoffs > `assemble.py` and `handoff.py` — Evidence Behavior`` (line 800); matrix row `874`.
- **Source responsibility:** The system shall assemble versioned baseline evidence and reproducibility hash from supplied results without recomputing metrics or external decisions.
- **Source class / function / method:** `build_optimization_evidence(request: EvidenceAssemblyRequest) -> OptimizationResult`
- **Source side effects:** None
- **Source raises:** `ValueError`: provenance/evidence is missing, inconsistent, or not canonicalizable
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_evidence.py::test_usage_assemble_build_optimization_evidence()`<br>**Unit:** `tests/optimization/unit/test_assemble.py::test_build_evidence_labels_missing_sections()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-048`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0502` [ ] `FR-OPT-049` - package chart-ready series and tables from OptimizationResult without rendering or recomputation.

- **Execution domain:** `Optimization`.
- **Execution position:** `502` of `675`.
- **Cannot start before:** Step `P01-S0501` (`FR-OPT-048`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.7 `evidence/` — Versioned Results and Handoffs > `assemble.py` and `handoff.py` — Evidence Behavior`` (line 801); matrix row `875`.
- **Source responsibility:** The system shall package chart-ready series and tables from `OptimizationResult` without rendering or recomputation.
- **Source class / function / method:** `build_report_package(result: OptimizationResult) -> dict[str, object]`
- **Source side effects:** None
- **Source raises:** `ValueError`: result version or chart data is invalid
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_evidence.py::test_usage_handoff_build_report_package()`<br>**Unit:** `tests/optimization/unit/test_handoff.py::test_report_package_uses_existing_evidence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-049`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0503` [ ] `FR-OPT-053` - atomically persist one canonical OptimizationResult v1 with its ranked-candidate evidence before reporting durable success.

- **Execution domain:** `Optimization`.
- **Execution position:** `503` of `675`.
- **Cannot start before:** Step `P01-S0502` (`FR-OPT-049`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 4. Module and Requirement Specifications > 4.8 `state/` — Optimization-Owned Durable State > Functional requirements`` (line 847); matrix row `876`.
- **Source responsibility:** The system shall atomically persist one canonical `OptimizationResult v1` with its ranked-candidate evidence before reporting durable success.
- **Source class / function / method:** `persist_optimization_result(result: OptimizationResult, store: OptimizationStateStore) -> OptimizationPersistenceReceipt`
- **Source side effects:** Persistence write
- **Source raises:** `OptimizationError`: schema mismatch, conflicting result, or store failure
- **Source usage / test:** **Usage:** `tests/optimization/usage/test_usage_state.py::test_usage_persist_result()`<br>**Unit:** `tests/optimization/unit/test_state_persistence.py::test_result_success_requires_atomic_receipt()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-OPT-053`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0504` [ ] `P-OPT-009` - public_api feature/component (provisional)

- **Execution domain:** `Optimization`.
- **Execution position:** `504` of `675`.
- **Cannot start before:** Step `P01-S0503` (`FR-OPT-053`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` module `4.9` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-OPT-009`); matrix row `881`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-OPT-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0505` [ ] `WF-OPT-001` - Package an Optimization or Robustness Request

- **Execution domain:** `Optimization`.
- **Execution position:** `505` of `675`.
- **Cannot start before:** Step `P01-S0504` (`P-OPT-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 3. Workflows > Workflow scope values`` (line 250); matrix row `882`.
- **Source scope:** Cross-domain
- **Source workflow:** Run an optimization or robustness request
- **Source requirement sequence:** `FR-OPT-021 → FR-OPT-024 → FR-OPT-044`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-OPT-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0506` [ ] `WF-OPT-002` - Execute a Bounded Parameter Sweep

- **Execution domain:** `Optimization`.
- **Execution position:** `506` of `675`.
- **Cannot start before:** Step `P01-S0505` (`WF-OPT-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Optimization` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/optimization/README.md` - ``Optimization > 3. Workflows > Workflow scope values`` (line 251); matrix row `883`.
- **Source scope:** Cross-domain
- **Source workflow:** Execute a bounded parameter sweep
- **Source requirement sequence:** `FR-OPT-003 → FR-OPT-025 → FR-OPT-026 → FR-OPT-020 → FR-OPT-027`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-OPT-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 12 - Research

Implement `Research` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0507` [ ] `P-RES-001` - contracts feature/component (provisional)

- **Execution domain:** `Research`.
- **Execution position:** `507` of `675`.
- **Cannot start before:** Step `P01-S0506` (`WF-OPT-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RES-001`); matrix row `972`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RES-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0508` [ ] `P-RES-002` - data feature/component (provisional)

- **Execution domain:** `Research`.
- **Execution position:** `508` of `675`.
- **Cannot start before:** Step `P01-S0507` (`P-RES-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` module `4.2` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RES-002`); matrix row `973`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RES-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0509` [ ] `P-RES-005` - metrics feature/component (provisional)

- **Execution domain:** `Research`.
- **Execution position:** `509` of `675`.
- **Cannot start before:** Step `P01-S0508` (`P-RES-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` module `4.5` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RES-005`); matrix row `974`
- **Source file:** `scorecard.py`
- **Source responsibility:** Build one deterministic advisory scorecard.
- **Source key exports:** `build_research_scorecard`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RES-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0510` [ ] `P-RES-011` - profiles feature/component (provisional)

- **Execution domain:** `Research`.
- **Execution position:** `510` of `675`.
- **Cannot start before:** Step `P01-S0509` (`P-RES-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` module `4.11` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-RES-011`); matrix row `975`
- **Source file:** `persistence.py`
- **Source responsibility:** Perform the single approved Research persistence side effect.
- **Source key exports:** `write_research_artifact`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-RES-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0511` [ ] `FR-RES-001` - define bounded row, duration, artifact-size, and advisory memory budgets without claiming unverified production performance.

- **Execution domain:** `Research`.
- **Execution position:** `511` of `675`.
- **Cannot start before:** Step `P01-S0510` (`P-RES-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 563); matrix row `920`.
- **Source responsibility:** The system shall define bounded row, duration, artifact-size, and advisory memory budgets without claiming unverified production performance.
- **Source class / function / method:** `ResearchResourceLimits(max_rows: int, max_duration_seconds: float, max_artifact_bytes: int, memory_budget_mb: int | None = None)`
- **Source side effects:** None
- **Source raises:** invalid/non-positive limit; invalid or non-positive approved limits
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_resource_limits`<br>**Unit:** `test_configurations.py::test_resource_limits_reject_non_positive`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0512` [ ] `FR-RES-002` - require explicit timestamp, duplicate, missing-bar, non-trading-period, and spread-cleaning policies and shall never silently fill or drop d...

- **Execution domain:** `Research`.
- **Execution position:** `512` of `675`.
- **Cannot start before:** Step `P01-S0511` (`FR-RES-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 564); matrix row `921`.
- **Source responsibility:** The system shall require explicit timestamp, duplicate, missing-bar, non-trading-period, and spread-cleaning policies and shall never silently fill or drop data.
- **Source class / function / method:** `CleaningConfig(timezone: str, duplicate_strategy: str, missing_bar_strategy: str, non_trading_period_strategy: str, spread_strategy: str)`
- **Source side effects:** None
- **Source raises:** unsupported or absent policy
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_cleaning`<br>**Unit:** `test_configurations.py::test_cleaning_requires_explicit_data_actions`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0513` [ ] `FR-RES-003` - define explicit pip, geometry, return-label, and calendar enrichment selections; canonical session tagging remains owned by seasonality/.

- **Execution domain:** `Research`.
- **Execution position:** `513` of `675`.
- **Cannot start before:** Step `P01-S0512` (`FR-RES-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 565); matrix row `922`.
- **Source responsibility:** The system shall define explicit pip, geometry, return-label, and calendar enrichment selections; canonical session tagging remains owned by `seasonality/`.
- **Source class / function / method:** `EnrichmentConfig(symbol: str, include_geometry: bool, include_returns: bool, include_forward_labels: bool, include_calendar: bool)`
- **Source side effects:** None
- **Source raises:** malformed symbol or incompatible selection
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_enrichment`<br>**Unit:** `test_configurations.py::test_enrichment_rejects_incompatible_fields`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0514` [ ] `FR-RES-004` - define feature windows, declared forward columns, warm-up/NaN policy, and non-mutation behavior.

- **Execution domain:** `Research`.
- **Execution position:** `514` of `675`.
- **Cannot start before:** Step `P01-S0513` (`FR-RES-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 566); matrix row `923`.
- **Source responsibility:** The system shall define feature windows, declared forward columns, warm-up/NaN policy, and non-mutation behavior.
- **Source class / function / method:** `FeatureConfig(windows: Mapping[str, int], forward_horizons: tuple[int,...], allowed_forward_columns: tuple[str,...], nan_policy: str)`
- **Source side effects:** None
- **Source raises:** invalid window/horizon/policy
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_features`<br>**Unit:** `test_configurations.py::test_feature_config_rejects_invalid_window`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0515` [ ] `FR-RES-005` - define bootstrap, permutation, null, correction, effective-seed, and bounded-iteration settings in one statistical contract.

- **Execution domain:** `Research`.
- **Execution position:** `515` of `675`.
- **Cannot start before:** Step `P01-S0514` (`FR-RES-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 567); matrix row `924`.
- **Source responsibility:** The system shall define bootstrap, permutation, null, correction, effective-seed, and bounded-iteration settings in one statistical contract.
- **Source class / function / method:** `StatisticalConfig(seed: int, bootstrap_samples: int, permutation_samples: int, block_size: int, null_samples: int, correction: str | None)`
- **Source side effects:** None
- **Source raises:** invalid seed, count, block, correction, or resource request
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_statistics`<br>**Unit:** `test_configurations.py::test_statistics_rejects_invalid_block_size`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0516` [ ] `FR-RES-006` - define mean-reversion, trend-persistence, session-study, confirmation, and explicit isolated-failure policy.

- **Execution domain:** `Research`.
- **Execution position:** `516` of `675`.
- **Cannot start before:** Step `P01-S0515` (`FR-RES-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 568); matrix row `925`.
- **Source responsibility:** The system shall define mean-reversion, trend-persistence, session-study, confirmation, and explicit isolated-failure policy.
- **Source class / function / method:** `StudyConfig(mean_reversion: Mapping[str, JSONValue], trend_persistence: Mapping[str, JSONValue], session: Mapping[str, JSONValue], continue_on_study_error: bool = False)`
- **Source side effects:** None
- **Source raises:** unsupported study/confirmation setting
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_studies`<br>**Unit:** `test_configurations.py::test_study_config_fails_closed_by_default`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0517` [ ] `FR-RES-007` - define one timezone-aware set of named windows and deterministic overlap precedence for all session consumers.

- **Execution domain:** `Research`.
- **Execution position:** `517` of `675`.
- **Cannot start before:** Step `P01-S0516` (`FR-RES-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 569); matrix row `926`.
- **Source responsibility:** The system shall define one timezone-aware set of named windows and deterministic overlap precedence for all session consumers.
- **Source class / function / method:** `SessionConfig(timezone: str, windows: Mapping[str, tuple[time, time]], overlap_precedence: tuple[str,...])`
- **Source side effects:** None
- **Source raises:** session policy unresolved or invalid
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_sessions`<br>**Unit:** `test_configurations.py::test_session_config_requires_overlap_precedence`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0518` [ ] `FR-RES-008` - define bounded structure detection, canonical scoring, quality, validation, and calibration settings.

- **Execution domain:** `Research`.
- **Execution position:** `518` of `675`.
- **Cannot start before:** Step `P01-S0517` (`FR-RES-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 570); matrix row `927`.
- **Source responsibility:** The system shall define bounded structure detection, canonical scoring, quality, validation, and calibration settings.
- **Source class / function / method:** `MarketStructureConfig(profile: Mapping[str, JSONValue], enable_quality: bool, quality_windows: tuple[int,...], calibration_candidates: int, validation_horizon: int)`
- **Source side effects:** None
- **Source raises:** invalid validation or calibration policy
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_market_structure`<br>**Unit:** `test_configurations.py::test_market_structure_bounds_candidates`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0519` [ ] `FR-RES-009` - define selected features, preprocessing, PCA components, cluster count, minimum sample, and effective seed.

- **Execution domain:** `Research`.
- **Execution position:** `519` of `675`.
- **Cannot start before:** Step `P01-S0518` (`FR-RES-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 571); matrix row `928`.
- **Source responsibility:** The system shall define selected features, preprocessing, PCA components, cluster count, minimum sample, and effective seed.
- **Source class / function / method:** `UnsupervisedResearchConfig(feature_columns: tuple[str,...], scale: bool, pca_components: int, clusters: int, minimum_samples: int, seed: int)`
- **Source side effects:** None
- **Source raises:** invalid dimension/sample/seed
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_modeling`<br>**Unit:** `test_configurations.py::test_unsupervised_config_rejects_excess_clusters`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0520` [ ] `FR-RES-010` - define an allowed root, format, encoding, overwrite, masking, and atomic-write policy for artifacts.

- **Execution domain:** `Research`.
- **Execution position:** `520` of `675`.
- **Cannot start before:** Step `P01-S0519` (`FR-RES-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 572); matrix row `929`.
- **Source responsibility:** The system shall define an allowed root, format, encoding, overwrite, masking, and atomic-write policy for artifacts.
- **Source class / function / method:** `ArtifactWriteConfig(allowed_root: Path, format: Literal["json", "markdown"], overwrite: bool = False, encoding: str = "utf-8", require_atomic: bool = True)`
- **Source side effects:** None
- **Source raises:** root or ownership invalid
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_artifacts`<br>**Unit:** `test_configurations.py::test_artifact_config_rejects_relative_root`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0521` [ ] `FR-RES-011` - aggregate explicit stage configs, selected stages, and resource limits without supplying hidden trading/data policies.

- **Execution domain:** `Research`.
- **Execution position:** `521` of `675`.
- **Cannot start before:** Step `P01-S0520` (`FR-RES-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `configurations.py` — Immutable Configuration Contracts`` (line 573); matrix row `930`.
- **Source responsibility:** The system shall aggregate explicit stage configs, selected stages, and resource limits without supplying hidden trading/data policies.
- **Source class / function / method:** `EdgeLabConfig(cleaning: CleaningConfig, enrichment: EnrichmentConfig, features: FeatureConfig, statistics: StatisticalConfig, studies: StudyConfig, sessions: SessionConfig, market_structure: MarketStructureConfig, modeling: UnsupervisedResearchConfig, artifacts: ArtifactWriteConfig, limits: ResearchResourceLimits, selected_stages: tuple[str,...])`
- **Source side effects:** None
- **Source raises:** absent/incompatible configuration
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_configurations_edge_lab`<br>**Unit:** `test_configurations.py::test_edge_lab_config_requires_stage_dependencies`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0522` [ ] `FR-RES-012` - carry prepared records, canonical schema metadata, quality evidence, dataset/config hashes, and provenance without provider objects.

- **Execution domain:** `Research`.
- **Execution position:** `522` of `675`.
- **Cannot start before:** Step `P01-S0521` (`FR-RES-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 579); matrix row `931`.
- **Source responsibility:** The system shall carry prepared records, canonical schema metadata, quality evidence, dataset/config hashes, and provenance without provider objects.
- **Source class / function / method:** `PreparedDataset(data: DataFrame, schema_version: str, quality: DataQualityReport, dataset_hash: str, configuration_hash: str, source_references: tuple[str,...])`
- **Source side effects:** None
- **Source raises:** invalid schema/hash/frame
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_prepared_dataset`<br>**Unit:** `test_results.py::test_prepared_dataset_rejects_provider_object`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0523` [ ] `FR-RES-013` - distinguish fatal issues, warnings, checks, and explicit cleaning actions with machine-readable codes.

- **Execution domain:** `Research`.
- **Execution position:** `523` of `675`.
- **Cannot start before:** Step `P01-S0522` (`FR-RES-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 580); matrix row `932`.
- **Source responsibility:** The system shall distinguish fatal issues, warnings, checks, and explicit cleaning actions with machine-readable codes.
- **Source class / function / method:** `DataQualityReport(fatal_issues: tuple[Mapping[str, JSONValue],...], warnings: tuple[ResearchWarning,...], checks: tuple[str,...], cleaning_actions: tuple[Mapping[str, JSONValue],...])`
- **Source side effects:** None
- **Source raises:** invalid severity/code/details
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_quality_report`<br>**Unit:** `test_results.py::test_quality_report_distinguishes_fatal_warning`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0524` [ ] `FR-RES-014` - identify suspected lookahead columns, severity, evidence, recommendation, allowed forward columns, target, and source metadata.

- **Execution domain:** `Research`.
- **Execution position:** `524` of `675`.
- **Cannot start before:** Step `P01-S0523` (`FR-RES-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 581); matrix row `933`.
- **Source responsibility:** The system shall identify suspected lookahead columns, severity, evidence, recommendation, allowed forward columns, target, and source metadata.
- **Source class / function / method:** `LeakageReport(suspected_columns: tuple[str,...], severity: str, evidence: Mapping[str, JSONValue], recommendation: str, allowed_forward_columns: tuple[str,...], target_column: str | None, source_references: tuple[str,...])`
- **Source side effects:** None
- **Source raises:** invalid severity/evidence
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_leakage_report`<br>**Unit:** `test_results.py::test_leakage_report_requires_evidence`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0525` [ ] `FR-RES-015` - represent deterministic chronological train/validation/test partitions and boundary identities.

- **Execution domain:** `Research`.
- **Execution position:** `525` of `675`.
- **Cannot start before:** Step `P01-S0524` (`FR-RES-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 582); matrix row `934`.
- **Source responsibility:** The system shall represent deterministic chronological train/validation/test partitions and boundary identities.
- **Source class / function / method:** `TimeSplitResult(train: DataFrame, validation: DataFrame, test: DataFrame, boundaries: Mapping[str, datetime], split_hash: str)`
- **Source side effects:** None
- **Source raises:** overlapping/invalid partitions
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_time_split`<br>**Unit:** `test_results.py::test_time_split_rejects_overlap`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0526` [ ] `FR-RES-016` - represent seven-family metric values with units, sample size, undefined-value reason, warnings, and reproducibility metadata.

- **Execution domain:** `Research`.
- **Execution position:** `526` of `675`.
- **Cannot start before:** Step `P01-S0525` (`FR-RES-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 583); matrix row `935`.
- **Source responsibility:** The system shall represent seven-family metric values with units, sample size, undefined-value reason, warnings, and reproducibility metadata.
- **Source class / function / method:** `CoreMetricProfile(schema_version: str, metrics: Mapping[str, JSONValue], quality: DataQualityReport, dataset_hash: str, configuration_hash: str, warnings: tuple[ResearchWarning,...])`
- **Source side effects:** None
- **Source raises:** invalid metric/metadata schema
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_core_metric_profile`<br>**Unit:** `test_results.py::test_metric_profile_requires_units`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-016`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0527` [ ] `FR-RES-017` - represent one advisory edge study with sample, rule/config, split identity, null evidence, uncertainty, confirmation, seed, warnings, and pr...

- **Execution domain:** `Research`.
- **Execution position:** `527` of `675`.
- **Cannot start before:** Step `P01-S0526` (`FR-RES-016`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 584); matrix row `936`.
- **Source responsibility:** The system shall represent one advisory edge study with sample, rule/config, split identity, null evidence, uncertainty, confirmation, seed, warnings, and provenance.
- **Source class / function / method:** `EdgeResult(schema_version: str, study: str, statistics: Mapping[str, JSONValue], null_evidence: Mapping[str, JSONValue], classification: str, seed: int, warnings: tuple[ResearchWarning,...], advisory_only: Literal[True])`
- **Source side effects:** None
- **Source raises:** invalid or contradictory result
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_edge_result`<br>**Unit:** `test_results.py::test_edge_result_is_advisory`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0528` [ ] `FR-RES-018` - represent reproducible swings, legs, distributions, regimes, canonical score, verdict, and advisory fit evidence.

- **Execution domain:** `Research`.
- **Execution position:** `528` of `675`.
- **Cannot start before:** Step `P01-S0527` (`FR-RES-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 585); matrix row `937`.
- **Source responsibility:** The system shall represent reproducible swings, legs, distributions, regimes, canonical score, verdict, and advisory fit evidence.
- **Source class / function / method:** `MarketStructureProfile(schema_version: str, structure: Mapping[str, JSONValue], score: float, verdict: str, strategy_fit: Mapping[str, JSONValue], warnings: tuple[ResearchWarning,...])`
- **Source side effects:** None
- **Source raises:** invalid score/profile
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_market_structure`<br>**Unit:** `test_results.py::test_market_structure_uses_canonical_score`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0529` [ ] `FR-RES-019` - represent opt-in stability, robustness, validation, calibration candidates, ranking criteria, windows, duration, and warnings.

- **Execution domain:** `Research`.
- **Execution position:** `529` of `675`.
- **Cannot start before:** Step `P01-S0528` (`FR-RES-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 586); matrix row `938`.
- **Source responsibility:** The system shall represent opt-in stability, robustness, validation, calibration candidates, ranking criteria, windows, duration, and warnings.
- **Source class / function / method:** `MarketStructureQualityReport(schema_version: str, stability: Mapping[str, JSONValue], robustness: Mapping[str, JSONValue], calibration: Mapping[str, JSONValue], duration_ms: float, warnings: tuple[ResearchWarning,...])`
- **Source side effects:** None
- **Source raises:** invalid validation truth
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_market_quality`<br>**Unit:** `test_results.py::test_quality_report_records_windows`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0530` [ ] `FR-RES-020` - represent preprocessing, features, dropped columns, scaler, PCA, clusters, factor/cluster evidence, seed, parameters, diagnostics, and advis...

- **Execution domain:** `Research`.
- **Execution position:** `530` of `675`.
- **Cannot start before:** Step `P01-S0529` (`FR-RES-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 587); matrix row `939`.
- **Source responsibility:** The system shall represent preprocessing, features, dropped columns, scaler, PCA, clusters, factor/cluster evidence, seed, parameters, diagnostics, and advisory status.
- **Source class / function / method:** `UnsupervisedResearchResult(schema_version: str, preprocessing: Mapping[str, JSONValue], pca: Mapping[str, JSONValue], clusters: Mapping[str, JSONValue], insights: Mapping[str, JSONValue], seed: int, warnings: tuple[ResearchWarning,...], advisory_only: Literal[True])`
- **Source side effects:** None
- **Source raises:** invalid model metadata
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_unsupervised`<br>**Unit:** `test_results.py::test_unsupervised_result_records_seed`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0531` [ ] `FR-RES-021` - represent deterministic score rows, uncertainty, final score, readiness reasons, versions, and advisory status.

- **Execution domain:** `Research`.
- **Execution position:** `531` of `675`.
- **Cannot start before:** Step `P01-S0530` (`FR-RES-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 588); matrix row `940`.
- **Source responsibility:** The system shall represent deterministic score rows, uncertainty, final score, readiness reasons, versions, and advisory status.
- **Source class / function / method:** `ResearchScorecard(schema_version: str, score_rows: tuple[Mapping[str, JSONValue],...], final_score: float, readiness: str, reasons: tuple[str,...], warnings: tuple[ResearchWarning,...], advisory_only: Literal[True])`
- **Source side effects:** None
- **Source raises:** invalid score/readiness schema
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_scorecard`<br>**Unit:** `test_results.py::test_scorecard_readiness_has_reasons`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0532` [ ] `FR-RES-022` - normalize approved stage outputs into one versioned snapshot with hashes, versions, warnings, and advisory status.

- **Execution domain:** `Research`.
- **Execution position:** `532` of `675`.
- **Cannot start before:** Step `P01-S0531` (`FR-RES-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 589); matrix row `941`.
- **Source responsibility:** The system shall normalize approved stage outputs into one versioned snapshot with hashes, versions, warnings, and advisory status.
- **Source class / function / method:** `ResearchProfileSnapshot(schema_version: str, stages: Mapping[str, JSONValue], scorecard: ResearchScorecard, dataset_hash: str, configuration_hash: str, generated_at: datetime, warnings: tuple[ResearchWarning,...], advisory_only: Literal[True])`
- **Source side effects:** None
- **Source raises:** missing required stage/version/hash
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_snapshot`<br>**Unit:** `test_results.py::test_snapshot_rejects_unversioned_stage`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-022`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0533` [ ] `FR-RES-023` - expose bounded structured warnings with code, message, severity, optional field path, and bounded details.

- **Execution domain:** `Research`.
- **Execution position:** `533` of `675`.
- **Cannot start before:** Step `P01-S0532` (`FR-RES-022`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 590); matrix row `942`.
- **Source responsibility:** The system shall expose bounded structured warnings with code, message, severity, optional field path, and bounded details.
- **Source class / function / method:** `ResearchWarning(code: str, message: str, severity: str, field_path: str | None = None, details: Mapping[str, JSONValue] | None = None)`
- **Source side effects:** None
- **Source raises:** invalid warning vocabulary
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_warning`<br>**Unit:** `test_results.py::test_warning_details_are_bounded`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0534` [ ] `FR-RES-024` - produce the fully defined ResearchReport v1 contract in Section 1 with advisory_only=True and complete reproducibility metadata.

- **Execution domain:** `Research`.
- **Execution position:** `534` of `675`.
- **Cannot start before:** Step `P01-S0533` (`FR-RES-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 591); matrix row `943`.
- **Source responsibility:** The system shall produce the fully defined `ResearchReport v1` contract in Section 1 with `advisory_only=True` and complete reproducibility metadata.
- **Source class / function / method:** `ResearchReport(...)`
- **Source side effects:** None
- **Source raises:** contract validation; pending leakage gate: unsafe evidence blocks construction/publication
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_research_report`<br>**Unit:** `test_results.py::test_research_report_v1_contract`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0535` [ ] `FR-RES-025` - return a safe artifact reference containing relative location, format, byte size, content hash, atomicity, schema version, and audit identity.

- **Execution domain:** `Research`.
- **Execution position:** `535` of `675`.
- **Cannot start before:** Step `P01-S0534` (`FR-RES-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `results.py` — Versioned Result Contracts`` (line 592); matrix row `944`.
- **Source responsibility:** The system shall return a safe artifact reference containing relative location, format, byte size, content hash, atomicity, schema version, and audit identity.
- **Source class / function / method:** `ArtifactReference(relative_path: Path, format: str, size_bytes: int, sha256: str, atomic: bool, schema_version: str, audit_event_id: str)`
- **Source side effects:** None
- **Source raises:** invalid/out-of-root reference
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_results_artifact_reference`<br>**Unit:** `test_results.py::test_artifact_reference_is_relative`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0536` [ ] `FR-RES-026` - expose a unique immutable mapping for every __all__ name with stable classification and lazy import target, without recursive scanning or ca...

- **Execution domain:** `Research`.
- **Execution position:** `536` of `675`.
- **Cannot start before:** Step `P01-S0535` (`FR-RES-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > `api.py` — Classified Public API`` (line 598); matrix row `945`.
- **Source responsibility:** The system shall expose a unique immutable mapping for every `__all__` name with `stable` classification and lazy import target, without recursive scanning or callable wrapping.
- **Source class / function / method:** `PUBLIC_API_CLASSIFICATIONS: Mapping[str, Literal["stable"]]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `test_usage_contracts.py::test_usage_api_classifications()`<br>**Unit:** `test_api.py::test_public_api_is_unique_resolvable_and_side_effect_free()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0537` [ ] `FR-RES-027` - validate required columns, UTC/time ordering, duplicates, gaps, OHLC consistency, spread quality, volume, finite values, and source metadata...

- **Execution domain:** `Research`.
- **Execution position:** `537` of `675`.
- **Cannot start before:** Step `P01-S0536` (`FR-RES-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 645); matrix row `946`.
- **Source responsibility:** The system shall validate required columns, UTC/time ordering, duplicates, gaps, OHLC consistency, spread quality, volume, finite values, and source metadata without mutating input.
- **Source class / function / method:** `validate_dataset(dataset: MarketDataset, *, limits: ResearchResourceLimits) -> DataQualityReport`
- **Source side effects:** Read-only
- **Source raises:** invalid input/schema/resource limit
- **Source usage / test:** **Usage:** `test_usage_data.py::test_usage_validation_validate_dataset`<br>**Unit:** `test_validation.py::test_validate_dataset_reports_fatal_ohlc_issue`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-027`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0538` [ ] `FR-RES-028` - clean a copy using only explicit approved strategies and record every action and unresolved warning.

- **Execution domain:** `Research`.
- **Execution position:** `538` of `675`.
- **Cannot start before:** Step `P01-S0537` (`FR-RES-027`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 646); matrix row `947`.
- **Source responsibility:** The system shall clean a copy using only explicit approved strategies and record every action and unresolved warning.
- **Source class / function / method:** `clean_dataset(dataset: MarketDataset, *, config: CleaningConfig, report: DataQualityReport, limits: ResearchResourceLimits) -> tuple[DataFrame, DataQualityReport]`
- **Source side effects:** Local state mutation
- **Source raises:** unsupported/absent policy or invalid data
- **Source usage / test:** **Usage:** `test_usage_data.py::test_usage_preparation_clean_dataset`<br>**Unit:** `test_preparation.py::test_clean_dataset_never_fills_implicitly`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0539` [ ] `FR-RES-029` - enrich a copy with selected pip/geometry/return-label/calendar fields, label forward fields as research-only, and preserve row alignment; se...

- **Execution domain:** `Research`.
- **Execution position:** `539` of `675`.
- **Cannot start before:** Step `P01-S0538` (`FR-RES-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 647); matrix row `948`.
- **Source responsibility:** The system shall enrich a copy with selected pip/geometry/return-label/calendar fields, label forward fields as research-only, and preserve row alignment; session tagging is a later `seasonality/` operation.
- **Source class / function / method:** `enrich_dataset(data: DataFrame, *, config: EnrichmentConfig, report: DataQualityReport) -> tuple[DataFrame, DataQualityReport]`
- **Source side effects:** Local state mutation
- **Source raises:** missing structural inputs or incompatible enrichment
- **Source usage / test:** **Usage:** `test_usage_data.py::test_usage_preparation_enrich_dataset`<br>**Unit:** `test_preparation.py::test_enrich_dataset_labels_forward_columns`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-029`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0540` [ ] `FR-RES-030` - execute validate clean revalidate enrich deterministically and return hashes, provenance, and quality evidence, never fetching p...

- **Execution domain:** `Research`.
- **Execution position:** `540` of `675`.
- **Cannot start before:** Step `P01-S0539` (`FR-RES-029`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 648); matrix row `949`.
- **Source responsibility:** The system shall execute validate → clean → revalidate → enrich deterministically and return hashes, provenance, and quality evidence, never fetching provider data.
- **Source class / function / method:** `prepare_research_dataset(dataset: MarketDataset, *, cleaning: CleaningConfig, enrichment: EnrichmentConfig, limits: ResearchResourceLimits) -> PreparedDataset`
- **Source side effects:** Read-only
- **Source raises:** fatal validation/config/resource failure
- **Source usage / test:** **Usage:** `test_usage_data.py::test_usage_preparation_prepare_research_dataset`<br>**Unit:** `test_preparation.py::test_prepare_dataset_is_deterministic_and_provider_free`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0541` [ ] `FR-RES-031` - Compute one-period log returns without mutating input and preserve index alignment.

- **Execution domain:** `Research`.
- **Execution position:** `541` of `675`.
- **Cannot start before:** Step `P01-S0540` (`FR-RES-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 688); matrix row `950`.
- **Source responsibility:** Compute one-period log returns without mutating input and preserve index alignment.
- **Source class / function / method:** `log_returns(close: Series) -> Series`
- **Source side effects:** Read-only
- **Source raises:** non-finite/non-positive/insufficient input
- **Source usage / test:** **Usage:** `test_usage_features.py::test_usage_calculations_log_returns`<br>**Unit:** `test_calculations.py::test_log_returns_preserves_alignment`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0542` [ ] `FR-RES-042` - Define the read-only contract implemented by one named metric-family calculator.

- **Execution domain:** `Research`.
- **Execution position:** `542` of `675`.
- **Cannot start before:** Step `P01-S0541` (`FR-RES-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 771); matrix row `951`.
- **Source responsibility:** Define the read-only contract implemented by one named metric-family calculator.
- **Source class / function / method:** `class MetricCalculator(Protocol)`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `test_usage_metrics.py::test_usage_registry_metric_calculator()`<br>**Unit:** `test_registry.py::test_calculator_protocol_contract()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-042`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0543` [ ] `FR-RES-043` - Compute normalized values for one family from an immutable metric context.

- **Execution domain:** `Research`.
- **Execution position:** `543` of `675`.
- **Cannot start before:** Step `P01-S0542` (`FR-RES-042`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 772); matrix row `952`.
- **Source responsibility:** Compute normalized values for one family from an immutable metric context.
- **Source class / function / method:** `MetricCalculator.compute(context: MetricContext) -> tuple[MetricValue,...]`
- **Source side effects:** Read-only
- **Source raises:** invalid/missing metric input
- **Source usage / test:** **Usage:** `test_usage_metrics.py::test_usage_registry_compute`<br>**Unit:** `test_registry.py::test_calculator_returns_normalized_values`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-043`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0544` [ ] `FR-RES-044` - Own unique bounded calculator membership without global mutable defaults.

- **Execution domain:** `Research`.
- **Execution position:** `544` of `675`.
- **Cannot start before:** Step `P01-S0543` (`FR-RES-043`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 773); matrix row `953`.
- **Source responsibility:** Own unique bounded calculator membership without global mutable defaults.
- **Source class / function / method:** `MetricRegistry`
- **Source side effects:** Local state mutation
- **Source raises:** duplicate/invalid calculator
- **Source usage / test:** **Usage:** `test_usage_metrics.py::test_usage_registry_metric_registry`<br>**Unit:** `test_registry.py::test_registry_rejects_duplicate_family`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-044`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0545` [ ] `FR-RES-045` - Construct an isolated registry from a bounded calculator iterable.

- **Execution domain:** `Research`.
- **Execution position:** `545` of `675`.
- **Cannot start before:** Step `P01-S0544` (`FR-RES-044`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 774); matrix row `954`.
- **Source responsibility:** Construct an isolated registry from a bounded calculator iterable.
- **Source class / function / method:** `MetricRegistry.from_calculators(calculators: Iterable[MetricCalculator]) -> MetricRegistry`
- **Source side effects:** Local state mutation
- **Source raises:** duplicate/empty/invalid calculators
- **Source usage / test:** **Usage:** `test_usage_metrics.py::test_usage_registry_from_calculators`<br>**Unit:** `test_registry.py::test_from_calculators_is_isolated`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-045`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0546` [ ] `FR-RES-046` - Resolve a calculator by exact family name.

- **Execution domain:** `Research`.
- **Execution position:** `546` of `675`.
- **Cannot start before:** Step `P01-S0545` (`FR-RES-045`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 775); matrix row `955`.
- **Source responsibility:** Resolve a calculator by exact family name.
- **Source class / function / method:** `MetricRegistry.resolve(family: str) -> MetricCalculator`
- **Source side effects:** Read-only
- **Source raises:** family not found
- **Source usage / test:** **Usage:** `test_usage_metrics.py::test_usage_registry_resolve`<br>**Unit:** `test_registry.py::test_resolve_missing_family`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-046`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0547` [ ] `FR-RES-047` - Return calculators in deterministic registration order without exposing mutable storage.

- **Execution domain:** `Research`.
- **Execution position:** `547` of `675`.
- **Cannot start before:** Step `P01-S0546` (`FR-RES-046`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 776); matrix row `956`.
- **Source responsibility:** Return calculators in deterministic registration order without exposing mutable storage.
- **Source class / function / method:** `MetricRegistry.all() -> tuple[MetricCalculator, ...]`
- **Source side effects:** Read-only
- **Source raises:** None
- **Source usage / test:** **Usage:** `test_usage_metrics.py::test_usage_registry_all()`<br>**Unit:** `test_registry.py::test_all_is_immutable_and_ordered()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-047`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0548` [ ] `FR-RES-048` - Build a new default registry containing the seven retained metric families.

- **Execution domain:** `Research`.
- **Execution position:** `548` of `675`.
- **Cannot start before:** Step `P01-S0547` (`FR-RES-047`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 777); matrix row `957`.
- **Source responsibility:** Build a new default registry containing the seven retained metric families.
- **Source class / function / method:** `build_default_registry -> MetricRegistry`
- **Source side effects:** Local state mutation
- **Source raises:** internal registration failure
- **Source usage / test:** **Usage:** `test_usage_metrics.py::test_usage_registry_build_default`<br>**Unit:** `test_registry.py::test_default_registry_has_seven_families`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-048`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0549` [ ] `FR-RES-049` - Build a deterministic profile with units, samples, undefined reasons, hashes, warnings, and duration from a prepared dataset.

- **Execution domain:** `Research`.
- **Execution position:** `549` of `675`.
- **Cannot start before:** Step `P01-S0548` (`FR-RES-048`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 778); matrix row `958`.
- **Source responsibility:** Build a deterministic profile with units, samples, undefined reasons, hashes, warnings, and duration from a prepared dataset.
- **Source class / function / method:** `build_core_metric_profile(prepared: PreparedDataset, *, registry: MetricRegistry | None = None, limits: ResearchResourceLimits) -> CoreMetricProfile`
- **Source side effects:** Read-only
- **Source raises:** invalid data/dependency/resource
- **Source usage / test:** **Usage:** `test_usage_metrics.py::test_usage_profile_build_core_metric_profile`<br>**Unit:** `test_profile.py::test_profile_contains_units_hashes_and_warnings`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-049`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0550` [ ] `FR-RES-050` - Generate a seeded block-bootstrap statistic distribution and record the effective parameters.

- **Execution domain:** `Research`.
- **Execution position:** `550` of `675`.
- **Cannot start before:** Step `P01-S0549` (`FR-RES-049`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 818); matrix row `959`.
- **Source responsibility:** Generate a seeded block-bootstrap statistic distribution and record the effective parameters.
- **Source class / function / method:** `block_bootstrap_distribution(values: NDArray, *, statistic: Callable[[NDArray], float], config: StatisticalConfig) -> NDArray`
- **Source side effects:** Read-only
- **Source raises:** invalid/insufficient/non-finite sample or limit
- **Source usage / test:** **Usage:** `test_usage_statistics.py::test_usage_resampling_distribution`<br>**Unit:** `test_resampling.py::test_distribution_is_seed_reproducible`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-050`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0551` [ ] `FR-RES-069` - Return every configured session active for a timezone-aware hour using canonical overlap precedence.

- **Execution domain:** `Research`.
- **Execution position:** `551` of `675`.
- **Cannot start before:** Step `P01-S0550` (`FR-RES-050`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 907); matrix row `960`.
- **Source responsibility:** Return every configured session active for a timezone-aware hour using canonical overlap precedence.
- **Source class / function / method:** `active_sessions_for_hour(hour: int, *, config: SessionConfig) -> tuple[str,...]`
- **Source side effects:** Read-only
- **Source raises:** invalid hour/session policy
- **Source usage / test:** **Usage:** `test_usage_seasonality.py::test_usage_sessions_active`<br>**Unit:** `test_sessions.py::test_active_sessions_handles_overlap`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-069`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0552` [ ] `FR-RES-075` - Build swings, directional legs, range/distribution/excursion/regime evidence, canonical score/verdict, warnings, hashes, and advisory fit.

- **Execution domain:** `Research`.
- **Execution position:** `552` of `675`.
- **Cannot start before:** Step `P01-S0551` (`FR-RES-069`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 949); matrix row `961`.
- **Source responsibility:** Build swings, directional legs, range/distribution/excursion/regime evidence, canonical score/verdict, warnings, hashes, and advisory fit.
- **Source class / function / method:** `build_market_structure_profile(prepared: PreparedDataset, *, config: MarketStructureConfig, limits: ResearchResourceLimits) -> MarketStructureProfile`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy/resource: invalid/insufficient data or limit
- **Source usage / test:** **Usage:** `test_usage_market_structure.py::test_usage_profile_build()`<br>**Unit:** `test_profile.py::test_profile_reuses_canonical_score()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-075`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0553` [ ] `FR-RES-077` - Label later bars as trend/reversion/mixed under one approved horizon/truth policy and return insufficiency as structured evidence.

- **Execution domain:** `Research`.
- **Execution position:** `553` of `675`.
- **Cannot start before:** Step `P01-S0552` (`FR-RES-075`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 951); matrix row `962`.
- **Source responsibility:** Label later bars as trend/reversion/mixed under one approved horizon/truth policy and return insufficiency as structured evidence.
- **Source class / function / method:** `label_realized_market_behavior(data: DataFrame, *, symbol: str, timeframe: str, config: MarketStructureConfig) -> Mapping[str, JSONValue]`
- **Source side effects:** Read-only
- **Source raises:** invalid truth policy or data
- **Source usage / test:** **Usage:** `test_usage_market_structure.py::test_usage_validation_label_behavior`<br>**Unit:** `test_validation.py::test_label_behavior_uses_approved_horizon`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-077`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0554` [ ] `FR-RES-081` - Scale selected finite numeric features and return PCA scores, loadings, variance, preprocessing, and diagnostics without mutation.

- **Execution domain:** `Research`.
- **Execution position:** `554` of `675`.
- **Cannot start before:** Step `P01-S0553` (`FR-RES-077`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 996); matrix row `963`.
- **Source responsibility:** Scale selected finite numeric features and return PCA scores, loadings, variance, preprocessing, and diagnostics without mutation.
- **Source class / function / method:** `run_pca(features: DataFrame, *, config: UnsupervisedResearchConfig) -> Mapping[str, JSONValue]`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy: invalid/constant/missing/insufficient dimensions
- **Source usage / test:** **Usage:** `test_usage_modeling.py::test_usage_decomposition_run_pca()`<br>**Unit:** `test_decomposition.py::test_pca_records_preprocessing()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-081`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0555` [ ] `FR-RES-089` - Build deterministic score rows, final score, uncertainty, readiness/reasons, versions, warnings, and advisory status from approved evidence.

- **Execution domain:** `Research`.
- **Execution position:** `555` of `675`.
- **Cannot start before:** Step `P01-S0554` (`FR-RES-081`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 1044); matrix row `964`.
- **Source responsibility:** Build deterministic score rows, final score, uncertainty, readiness/reasons, versions, warnings, and advisory status from approved evidence.
- **Source class / function / method:** `build_research_scorecard(*, metric_profile: CoreMetricProfile, seasonality: Mapping[str, JSONValue] | None, edges: Sequence[EdgeResult], market_structure: MarketStructureProfile | None, modeling: UnsupervisedResearchResult | None, performance: PerformanceReport | None = None) -> ResearchScorecard`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy/dependency: absent prerequisite or incompatible versions
- **Source usage / test:** **Usage:** `test_usage_profiles.py::test_usage_scorecard_build()`<br>**Unit:** `test_scorecard.py::test_scorecard_is_deterministic_and_advisory()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-089`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0556` [ ] `FR-RES-090` - Build one canonical versioned snapshot from approved stage outputs and reject route-specific/unversioned payloads.

- **Execution domain:** `Research`.
- **Execution position:** `556` of `675`.
- **Cannot start before:** Step `P01-S0555` (`FR-RES-089`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 1045); matrix row `965`.
- **Source responsibility:** Build one canonical versioned snapshot from approved stage outputs and reject route-specific/unversioned payloads.
- **Source class / function / method:** `build_research_profile_snapshot(*, stages: Mapping[str, JSONValue], scorecard: ResearchScorecard, dataset_hash: str, configuration_hash: str) -> ResearchProfileSnapshot`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy: missing/incompatible stage/hash
- **Source usage / test:** **Usage:** `test_usage_profiles.py::test_usage_snapshot_build()`<br>**Unit:** `test_snapshot.py::test_snapshot_rejects_unversioned_stage()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-090`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0557` [ ] `FR-RES-091` - Return a concise observation/uncertainty/readiness summary from a canonical snapshot.

- **Execution domain:** `Research`.
- **Execution position:** `557` of `675`.
- **Cannot start before:** Step `P01-S0556` (`FR-RES-090`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 1046); matrix row `966`.
- **Source responsibility:** Return a concise observation/uncertainty/readiness summary from a canonical snapshot.
- **Source class / function / method:** `build_profile_summary(snapshot: ResearchProfileSnapshot) -> Mapping[str, JSONValue]`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy: invalid snapshot
- **Source usage / test:** **Usage:** `test_usage_profiles.py::test_usage_snapshot_profile_summary()`<br>**Unit:** `test_snapshot.py::test_profile_summary_preserves_warnings()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-091`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0558` [ ] `FR-RES-092` - Return a bounded UI-ready block from a canonical snapshot without presentation-side calculation.

- **Execution domain:** `Research`.
- **Execution position:** `558` of `675`.
- **Cannot start before:** Step `P01-S0557` (`FR-RES-091`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 1047); matrix row `967`.
- **Source responsibility:** Return a bounded UI-ready block from a canonical snapshot without presentation-side calculation.
- **Source class / function / method:** `build_dashboard_summary(snapshot: ResearchProfileSnapshot) -> Mapping[str, JSONValue]`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy/resource: invalid/oversized snapshot
- **Source usage / test:** **Usage:** `test_usage_profiles.py::test_usage_snapshot_dashboard()`<br>**Unit:** `test_snapshot.py::test_dashboard_summary_is_bounded()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-092`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0559` [ ] `FR-RES-093` - Render a canonical report as JSON-compatible data or Markdown with UTC metadata and no persistence side effect.

- **Execution domain:** `Research`.
- **Execution position:** `559` of `675`.
- **Cannot start before:** Step `P01-S0558` (`FR-RES-092`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 1048); matrix row `968`.
- **Source responsibility:** Render a canonical report as JSON-compatible data or Markdown with UTC metadata and no persistence side effect.
- **Source class / function / method:** `render_research_report(report: ResearchReport, *, format: Literal["json", "markdown"]) -> JSONValue | str`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy: unsupported format/non-serializable report
- **Source usage / test:** **Usage:** `test_usage_profiles.py::test_usage_rendering_report()`<br>**Unit:** `test_rendering.py::test_render_report_uses_utc_and_no_io()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-093`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0560` [ ] `FR-RES-094` - Render a Markdown comparison of two compatible snapshots while exposing schema/config/dataset differences.

- **Execution domain:** `Research`.
- **Execution position:** `560` of `675`.
- **Cannot start before:** Step `P01-S0559` (`FR-RES-093`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 1049); matrix row `969`.
- **Source responsibility:** Render a Markdown comparison of two compatible snapshots while exposing schema/config/dataset differences.
- **Source class / function / method:** `render_profile_comparison(left: ResearchProfileSnapshot, right: ResearchProfileSnapshot) -> str`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy: incompatible schema/snapshot
- **Source usage / test:** **Usage:** `test_usage_profiles.py::test_usage_rendering_comparison()`<br>**Unit:** `test_rendering.py::test_comparison_rejects_incompatible_schema()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-094`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0561` [ ] `FR-RES-095` - Render per-symbol and combined advisory summaries in memory while preserving individual failures/warnings; it shall not write files.

- **Execution domain:** `Research`.
- **Execution position:** `561` of `675`.
- **Cannot start before:** Step `P01-S0560` (`FR-RES-094`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 1050); matrix row `970`.
- **Source responsibility:** Render per-symbol and combined advisory summaries in memory while preserving individual failures/warnings; it shall not write files.
- **Source class / function / method:** `generate_multi_symbol_report(reports: Mapping[str, ResearchReport], *, format: Literal["json", "markdown"]) -> JSONValue | str`
- **Source side effects:** Read-only
- **Source raises:** Pending taxonomy/resource: empty/invalid/oversized report set
- **Source usage / test:** **Usage:** `test_usage_profiles.py::test_usage_rendering_multi_symbol()`<br>**Unit:** `test_rendering.py::test_multi_symbol_preserves_partial_warnings()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-095`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0562` [ ] `FR-RES-096` - Execute selected deterministic stage APIs over supplied contracts and return ResearchReport v1 while leaving provider reads, cache, scheduling, database writ...

- **Execution domain:** `Research`.
- **Execution position:** `562` of `675`.
- **Cannot start before:** Step `P01-S0561` (`FR-RES-095`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` package-wide functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 4. Module and Requirement Specifications > Configuration and Limits Manifest > Functional requirements`` (line 1051); matrix row `971`.
- **Source responsibility:** Execute selected deterministic stage APIs over supplied contracts and return `ResearchReport v1` while leaving provider reads, cache, scheduling, database writes, and strategy submission external.
- **Source class / function / method:** `run_edge_lab_profile(dataset: MarketDataset, *, config: EdgeLabConfig, performance: PerformanceReport | None = None) -> ResearchReport`
- **Source side effects:** Read-only
- **Source raises:** stage config/data/dependency/resource failure
- **Source usage / test:** **Usage:** `test_usage_profiles.py::test_usage_workflow_run_edge_lab`<br>**Unit:** `test_workflow.py::test_edge_lab_has_no_external_or_live_side_effects`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-RES-096`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0563` [ ] `WF-RES-001` - Prepare Research Dataset

- **Execution domain:** `Research`.
- **Execution position:** `563` of `675`.
- **Cannot start before:** Step `P01-S0562` (`FR-RES-096`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 3. Workflows > Workflow scope values`` (line 354); matrix row `976`.
- **Source scope:** Cross-domain
- **Source workflow:** Prepare Research Dataset
- **Source requirement sequence:** `FR-RES-027 → 030`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-RES-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0564` [ ] `WF-RES-002` - Build Core Metric Profile

- **Execution domain:** `Research`.
- **Execution position:** `564` of `675`.
- **Cannot start before:** Step `P01-S0563` (`WF-RES-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Research` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/research/README.md` - ``Research > 3. Workflows > Workflow scope values`` (line 355); matrix row `977`.
- **Source scope:** Internal
- **Source workflow:** Build Core Metric Profile
- **Source requirement sequence:** `FR-RES-042 → 049`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-RES-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 13 - Portfolio

Implement `Portfolio` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0565` [ ] `P-PORT-001` - contracts feature/component (provisional)

- **Execution domain:** `Portfolio`.
- **Execution position:** `565` of `675`.
- **Cannot start before:** Step `P01-S0564` (`WF-RES-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-PORT-001`); matrix row `1079`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-PORT-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0566` [ ] `FR-PORT-001` - Reject unknown fields and unsafe runtime objects.

- **Execution domain:** `Portfolio`.
- **Execution position:** `566` of `675`.
- **Cannot start before:** Step `P01-S0565` (`P-PORT-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.1 `contracts/` — Portfolio Boundary Schemas > Configuration and Limits Manifest`` (line 281); matrix row `1055`.
- **Source requirement:** Reject unknown fields and unsafe runtime objects.
- **Source verification:** Contract unit tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0567` [ ] `FR-PORT-002` - Separate contract_version from namespaced schema_id.

- **Execution domain:** `Portfolio`.
- **Execution position:** `567` of `675`.
- **Cannot start before:** Step `P01-S0566` (`FR-PORT-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.1 `contracts/` — Portfolio Boundary Schemas > Configuration and Limits Manifest`` (line 282); matrix row `1056`.
- **Source requirement:** Separate `contract_version` from namespaced `schema_id`.
- **Source verification:** Serialization tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0568` [ ] `FR-PORT-003` - Require UTC timestamps, trace IDs, immutable owner references, and finite numbers.

- **Execution domain:** `Portfolio`.
- **Execution position:** `568` of `675`.
- **Cannot start before:** Step `P01-S0567` (`FR-PORT-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.1 `contracts/` — Portfolio Boundary Schemas > Configuration and Limits Manifest`` (line 283); matrix row `1057`.
- **Source requirement:** Require UTC timestamps, trace IDs, immutable owner references, and finite numbers.
- **Source verification:** Validation tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0569` [ ] `FR-PORT-004` - Represent capital weights separately from Risk-authoritative budget projection references.

- **Execution domain:** `Portfolio`.
- **Execution position:** `569` of `675`.
- **Cannot start before:** Step `P01-S0568` (`FR-PORT-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.1 `contracts/` — Portfolio Boundary Schemas > Configuration and Limits Manifest`` (line 284); matrix row `1058`.
- **Source requirement:** Represent capital weights separately from Risk-authoritative budget projection references.
- **Source verification:** Schema tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0570` [ ] `FR-PORT-005` - Version breaking contract changes and update every producer/consumer document together.

- **Execution domain:** `Portfolio`.
- **Execution position:** `570` of `675`.
- **Cannot start before:** Step `P01-S0569` (`FR-PORT-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.1 `contracts/` — Portfolio Boundary Schemas > Configuration and Limits Manifest`` (line 285); matrix row `1059`.
- **Source requirement:** Version breaking contract changes and update every producer/consumer document together.
- **Source verification:** Review
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0571` [ ] `FR-PORT-006` - Require a current approving eligibility decision for every exact strategy/version/scope.

- **Execution domain:** `Portfolio`.
- **Execution position:** `571` of `675`.
- **Cannot start before:** Step `P01-S0570` (`FR-PORT-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.2 `evidence/` — Evidence and Eligibility Validation`` (line 300); matrix row `1060`.
- **Source requirement:** Require a current approving eligibility decision for every exact strategy/version/scope.
- **Source verification:** Eligibility tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0572` [ ] `FR-PORT-009` - Detect a reference/version change before publication or activation.

- **Execution domain:** `Portfolio`.
- **Execution position:** `572` of `675`.
- **Cannot start before:** Step `P01-S0571` (`FR-PORT-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.2 `evidence/` — Evidence and Eligibility Validation`` (line 303); matrix row `1061`.
- **Source requirement:** Detect a reference/version change before publication or activation.
- **Source verification:** Concurrency tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0573` [ ] `P-PORT-003` - construction feature/component (provisional)

- **Execution domain:** `Portfolio`.
- **Execution position:** `573` of `675`.
- **Cannot start before:** Step `P01-S0572` (`FR-PORT-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.3` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-PORT-003`); matrix row `1080`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-PORT-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0574` [ ] `FR-PORT-010` - Support fixed, equal, and inverse-volatility methods only.

- **Execution domain:** `Portfolio`.
- **Execution position:** `574` of `675`.
- **Cannot start before:** Step `P01-S0573` (`P-PORT-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.3 `construction/` — Deterministic Construction`` (line 327); matrix row `1062`.
- **Source requirement:** Support fixed, equal, and inverse-volatility methods only.
- **Source verification:** Method tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0575` [ ] `FR-PORT-011` - Reject zero/negative volatility, insufficient observations, non-finite values, and invalid weight totals.

- **Execution domain:** `Portfolio`.
- **Execution position:** `575` of `675`.
- **Cannot start before:** Step `P01-S0574` (`FR-PORT-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.3 `construction/` — Deterministic Construction`` (line 328); matrix row `1063`.
- **Source requirement:** Reject zero/negative volatility, insufficient observations, non-finite values, and invalid weight totals.
- **Source verification:** Edge-case tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0576` [ ] `FR-PORT-012` - Return identical bytes and hash for identical inputs/configuration.

- **Execution domain:** `Portfolio`.
- **Execution position:** `576` of `675`.
- **Cannot start before:** Step `P01-S0575` (`FR-PORT-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.3 `construction/` — Deterministic Construction`` (line 329); matrix row `1064`.
- **Source requirement:** Return identical bytes and hash for identical inputs/configuration.
- **Source verification:** Reproducibility test
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0577` [ ] `FR-PORT-013` - Exclude MVO, Black-Litterman, CVaR, and implicit optimizer delegation.

- **Execution domain:** `Portfolio`.
- **Execution position:** `577` of `675`.
- **Cannot start before:** Step `P01-S0576` (`FR-PORT-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.3 `construction/` — Deterministic Construction`` (line 330); matrix row `1065`.
- **Source requirement:** Exclude MVO, Black-Litterman, CVaR, and implicit optimizer delegation.
- **Source verification:** Import/API review
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0578` [ ] `FR-PORT-014` - Publish nothing on partial construction failure.

- **Execution domain:** `Portfolio`.
- **Execution position:** `578` of `675`.
- **Cannot start before:** Step `P01-S0577` (`FR-PORT-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.3 `construction/` — Deterministic Construction`` (line 331); matrix row `1066`.
- **Source requirement:** Publish nothing on partial construction failure.
- **Source verification:** Failure tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0579` [ ] `FR-PORT-015` - Require Simulation validation and current Risk authorization before activation.

- **Execution domain:** `Portfolio`.
- **Execution position:** `579` of `675`.
- **Cannot start before:** Step `P01-S0578` (`FR-PORT-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.5 `allocation/` — Version and Activation Governance`` (line 370); matrix row `1067`.
- **Source requirement:** Require Simulation validation and current Risk authorization before activation.
- **Source verification:** Gate tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0580` [ ] `FR-PORT-018` - Use optimistic concurrency and one active version per scope.

- **Execution domain:** `Portfolio`.
- **Execution position:** `580` of `675`.
- **Cannot start before:** Step `P01-S0579` (`FR-PORT-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.5 `allocation/` — Version and Activation Governance`` (line 373); matrix row `1068`.
- **Source requirement:** Use optimistic concurrency and one active version per scope.
- **Source verification:** Repository tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0581` [ ] `FR-PORT-019` - Implement rollback only as a new governed version.

- **Execution domain:** `Portfolio`.
- **Execution position:** `581` of `675`.
- **Cannot start before:** Step `P01-S0580` (`FR-PORT-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.5 `allocation/` — Version and Activation Governance`` (line 374); matrix row `1069`.
- **Source requirement:** Implement rollback only as a new governed version.
- **Source verification:** History tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0582` [ ] `FR-PORT-020` - Bind drift to an active allocation version and fresh actual-exposure evidence.

- **Execution domain:** `Portfolio`.
- **Execution position:** `582` of `675`.
- **Cannot start before:** Step `P01-S0581` (`FR-PORT-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.6 `rebalancing/` — Drift and Rebalance Planning`` (line 394); matrix row `1070`.
- **Source requirement:** Bind drift to an active allocation version and fresh actual-exposure evidence.
- **Source verification:** Drift tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0583` [ ] `FR-PORT-024` - Block planning/submission on kill switch, expiry, stale evidence, or target-version change.

- **Execution domain:** `Portfolio`.
- **Execution position:** `583` of `675`.
- **Cannot start before:** Step `P01-S0582` (`FR-PORT-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.6 `rebalancing/` — Drift and Rebalance Planning`` (line 398); matrix row `1071`.
- **Source requirement:** Block planning/submission on kill switch, expiry, stale evidence, or target-version change.
- **Source verification:** Fail-closed tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0584` [ ] `FR-PORT-025` - Submit only receiver-owned Risk, Simulation, and Trading request contracts.

- **Execution domain:** `Portfolio`.
- **Execution position:** `584` of `675`.
- **Cannot start before:** Step `P01-S0583` (`FR-PORT-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.7 `orchestration/` — Cross-Domain Workflow Coordination`` (line 413); matrix row `1072`.
- **Source requirement:** Submit only receiver-owned Risk, Simulation, and Trading request contracts.
- **Source verification:** Contract tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0585` [ ] `FR-PORT-029` - Never retry a potentially accepted mutation without receiver-provided idempotency semantics.

- **Execution domain:** `Portfolio`.
- **Execution position:** `585` of `675`.
- **Cannot start before:** Step `P01-S0584` (`FR-PORT-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.7 `orchestration/` — Cross-Domain Workflow Coordination`` (line 417); matrix row `1073`.
- **Source requirement:** Never retry a potentially accepted mutation without receiver-provided idempotency semantics.
- **Source verification:** Failure tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-029`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0586` [ ] `FR-PORT-030` - Prevent direct writes by other domains.

- **Execution domain:** `Portfolio`.
- **Execution position:** `586` of `675`.
- **Cannot start before:** Step `P01-S0585` (`FR-PORT-029`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.7 `orchestration/` — Cross-Domain Workflow Coordination`` (line 418); matrix row `1074`.
- **Source requirement:** After reconciled execution, request Analytics measurement from immutable Trading facts; preserve executed-but-unmeasured truth on Analytics failure and support deterministic recomputation without rewriting execution.
- **Source verification:** `SYS-WF-008` integration tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0587` [ ] `P-PORT-008` - api feature/component (provisional)

- **Execution domain:** `Portfolio`.
- **Execution position:** `587` of `675`.
- **Cannot start before:** Step `P01-S0586` (`FR-PORT-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.8` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-PORT-008`); matrix row `1081`
- **Source file:** `__init__.py`
- **Source responsibility:** Expose supported package API only.
- **Source key exports:** `PortfolioService` and public contracts
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-PORT-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0588` [ ] `FR-PORT-034` - Expose construction, status, activation, drift/rebalance, rollback, and history operations.

- **Execution domain:** `Portfolio`.
- **Execution position:** `588` of `675`.
- **Cannot start before:** Step `P01-S0587` (`P-PORT-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.8 `api.py` — Public Portfolio API`` (line 433); matrix row `1075`.
- **Source requirement:** Expose construction, status, activation, drift/rebalance, rollback, and history operations.
- **Source verification:** API tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-034`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0589` [ ] `FR-PORT-035` - Accept AuthContext and request_id: str \

- **Execution domain:** `Portfolio`.
- **Execution position:** `589` of `675`.
- **Cannot start before:** Step `P01-S0588` (`FR-PORT-034`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.8 `api.py` — Public Portfolio API`` (line 434); matrix row `1076`.
- **Source requirement:** Accept `AuthContext` and `request_id: str \| None = None` on governed entry points.
- **Source verification:** Signature tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-035`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0590` [ ] `FR-PORT-036` - Return structured success/error envelopes; never None or raw exceptions.

- **Execution domain:** `Portfolio`.
- **Execution position:** `590` of `675`.
- **Cannot start before:** Step `P01-S0589` (`FR-PORT-035`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.8 `api.py` — Public Portfolio API`` (line 435); matrix row `1077`.
- **Source requirement:** Return structured success/error envelopes; never `None` or raw exceptions.
- **Source verification:** Contract tests
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-036`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0591` [ ] `FR-PORT-037` - Keep authentication and presentation logic outside Portfolio.

- **Execution domain:** `Portfolio`.
- **Execution position:** `591` of `675`.
- **Cannot start before:** Step `P01-S0590` (`FR-PORT-036`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 4. Module and Requirement Specifications > 4.8 `api.py` — Public Portfolio API`` (line 436); matrix row `1078`.
- **Source requirement:** Keep authentication and presentation logic outside Portfolio.
- **Source verification:** Import review
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-PORT-037`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0592` [ ] `WF-PORT-002` - Construct Allocation Candidate

- **Execution domain:** `Portfolio`.
- **Execution position:** `592` of `675`.
- **Cannot start before:** Step `P01-S0591` (`FR-PORT-037`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `Portfolio` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/portfolio/README.md` - ``Portfolio > 3. Workflows`` (line 169); matrix row `1082`.
- **Source scope:** Internal
- **Source workflow:** Construct allocation candidate
- **Source requirement sequence:** `FR-PORT-010 → FR-PORT-014`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-PORT-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 14 - UI/API

Implement `UI/API` in authoritative order: all component and functional work first, then domain workflows, capability acceptance, and NFR verification.

#### Step `P01-S0593` [ ] `P-API-001` - contracts feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `593` of `675`.
- **Cannot start before:** Step `P01-S0592` (`WF-PORT-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-001`); matrix row `1174`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0594` [ ] `FR-API-001` - Carry request/trace, route, operation, side-effect, timing, timestamp, stale, pagination, and idempotency-replay metadata.

- **Execution domain:** `UI/API`.
- **Execution position:** `594` of `675`.
- **Cannot start before:** Step `P01-S0593` (`P-API-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.1 `contracts/` — Boundary contracts`` (line 429); matrix row `1124`.
- **Source responsibility:** Carry request/trace, route, operation, side-effect, timing, timestamp, stale, pagination, and idempotency-replay metadata.
- **Source class / function / method:** `ApiMetadata(BaseModel)`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid or missing required metadata
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_contracts.py::test_usage_api_metadata()`<br>**Unit:** `tests/api/unit/test_contract_models.py::test_api_metadata_rejects_invalid_time()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0595` [ ] `FR-API-002` - Expose a bounded redacted error with deterministic code, message, details, request/trace IDs, and retryability.

- **Execution domain:** `UI/API`.
- **Execution position:** `595` of `675`.
- **Cannot start before:** Step `P01-S0594` (`FR-API-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.1 `contracts/` — Boundary contracts`` (line 430); matrix row `1125`.
- **Source responsibility:** Expose a bounded redacted error with deterministic code, message, details, request/trace IDs, and retryability.
- **Source class / function / method:** `ApiError(BaseModel)`
- **Source side effects:** None
- **Source raises:** `ValidationError`: unbounded or invalid error data
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_contracts.py::test_usage_api_error()`<br>**Unit:** `tests/api/unit/test_contract_models.py::test_api_error_bounds_details()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0596` [ ] `FR-API-003` - Return exactly status, message, data, error, and metadata for non-streaming responses; HTTP 204 has no body.

- **Execution domain:** `UI/API`.
- **Execution position:** `596` of `675`.
- **Cannot start before:** Step `P01-S0595` (`FR-API-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.1 `contracts/` — Boundary contracts`` (line 431); matrix row `1126`.
- **Source responsibility:** Return exactly `status`, `message`, `data`, `error`, and `metadata` for non-streaming responses; HTTP 204 has no body.
- **Source class / function / method:** `ApiResponse[T](BaseModel)`
- **Source side effects:** None
- **Source raises:** `ValidationError`: success/error fields conflict
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_contracts.py::test_usage_api_response()`<br>**Unit:** `tests/api/unit/test_contract_models.py::test_response_envelope_shape()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0597` [ ] `FR-API-004` - Validate ordered stream events with type, data, request/trace IDs, sequence, UTC timestamp, heartbeat, and terminal error.

- **Execution domain:** `UI/API`.
- **Execution position:** `597` of `675`.
- **Cannot start before:** Step `P01-S0596` (`FR-API-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.1 `contracts/` — Boundary contracts`` (line 432); matrix row `1127`.
- **Source responsibility:** Validate ordered stream events with type, data, request/trace IDs, sequence, UTC timestamp, heartbeat, and terminal error.
- **Source class / function / method:** `StreamEvent[T](BaseModel)`
- **Source side effects:** None
- **Source raises:** `ValidationError`: invalid sequence or event shape
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_contracts.py::test_usage_stream_event()`<br>**Unit:** `tests/api/unit/test_contract_models.py::test_stream_event_requires_sequence()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0598` [ ] `FR-API-005` - Declare classification, stability, method/path, auth, permission, schemas, status/errors, side effects, owner, pagination, idempotency, audit, rate class, an...

- **Execution domain:** `UI/API`.
- **Execution position:** `598` of `675`.
- **Cannot start before:** Step `P01-S0597` (`FR-API-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.1 `contracts/` — Boundary contracts`` (line 433); matrix row `1128`.
- **Source responsibility:** Declare classification, stability, method/path, auth, permission, schemas, status/errors, side effects, owner, pagination, idempotency, audit, rate class, and observability for each route/stream.
- **Source class / function / method:** `RouteContract(BaseModel)`
- **Source side effects:** None
- **Source raises:** `ValidationError`: required route metadata is absent
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_contracts.py::test_usage_route_contract()`<br>**Unit:** `tests/api/unit/test_contract_catalog.py::test_incomplete_route_contract_rejected()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0599` [ ] `FR-API-006` - Carry validated request, workflow, permission, approval, audit, idempotency, and safety context without granting authority itself.

- **Execution domain:** `UI/API`.
- **Execution position:** `599` of `675`.
- **Cannot start before:** Step `P01-S0598` (`FR-API-005`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.1 `contracts/` — Boundary contracts`` (line 434); matrix row `1129`.
- **Source responsibility:** Carry validated request, workflow, permission, approval, audit, idempotency, and safety context without granting authority itself.
- **Source class / function / method:** `GovernedRequestContext(BaseModel)`
- **Source side effects:** None
- **Source raises:** `ValidationError`: governed context is incomplete
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_contracts.py::test_usage_governed_context()`<br>**Unit:** `tests/api/unit/test_contract_models.py::test_governed_context_requires_ids()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0600` [ ] `FR-API-007` - Bound and redact current route, visible entity IDs, and approved actions before context leaves the frontend.

- **Execution domain:** `UI/API`.
- **Execution position:** `600` of `675`.
- **Cannot start before:** Step `P01-S0599` (`FR-API-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.1 `contracts/` — Boundary contracts`` (line 435); matrix row `1130`.
- **Source responsibility:** Bound and redact current route, visible entity IDs, and approved actions before context leaves the frontend.
- **Source class / function / method:** `PageContext(BaseModel)`
- **Source side effects:** None
- **Source raises:** `ValidationError`: context exceeds limit or contains forbidden fields
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_contracts.py::test_usage_page_context()`<br>**Unit:** `tests/api/unit/test_contract_models.py::test_page_context_rejects_secret()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0601` [ ] `FR-API-008` - Register each route contract exactly once and reject collisions or incomplete declarations.

- **Execution domain:** `UI/API`.
- **Execution position:** `601` of `675`.
- **Cannot start before:** Step `P01-S0600` (`FR-API-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.1` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.1 `contracts/` — Boundary contracts`` (line 436); matrix row `1131`.
- **Source responsibility:** Register each route contract exactly once and reject collisions or incomplete declarations.
- **Source class / function / method:** `register_route_contract(contract: RouteContract) -> None`
- **Source side effects:** Local state mutation
- **Source raises:** `ValueError`: duplicate or conflicting route contract
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_contracts.py::test_usage_register_contract()`<br>**Unit:** `tests/api/unit/test_contract_catalog.py::test_duplicate_contract_rejected()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0602` [ ] `P-API-002` - identity feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `602` of `675`.
- **Cannot start before:** Step `P01-S0601` (`FR-API-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.2` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-002`); matrix row `1175`
- **Source setting / limit:** `DATABASE_URL` / `DATA_DIR`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0603` [ ] `FR-API-013` - Produce Utils AuthContext from validated authority claims and trace context, never caller-controlled role headers.

- **Execution domain:** `UI/API`.
- **Execution position:** `603` of `675`.
- **Cannot start before:** Step `P01-S0602` (`P-API-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.2 `identity/` — Authentication and authorization`` (line 481); matrix row `1132`.
- **Source responsibility:** Produce Utils `AuthContext` from validated authority claims and trace context, never caller-controlled role headers.
- **Source class / function / method:** `build_auth_context(principal: AuthenticatedPrincipal, trace: TraceContext) -> AuthContext`
- **Source side effects:** None
- **Source raises:** `AuthenticationError`: authority claims cannot be verified
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_identity.py::test_usage_build_auth_context()`<br>**Unit:** `tests/api/unit/test_authorization.py::test_role_header_cannot_create_principal()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0604` [ ] `FR-API-014` - Enforce the approved permission at the backend boundary and return the standard 403 envelope on failure.

- **Execution domain:** `UI/API`.
- **Execution position:** `604` of `675`.
- **Cannot start before:** Step `P01-S0603` (`FR-API-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.2 `identity/` — Authentication and authorization`` (line 482); matrix row `1133`.
- **Source responsibility:** Enforce the approved permission at the backend boundary and return the standard 403 envelope on failure.
- **Source class / function / method:** `require_permission(context: AuthContext, permission: str) -> None`
- **Source side effects:** Read-only
- **Source raises:** `AuthorizationError`: permission absent
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_identity.py::test_usage_require_permission()`<br>**Unit:** `tests/api/unit/test_authorization.py::test_missing_permission_rejected()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0605` [ ] `FR-API-015` - Validate governed context, CSRF when applicable, approval scope, idempotency dependency, stale evidence, and audit intent before delegation.

- **Execution domain:** `UI/API`.
- **Execution position:** `605` of `675`.
- **Cannot start before:** Step `P01-S0604` (`FR-API-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.2 `identity/` — Authentication and authorization`` (line 483); matrix row `1134`.
- **Source responsibility:** Validate governed context, CSRF when applicable, approval scope, idempotency dependency, stale evidence, and audit intent before delegation.
- **Source class / function / method:** `validate_governed_request(context: AuthContext, governed: GovernedRequestContext) -> None`
- **Source side effects:** Read-only
- **Source raises:** `GovernanceError`: context missing/stale; `DependencyUnavailableError`: idempotency unavailable
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_identity.py::test_usage_validate_governed_request()`<br>**Unit:** `tests/api/unit/test_authorization.py::test_missing_governed_context_fails_closed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0606` [ ] `FR-API-057` - Encrypt credential material before persistence with authenticated encryption, store key ID/version and integrity metadata but never the key, select exactly t...

- **Execution domain:** `UI/API`.
- **Execution position:** `606` of `675`.
- **Cannot start before:** Step `P01-S0605` (`FR-API-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.2 `identity/` — Authentication and authorization`` (line 484); matrix row `1172`.
- **Source responsibility:** Encrypt credential material before persistence with authenticated encryption, store key ID/version and integrity metadata but never the key, select exactly the configured active key from an injected externally provisioned key set, and decrypt only for an authorized composition request.
- **Source class / function / method:** `store_credential`, `resolve_credential_reference`
- **Source side effects:** Persistence write/read
- **Source raises:** `CredentialSecurityError`: missing/multiple active keys, tamper, unknown key ID, unauthorized access, or storage failure
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_identity.py::test_usage_store_and_resolve_credential()`<br>**Unit:** `tests/api/unit/test_credentials.py::test_credential_store_never_persists_key_or_plaintext()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-057`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0607` [ ] `FR-API-058` - Resolve an opaque secret:// reference only at composition, build one immutable Brokers-owned BrokerConnectionConfig v1 with SecretStr values, and discard pla...

- **Execution domain:** `UI/API`.
- **Execution position:** `607` of `675`.
- **Cannot start before:** Step `P01-S0606` (`FR-API-057`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.2` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.2 `identity/` — Authentication and authorization`` (line 485); matrix row `1173`.
- **Source responsibility:** Resolve an opaque `secret://` reference only at composition, build one immutable Brokers-owned `BrokerConnectionConfig v1` with `SecretStr` values, and discard plaintext after construction without logging, caching, or returning it through UI/API contracts.
- **Source class / function / method:** `build_broker_connection_config`
- **Source side effects:** Credential-store read
- **Source raises:** `CredentialSecurityError`, `DependencyUnavailableError`: unsafe/unresolved reference, unavailable key source, or invalid Brokers config
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_composition.py::test_usage_build_broker_config()`<br>**Unit:** `tests/api/unit/test_broker_config.py::test_plaintext_never_crosses_composition_boundary()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-058`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0608` [ ] `P-API-003` - middleware feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `608` of `675`.
- **Cannot start before:** Step `P01-S0607` (`FR-API-058`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.3` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-003`); matrix row `1176`
- **Source setting / limit:** `RATE_LIMITS_BY_CLASS`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0609` [ ] `FR-API-017` - Create/validate request and correlation IDs, classify registered route intent, authenticate where required, and attach canonical context.

- **Execution domain:** `UI/API`.
- **Execution position:** `609` of `675`.
- **Cannot start before:** Step `P01-S0608` (`P-API-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.3` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.3 `middleware/` — Request security and context`` (line 513); matrix row `1135`.
- **Source responsibility:** Create/validate request and correlation IDs, classify registered route intent, authenticate where required, and attach canonical context.
- **Source class / function / method:** `RequestContextMiddleware`
- **Source side effects:** Local state mutation
- **Source raises:** `AuthenticationError`: protected request lacks valid authority; `ValidationError`: invalid identifiers
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_middleware.py::test_usage_request_context()`<br>**Unit:** `tests/api/unit/test_context.py::test_unknown_route_has_bounded_metadata()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-017`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0610` [ ] `P-API-004` - health feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `610` of `675`.
- **Cannot start before:** Step `P01-S0609` (`FR-API-017`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.4` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-004`); matrix row `1177`
- **Source file:** `application.py`
- **Source responsibility:** Build the single app with exact-origin CORS, middleware, routes, and probes
- **Source key exports:** `create_app`, `app`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0611` [ ] `FR-API-018` - Return HTTP 200 with coarse service status only when the process accepts requests; expose no private dependency data.

- **Execution domain:** `UI/API`.
- **Execution position:** `611` of `675`.
- **Cannot start before:** Step `P01-S0610` (`P-API-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.4 `health/` — Liveness and readiness`` (line 533); matrix row `1136`.
- **Source responsibility:** Return HTTP 200 with coarse service status only when the process accepts requests; expose no private dependency data.
- **Source class / function / method:** `get_liveness() -> ApiResponse[Liveness]`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_health.py::test_usage_liveness()`<br>**Unit:** `tests/api/unit/test_health.py::test_liveness_contains_no_private_data()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-018`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0612` [ ] `FR-API-019` - Return protected required/optional component readiness with degraded reasons and timestamps.

- **Execution domain:** `UI/API`.
- **Execution position:** `612` of `675`.
- **Cannot start before:** Step `P01-S0611` (`FR-API-018`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.4` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.4 `health/` — Liveness and readiness`` (line 534); matrix row `1137`.
- **Source responsibility:** Return protected required/optional component readiness with degraded reasons and timestamps.
- **Source class / function / method:** `get_readiness(context: AuthContext) -> ApiResponse[Readiness]`
- **Source side effects:** Read-only
- **Source raises:** `AuthorizationError`: detail not permitted; `DependencyUnavailableError`: required dependency failed
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_health.py::test_usage_readiness()`<br>**Unit:** `tests/api/unit/test_health.py::test_required_failure_is_not_healthy()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-019`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0613` [ ] `FR-API-020` - Translate a validated authoritative owner event into a redacted ordered StreamEvent.

- **Execution domain:** `UI/API`.
- **Execution position:** `613` of `675`.
- **Cannot start before:** Step `P01-S0612` (`FR-API-019`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.5 `streams/` — Ordered event delivery`` (line 554); matrix row `1138`.
- **Source responsibility:** Translate a validated authoritative owner event into a redacted ordered `StreamEvent`.
- **Source class / function / method:** `build_stream_event(event: OwnerEvent, trace: TraceContext) -> StreamEvent[Any]`
- **Source side effects:** None
- **Source raises:** `ValidationError`: malformed/secret-bearing event
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_streams.py::test_usage_build_stream_event()`<br>**Unit:** `tests/api/unit/test_stream_events.py::test_malformed_event_rejected()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-020`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0614` [ ] `FR-API-021` - Authenticate connection, enforce quota policy, deliver ordered events, detect gaps/backpressure, resume when supported, emit terminal errors, and clean up on...

- **Execution domain:** `UI/API`.
- **Execution position:** `614` of `675`.
- **Cannot start before:** Step `P01-S0613` (`FR-API-020`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.5` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.5 `streams/` — Ordered event delivery`` (line 555); matrix row `1139`.
- **Source responsibility:** Authenticate connection, enforce quota policy, deliver ordered events, detect gaps/backpressure, resume when supported, emit terminal errors, and clean up on disconnect.
- **Source class / function / method:** `StreamConnectionManager`
- **Source side effects:** Local state mutation; event publication
- **Source raises:** `AuthenticationError`; `StreamLimitError`; `StreamGapError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_streams.py::test_usage_stream_lifecycle()`<br>**Unit:** `tests/api/unit/test_stream_lifecycle.py::test_disconnect_releases_resources()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-021`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0615` [ ] `P-API-006` - routes feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `615` of `675`.
- **Cannot start before:** Step `P01-S0614` (`FR-API-021`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-006`); matrix row `1178`
- **Source type:** Imports
- **Source responsibility:** Import routes validate content type/size, duplicates, parse failure, cleanup, and compensating behavior before state publication.
- **Source verification:** Import security tests
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0616` [ ] `FR-API-022` - Expose typed registration, login, and logout without fallback identities.

- **Execution domain:** `UI/API`.
- **Execution position:** `616` of `675`.
- **Cannot start before:** Step `P01-S0615` (`P-API-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 598); matrix row `1140`.
- **Source responsibility:** Expose typed registration, login, and logout without fallback identities.
- **Source class / function / method:** `auth.router: APIRouter`
- **Source side effects:** Persistence write
- **Source raises:** Standard 400/401/403/422/429/500/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_auth_routes.py::test_usage_auth_routes()`<br>**Unit:** `tests/api/unit/test_auth_routes.py::test_auth_contracts()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-022`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0617` [ ] `FR-API-023` - Expose authenticated settings read/update through one canonical path.

- **Execution domain:** `UI/API`.
- **Execution position:** `617` of `675`.
- **Cannot start before:** Step `P01-S0616` (`FR-API-022`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 599); matrix row `1141`.
- **Source responsibility:** Expose authenticated settings read/update through one canonical path.
- **Source class / function / method:** `settings.router: APIRouter`
- **Source side effects:** Read-only; persistence write
- **Source raises:** Standard 401/403/404/409/422/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_settings_routes.py::test_usage_settings_routes()`<br>**Unit:** `tests/api/unit/test_settings_routes.py::test_settings_contracts()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-023`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0618` [ ] `FR-API-024` - Expose authenticated symbol discovery and bounded delegated dataset preparation.

- **Execution domain:** `UI/API`.
- **Execution position:** `618` of `675`.
- **Cannot start before:** Step `P01-S0617` (`FR-API-023`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 600); matrix row `1142`.
- **Source responsibility:** Expose authenticated symbol discovery and bounded delegated dataset preparation.
- **Source class / function / method:** `data.router: APIRouter`
- **Source side effects:** Read-only; external API call
- **Source raises:** Standard 401/403/413/422/502/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_data_routes.py::test_usage_data_routes()`<br>**Unit:** `tests/api/unit/test_data_routes.py::test_data_contracts()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0619` [ ] `FR-API-025` - Expose strategy catalogue/version reads and explicitly approved Optimization-result adoption through Strategy-owned registration/parameter commands, returnin...

- **Execution domain:** `UI/API`.
- **Execution position:** `619` of `675`.
- **Cannot start before:** Step `P01-S0618` (`FR-API-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 601); matrix row `1143`.
- **Source responsibility:** Expose strategy catalogue/version reads and explicitly approved Optimization-result adoption through Strategy-owned registration/parameter commands, returning `StrategyMutationResult v1`. Raw import/export, SQX parsing/scoring, executable content, and artifact lifecycle are absent.
- **Source class / function / method:** `strategies.router: APIRouter`
- **Source side effects:** Read-only; owner-domain persistence write
- **Source raises:** Standard 401/403/404/409/422/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_strategy_routes.py::test_usage_strategy_routes()`<br>**Unit:** `tests/api/unit/test_strategy_routes.py::test_strategy_contracts()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-025`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0620` [ ] `FR-API-026` - Expose one exact synchronous SimulationBacktestRequestV1 run returning a terminal SimulationResult/report or structured error through Simulation/Analytics; n...

- **Execution domain:** `UI/API`.
- **Execution position:** `620` of `675`.
- **Cannot start before:** Step `P01-S0619` (`FR-API-025`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 602); matrix row `1144`.
- **Source responsibility:** Expose one exact synchronous `SimulationBacktestRequestV1` run returning a terminal `SimulationResult`/report or structured error through Simulation/Analytics; no queued/query/session/log-stream lifecycle.
- **Source class / function / method:** `backtests.router: APIRouter`
- **Source side effects:** Read-only external-domain calls
- **Source raises:** Standard 401/403/409/422/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_backtest_routes.py::test_usage_synchronous_backtest_routes()`<br>**Unit:** `tests/api/unit/test_backtest_routes.py::test_backtest_contract_matches_adr_0014()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-026`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0621` [ ] `FR-API-028` - Expose position sizing, regime, allocation, governance, and bounded advisory scenario evaluation solely through Risk; scenario responses use registered Scena...

- **Execution domain:** `UI/API`.
- **Execution position:** `621` of `675`.
- **Cannot start before:** Step `P01-S0620` (`FR-API-026`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 604); matrix row `1145`.
- **Source responsibility:** Expose position sizing, regime, allocation, governance, and bounded advisory scenario evaluation solely through Risk; scenario responses use registered `ScenarioResult v1`.
- **Source class / function / method:** `risk.router: APIRouter`
- **Source side effects:** Read-only
- **Source raises:** Standard 401/403/409/422/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_risk_routes.py::test_usage_risk_routes()`<br>**Unit:** `tests/api/unit/test_risk_routes.py::test_risk_contracts()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-028`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0622` [ ] `FR-API-029` - Expose live/paper session lifecycle, reads, strategy assignment, governed orders/positions, and events solely through Trading after Risk clearance.

- **Execution domain:** `UI/API`.
- **Execution position:** `622` of `675`.
- **Cannot start before:** Step `P01-S0621` (`FR-API-028`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 605); matrix row `1146`.
- **Source responsibility:** Expose live/paper session lifecycle, reads, strategy assignment, governed orders/positions, and events solely through Trading after Risk clearance.
- **Source class / function / method:** `trading.router: APIRouter`
- **Source side effects:** Read-only; broker mutation; persistence write; event publication
- **Source raises:** Standard 401/403/404/409/422/503 envelopes; unknown broker state freezes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_trading_routes.py::test_usage_trading_routes()`<br>**Unit:** `tests/api/unit/test_trading_routes.py::test_live_gate_cannot_be_bypassed()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-029`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0623` [ ] `FR-API-030` - Expose bounded synchronous optimization, walk-forward, unsupervised, and Monte Carlo/scenario operations returning one terminal OptimizationResult or structu...

- **Execution domain:** `UI/API`.
- **Execution position:** `623` of `675`.
- **Cannot start before:** Step `P01-S0622` (`FR-API-029`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 606); matrix row `1147`.
- **Source responsibility:** Expose bounded synchronous optimization, walk-forward, unsupervised, and Monte Carlo/scenario operations returning one terminal `OptimizationResult` or structured error; no job persistence, cancellation, progress, or job WebSocket.
- **Source class / function / method:** `optimization.router: APIRouter`
- **Source side effects:** Read-only external-domain calls; in-memory calculation
- **Source raises:** Standard 401/403/409/413/422/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_optimization_routes.py::test_usage_synchronous_optimization_routes()`<br>**Unit:** `tests/api/unit/test_optimization_routes.py::test_async_job_routes_absent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-030`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0624` [ ] `FR-API-031` - Submit one bounded initial Research request and return only registered ResearchReport v1 advisory evidence; Research-internal datasets, stage profiles, score...

- **Execution domain:** `UI/API`.
- **Execution position:** `624` of `675`.
- **Cannot start before:** Step `P01-S0623` (`FR-API-030`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 607); matrix row `1148`.
- **Source responsibility:** Submit one bounded initial Research request and return only registered `ResearchReport v1` advisory evidence; Research-internal datasets, stage profiles, scorecards, snapshots, and artifact types never cross the API boundary directly.
- **Source class / function / method:** `research.router: APIRouter`
- **Source side effects:** Read-only external-domain call
- **Source raises:** Standard 401/403/409/413/422/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_research_routes.py::test_usage_research_routes()`<br>**Unit:** `tests/api/unit/test_research_routes.py::test_only_registered_report_crosses_boundary()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-031`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0625` [ ] `FR-API-032` - Expose broker/equity/summary/resource/market-hours/calendar snapshots with timestamps and stale/unavailable states; merge system status into readiness.

- **Execution domain:** `UI/API`.
- **Execution position:** `625` of `675`.
- **Cannot start before:** Step `P01-S0624` (`FR-API-031`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 608); matrix row `1149`.
- **Source responsibility:** Expose broker/equity/summary/resource/market-hours/calendar snapshots with timestamps and stale/unavailable states; merge system status into readiness.
- **Source class / function / method:** `dashboards.router: APIRouter`
- **Source side effects:** Read-only; external API call
- **Source raises:** Standard 401/403/404/422/502/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_dashboard_routes.py::test_usage_dashboard_routes()`<br>**Unit:** `tests/api/unit/test_dashboard_routes.py::test_currency_strength_absent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-032`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0626` [ ] `FR-API-034` - Authenticate/authorize a human operator; construct KillSwitchCommand v1 with explicit global/portfolio/strategy/symbol scope and applicable identifiers; subm...

- **Execution domain:** `UI/API`.
- **Execution position:** `626` of `675`.
- **Cannot start before:** Step `P01-S0625` (`FR-API-032`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 609); matrix row `1150`.
- **Source responsibility:** Authenticate/authorize a human operator; construct `KillSwitchCommand v1` with explicit `global`/`portfolio`/`strategy`/`symbol` scope and applicable identifiers; submit activation with separate `AuthContext`; require and submit a matching current `ApprovalAttestation v1` for clearance; and expose protected readiness/`OperationalEvent v1` views plus bounded Data-owned audit pages without issuing Risk tokens, policy verdicts, or direct store access.
- **Source class / function / method:** `operator.router: APIRouter`
- **Source side effects:** UI/API persistence write; event publication; Data read
- **Source raises:** Standard 401/403/404/409/422/503 envelopes
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_operator_routes.py::test_usage_operator_routes()`<br>**Unit:** `tests/api/unit/test_operator_routes.py::test_kill_switch_scope_and_clearance_attestation_are_required()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-034`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0627` [ ] `FR-API-056` - Expose Portfolio construction/result/history/drift and governed activation/rollback/rebalance operations through Portfolio, with Risk eligibility/review and ...

- **Execution domain:** `UI/API`.
- **Execution position:** `627` of `675`.
- **Cannot start before:** Step `P01-S0626` (`FR-API-034`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.6` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.6 `routes/` — Thin HTTP and streaming boundaries > Route-family functional requirements`` (line 610); matrix row `1171`.
- **Source responsibility:** Expose Portfolio construction/result/history/drift and governed activation/rollback/rebalance operations through Portfolio, with Risk eligibility/review and human approval where required; perform no gateway calculation or execution.
- **Source class / function / method:** `portfolio.router: APIRouter`
- **Source side effects:** Read-only; owner-domain persistence; governed command submission
- **Source raises:** Standard 401/403/404/409/422/503 envelopes; any stale/missing gate fails closed
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_portfolio_routes.py::test_usage_portfolio_routes()`<br>**Unit:** `tests/api/unit/test_portfolio_routes.py::test_gateway_cannot_bypass_risk_or_trading()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-056`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0628` [ ] `P-API-007` - composition feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `628` of `675`.
- **Cannot start before:** Step `P01-S0627` (`FR-API-056`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.7` component/package establishment before its functional behavior.
- **Delivery type:** Confirm component contract, then implement.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-007`); matrix row `1179`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0629` [ ] `FR-API-035` - Initialize required storage/migrations and approved cleanup/scheduler work, surface optional degradation, and close only gateway-owned resources.

- **Execution domain:** `UI/API`.
- **Execution position:** `629` of `675`.
- **Cannot start before:** Step `P01-S0628` (`P-API-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.7 `composition/` — Canonical application lifecycle`` (line 672); matrix row `1151`.
- **Source responsibility:** Initialize required storage/migrations and approved cleanup/scheduler work, surface optional degradation, and close only gateway-owned resources.
- **Source class / function / method:** `lifespan(app: FastAPI) -> AsyncIterator[None]`
- **Source side effects:** Local state mutation; approved persistence setup
- **Source raises:** `StartupError`: required dependency cannot initialize
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_application.py::test_usage_lifespan()`<br>**Unit:** `tests/api/unit/test_lifecycle.py::test_required_startup_failure_propagates()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-035`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0630` [ ] `FR-API-036` - Construct one canonical FastAPI app with configured exact-origin CORS, redaction/context middleware, required/optional routers, liveness, and readiness.

- **Execution domain:** `UI/API`.
- **Execution position:** `630` of `675`.
- **Cannot start before:** Step `P01-S0629` (`FR-API-035`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.7 `composition/` — Canonical application lifecycle`` (line 673); matrix row `1152`.
- **Source responsibility:** Construct one canonical FastAPI app with configured exact-origin CORS, redaction/context middleware, required/optional routers, liveness, and readiness.
- **Source class / function / method:** `create_app(config: ApiConfig) -> FastAPI`
- **Source side effects:** Local state mutation
- **Source raises:** `ConfigurationError`: unsafe or incomplete configuration
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_application.py::test_usage_create_app()`<br>**Unit:** `tests/api/unit/test_application.py::test_single_operator_and_general_app()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-036`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0631` [ ] `FR-API-037` - Expose the canonical ASGI application at app.services.api.main:app.

- **Execution domain:** `UI/API`.
- **Execution position:** `631` of `675`.
- **Cannot start before:** Step `P01-S0630` (`FR-API-036`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.7` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.7 `composition/` — Canonical application lifecycle`` (line 674); matrix row `1153`.
- **Source responsibility:** Expose the canonical ASGI application at `app.services.api.main:app`.
- **Source class / function / method:** `app: FastAPI`
- **Source side effects:** Multiple boundary effects
- **Source raises:** `StartupError`: required initialization fails
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_application.py::test_usage_asgi_app()`<br>**Unit:** `tests/api/unit/test_application.py::test_canonical_app_import()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-037`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0632` [ ] `P-API-008` - ui_clients feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `632` of `675`.
- **Cannot start before:** Step `P01-S0631` (`FR-API-037`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.8` component/package establishment before its functional behavior.
- **Delivery type:** Resolve specification before implementation.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-008`); matrix row `1180`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-008`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0633` [ ] `FR-API-038` - Send typed requests with configured base URL, approved auth transport, request/trace IDs, safe JSON/204 parsing, contract validation, one opt-in transient GE...

- **Execution domain:** `UI/API`.
- **Execution position:** `633` of `675`.
- **Cannot start before:** Step `P01-S0632` (`P-API-008`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.8 `ui/clients/` — Typed frontend transport`` (line 698); matrix row `1154`.
- **Source responsibility:** Send typed requests with configured base URL, approved auth transport, request/trace IDs, safe JSON/204 parsing, contract validation, one opt-in transient GET retry, and stale metadata.
- **Source class / function / method:** `request<T>(contract: RouteContract, options: RequestOptions) => Promise<ApiResponse<T>>`
- **Source side effects:** External API call; telemetry
- **Source raises:** `ApiClientError`: typed HTTP/contract/transport failure
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_clients.ts::testUsageRequest()`<br>**Unit:** `ui/clients/request.test.ts::parses204AndErrors()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-038`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0634` [ ] `FR-API-039` - Expose only data from a successful envelope without creating another transport stack.

- **Execution domain:** `UI/API`.
- **Execution position:** `634` of `675`.
- **Cannot start before:** Step `P01-S0633` (`FR-API-038`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.8 `ui/clients/` — Typed frontend transport`` (line 699); matrix row `1155`.
- **Source responsibility:** Expose only `data` from a successful envelope without creating another transport stack.
- **Source class / function / method:** `unwrapData<T>(response: ApiResponse<T>) => T`
- **Source side effects:** None
- **Source raises:** `ApiClientError`: response is not successful
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_clients.ts::testUsageUnwrapData()`<br>**Unit:** `ui/clients/request.test.ts::rejectsErrorEnvelope()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-039`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0635` [ ] `FR-API-040` - Carry status, code, request/trace IDs, retryability, and bounded details for frontend failures.

- **Execution domain:** `UI/API`.
- **Execution position:** `635` of `675`.
- **Cannot start before:** Step `P01-S0634` (`FR-API-039`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.8 `ui/clients/` — Typed frontend transport`` (line 700); matrix row `1156`.
- **Source responsibility:** Carry status, code, request/trace IDs, retryability, and bounded details for frontend failures.
- **Source class / function / method:** `ApiClientError extends Error`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_clients.ts::testUsageApiClientError()`<br>**Unit:** `ui/clients/request.test.ts::errorIsTraceable()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-040`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0636` [ ] `FR-API-041` - Provide one catalog containing typed clients only for auth, settings, data, strategies, backtests, simulation, risk, Trading, portfolio, optimization, core r...

- **Execution domain:** `UI/API`.
- **Execution position:** `636` of `675`.
- **Cannot start before:** Step `P01-S0635` (`FR-API-040`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.8` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.8 `ui/clients/` — Typed frontend transport`` (line 701); matrix row `1157`.
- **Source responsibility:** Provide one catalog containing typed clients only for auth, settings, data, strategies, backtests, simulation, risk, Trading, portfolio, optimization, core research, dashboards, docs, and operator contracts.
- **Source class / function / method:** `apiClients: ApiClients`
- **Source side effects:** External API call
- **Source raises:** `ApiClientError`: route contract fails
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_clients.ts::testUsageFocusedClients()`<br>**Unit:** `ui/clients/clients.contract.test.ts::clientsMatchRouteCatalog()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-041`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0637` [ ] `P-API-009` - ui_context feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `637` of `675`.
- **Cannot start before:** Step `P01-S0636` (`FR-API-041`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.9` component/package establishment before its functional behavior.
- **Delivery type:** Resolve specification before implementation.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-009`); matrix row `1181`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-009`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0638` [ ] `FR-API-042` - Recover the approved browser session, protect layouts, and clear/redirect on expiration without exposing credentials.

- **Execution domain:** `UI/API`.
- **Execution position:** `638` of `675`.
- **Cannot start before:** Step `P01-S0637` (`P-API-009`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.9 `ui/context/` — Session, governed, page, and stream context`` (line 726); matrix row `1158`.
- **Source responsibility:** Recover the approved browser session, protect layouts, and clear/redirect on expiration without exposing credentials.
- **Source class / function / method:** `AuthProvider(props: PropsWithChildren) => JSX.Element`
- **Source side effects:** Local state mutation; external API call
- **Source raises:** `ApiClientError`: session recovery fails
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_context.tsx::testUsageAuthProvider()`<br>**Unit:** `ui/context/auth.test.tsx::expiredSessionRedirects()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-042`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0639` [ ] `FR-API-043` - Register only bounded, redacted current page entities/actions for route-aware workflows.

- **Execution domain:** `UI/API`.
- **Execution position:** `639` of `675`.
- **Cannot start before:** Step `P01-S0638` (`FR-API-042`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.9 `ui/context/` — Session, governed, page, and stream context`` (line 727); matrix row `1159`.
- **Source responsibility:** Register only bounded, redacted current page entities/actions for route-aware workflows.
- **Source class / function / method:** `PageContextProvider(props: PageContextProps) => JSX.Element`
- **Source side effects:** Local state mutation
- **Source raises:** `PageContextError`: context is invalid
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_context.tsx::testUsagePageContext()`<br>**Unit:** `ui/context/page.test.tsx::secretsAreRejected()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-043`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0640` [ ] `FR-API-044` - Build governed options and block obviously incomplete/stale requests before fetch while treating backend checks as authoritative.

- **Execution domain:** `UI/API`.
- **Execution position:** `640` of `675`.
- **Cannot start before:** Step `P01-S0639` (`FR-API-043`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.9 `ui/context/` — Session, governed, page, and stream context`` (line 728); matrix row `1160`.
- **Source responsibility:** Build governed options and block obviously incomplete/stale requests before fetch while treating backend checks as authoritative.
- **Source class / function / method:** `buildGovernedOptions(input: GovernedInput) => GovernedRequestOptions`
- **Source side effects:** Telemetry publication
- **Source raises:** `GovernedPreflightError`: required client context missing
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_context.ts::testUsageGovernedOptions()`<br>**Unit:** `ui/context/governed.test.ts::missingApprovalBlocksFetch()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-044`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0641` [ ] `FR-API-045` - Validate ordered events, heartbeat/reconnect/backpressure/terminal behavior, clean up on disconnect, and refresh authoritative state after a gap.

- **Execution domain:** `UI/API`.
- **Execution position:** `641` of `675`.
- **Cannot start before:** Step `P01-S0640` (`FR-API-044`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.9` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.9 `ui/context/` — Session, governed, page, and stream context`` (line 729); matrix row `1161`.
- **Source responsibility:** Validate ordered events, heartbeat/reconnect/backpressure/terminal behavior, clean up on disconnect, and refresh authoritative state after a gap.
- **Source class / function / method:** `consumeStream<T>(contract: StreamContract<T>, options: StreamOptions) => AsyncIterable<StreamEvent<T>>`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `ApiClientError`; `StreamGapError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_context.ts::testUsageConsumeStream()`<br>**Unit:** `ui/context/streams.test.ts::gapTriggersRefresh()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-045`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0642` [ ] `P-API-010` - ui_components feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `642` of `675`.
- **Cannot start before:** Step `P01-S0641` (`FR-API-045`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.10` component/package establishment before its functional behavior.
- **Delivery type:** Resolve specification before implementation.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-010`); matrix row `1182`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-010`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0643` [ ] `FR-API-046` - Provide accessible shell/navigation/error boundary and render stale/offline/unavailable states without hiding governed controls.

- **Execution domain:** `UI/API`.
- **Execution position:** `643` of `675`.
- **Cannot start before:** Step `P01-S0642` (`P-API-010`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.10 `ui/components/` — Approved workflow presentation`` (line 756); matrix row `1162`.
- **Source responsibility:** Provide accessible shell/navigation/error boundary and render stale/offline/unavailable states without hiding governed controls.
- **Source class / function / method:** `AppShell(props: AppShellProps) => JSX.Element`
- **Source side effects:** Local state mutation
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_components.tsx::testUsageAppShell()`<br>**Unit:** `ui/components/shell.test.tsx::controlsRemainAccessible()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-046`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0644` [ ] `FR-API-047` - Render approved dashboard snapshots with time/freshness and without currency strength.

- **Execution domain:** `UI/API`.
- **Execution position:** `644` of `675`.
- **Cannot start before:** Step `P01-S0643` (`FR-API-046`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.10 `ui/components/` — Approved workflow presentation`` (line 757); matrix row `1163`.
- **Source responsibility:** Render approved dashboard snapshots with time/freshness and without currency strength.
- **Source class / function / method:** `DashboardView(props: DashboardProps) => JSX.Element`
- **Source side effects:** None
- **Source raises:** None
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_components.tsx::testUsageDashboard()`<br>**Unit:** `ui/components/dashboard.test.tsx::staleSnapshotWarns()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-047`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0645` [ ] `FR-API-048` - Render registered strategy catalogue/version commands using typed clients only; raw import/export/SQX controls are absent.

- **Execution domain:** `UI/API`.
- **Execution position:** `645` of `675`.
- **Cannot start before:** Step `P01-S0644` (`FR-API-047`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.10 `ui/components/` — Approved workflow presentation`` (line 758); matrix row `1164`.
- **Source responsibility:** Render registered strategy catalogue/version commands using typed clients only; raw import/export/SQX controls are absent.
- **Source class / function / method:** `StrategyWorkspace(props: StrategyWorkspaceProps) => JSX.Element`
- **Source side effects:** External API call through client
- **Source raises:** `ApiClientError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_components.tsx::testUsageStrategies()`<br>**Unit:** `ui/components/strategies.test.tsx::usesTypedClient()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-048`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0646` [ ] `FR-API-049` - Render synchronous backtest configuration, local request activity, and completed SimulationResult/report evidence without presenting local activity as author...

- **Execution domain:** `UI/API`.
- **Execution position:** `646` of `675`.
- **Cannot start before:** Step `P01-S0645` (`FR-API-048`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.10 `ui/components/` — Approved workflow presentation`` (line 759); matrix row `1165`.
- **Source responsibility:** Render synchronous backtest configuration, local request activity, and completed `SimulationResult`/report evidence without presenting local activity as authoritative domain progress or exposing interactive controls.
- **Source class / function / method:** `SimulationWorkspace(props: SimulationWorkspaceProps) => JSX.Element`
- **Source side effects:** External API call through client
- **Source raises:** `ApiClientError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_components.tsx::testUsageSimulation()`<br>**Unit:** `ui/components/simulation.test.tsx::interactive_controls_are_absent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-049`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0647` [ ] `FR-API-050` - Render Risk decision support and live/paper Trading monitoring/controls only when authoritative backend gates are available.

- **Execution domain:** `UI/API`.
- **Execution position:** `647` of `675`.
- **Cannot start before:** Step `P01-S0646` (`FR-API-049`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.10 `ui/components/` — Approved workflow presentation`` (line 760); matrix row `1166`.
- **Source responsibility:** Render Risk decision support and live/paper Trading monitoring/controls only when authoritative backend gates are available.
- **Source class / function / method:** `TradingWorkspace(props: TradingWorkspaceProps) => JSX.Element`
- **Source side effects:** External API call through client
- **Source raises:** `ApiClientError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_components.tsx::testUsageTrading()`<br>**Unit:** `ui/components/trading.test.tsx::closedGateDisablesMutation()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-050`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0648` [ ] `FR-API-051` - Render terminal synchronous OptimizationResult and registered ResearchReport evidence, excluding optimization job progress/cancellation and direct Research-i...

- **Execution domain:** `UI/API`.
- **Execution position:** `648` of `675`.
- **Cannot start before:** Step `P01-S0647` (`FR-API-050`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.10` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.10 `ui/components/` — Approved workflow presentation`` (line 761); matrix row `1167`.
- **Source responsibility:** Render terminal synchronous `OptimizationResult` and registered `ResearchReport` evidence, excluding optimization job progress/cancellation and direct Research-internal profile/scorecard/snapshot views.
- **Source class / function / method:** `ResearchWorkspace(props: ResearchWorkspaceProps) => JSX.Element`
- **Source side effects:** External API call through client
- **Source raises:** `ApiClientError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_components.tsx::testUsageResearch()`<br>**Unit:** `ui/components/research.test.tsx::unregistered_research_types_are_absent()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-051`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0649` [ ] `P-API-011` - ui_app feature/component (provisional)

- **Execution domain:** `UI/API`.
- **Execution position:** `649` of `675`.
- **Cannot start before:** Step `P01-S0648` (`FR-API-051`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.11` component/package establishment before its functional behavior.
- **Delivery type:** Resolve specification before implementation.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``Appendix P — Provisional Component Requirements`` (`P-API-011`); matrix row `1183`
**Specification control:** Promoted roadmap requirement (authoritative): implement the named component seam and the behavior in the authoritative source, fixing the public seam from first implementation and adding depth behind it in later phases; if a normative contract or acceptance condition is still absent, stop for owner resolution.
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `P-API-011`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0650` [ ] `FR-API-053` - Render login/register routes and recover cleanly from invalid or expired sessions.

- **Execution domain:** `UI/API`.
- **Execution position:** `650` of `675`.
- **Cannot start before:** Step `P01-S0649` (`P-API-011`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.11` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.11 `ui/app/` — Protected workflow pages`` (line 784); matrix row `1168`.
- **Source responsibility:** Render login/register routes and recover cleanly from invalid or expired sessions.
- **Source class / function / method:** `AuthenticationPage(props: AuthenticationPageProps) => JSX.Element`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `ApiClientError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_pages.tsx::testUsageAuthenticationPages()`<br>**Unit:** `ui/app/auth.e2e.test.ts::loginLogoutRecovery()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-053`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0651` [ ] `FR-API-054` - Protect dashboard, settings, strategies, backtests, simulation, risk, live, optimization, and Edge Lab routes.

- **Execution domain:** `UI/API`.
- **Execution position:** `651` of `675`.
- **Cannot start before:** Step `P01-S0650` (`FR-API-053`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.11` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.11 `ui/app/` — Protected workflow pages`` (line 785); matrix row `1169`.
- **Source responsibility:** Protect dashboard, settings, strategies, backtests, simulation, risk, live, optimization, and Edge Lab routes.
- **Source class / function / method:** `ProtectedLayout(props: PropsWithChildren) => JSX.Element`
- **Source side effects:** External API call; local state mutation
- **Source raises:** `ApiClientError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_pages.tsx::testUsageProtectedLayout()`<br>**Unit:** `ui/app/protected.e2e.test.ts::unauthenticatedAccessRedirects()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-054`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0652` [ ] `FR-API-055` - Compose an approved workflow route exclusively from public clients, context, and workflow components.

- **Execution domain:** `UI/API`.
- **Execution position:** `652` of `675`.
- **Cannot start before:** Step `P01-S0651` (`FR-API-054`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` module `4.11` functional behavior in authoritative source order.
- **Delivery type:** Functional implementation.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 4. Module and Requirement Specifications > 4.11 `ui/app/` — Protected workflow pages`` (line 786); matrix row `1170`.
- **Source responsibility:** Compose an approved workflow route exclusively from public clients, context, and workflow components.
- **Source class / function / method:** `WorkflowPage(props: WorkflowPageProps) => JSX.Element`
- **Source side effects:** External API call through clients
- **Source raises:** `ApiClientError`
- **Source usage / test:** **Usage:** `tests/api/usage/test_usage_frontend_pages.tsx::testUsageApprovedPages()`<br>**Unit:** `ui/app/pages.contract.test.ts::everyPageHasClientContract()`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `FR-API-055`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0653` [ ] `WF-API-001` - Internal

- **Execution domain:** `UI/API`.
- **Execution position:** `653` of `675`.
- **Cannot start before:** Step `P01-S0652` (`FR-API-055`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 3. Workflows > Workflow manifest`` (line 354); matrix row `1184`.
- **Source scope:** Internal
- **Source workflow:** Gateway startup and readiness
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-API-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0654` [ ] `WF-API-002` - Internal

- **Execution domain:** `UI/API`.
- **Execution position:** `654` of `675`.
- **Cannot start before:** Step `P01-S0653` (`WF-API-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 3. Workflows > Workflow manifest`` (line 355); matrix row `1185`.
- **Source scope:** Internal
- **Source workflow:** Authenticated request boundary
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-API-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0655` [ ] `WF-API-004` - Cross-domain

- **Execution domain:** `UI/API`.
- **Execution position:** `655` of `675`.
- **Cannot start before:** Step `P01-S0654` (`WF-API-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 3. Workflows > Workflow manifest`` (line 357); matrix row `1186`.
- **Source scope:** Cross-domain
- **Source workflow:** Market data and dataset preparation
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-API-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0656` [ ] `WF-API-006` - Cross-domain

- **Execution domain:** `UI/API`.
- **Execution position:** `656` of `675`.
- **Cannot start before:** Step `P01-S0655` (`WF-API-004`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 3. Workflows > Workflow manifest`` (line 359); matrix row `1187`.
- **Source scope:** Cross-domain
- **Source workflow:** Synchronous backtest run and result review
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-API-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0657` [ ] `WF-API-012` - Cross-domain

- **Execution domain:** `UI/API`.
- **Execution position:** `657` of `675`.
- **Cannot start before:** Step `P01-S0656` (`WF-API-006`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 3. Workflows > Workflow manifest`` (line 365); matrix row `1188`.
- **Source scope:** Cross-domain
- **Source workflow:** Live/paper session and governed broker action
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-API-012`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0658` [ ] `WF-API-013` - Cross-domain

- **Execution domain:** `UI/API`.
- **Execution position:** `658` of `675`.
- **Cannot start before:** Step `P01-S0657` (`WF-API-012`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 3. Workflows > Workflow manifest`` (line 366); matrix row `1189`.
- **Source scope:** Cross-domain
- **Source workflow:** Operator approval, scoped kill switch, and event review
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-API-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0659` [ ] `WF-API-015` - Cross-domain

- **Execution domain:** `UI/API`.
- **Execution position:** `659` of `675`.
- **Cannot start before:** Step `P01-S0658` (`WF-API-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` workflow integration after all component and functional steps in this phase.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 3. Workflows > Workflow manifest`` (line 367); matrix row `1190`.
- **Source scope:** Cross-domain
- **Source workflow:** Frontend governed request
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `WF-API-015`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0660` [ ] `CAP-UI-001` - canonical composition/lifecycle

- **Execution domain:** `UI/API`.
- **Execution position:** `660` of `675`.
- **Cannot start before:** Step `P01-S0659` (`WF-API-015`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 2. Final Package Structure > Reconciliation coverage manifest`` (line 293); matrix row `1117`.
- **Source final destination:** `composition/`, `health/`; `FR-API-018`, `FR-API-019`, `FR-API-035`–`FR-API-037`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-UI-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0661` [ ] `CAP-UI-002` - contracts/envelopes/errors

- **Execution domain:** `UI/API`.
- **Execution position:** `661` of `675`.
- **Cannot start before:** Step `P01-S0660` (`CAP-UI-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 2. Final Package Structure > Reconciliation coverage manifest`` (line 294); matrix row `1118`.
- **Source final destination:** `contracts/`; `FR-API-001`–`FR-API-008`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-UI-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0662` [ ] `CAP-UI-003` - canonical identity/sessions

- **Execution domain:** `UI/API`.
- **Execution position:** `662` of `675`.
- **Cannot start before:** Step `P01-S0661` (`CAP-UI-002`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 2. Final Package Structure > Reconciliation coverage manifest`` (line 295); matrix row `1119`.
- **Source final destination:** `identity/`; `FR-API-009`–`FR-API-013`; UI/API-owned state and opaque-cookie/bearer transport
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-UI-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0663` [ ] `CAP-UI-007` - operator approvals/events

- **Execution domain:** `UI/API`.
- **Execution position:** `663` of `675`.
- **Cannot start before:** Step `P01-S0662` (`CAP-UI-003`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 2. Final Package Structure > Reconciliation coverage manifest`` (line 299); matrix row `1120`.
- **Source final destination:** `routes/operator.py`; `FR-API-034`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-UI-007`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0664` [ ] `CAP-UI-013` - risk decision support

- **Execution domain:** `UI/API`.
- **Execution position:** `664` of `675`.
- **Cannot start before:** Step `P01-S0663` (`CAP-UI-007`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 2. Final Package Structure > Reconciliation coverage manifest`` (line 305); matrix row `1121`.
- **Source final destination:** `routes/risk.py`; `FR-API-028`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-UI-013`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0665` [ ] `CAP-UI-014` - live monitoring/mutations

- **Execution domain:** `UI/API`.
- **Execution position:** `665` of `675`.
- **Cannot start before:** Step `P01-S0664` (`CAP-UI-013`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 2. Final Package Structure > Reconciliation coverage manifest`` (line 306); matrix row `1122`.
- **Source final destination:** `routes/trading.py`; `FR-API-029`; owner corrected to Trading by system ADRs
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-UI-014`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0666` [ ] `CAP-UI-024` - contract/security/workflow tests

- **Execution domain:** `UI/API`.
- **Execution position:** `666` of `675`.
- **Cannot start before:** Step `P01-S0665` (`CAP-UI-014`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** `UI/API` capability acceptance after its functional and workflow steps.
- **Delivery type:** Capability delivery.
- **Classification:** T0 Skeleton; size `M`; source status `Specified`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `app/services/api/README.md` - ``UI/API > 2. Final Package Structure > Reconciliation coverage manifest`` (line 315); matrix row `1123`.
- **Source final destination:** Section 7 and `NFR-API-001`–`NFR-API-018`
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `CAP-UI-024`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 15 - System integration workflows

Execute cross-domain workflows only after every contributing domain stage above is complete and evidenced.

#### Step `P01-S0667` [ ] `SYS-WF-001` - Historical backtest

- **Execution domain:** `System`.
- **Execution position:** `667` of `675`.
- **Cannot start before:** Step `P01-S0666` (`CAP-UI-024`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** Cross-domain integration after all contributing domain stages.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 4. Cross-Domain Workflows > Status and scope`` (line 380); matrix row `19`.
- **Source workflow:** Historical backtest
- **Source final outcome:** Deterministic simulation result and performance report
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-WF-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0668` [ ] `SYS-WF-005` - Operator monitoring and kill switch

- **Execution domain:** `System`.
- **Execution position:** `668` of `675`.
- **Cannot start before:** Step `P01-S0667` (`SYS-WF-001`).
- **Authoritative dependency evidence:** `P-SYS-001` at Step `P01-S0001`.
- **Ordering basis:** Cross-domain integration after all contributing domain stages.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `P-SYS-001`.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 4. Cross-Domain Workflows > Status and scope`` (line 384); matrix row `21`.
- **Source workflow:** Operator monitoring and kill switch
- **Source final outcome:** Kill switch engaged; all execution halted fail-closed
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-WF-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0669` [ ] `SYS-WF-002` - Signal to live execution

- **Execution domain:** `System`.
- **Execution position:** `669` of `675`.
- **Cannot start before:** Step `P01-S0668` (`SYS-WF-005`).
- **Authoritative dependency evidence:** `SYS-WF-001` at Step `P01-S0667`; `SYS-WF-005` at Step `P01-S0668`.
- **Ordering basis:** Cross-domain integration after all contributing domain stages.
- **Delivery type:** Workflow integration.
- **Classification:** T0 Skeleton; size `M`; source status `Missing`.
- **Dependencies:** `SYS-WF-001`, `SYS-WF-005`.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 4. Cross-Domain Workflows > Status and scope`` (line 381); matrix row `20`.
- **Source workflow:** Signal to live execution
- **Source final outcome:** Approved order executed at broker, reconciled and recorded
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-WF-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

### Stage 16 - System quality verification

Verify system-wide qualities only after domain delivery and cross-domain workflow integration are complete.

#### Step `P01-S0670` [ ] `SYS-NFR-001` - Architecture

- **Execution domain:** `System`.
- **Execution position:** `670` of `675`.
- **Cannot start before:** Step `P01-S0669` (`SYS-WF-002`).
- **Authoritative dependency evidence:** None beyond the immediately preceding ledger step and phase entry gate.
- **Ordering basis:** System quality verification after system workflow integration.
- **Delivery type:** Quality and constraint verification.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** None.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 7. System-Wide Requirements`` (line 894); matrix row `13`.
- **Source type:** Architecture
- **Source responsibility:** Domains shall communicate only through documented public contracts; no internal imports across domains.
- **Source verification:** Dependency audit
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-NFR-001`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0671` [ ] `SYS-NFR-002` - Maintainability

- **Execution domain:** `System`.
- **Execution position:** `671` of `675`.
- **Cannot start before:** Step `P01-S0670` (`SYS-NFR-001`).
- **Authoritative dependency evidence:** None beyond the immediately preceding ledger step and phase entry gate.
- **Ordering basis:** System quality verification after system workflow integration.
- **Delivery type:** Quality and constraint verification.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** None.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 7. System-Wide Requirements`` (line 895); matrix row `14`.
- **Source type:** Maintainability
- **Source responsibility:** Each responsibility shall have exactly one owning domain.
- **Source verification:** Ownership review
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-NFR-002`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0672` [ ] `SYS-NFR-003` - Reliability

- **Execution domain:** `System`.
- **Execution position:** `672` of `675`.
- **Cannot start before:** Step `P01-S0671` (`SYS-NFR-002`).
- **Authoritative dependency evidence:** None beyond the immediately preceding ledger step and phase entry gate.
- **Ordering basis:** System quality verification after system workflow integration.
- **Delivery type:** Quality and constraint verification.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** None.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 7. System-Wide Requirements`` (line 896); matrix row `15`.
- **Source type:** Reliability
- **Source responsibility:** The system shall fail closed: unverifiable safety, context, or broker state blocks execution.
- **Source verification:** Integration test
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-NFR-003`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0673` [ ] `SYS-NFR-004` - Security

- **Execution domain:** `System`.
- **Execution position:** `673` of `675`.
- **Cannot start before:** Step `P01-S0672` (`SYS-NFR-003`).
- **Authoritative dependency evidence:** None beyond the immediately preceding ledger step and phase entry gate.
- **Ordering basis:** System quality verification after system workflow integration.
- **Delivery type:** Quality and constraint verification.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** None.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 7. System-Wide Requirements`` (line 897); matrix row `16`.
- **Source type:** Security
- **Source responsibility:** No secret shall appear in any log, error, audit event, or returned diagnostic.
- **Source verification:** Security test
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-NFR-004`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0674` [ ] `SYS-NFR-005` - Determinism

- **Execution domain:** `System`.
- **Execution position:** `674` of `675`.
- **Cannot start before:** Step `P01-S0673` (`SYS-NFR-004`).
- **Authoritative dependency evidence:** None beyond the immediately preceding ledger step and phase entry gate.
- **Ordering basis:** System quality verification after system workflow integration.
- **Delivery type:** Quality and constraint verification.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** None.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 7. System-Wide Requirements`` (line 898); matrix row `17`.
- **Source type:** Determinism
- **Source responsibility:** Historical processing and construction (Indicators, Simulation, Optimization, Portfolio) shall be deterministic and reproducible.
- **Source verification:** Replay test
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-NFR-005`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

#### Step `P01-S0675` [ ] `SYS-NFR-006` - Observability

- **Execution domain:** `System`.
- **Execution position:** `675` of `675`.
- **Cannot start before:** Step `P01-S0674` (`SYS-NFR-005`).
- **Authoritative dependency evidence:** None beyond the immediately preceding ledger step and phase entry gate.
- **Ordering basis:** System quality verification after system workflow integration.
- **Delivery type:** Quality and constraint verification.
- **Classification:** T0 Skeleton; size `S`; source status `Missing`.
- **Dependencies:** None.
- **Authoritative source:** `docs/PROJECT.md` - ``HaruQuantAI > 7. System-Wide Requirements`` (line 899); matrix row `18`.
- **Source type:** Observability
- **Source responsibility:** Every cross-domain action shall carry trace/correlation IDs. Governed actions and durable mutations shall emit redacted audit events through the explicit producer policy in Section 5.
- **Source verification:** Inspection / test
- **Implementation instructions:**
  1. Confirm every dependency above is complete and evidenced before starting `SYS-NFR-006`.
  2. Implement only the behavior, boundary, side effects, and failure semantics in the authoritative source; preserve final public seams from the first implementation.
  3. Add or update the source-named unit and usage tests. Add integration and negative-path coverage when the item crosses a domain or performs I/O.
  4. Run the narrow test files first, then the phase quality gates. Do not mark complete on mocked proof when the requirement demands an actual provider or persisted-state boundary.
- **Acceptance evidence required:** implementation `path:line`; targeted test `path:line`; passing command/result; contract or runtime artifact when specified.
- **Evidence:** Pending. Replace this line only at completion with concrete `path:line` evidence.

## 9. Phase exit demonstration

Execute the roadmap exit criteria exactly as written in Section 2.2. Before the demonstration:

- [ ] All Section 8 requirement items are checked with valid evidence. Evidence: Pending.
- [ ] All Section 7 integration gates pass. Evidence: Pending.
- [ ] Required provider credentials and environment selectors are loaded without logging secret values. Evidence: Pending.
- [ ] Any broker-mutating route proves `MT5_ENVIRONMENT=demo` and all required authorization/risk/kill-switch gates immediately before dispatch. Evidence: Pending.
- [ ] Demonstration inputs, configuration identity, artifact hashes, provider-returned facts, and timestamps are captured. Evidence: Pending.
- [ ] Failure and recovery evidence is captured without fabricating a successful result. Evidence: Pending.

## 10. Phase-close documentation reconciliation

Only after the exit demonstration passes:

1. Update each affected domain README status from `Missing`/`Partial` to the factual implemented state.
2. Cross-reference implemented files, public exports, usage examples, tests, migrations, and operational commands.
3. Remove resolved owner-decision rows and write the resolution as an ordinary authoritative requirement or boundary.
4. Update `docs/ARCHITECTURE.md` for implemented architecture, API, model, dependency, persistence, or runtime changes.
5. Update `docs/CHANGELOG.md` with completed scope, validation evidence, and resolved decisions.
6. Re-run the phase coverage audit and confirm no requirement was moved, duplicated, or silently omitted.
7. Record final README and active-document `path:line` evidence below.

- [ ] Affected domain READMEs reflect factual end-of-phase implementation state. Evidence: Pending.
- [ ] Architecture and changelog updates are complete and consistent. Evidence: Pending.
- [ ] Requirement coverage remains exact after documentation reconciliation. Evidence: Pending.

## 11. Traceability and completion summary

- **Expected assigned IDs:** 675.
- **Plan requirement entries:** 675.
- **Matrix phase:** `1`.
- **Unchecked items at plan creation:** all.
- **Completion rule:** zero unchecked requirement entries, zero missing evidence references, and all exit/close gates checked.

### 11.1 Assigned ID manifest

This manifest is a coverage index only. It must not be used to choose the next implementation task.

- **System (11):** `P-SYS-001`, `P-SYS-002`, `SYS-NFR-001`, `SYS-NFR-002`, `SYS-NFR-003`, `SYS-NFR-004`, `SYS-NFR-005`, `SYS-NFR-006`, `SYS-WF-001`, `SYS-WF-002`, `SYS-WF-005`
- **Utils (38):** `FR-UTL-001`, `FR-UTL-002`, `FR-UTL-003`, `FR-UTL-004`, `FR-UTL-005`, `FR-UTL-006`, `FR-UTL-007`, `FR-UTL-008`, `FR-UTL-009`, `FR-UTL-010`, `FR-UTL-011`, `FR-UTL-012`, `FR-UTL-013`, `FR-UTL-014`, `FR-UTL-015`, `FR-UTL-021`, `FR-UTL-022`, `FR-UTL-025`, `FR-UTL-026`, `FR-UTL-027`, `FR-UTL-028`, `FR-UTL-029`, `FR-UTL-030`, `FR-UTL-031`, `FR-UTL-032`, `FR-UTL-033`, `FR-UTL-034`, `FR-UTL-035`, `FR-UTL-039`, `FR-UTL-040`, `FR-UTL-041`, `P-UTL-001`, `P-UTL-002`, `P-UTL-003`, `P-UTL-004`, `P-UTL-005`, `P-UTL-008`, `WF-UTL-001`
- **Brokers (125):** `CAP-BRK-001`, `CAP-BRK-003`, `CAP-BRK-012`, `FR-BRK-001`, `FR-BRK-002`, `FR-BRK-003`, `FR-BRK-004`, `FR-BRK-005`, `FR-BRK-006`, `FR-BRK-007`, `FR-BRK-008`, `FR-BRK-009`, `FR-BRK-010`, `FR-BRK-011`, `FR-BRK-012`, `FR-BRK-013`, `FR-BRK-014`, `FR-BRK-015`, `FR-BRK-016`, `FR-BRK-017`, `FR-BRK-018`, `FR-BRK-019`, `FR-BRK-020`, `FR-BRK-021`, `FR-BRK-022`, `FR-BRK-023`, `FR-BRK-024`, `FR-BRK-025`, `FR-BRK-026`, `FR-BRK-027`, `FR-BRK-028`, `FR-BRK-029`, `FR-BRK-030`, `FR-BRK-031`, `FR-BRK-032`, `FR-BRK-033`, `FR-BRK-034`, `FR-BRK-035`, `FR-BRK-036`, `FR-BRK-037`, `FR-BRK-038`, `FR-BRK-039`, `FR-BRK-040`, `FR-BRK-041`, `FR-BRK-042`, `FR-BRK-043`, `FR-BRK-044`, `FR-BRK-045`, `FR-BRK-046`, `FR-BRK-047`, `FR-BRK-048`, `FR-BRK-049`, `FR-BRK-050`, `FR-BRK-051`, `FR-BRK-052`, `FR-BRK-053`, `FR-BRK-054`, `FR-BRK-055`, `FR-BRK-056`, `FR-BRK-057`, `FR-BRK-058`, `FR-BRK-059`, `FR-BRK-060`, `FR-BRK-061`, `FR-BRK-062`, `FR-BRK-063`, `FR-BRK-064`, `FR-BRK-065`, `FR-BRK-066`, `FR-BRK-067`, `FR-BRK-068`, `FR-BRK-069`, `FR-BRK-070`, `FR-BRK-071`, `FR-BRK-072`, `FR-BRK-073`, `FR-BRK-074`, `FR-BRK-075`, `FR-BRK-076`, `FR-BRK-077`, `FR-BRK-078`, `FR-BRK-079`, `FR-BRK-080`, `FR-BRK-081`, `FR-BRK-082`, `FR-BRK-083`, `FR-BRK-084`, `FR-BRK-085`, `FR-BRK-086`, `FR-BRK-087`, `FR-BRK-088`, `FR-BRK-089`, `FR-BRK-090`, `FR-BRK-091`, `FR-BRK-092`, `FR-BRK-093`, `FR-BRK-094`, `FR-BRK-095`, `FR-BRK-096`, `FR-BRK-097`, `FR-BRK-098`, `FR-BRK-099`, `FR-BRK-100`, `FR-BRK-101`, `FR-BRK-102`, `FR-BRK-103`, `FR-BRK-104`, `FR-BRK-105`, `FR-BRK-110`, `FR-BRK-111`, `FR-BRK-112`, `FR-BRK-113`, `FR-BRK-114`, `FR-BRK-116`, `FR-BRK-117`, `FR-BRK-118`, `FR-BRK-133`, `P-BRK-001`, `P-BRK-003`, `P-BRK-004`, `WF-BRK-001`, `WF-BRK-002`, `WF-BRK-003`, `WF-BRK-008`, `WF-BRK-009`
- **Data (38):** `CAP-DATA-003`, `CAP-DATA-004`, `FR-DATA-001`, `FR-DATA-002`, `FR-DATA-003`, `FR-DATA-004`, `FR-DATA-005`, `FR-DATA-006`, `FR-DATA-007`, `FR-DATA-008`, `FR-DATA-010`, `FR-DATA-011`, `FR-DATA-012`, `FR-DATA-013`, `FR-DATA-016`, `FR-DATA-023`, `FR-DATA-026`, `FR-DATA-028`, `FR-DATA-030`, `FR-DATA-031`, `FR-DATA-032`, `FR-DATA-033`, `FR-DATA-034`, `FR-DATA-035`, `FR-DATA-036`, `FR-DATA-039`, `FR-DATA-041`, `FR-DATA-046`, `FR-DATA-075`, `FR-DATA-076`, `FR-DATA-078`, `FR-DATA-079`, `P-DATA-001`, `P-DATA-004`, `P-DATA-008`, `WF-DATA-001`, `WF-DATA-013`, `WF-DATA-014`
- **Indicators (21):** `FR-INDI-001`, `FR-INDI-002`, `FR-INDI-003`, `FR-INDI-004`, `FR-INDI-005`, `FR-INDI-006`, `FR-INDI-007`, `FR-INDI-008`, `FR-INDI-009`, `FR-INDI-010`, `FR-INDI-011`, `FR-INDI-012`, `FR-INDI-013`, `FR-INDI-014`, `FR-INDI-015`, `FR-INDI-016`, `FR-INDI-017`, `P-INDI-001`, `P-INDI-002`, `WF-INDI-001`, `WF-INDI-002`
- **Strategy (36):** `FR-STR-001`, `FR-STR-002`, `FR-STR-003`, `FR-STR-004`, `FR-STR-005`, `FR-STR-006`, `FR-STR-007`, `FR-STR-008`, `FR-STR-009`, `FR-STR-010`, `FR-STR-011`, `FR-STR-012`, `FR-STR-013`, `FR-STR-014`, `FR-STR-015`, `FR-STR-016`, `FR-STR-017`, `FR-STR-018`, `FR-STR-019`, `FR-STR-020`, `FR-STR-021`, `FR-STR-023`, `FR-STR-024`, `FR-STR-025`, `FR-STR-026`, `FR-STR-029`, `FR-STR-030`, `FR-STR-031`, `FR-STR-032`, `FR-STR-033`, `P-STR-001`, `P-STR-004`, `P-STR-006`, `WF-STR-001`, `WF-STR-002`, `WF-STR-004`
- **Risk (56):** `FR-RISK-001`, `FR-RISK-002`, `FR-RISK-003`, `FR-RISK-004`, `FR-RISK-005`, `FR-RISK-006`, `FR-RISK-007`, `FR-RISK-008`, `FR-RISK-009`, `FR-RISK-010`, `FR-RISK-011`, `FR-RISK-012`, `FR-RISK-013`, `FR-RISK-014`, `FR-RISK-015`, `FR-RISK-016`, `FR-RISK-017`, `FR-RISK-018`, `FR-RISK-019`, `FR-RISK-020`, `FR-RISK-021`, `FR-RISK-023`, `FR-RISK-025`, `FR-RISK-026`, `FR-RISK-027`, `FR-RISK-029`, `FR-RISK-030`, `FR-RISK-031`, `FR-RISK-033`, `FR-RISK-034`, `FR-RISK-036`, `FR-RISK-037`, `FR-RISK-038`, `FR-RISK-039`, `FR-RISK-040`, `FR-RISK-041`, `FR-RISK-042`, `FR-RISK-043`, `FR-RISK-044`, `FR-RISK-045`, `FR-RISK-046`, `FR-RISK-058`, `FR-RISK-059`, `FR-RISK-060`, `P-RISK-001`, `P-RISK-002`, `P-RISK-004`, `P-RISK-005`, `P-RISK-007`, `P-RISK-008`, `P-RISK-009`, `WF-RISK-002`, `WF-RISK-004`, `WF-RISK-008`, `WF-RISK-009`, `WF-RISK-012`
- **Trading (71):** `FR-TRD-001`, `FR-TRD-002`, `FR-TRD-003`, `FR-TRD-004`, `FR-TRD-005`, `FR-TRD-006`, `FR-TRD-007`, `FR-TRD-008`, `FR-TRD-009`, `FR-TRD-010`, `FR-TRD-012`, `FR-TRD-013`, `FR-TRD-014`, `FR-TRD-015`, `FR-TRD-016`, `FR-TRD-017`, `FR-TRD-018`, `FR-TRD-019`, `FR-TRD-020`, `FR-TRD-021`, `FR-TRD-022`, `FR-TRD-023`, `FR-TRD-024`, `FR-TRD-025`, `FR-TRD-026`, `FR-TRD-027`, `FR-TRD-028`, `FR-TRD-030`, `FR-TRD-031`, `FR-TRD-032`, `FR-TRD-033`, `FR-TRD-034`, `FR-TRD-035`, `FR-TRD-036`, `FR-TRD-037`, `FR-TRD-038`, `FR-TRD-039`, `FR-TRD-042`, `FR-TRD-043`, `FR-TRD-044`, `FR-TRD-045`, `FR-TRD-046`, `FR-TRD-048`, `FR-TRD-049`, `FR-TRD-050`, `FR-TRD-051`, `FR-TRD-052`, `FR-TRD-053`, `FR-TRD-054`, `FR-TRD-055`, `FR-TRD-056`, `FR-TRD-057`, `FR-TRD-058`, `FR-TRD-061`, `FR-TRD-062`, `P-TRD-001`, `P-TRD-002`, `P-TRD-003`, `P-TRD-004`, `P-TRD-005`, `P-TRD-007`, `P-TRD-008`, `P-TRD-009`, `WF-TRD-001`, `WF-TRD-002`, `WF-TRD-003`, `WF-TRD-004`, `WF-TRD-006`, `WF-TRD-008`, `WF-TRD-011`, `WF-TRD-012`
- **Simulation (30):** `CAP-SIM-001`, `CAP-SIM-004`, `FR-SIM-001`, `FR-SIM-002`, `FR-SIM-003`, `FR-SIM-005`, `FR-SIM-006`, `FR-SIM-010`, `FR-SIM-014`, `FR-SIM-016`, `FR-SIM-018`, `FR-SIM-019`, `FR-SIM-020`, `FR-SIM-021`, `FR-SIM-022`, `FR-SIM-023`, `FR-SIM-024`, `FR-SIM-025`, `FR-SIM-026`, `FR-SIM-027`, `FR-SIM-028`, `FR-SIM-029`, `FR-SIM-030`, `FR-SIM-031`, `P-SIM-001`, `P-SIM-005`, `P-SIM-006`, `P-SIM-007`, `WF-SIM-001`, `WF-SIM-002`
- **Analytics (43):** `FR-ANLT-001`, `FR-ANLT-002`, `FR-ANLT-003`, `FR-ANLT-004`, `FR-ANLT-005`, `FR-ANLT-006`, `FR-ANLT-007`, `FR-ANLT-008`, `FR-ANLT-009`, `FR-ANLT-010`, `FR-ANLT-011`, `FR-ANLT-012`, `FR-ANLT-013`, `FR-ANLT-014`, `FR-ANLT-016`, `FR-ANLT-017`, `FR-ANLT-018`, `FR-ANLT-020`, `FR-ANLT-021`, `FR-ANLT-022`, `FR-ANLT-023`, `FR-ANLT-025`, `FR-ANLT-027`, `FR-ANLT-028`, `FR-ANLT-033`, `FR-ANLT-034`, `FR-ANLT-036`, `FR-ANLT-038`, `FR-ANLT-039`, `FR-ANLT-040`, `FR-ANLT-041`, `FR-ANLT-042`, `FR-ANLT-043`, `FR-ANLT-044`, `FR-ANLT-045`, `FR-ANLT-046`, `P-ANLT-001`, `P-ANLT-002`, `P-ANLT-004`, `P-ANLT-006`, `WF-ANLT-001`, `WF-ANLT-005`, `WF-ANLT-006`
- **Optimization (46):** `FR-OPT-001`, `FR-OPT-002`, `FR-OPT-003`, `FR-OPT-004`, `FR-OPT-005`, `FR-OPT-006`, `FR-OPT-007`, `FR-OPT-010`, `FR-OPT-011`, `FR-OPT-012`, `FR-OPT-013`, `FR-OPT-015`, `FR-OPT-016`, `FR-OPT-017`, `FR-OPT-018`, `FR-OPT-019`, `FR-OPT-020`, `FR-OPT-021`, `FR-OPT-022`, `FR-OPT-023`, `FR-OPT-024`, `FR-OPT-025`, `FR-OPT-026`, `FR-OPT-027`, `FR-OPT-028`, `FR-OPT-032`, `FR-OPT-033`, `FR-OPT-038`, `FR-OPT-039`, `FR-OPT-040`, `FR-OPT-042`, `FR-OPT-043`, `FR-OPT-044`, `FR-OPT-045`, `FR-OPT-046`, `FR-OPT-047`, `FR-OPT-048`, `FR-OPT-049`, `FR-OPT-053`, `P-OPT-001`, `P-OPT-003`, `P-OPT-004`, `P-OPT-007`, `P-OPT-009`, `WF-OPT-001`, `WF-OPT-002`
- **Research (58):** `FR-RES-001`, `FR-RES-002`, `FR-RES-003`, `FR-RES-004`, `FR-RES-005`, `FR-RES-006`, `FR-RES-007`, `FR-RES-008`, `FR-RES-009`, `FR-RES-010`, `FR-RES-011`, `FR-RES-012`, `FR-RES-013`, `FR-RES-014`, `FR-RES-015`, `FR-RES-016`, `FR-RES-017`, `FR-RES-018`, `FR-RES-019`, `FR-RES-020`, `FR-RES-021`, `FR-RES-022
