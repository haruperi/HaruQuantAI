# Phase Implementation Changelog

All notable implementation updates for phase plans are recorded here.

## [Unreleased]

| ID | Area | Notes |
| -- | ---- | ----- |
| DATA-UPG-S01 | Data brownfield boundary contracts | Added the Data public API classification table, official tool name catalog, read-only `BrokerMarketDataPort`, source adapter contract aliases, characterization tests, README boundary notes, and usage example for Phase 02 sections 1-5. |
| DATA-UPG-S02 | Data broker-read hardening | Hardened `BrokerBackedAdapter` read paths with DataFrame shape checks, deterministic timeout/auth/schema error mapping, circuit-breaker failure recording, and fake-broker tests for Phase 02 broker/data boundary tasks. |
| DATA-UPG-S03 | Data Phase 2.0 characterization safety net | Added Phase 2.0 characterization tests for public import safety, synthetic gateway limits/request IDs, local Parquet round-trip behavior, lazy broker factories, and deterministic no-lookahead transforms before deeper module rewrites. |
| DATA-UPG-S04 | Data official tool wrappers (Phase 2.1) | Added `errors.py` with `DATA_ERROR_CODES` and `to_data_error_payload`; added `public_api.py` with `get_data_tool`, `list_symbols_tool`, `get_market_hours_tool`, `get_feed_status_tool` (each taking individual kwargs, not a request object); added `OFFICIAL_DATA_TOOLS` catalog to `__init__.py` without touching the frozen 23-name `__all__`. Added `UNSUPPORTED_TIMEFRAME` and `DATA_SERIALIZATION_FAILED` to the shared `app/utils/errors.py` code registry. (Originally also added unused `contracts.py` request DTOs and envelope aliases per this task's checklist wording; removed in DATA-UPG-S13 after a usage audit found nothing referenced them.) |
| DATA-UPG-S05 | Data import safety and lazy initialization (Phase 2.2) | Made `storage.DatabaseHelper` construction perform no I/O (directory/migrations now created lazily on first `get_connection()`), added `get_db_helper()` accessor, removed `scheduler.py`'s import-time crash recovery in favor of explicit `recover_data_jobs_on_startup()`/`initialize_data_scheduler()`, and made `validation._ensure_licensing_table()` lazy. Confirmed `import app.services.data` performs no filesystem writes. |
| DATA-UPG-S06 | Data gateway hardening (Phase 2.3) | Added `validate_workflow_context`/`validate_stale_data_behavior` to `validation.py` and wired them into `gateway.get_data`; enforced strict start<end ordering; mapped unsupported `data_kind`/timeframe to `UNSUPPORTED_OPERATION`/`UNSUPPORTED_TIMEFRAME`; added `gateway.get_data_with_metadata` (source/schema/cache/quality/lineage result metadata) without changing native `get_data`'s return shape; added real gap-window detection to `get_data_availability`. |
| DATA-UPG-S07 | Data persistence hardening (Phase 2.5) | Added `storage.PersistenceResult` and made `set_cached_data` distinguish insert/update/no_op/conflict instead of blind `INSERT OR REPLACE`; added `compute_raw_hash` wired through `execute_gateway_request`; added an auditable `sys_migrations` v2 migration (`source_version`/`target_version`/`rollback_notes`). |
| DATA-UPG-S08 | Data feed observability extraction (Phase 2.6) | Extracted feed state (`ACTIVE_FEEDS`, `register_mock_feed`, `handle_feed_overflow`, `get_feed_status`) from `scheduler.py` into a new `feeds.py`, re-exported for compatibility. Added bounded-buffer (`check_feed_buffer_capacity`), heartbeat-timeout (`check_feed_heartbeat_timeout`, `record_feed_heartbeat`), and `ReconnectPolicy`/`compute_reconnect_delay` backoff modeling; `get_feed_status` now discloses these as read-only diagnostics. |
| DATA-UPG-S09 | Data quality, volume-kind, and lineage metadata (Phase 2.7) | Added `normalization.build_data_quality_flags`/`summarize_data_quality` (duplicate/non-monotonic timestamps, non-finite/negative/zero prices, out-of-range OHLC, inverted bid/ask) and `resolve_volume_kind`; added `models.DataQualitySummary`/`DataLineage`; wired both into `gateway.get_data_with_metadata`. |
| DATA-UPG-S10 | Data transform hardening tests + docs (Phase 2.8-2.9) | Added no-lookahead, deterministic-seed, JSON-safety, and a 100k-bar resampling performance test (configurable via `validation.RESAMPLE_PERFORMANCE_THRESHOLD_SECONDS`). Rewrote `app/services/data/README.md` (boundary, import safety, adapter/persistence/feed lifecycle, quality/lineage, migration notes) and added `example_18_public_api_tool_wrappers`, `example_19_feeds_observability`, `example_20_data_quality_and_lineage` to `tests/usage/02_data.py`. |
| DATA-UPG-S11 | Data supplemental hardening (DATA-UPG-055..064) | Added `validation.SOURCE_READINESS_REGISTRY`/`validate_source_readiness` (production/staging/experimental/not_available, enforced in both `gateway.execute_gateway_request` and `scheduler.create_data_update_job`); redacted broker exception text in `sources.BrokerBackedAdapter` via `redact_text` before it crosses into `ExternalServiceError`; added golden dataset fixtures (`tests/fixtures/data/golden/`) validated against canonical models and generator reproducibility; appended README sections 13-19 (central limits manifest, source readiness/license manifest, credential handling, path safety, retention/backup policy, operational runbook, downstream contract alignment); added `tests/unit/app/services/data/test_supplemental_hardening.py` (13 tests). |
| DATA-UPG-S12 | Data coverage-matrix audit | Audited all 139 rows of the section 14 V1 requirement coverage matrix against the current implementation. Found and corrected 3 genuine coverage gaps (DATA-FR-037/038/039 — chunked/checkpointed historical backfill remains simulated, not implemented) and 3 deterministic-code-name deviations (DATA-FR-009 `TIMESTAMP_OVERLAP`→`INVALID_INPUT`, DATA-FR-079 `RATE_LIMIT_EXCEEDED`→`BACKPRESSURE_EXCEEDED`, DATA-FR-102 `DATA_QUALITY_FAILED`→diagnostic flag summary). Corrected fictional artifact references (`persistence.py`, `DOMAIN.md`, `examples/data_examples.py`, `tests/services/data/*`) to the files that actually exist. Rewrote sections 7 (File-by-File Upgrade Map), 8 (Dependency Order), and 9 (Testing Strategy) with per-bullet evidence and a test-file-name reconciliation table. |
| DATA-UPG-S13 | Data `contracts.py` dead-code cleanup | Removed `DataRequest`, `ListSymbolsRequest`, `MarketHoursRequest`, `FeedStatusRequest` (never constructed by `public_api.py`, which takes kwargs directly), `DataToolMetadata`/`DataToolEnvelope` (aliases nothing imported; `public_api.py` uses `app.utils.standard.StandardResponse` directly), and `VALID_WORKFLOW_CONTEXTS`/`VALID_STALE_DATA_BEHAVIORS`/`VALID_DATA_KINDS` (unsynchronized duplicates of the copies actually enforced in `validation.py` and `gateway.get_data`). `contracts.py` now contains only the genuinely-used `BrokerMarketDataPort`, `BrokerMarketDataFactory`, `SourceAdapterPort`, and the `JsonValue`/`DataRecords`/etc. type aliases. Removed the now-dead `test_contracts_dtos_hold_expected_defaults` test. |
