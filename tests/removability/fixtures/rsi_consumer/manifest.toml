[provider]
id = "test.rsi_consumer.default"
version = "1.0.0"
entry_point = "tests.removability.fixtures.rsi_consumer.plugin:create_provider"

[[provides]]
capability_id = "test.rsi_consumer.v1"
contract_version = "1.0.0"
cardinality = "exactly_one"

[[requires]]
capability_id = "indicator.rsi.v1"
supported_majors = [1]
cardinality = "exactly_one"
on_missing = "fail_closed"

[runtime]
profiles = ["simulation", "research", "demo", "live"]
scopes = ["process"]
effect_classes = []
lifecycle = "scoped"
reload = "process_restart"
