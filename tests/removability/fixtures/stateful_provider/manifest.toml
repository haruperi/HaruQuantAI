[provider]
id = "test.stateful.default"
version = "1.0.0"
entry_point = "fake_module:factory"

[[provides]]
capability_id = "test.stateful.v1"
contract_version = "1.0.0"
cardinality = "exactly_one"

[runtime]
profiles = ["simulation"]
scopes = ["process"]
effect_classes = ["durable_compensatable"]
lifecycle = "scoped"
reload = "process_restart"

[state]
schema_id = "test_stateful"
schema_version = "1.0.0"
migration_manifest = "schema:test.stateful.v1"
compatible_prior_majors = [1]
downgrade_policy = "reject"
uninstall_retention = "retain"
purge_requires_authorization = true
